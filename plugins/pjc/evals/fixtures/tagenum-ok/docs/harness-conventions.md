# 하니스 규약 (픽스처)

## 검증 명령 상세 (무엇을 대조하는가 · 함정)

**Hook 골든 회귀** — 케이스 정본은 `plugins/pjc/hooks/evals/hook-cases.json` + 러너 내장 시나리오이고 **기준선 9케이스**다. **이 수는 기계로 세어지지 않는다** — `hook-cases.json`은 3건이고 나머지는 러너 내장 시나리오라 파일 하나를 세면 어긋난다.

**evals 골든 러너** — 케이스 정본은 `plugins/pjc/evals/cases.json`이고 **기준선 2케이스**다.

**러너 내장 케이스** — 매니페스트가 없어 **4케이스(기계 미대조)** 다.

## 검증 매핑 (task 검증 선택)

| 변경 파일 패턴 | 필수 검증 |
|---|---|
| `plugins/pjc/evals/**` | `python plugins/pjc/evals/check-harness-consistency.py` |

이 절의 계수도 같은 축이 잰다 — 종료 코드 판정 **5케이스(기계 미대조)** 다.

## 조건부 참조 문서 크기 임계

> **기계 대조 대상이다.** 「문서 예산」 축이 이 표를 파싱해 **기록값 == 실측**과 **실측 <= 상한** 둘 다 대조한다.

| 파일 | 파일 문자 | 상한 |
|---|---|---|
| `docs/golden-runner.md` | 118 | 4,000 |

> **이 표에 있는 파일을 편집한 task가 같은 task 안에서 이 표를 갱신한다.**

구 근거였던 「F-4 스캔」은 대상이 소멸했다 — Phase F 8단계가 사라졌기 때문이다.
