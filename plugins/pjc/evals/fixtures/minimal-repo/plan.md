# plan — 픽스처

> 축 11(등재 마커 실재) 전용 픽스처. 이 레포의 `plan.md`는 gitignore이지만 픽스처에는
> 실파일로 둔다 — 검사기가 읽을 대상이 없으면 그 축이 0항목으로 조용히 통과한다.
> 제목은 대장의 기존 항목을 그대로 쓴다(대장을 고치면 다른 케이스의 변이 앵커가 깨진다).

## Deferred / Follow-up

- `[등재]` **첫 번째 미처리 항목** — 대장에 같은 제목이 있어야 통과한다.
- `[미등재:이번 회차가 처리]` **픽스처 미등재 항목** — 대장에 없는 것이 정상이라 대조 대상이 아니다.
