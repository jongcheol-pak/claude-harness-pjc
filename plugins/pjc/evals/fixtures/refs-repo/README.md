# 픽스처 README

루트 `*.md` 분기가 스캔 대상에 들어가는지 재는 대상이다 — 이 파일에는 삭제된 자산의 이름이 없다.

## 산문 대상 둘째 문서

`PROSE_TARGETS` 가 여럿일 때 **루프가 실제로 도는지**를 재는 자리다 — 대상이 하나뿐이면
첫 항목만 보는 구현도 green 이다.

- 정상 경로: `plugins/pjc/skills/sample/SKILL.md`
- 정상 심볼: `old-ref.md`
