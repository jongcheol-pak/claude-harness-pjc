# 픽스처용 — 기본은 아무것도 무시하지 않는다.
# 아래 자리표시 줄을 실제 경로로 바꾸는 변이가 「디스크에 있으나 추적되지 않는 경로」를 만든다
# (골든 `stale-prose-untracked`). 러너는 케이스마다 이 트리를 복사해 `git init` + `git add -A`
# 하므로, 여기 적힌 패턴은 그 임시 트리에서 실제로 추적 대상에서 빠진다.
#PLACEHOLDER
