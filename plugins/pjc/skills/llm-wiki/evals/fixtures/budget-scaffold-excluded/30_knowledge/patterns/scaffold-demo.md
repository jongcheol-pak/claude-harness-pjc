---
type: concept
concept_name: "비계 예산 제외 데모 (scaffold budget exclusion)"
origin: agent-synthesized
confidence: medium
updated: 2026-08-18
related_projects: [Demo]
tags: [concept, demo]
index_label: "비계 예산 제외 데모 (scaffold budget exclusion)"
---

# 비계 예산 제외 데모

## 개요

이 문단은 허브에 남는 산문이다 — `## 개요`는 정체성 절이라 이동 대상이 아니고, 예산이 실제로 재야 하는 것이 바로 이 분량이다. 아래 여섯 절은 본문을 하위로 옮긴 뒤 정본 포인터만 남은 스텁이며, 그 포인터와 `## 하위 문서` 목록은 분할 처방이 만들어 낸 비계라 사람이 줄일 수 없다.

이 문단은 허브에 남는 산문이다 — `## 개요`는 정체성 절이라 이동 대상이 아니고, 예산이 실제로 재야 하는 것이 바로 이 분량이다. 아래 여섯 절은 본문을 하위로 옮긴 뒤 정본 포인터만 남은 스텁이며, 그 포인터와 `## 하위 문서` 목록은 분할 처방이 만들어 낸 비계라 사람이 줄일 수 없다.

이 문단은 허브에 남는 산문이다 — `## 개요`는 정체성 절이라 이동 대상이 아니고, 예산이 실제로 재야 하는 것이 바로 이 분량이다. 아래 여섯 절은 본문을 하위로 옮긴 뒤 정본 포인터만 남은 스텁이며, 그 포인터와 `## 하위 문서` 목록은 분할 처방이 만들어 낸 비계라 사람이 줄일 수 없다.

이 문단은 허브에 남는 산문이다 — `## 개요`는 정체성 절이라 이동 대상이 아니고, 예산이 실제로 재야 하는 것이 바로 이 분량이다. 아래 여섯 절은 본문을 하위로 옮긴 뒤 정본 포인터만 남은 스텁이며, 그 포인터와 `## 하위 문서` 목록은 분할 처방이 만들어 낸 비계라 사람이 줄일 수 없다.

이 문단은 허브에 남는 산문이다 — `## 개요`는 정체성 절이라 이동 대상이 아니고, 예산이 실제로 재야 하는 것이 바로 이 분량이다. 아래 여섯 절은 본문을 하위로 옮긴 뒤 정본 포인터만 남은 스텁이며, 그 포인터와 `## 하위 문서` 목록은 분할 처방이 만들어 낸 비계라 사람이 줄일 수 없다.

이 문단은 허브에 남는 산문이다 — `## 개요`는 정체성 절이라 이동 대상이 아니고, 예산이 실제로 재야 하는 것이 바로 이 분량이다. 아래 여섯 절은 본문을 하위로 옮긴 뒤 정본 포인터만 남은 스텁이며, 그 포인터와 `## 하위 문서` 목록은 분할 처방이 만들어 낸 비계라 사람이 줄일 수 없다.

이 문단은 허브에 남는 산문이다 — `## 개요`는 정체성 절이라 이동 대상이 아니고, 예산이 실제로 재야 하는 것이 바로 이 분량이다. 아래 여섯 절은 본문을 하위로 옮긴 뒤 정본 포인터만 남은 스텁이며, 그 포인터와 `## 하위 문서` 목록은 분할 처방이 만들어 낸 비계라 사람이 줄일 수 없다.

이 문단은 허브에 남는 산문이다 — `## 개요`는 정체성 절이라 이동 대상이 아니고, 예산이 실제로 재야 하는 것이 바로 이 분량이다. 아래 여섯 절은 본문을 하위로 옮긴 뒤 정본 포인터만 남은 스텁이며, 그 포인터와 `## 하위 문서` 목록은 분할 처방이 만들어 낸 비계라 사람이 줄일 수 없다.

이 문단은 허브에 남는 산문이다 — `## 개요`는 정체성 절이라 이동 대상이 아니고, 예산이 실제로 재야 하는 것이 바로 이 분량이다. 아래 여섯 절은 본문을 하위로 옮긴 뒤 정본 포인터만 남은 스텁이며, 그 포인터와 `## 하위 문서` 목록은 분할 처방이 만들어 낸 비계라 사람이 줄일 수 없다.

이 문단은 허브에 남는 산문이다 — `## 개요`는 정체성 절이라 이동 대상이 아니고, 예산이 실제로 재야 하는 것이 바로 이 분량이다. 아래 여섯 절은 본문을 하위로 옮긴 뒤 정본 포인터만 남은 스텁이며, 그 포인터와 `## 하위 문서` 목록은 분할 처방이 만들어 낸 비계라 사람이 줄일 수 없다.

이 문단은 허브에 남는 산문이다 — `## 개요`는 정체성 절이라 이동 대상이 아니고, 예산이 실제로 재야 하는 것이 바로 이 분량이다. 아래 여섯 절은 본문을 하위로 옮긴 뒤 정본 포인터만 남은 스텁이며, 그 포인터와 `## 하위 문서` 목록은 분할 처방이 만들어 낸 비계라 사람이 줄일 수 없다.

이 문단은 허브에 남는 산문이다 — `## 개요`는 정체성 절이라 이동 대상이 아니고, 예산이 실제로 재야 하는 것이 바로 이 분량이다. 아래 여섯 절은 본문을 하위로 옮긴 뒤 정본 포인터만 남은 스텁이며, 그 포인터와 `## 하위 문서` 목록은 분할 처방이 만들어 낸 비계라 사람이 줄일 수 없다.

이 문단은 허브에 남는 산문이다 — `## 개요`는 정체성 절이라 이동 대상이 아니고, 예산이 실제로 재야 하는 것이 바로 이 분량이다. 아래 여섯 절은 본문을 하위로 옮긴 뒤 정본 포인터만 남은 스텁이며, 그 포인터와 `## 하위 문서` 목록은 분할 처방이 만들어 낸 비계라 사람이 줄일 수 없다.

이 문단은 허브에 남는 산문이다 — `## 개요`는 정체성 절이라 이동 대상이 아니고, 예산이 실제로 재야 하는 것이 바로 이 분량이다. 아래 여섯 절은 본문을 하위로 옮긴 뒤 정본 포인터만 남은 스텁이며, 그 포인터와 `## 하위 문서` 목록은 분할 처방이 만들어 낸 비계라 사람이 줄일 수 없다.

이 문단은 허브에 남는 산문이다 — `## 개요`는 정체성 절이라 이동 대상이 아니고, 예산이 실제로 재야 하는 것이 바로 이 분량이다. 아래 여섯 절은 본문을 하위로 옮긴 뒤 정본 포인터만 남은 스텁이며, 그 포인터와 `## 하위 문서` 목록은 분할 처방이 만들어 낸 비계라 사람이 줄일 수 없다.

이 문단은 허브에 남는 산문이다 — `## 개요`는 정체성 절이라 이동 대상이 아니고, 예산이 실제로 재야 하는 것이 바로 이 분량이다. 아래 여섯 절은 본문을 하위로 옮긴 뒤 정본 포인터만 남은 스텁이며, 그 포인터와 `## 하위 문서` 목록은 분할 처방이 만들어 낸 비계라 사람이 줄일 수 없다.

이 문단은 허브에 남는 산문이다 — `## 개요`는 정체성 절이라 이동 대상이 아니고, 예산이 실제로 재야 하는 것이 바로 이 분량이다. 아래 여섯 절은 본문을 하위로 옮긴 뒤 정본 포인터만 남은 스텁이며, 그 포인터와 `## 하위 문서` 목록은 분할 처방이 만들어 낸 비계라 사람이 줄일 수 없다.

이 문단은 허브에 남는 산문이다 — `## 개요`는 정체성 절이라 이동 대상이 아니고, 예산이 실제로 재야 하는 것이 바로 이 분량이다. 아래 여섯 절은 본문을 하위로 옮긴 뒤 정본 포인터만 남은 스텁이며, 그 포인터와 `## 하위 문서` 목록은 분할 처방이 만들어 낸 비계라 사람이 줄일 수 없다.

이 문단은 허브에 남는 산문이다 — `## 개요`는 정체성 절이라 이동 대상이 아니고, 예산이 실제로 재야 하는 것이 바로 이 분량이다. 아래 여섯 절은 본문을 하위로 옮긴 뒤 정본 포인터만 남은 스텁이며, 그 포인터와 `## 하위 문서` 목록은 분할 처방이 만들어 낸 비계라 사람이 줄일 수 없다.

이 문단은 허브에 남는 산문이다 — `## 개요`는 정체성 절이라 이동 대상이 아니고, 예산이 실제로 재야 하는 것이 바로 이 분량이다. 아래 여섯 절은 본문을 하위로 옮긴 뒤 정본 포인터만 남은 스텁이며, 그 포인터와 `## 하위 문서` 목록은 분할 처방이 만들어 낸 비계라 사람이 줄일 수 없다.

## 프로젝트 사례

- Demo — 여기서 관측했다(픽스처라 링크 대상을 두지 않는다).

## 첫째 절

**정본은 [[30_knowledge/patterns/scaffold-demo-2|비계 예산 제외 데모 (scaffold budget exclusion) — 첫째 절]]의 「첫째 절」이다** — 본문 300자를 옮겼다(§7-2 발동 처방).

## 둘째 절

**정본은 [[30_knowledge/patterns/scaffold-demo-3|비계 예산 제외 데모 (scaffold budget exclusion) — 둘째 절]]의 「둘째 절」이다** — 본문 300자를 옮겼다(§7-2 발동 처방).

## 셋째 절

**정본은 [[30_knowledge/patterns/scaffold-demo-4|비계 예산 제외 데모 (scaffold budget exclusion) — 셋째 절]]의 「셋째 절」이다** — 본문 300자를 옮겼다(§7-2 발동 처방).

## 넷째 절

**정본은 [[30_knowledge/patterns/scaffold-demo-5|비계 예산 제외 데모 (scaffold budget exclusion) — 넷째 절]]의 「넷째 절」이다** — 본문 300자를 옮겼다(§7-2 발동 처방).

## 다섯째 절

**정본은 [[30_knowledge/patterns/scaffold-demo-6|비계 예산 제외 데모 (scaffold budget exclusion) — 다섯째 절]]의 「다섯째 절」이다** — 본문 300자를 옮겼다(§7-2 발동 처방).

## 여섯째 절

**정본은 [[30_knowledge/patterns/scaffold-demo-7|비계 예산 제외 데모 (scaffold budget exclusion) — 여섯째 절]]의 「여섯째 절」이다** — 본문 300자를 옮겼다(§7-2 발동 처방).

## 하위 문서

- [[30_knowledge/patterns/scaffold-demo-2|비계 예산 제외 데모 (scaffold budget exclusion) — 첫째 절]] — 첫째 절
- [[30_knowledge/patterns/scaffold-demo-3|비계 예산 제외 데모 (scaffold budget exclusion) — 둘째 절]] — 둘째 절
- [[30_knowledge/patterns/scaffold-demo-4|비계 예산 제외 데모 (scaffold budget exclusion) — 셋째 절]] — 셋째 절
- [[30_knowledge/patterns/scaffold-demo-5|비계 예산 제외 데모 (scaffold budget exclusion) — 넷째 절]] — 넷째 절
- [[30_knowledge/patterns/scaffold-demo-6|비계 예산 제외 데모 (scaffold budget exclusion) — 다섯째 절]] — 다섯째 절
- [[30_knowledge/patterns/scaffold-demo-7|비계 예산 제외 데모 (scaffold budget exclusion) — 여섯째 절]] — 여섯째 절
