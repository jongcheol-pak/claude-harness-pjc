# AGENTS.md — Agent Guide

> 이관 골든용 픽스처.

## 위키
- **프로젝트 페이지**: `20_projects/personal/demo.md`
- 프로젝트 정보는 위키가 정본

## Build & Test
- Build: `python -m compileall .`
- Test: `python evals/run.py`

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[BuildDetail] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.


## DO NOT
- 실제 시크릿을 코드·문서에 기록.

## Plan Location
- 단일 plan: `plan.md`.

## Conventions

상세는 `docs/harness-conventions.md`가 정본이다.

[Conventions] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Conventions] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Conventions] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Conventions] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Conventions] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Conventions] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Conventions] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Conventions] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Conventions] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Conventions] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Conventions] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Conventions] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Conventions] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Conventions] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Conventions] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Conventions] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Conventions] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.


## Repository Structure

구조 설명은 `docs/harness-conventions.md`를 함께 본다.

[Structure] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Structure] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Structure] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Structure] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Structure] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Structure] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Structure] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Structure] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.


## OS

`docs/harness-conventions.md` 참조. 부록은 `docs/appendix.md`.
