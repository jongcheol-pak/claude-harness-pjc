# AGENTS.md — Agent Guide

> 이관 골든용 픽스처.

## Stack

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Stack] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.


## Build & Test

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.

[Build] 이 문단은 파일 크기를 맞추기 위한 본문이다. 이관은 내용을 바꾸지 않고 위치만 옮긴다. 포인터가 남으므로 어디로 갔는지 알 수 있다.


## DO NOT

금지 항목.

## Plan Location

`plan.md`.

## Notes

옮길 수 있는 작은 절이다.
