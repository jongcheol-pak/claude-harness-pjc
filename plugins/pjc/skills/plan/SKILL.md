---
name: plan
description: 코드 변경을 하나의 plan.md로 계획한다 — 요구를 1문1답 인터뷰로 확정하고, 영향 범위를 명령으로 실측하고, 자율 실행 가능한 task로 분해한다. 한국어(계획/설계/기능 추가/리팩토링/구조 변경/여러 곳 고쳐야/전체적으로/만들어줘/앱·도구 만들어)와 영어(plan/design/refactor/build an app/create a tool)에 발동하고, 서술형 문제 제기("…해야 해", "…가 너무 많아", "…를 정리해야 한다")도 요청으로 본다. **할 일을 여러 항목으로 나열한 요청**(줄바꿈·불릿·번호 목록)과 **한 문장에 변경이 둘 이상인 요청**("A 바꾸고 B도 바꿔줘")은 항목 하나하나가 작아 보여도 발동한다 — 여러 곳을 손대는 순간 순서와 영향 범위를 정해야 하기 때문이다. 여러 파일에 걸치거나, 로직·시그니처를 바꾸거나, 함수·클래스·메서드를 새로 만드는 변경도 발동한다. 발동하지 않는 것 — 변경이 **하나뿐이면서** 한 줄 텍스트·라벨·색·크기, 오타, 주석, 설정값, 새 심볼을 만들지 않는 3줄 이하 코드, **바꾸는 값의 종류가 하나뿐인** 단순 치환(그 종류를 몇 곳에 적용하든 — 그건 직접 편집한다). 단 **값 치환에 조건 분기·동작 변경이 섞이면** 단순 치환이 아니라 발동한다. 승인된 plan.md가 이미 있고 "구현/진행해/계속/go"라면 이 스킬이 아니라 pjc:implement다. 버그·에러·크래시 보고는 pjc-systematic-debugging이 먼저다.
---

# Plan

승인 가능한 `plan.md` 하나를 만든다. 이 스킬은 **코드를 고치지 않는다** — 산출물은 `plan.md`·`intent/` 파일과, Step 1이 없을 때 만드는 `AGENTS.md`뿐이다.

## 이 스킬을 건너뛰는 경우

**다음은 계획 없이 바로 편집한다 — 단 요청된 변경이 하나일 때만이다.** 계획을 세우는 비용이 변경 자체보다 크면 계획이 낭비이고, 변경이 둘 이상이면 순서와 영향 범위를 정해야 한다(`description` 의 「여러 항목으로 나열한 요청」).

- **무엇이 해당하는지는 `description` 의 「발동하지 않는 것」이 정본이다** — 그 문면은 스킬 목록과 함께 이미 로드돼 있어, 여기 복창하면 정본이 둘이 되고 한쪽만 고쳐진다. 「변경 하나」는 **값·문구의 종류**로 세지 파일 수나 출현 횟수로 세지 않는다
- 사용자가 이미 원인을 진단하고 그 한 줄만 고치라고 지목한 경우 — 이것만 `description` 에 없다

**애매하면 묻는다** — `A) 바로 수정 / B) 계획부터` 두 선택지로 제시해 사용자가 한 단어로 답하게 한다.

**발동한 뒤에 계획이 불필요하다고 판명돼도 마찬가지다** — 판정·면제 표식·그 절차는 `references/interview.md`의 「계획 불필요 판정과 면제 표식」이 정본이다.

## Step 1. 컨텍스트 수집

1. **`AGENTS.md`** — 빌드·테스트 명령, 디렉터리 구조, 프로젝트 규약. **없으면 여기서 최소 생성한다** — 아래 「AGENTS.md가 없을 때」.
2. **위키** (vault가 설정돼 있으면) — 대상 프로젝트의 `conventions.md`(함정·규약)와 `decisions.md`(보류·기각 이력). 절차와 기록 형식은 `../WIKI.md`.
3. **그 레포의 Deferred 대장** (있으면 — 이 레포는 `docs/plans/deferred.md`) — 이번 요청과 겹치는 미처리 항목이 있으면 이번 plan에 흡수하거나 명시적으로 제외한다. **종결분(`deferred-closed.md`)도 요청 키워드로 grep 한다** — 이미 기각·보류한 것을 모르고 다시 제안하는 것을 막는다. 전건 읽기가 아니라 키워드 조회다. **없는 레포에서는 그 사실을 Log에 1줄 남긴다** — 「없음」과 「확인 안 함」은 다르다.
   - **흡수하는 항목의 「착수 조건」은 지금 다시 잰다** — 등재 시점 값이라 낡았고, 믿고 task 를 세우면 실행 시점에 조건이 안 맞아 멈춘다(2026-09-10 실측). 잰 값을 착수 기준선에 적는다.
4. **진행 중 `plan.md`** — 있으면 이번 요청이 그것과 중복·모순되는지 먼저 확인한다.
5. **`intent/` 의 최근 파일** (있으면) — 미해소 `Open questions` 가 남아 있으면 이번 요구로 이월한다(형식은 `references/intent-rules.md`). **폴더가 없으면 그 사실을 Investigation Log에 1줄 남긴다.**

- **`AGENTS.md`에 아키텍처 선언이 없으면 이번 계획이 확정한다** — 기본값과 「두지 않으면 근거를 남긴다」는 `../AGENTS-BOUNDARY.md` 「Conventions」가 정본이다. **Step 2의 질문 하나로 확정한다**(`GUESS:`) — 말없이 적용하면 의도적 비채택인지 누락인지 갈리지 않는다.

- **읽은 결과는 Investigation Log에 남긴다** — 다음 세션이 같은 근거를 재조회 없이 쓴다.

### AGENTS.md가 없을 때

**절차 정본은 `../AGENTS-BOUNDARY.md`「AGENTS.md 최소 생성」이다** — 뼈대·무엇을 채우고 무엇을 비우는가·저장 승인까지 그 문서가 갖는다. 이 스킬에서 드물게 쓰이는 경로라 본문에 두지 않는다.

## Step 2. 요구 인터뷰 (1문1답)

**사용자가 원하는 것을 95% 확신할 때까지 한 번에 하나씩 묻는다.** 묶어 던지면 각각을 대충 답하게 되고, 그 답으로 세운 계획이 실제 요구와 어긋난다.

- **인터뷰의 형식·정지 조건·재진술 항목은 `references/interview.md`의 「인터뷰 절차」에 있다** — 가설과 신뢰도, `GUESS:`를 붙인 한 질문, 표준 용어 되묻기, 정지 판정, 의도 재진술까지 그 문서가 정본이다.
- **사용자가 응답할 수 없는 실행에서는 인터뷰를 시작하지 않는다** — `/loop`·예약 실행·자율 루프 안에서는 질문이 답을 받지 못한 채 자기 추측으로 메워지고, 그 추측이 확정된 요구로 plan에 박힌다. 그런 자리에서는 계획을 쓰지 말고 요구가 확정되지 않았다는 것을 결과로 보고한다.

## Step 3. 영향 범위 실측

**모든 사실 주장은 명령을 실행해 얻는다.** 산출물은 3열 표(`references/plan-template.md`「Investigation Log」가 서식 정본이다)이고, **명령 열이 빈 행은 plan에 주장으로 실을 수 없다.**

- **명령의 사정거리가 주장의 범위와 같은지 본다** — 다르면 그 행은 주장이 아니라 참고다. 단일 키워드 `grep` 은 *「그 문자열이 있다」*를 낼 뿐 *「그 규약의 사본이 있다」*를 못 낸다(2026-09-11 실측: 매치 9곳이 전부 다른 주제라 이중화 3건이 실제로는 1건이었다). 계수도 같다 — 문서 전체를 세는 명령으로 표의 행 수를 주장할 수 없다.
- **"없다"를 결론으로 쓸 때는 검색어 2개 이상으로 확인하고, 검색 **범위**를 대상 파일 집합으로 좁히지 않는다** — 이름으로만 찾으면 다른 이름의 같은 구현이 안 나오고, 경로를 좁히면 **나머지가 그 밖에 남는다**(회차 38 실측: `V-2` 잔존을 2건이라 적었는데 전역은 11건). 각 검색어·범위와 0건을 표에 적는다.
- **hit가 30건을 넘으면 전건 Read 대신 `grep -C`로 영향 여부를 가르고 그 근거를 출력 요지에 적는다** — 개수만 세면 영향 판단이 없는 것과 같다.
- **변경 대상의 호출자·구현체·직렬화·테스트를 task의 Files에 모두 적는다 — 재는 케이스가 없으면 어디에 만들지도 적는다** — 빠진 파일은 구현 중에 발견되고 그때는 다른 task가 그 위에 쌓인다(면제 5범주는 `references/plan-template.md`「작업 단계」).

## Step 4. 작업 분해

- **task 하나는 한 번에 검증 가능한 크기로 자른다** — 검증이 붙지 않는 task는 완료 여부를 판정할 수 없다.
- **각 task에 Files·acceptance·검증 명령을 적는다** — 없으면 완료를 판정할 수 없다(서식은 `references/plan-template.md` 「작업 단계」).
- **acceptance에 판정용 명령을 넣을 때는 지금 1회 실행해 현재값을 병기한다** — 형식은 `<기대값> (착수 N)`. 돌려보지 않고 적은 기대값은 구조상 도달 불가능한 경우가 있다.
- **의존 관계를 적는다** — 뒤 task가 앞 task의 산출물을 전제하는지, 앞 task의 변경이 뒤 task의 Files를 무효화하는지.
- **task 번호는 `T1`·`T2`를 쓴다** — "Phase"·"단계"는 실행 스킬의 내부 단계명과 섞인다.

## Step 5. plan.md 작성

템플릿·작성 규칙은 `references/plan-template.md`다. 루트 `plan.md` 하나에 쓰고(덮어쓰기), 요구는 `references/intent-rules.md` 형식으로 `intent/` 에 쓴다.

- **task가 많아 한 회차에 담기 어려우면 파일을 쪼개지 말고 회차를 나눈다** — 나머지는 `## Deferred / Follow-up`에 `[다음 회차]` 마커로 적는다(마커 규약은 `references/deferred-rules.md`).
- **`plan.md`는 `Write`(전체 교체)·`Edit`(부분 수정) 도구로만 고친다** — `sed`·`cp`·python 쓰기는 `guard-bash` 가 `exit 2` 로 막고 근거는 그 차단 메시지가 낸다.
- **쓰기가 실패하면 오류 원문을 그대로 내고 멈춘다** — 삼키면 **승인받은 계획이 디스크에 없다.**

## Step 6. 리뷰와 승인

- **`plan-reviewer` 서브에이전트를 호출한다(최소 1회)** — 전달: `plan.md` 경로, 대상 레포 경로, `AGENTS.md` 경로.
- **리뷰어는 read-only 라 고치는 것은 이 세션이다.** **호출되지 않는 환경이면 그 사실을 plan 에 적고 승인 단계로 간다** — 리뷰 부재를 통과로 적지 않는다.
- **BLOCKER·MAJOR는 plan을 고쳐 해소한 뒤 다시 호출한다** — 지적이 남은 계획을 승인받으면 그 결함이 구현 중에 드러난다. **MINOR는 「필터 패스」가 가른다 — 종결로는 「고친다 / 버린다」 둘이고 대장 등재는 종결로가 아니다**(정본 `../implement/references/final-review.md`「필터 패스」).
- **같은 지적이 3회 연속이면 멈춘다. 고칠 때마다 새 지적이 나서 라운드가 늘어도 멈춘다** — 매 라운드 조금씩 나아지는 것은 실패로 집계되지 않아 「동일 원인 3회」에 걸리지 않는다.
- **승인이 필요한 항목(파괴적·외부 비가역 작업, 대량 삭제)이 `## 승인 필요 항목`에 있는지 확인한다** — `plan-reviewer`의 검사 항목에 없는 자리라, 여기서 빠지면 실행 시점에 루프가 멈춘다.
- **`plan.md`를 쓴 뒤 화면에 요약을 내고 승인을 받는다** — 서식은 `references/interview.md`의 「승인 제시」.
- **승인 직후 `references/intent-rules.md`의 커밋 절차를 실행한다** — 승인 시점의 요구를 이력에 남긴다.

## See also

- `pjc:implement` — 승인된 plan을 끝까지 실행한다
- `pjc:pjc-systematic-debugging` — 버그·크래시는 원인 규명이 먼저다
- `../DESIGN.md` — 이 스킬의 문면이 따르는 설계 원칙
