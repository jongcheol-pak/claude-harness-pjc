> **본 문서는 임시 작업 메모다.** 영구 기록은 git 커밋이며, 미처리 Deferred는 `docs/plans/deferred.md`가 담는다.
>
> **다음 plan**: `docs/plans/2026-08-25-harness-context-efficiency-part2.md` — 이 part가 끝난 뒤 진행한다.

# Plan (part1/2): 하니스 컨텍스트·토큰 효율 개선 — 대장 분할 · 출력 예산 · 위임 경계 · 계획 품질

> **분할 사유**: 다른 LLM 검토에서 나온 3건(문서 크기 회귀 축 · llm-wiki rationale 분리 · 위험값 대입 경고)을 더하면 task가 11개가 되어 `plan-feature` Step 5의 PRD 복귀 게이트(10개 이상)에 걸린다. 두 덩어리의 경계가 자연스럽고(**컨텍스트 절감** vs **문서 무게·방어선**), **part1의 T1 결과가 part2의 크기 상한값 판단을 바꿀 여지**가 있어 분할을 택했다.

## 요구 이해

- **원문 요청**: 9개 항목 검토 요청 중 항목 2(*"노이즈가 많은 명령 필터링 및 서브 에이전트 활용"*), 3(*"골든·코드리뷰에서 수정한 부분만 다시 하는지"*), 4(*"리뷰가 여러 번 실행되어 작업 속도가 느림"*), 5(*"deferred.md 중복·비대화·등재 판정"*), 7(*"계획 작성 품질이 계속 떨어지는 문제"*), 8(*"불필요한 도구나 내용"*)에서 도출된 실행 항목. 이어 **"전체 다시 계획을 작성해줘, 그리고 git는 플래그로 사용해도 기록을 보고 이해할 수 있는지도 확인해서 문제가 없으면 추가"**.
- **직전 맥락**: 검토 과정에서 제안 8건 중 **2건의 전제가 반증**됐고(⑦ 선두 재구성 · ⑧ 리뷰어 Bash 회수), 사용자가 **둘 다 방향을 수정해 포함**하기로 결정했다. deferred 분할 방식과 재확인 이력 압축도 사용자가 선택했다(Step 8 참조).
- **이해한 요구**: 메인 컨텍스트를 먹는 실제 소비원을 **규약 충돌 없이** 줄인다 — ⓐ 296KB 대장을 3분할하고 재확인 이력 누적을 규약으로 막는다 ⓑ 명령 출력을 판정에 필요한 최소 형식으로 제한하는 예산 규약을 세우고 **무손실이 실증된 플래그만** 채택한다 ⓒ 서브에이전트 위임 경계를 "탐색은 위임·판정은 메인" 축으로 넓힌다 ⓓ 계획 품질 저하의 두 사각(task 간 상호 간섭 미검증 · 압축 후 `plan-feature` 미커버)을 메운다.
- **포함하지 않는 것으로 이해**: 빌드·테스트 로그의 정제 위임(「검증 목적 위임 금지」 정면 충돌), `git diff` 정제 위임(이미 리뷰어 서브에이전트 안에서 실행 — 요약의 요약), git 쓰기 위임, **SKILL.md 선두 9,000B 구간 재구성**(아래 Out of Scope), 대기 항목 131건의 **소급 압축**(규약만 신설하고 소급은 다음 소진 batch에 위임).

## Goal

**(part1 범위)** 메인 컨텍스트의 최대 단일 소비원인 `deferred.md`를 296KB → 144KB로 줄이고, 명령 출력·위임 경계·계획 검증의 세 공백을 규약으로 메워 **같은 작업을 더 적은 컨텍스트로** 끝낸다. 무손실이 실증되지 않은 축약은 채택하지 않는다.

**part2가 다루는 것**(이 part의 범위 아님): SKILL.md 본체 크기의 **기계 회귀 축** · `llm-wiki` rationale 분리와 이관 임계 · `block-destructive`의 「위험값 대입」 경고 룰과 적대적 코퍼스 확대.

## Out of Scope

- **SKILL.md 선두 9,000B 구간 재구성** — 구 PRD FR-5. `docs/prd.md:21`에서 **REMOVED**로 처리됐고 사유가 *"선두 9,000B는 제로섬이고 17항목 규칙 재구성이 plan-reviewer 4회차까지 BLOCKER를 해소하지 못했다"*(`docs/prd.md:56`)다. 대장에도 2건(`[2026-07-29]`·`[2026-08-05]`)이 *"별도 plan 대상"*으로 남아 있다. **이번은 그 재구성을 건드리지 않고 T8의 압축 리마인더 커버리지 확장으로 우회**한다 — 접근이 달라 과거 BLOCKER와 무관하다.
- **리뷰어 4종의 `Bash` 권한 회수** — 리뷰어는 Bash로 `git diff <BASE> <HEAD>`(Step 1/2)와 빌드·테스트 재실행(`spec-compliance-reviewer.md:138`)을 수행하므로 회수하면 리뷰가 성립하지 않는다. **read-only 규약은 이미 명문화돼 있다**(`spec-compliance-reviewer.md:229`·`code-quality-reviewer.md:213` — 조회형 git·빌드·테스트만 허용, 쓰기 명령 전수 금지). 관측된 2건은 규약 부재가 아니라 위반이며, 대장 `[2026-08-17]`이 지목한 해소안이 T5다.
- **대기 항목 131건의 재확인 이력 소급 압축** — T2가 규약을 신설하고 **소급 적용은 다음 소진 batch가 수행**한다(전건 편집은 이 plan의 범위를 넘고, batch가 이미 전건을 여는 정기 지점이다).
- **`deferred.md` 항목당 바이트 상한 신설** — Step 8 Q2에서 선택되지 않은 안. 대장 규약 ③(*"삭제되는 항목도 근거 의무를 면제받지 않는다"*)과 경계를 그어야 해 별도 판단이 필요하다.

## Deferred / Follow-up

- **대장 대기 항목의 재확인 이력 소급 압축** — T2가 신설하는 규약을 기존 131건에 적용하는 것. 다음 소진 batch가 수행한다(T2 acceptance가 그 위임을 규약 문면에 명시).
- **`docs/plans/deferred.md` 항목당 크기 상한** — Step 8 Q2 미채택 안. 근거 의무 규약과의 경계 판단 필요.

## Investigation Log

- **AGENTS.md 신선도 점검**: 이번 계획이 참조하는 검증 명령 5종의 대상 파일 실재 확인 — `plugins/pjc/evals/check-harness-consistency.py` ✓ · `plugins/pjc/hooks/evals/run-hook-evals.ps1` ✓ · `docs/harness-conventions.md` ✓. 어긋남 0건.
- **Deferred 대장 ① 할 일 후보 (5건 채택)**:
  - `[2026-08-22]` **`deferred.md`에 크기 임계를 신설할지 — 신규 정책 판단 필요** (deferred.md:214) — *"임계·처방(연도별 분할? 종결분 아카이브?)이 정의돼 있지 않아 자동화 대상이 아니다. 정하려면 '무엇을 어디로'를 사용자가 결정해야 한다."* **Step 8 Q1이 그 결정을 받았다** → T1이 수행. **반영 종결 대상**.
  - `[2026-08-12]` **종결 109건의 소급 정리** (3분기 규약 적용) — T1의 종결 분리와 같은 대상. 분리 후에도 정리 자체는 미수행이므로 **유지**(요지에 분리 사실만 부기).
  - `[2026-08-15]` **`deferred.md`와 `skill-feedback.md`가 같은 건을 이중 관리한다** — T2의 중복 억제 확장과 인접하나 **대상이 다르다**(파일 간 vs 파일 내). 이번엔 유지.
  - `[2026-08-15]` **대장 항목을 라인 번호로 지목하는 관행이 stale 사고를 낸다** (deferred.md:198) — **T1이 라인을 전부 이동시키므로 이번 회차가 직접 밟는다.** T1 acceptance에 "이 plan 자신이 대장을 라인 번호로 지목하지 않는다"를 넣고, 기존 plan·문서의 `deferred.md:NN` 지목이 T1 후 무효화되는지 확인한다. **유지**(관행 자체는 남음).
  - `[2026-08-20]` **`plan-feature`·`pjc-systematic-debugging`에 진행 중 출력 규약이 없다** — T3(출력 예산)과 주제가 인접하나 **대상이 다르다**(대화 출력 vs 명령 출력). 이번엔 유지.
- **Deferred 대장 ② 전제 반증 (2건 적중)**:
  - `[2026-07-29]`·`[2026-08-05]` **선두 9,000B 재구성** — 초기 제안 ⑦의 전제(*"압축 유실은 선두 재구성으로 푼다"*)를 반증했다. 4회차 BLOCKER 이력 때문에 **Out of Scope**로 내리고 T8의 다른 접근을 택했다.
  - `[2026-08-17]` **리뷰어가 워킹트리를 바꿀 수 있다** — 초기 제안 ⑧의 전제(*"read-only 선언과 실제 권한이 어긋난다"*)를 반증했다. 규약은 이미 있고 항목이 제안한 해소안이 *"리뷰어 정의 4종보다 **호출 지점(V-5·V-6·F-7)의 공통 프롬프트**에 넣는 쪽"* 이라 **T5가 그것을 그대로 채택**한다.
- **Deferred 대장 ③ 소진 batch 착수 판정 — 미달, batch task를 넣지 않는다**: ⓐ 대기 131 > 100이나 직전 batch(11차, v1.195.0) 이후 신규 등재분이 30건 미달 ⓑ 판정일 최솟값 30일 미도달(11차가 2026-08-24에 27건 전건 재확인) ⓒ 절대 상한 131 > 130이나 **직전 batch로부터 14일 미경과**(AND 조건 불충족). 세 축 전부 미달. **11차가 건너뛴 구간 판단도 승계한다** — 11차 기록이 *"같은 구간을 다시 열어 소득 0"* 이라 적었다.
- **대장 중복 실측 (신규 관측)**: `deferred.md:53`(`### 대장 운영`)과 `:114`(`### 하니스 스킬`)가 **같은 등록일·같은 제목·같은 사고**(`[2026-08-17] 리뷰어가 워킹트리 줄바꿈을 바꿀 수 있다`)를 이중 등재하고 있다. F-6.5 「중복 억제」(*"같은 요지의 항목이 이미 `## 대기`에 있으면 append를 생략한다"*)가 있는데도 발생했고, **그룹이 다르면 요지 대조가 사실상 그룹 안에서만 이뤄지는 것**이 원인으로 보인다. T2가 해소한다.
- **구간별 실측 (T1 근거)**: 전체 296,450B / 머리말 2,280B(1~9행) / **대기 160,724B**(10~226행, 그중 batch 회고 인용 16,585B) / **종결 133,001B**(227~445행). 대기 131건 · 종결 140건.
- **git 플래그 무손실 실증 (T3 근거 — 이번 세션 실측)**:

  | 명령 | 무플래그 | 플래그 | 판정 |
  |---|---|---|---|
  | `git status` (clean) | 100B | `--porcelain` **0B** | 무손실 — 브랜치 줄은 clean 판정에 불요 |
  | `git status` (dirty) | 241B | `--porcelain` **21B** | 무손실 — 파일명·상태코드 보존, 안내 문단만 제거 |
  | `git diff HEAD~3 HEAD` | **462,547B** | `--stat` **2,280B** | 규모 판정 용도에 무손실 — 파일 목록·증감량 보존(203배) |
  | `git log` | 본문 전문 | `--oneline` | **손실** — pjc는 커밋 본문에 회차 서사·`Type/Build/Tests/Review`를 담는다 |

  → **`--porcelain`·`--stat`만 채택하고 `--oneline`은 채택하지 않는다.** F-4가 *"모든 commit 메시지에서 follow-up·TODO·MINOR 언급"*을 스캔하므로 본문 제거는 그 스캔을 무력화한다.
- **위키 참조**: `pjc:llm-wiki` 절차 K — vault 설정됨(`D:/Personal Project/Obsidian Vault/LLM WIKI`, SessionStart 라인으로 확인). 이 레포는 스택 마커가 없어 자동 등록 비대상이며 허브 미등록 상태다. **위키 참조: 관련 위키 자료 없음 — 코드 1차 출처로 진행**(대상이 하니스 자신의 규약 문서라 위키에 정본이 없다).
- **스킬 개선 큐**: 대기 4건 조회. `[2026-08-23] 리뷰어 4종: 지적의 Where에 자리가 둘 이상 열거되면 하나만 고치고 넘어가기 쉽다` — T4·T5가 리뷰어 정의를 건드리나 **그 항목이 요구하는 것은 「수정 전 전수 세기」 절차 명문화**라 대상이 다르다. **미채택 — 이번 변경과 축이 다르고, 별도 회차의 `recovery.md` 개정이 필요하다.** 나머지 3건(`[2026-07-22]` V-5/V-6 조건부 도달성 · `[2026-08-24]` 실패 실증 수단 · `[2026-08-25]` Trivial Bypass 위키 1구)은 이번 범위 밖.
- **PRD 경량 확인**: `docs/prd.md`(Opus 5 대응 — 상태 *초안, 승인 대기*) · `docs/prds/` 3건. **이번 변경은 어느 active FR에도 닿지 않는다** — `docs/prd.md`의 FR-5는 이미 REMOVED이고 이번 Out of Scope와 같은 대상이며, `2026-08-24-agents-md-migration.md`는 AGENTS.md 이관, 나머지 둘은 위키·경계 주제다. **`**PRD**:` 줄을 두지 않는다** → Phase G 미진입.

### 전제 검증

| # | 이 plan이 참으로 삼는 것 | 확인 근거 | 상태 |
|---|---|---|---|
| P1 | 종결·회고를 분리해도 불변식(`대기+종결+삭제누계==총등재누계`)을 유지할 수 있다 | `check-harness-consistency.py:640` `check_deferred_stats()`가 `LEDGER_MD` 단일 파일을 읽어 `## 대기`/`## 종결` 헤딩으로 구간을 가른다 — 두 파일을 읽어 합산하도록 고치면 식은 그대로 성립 | 확인함(구현은 T1) |
| P2 | hook 3종은 파일명이 아니라 **체크박스 유무**로 plan을 판정하므로 새 파일이 추가돼도 오작동하지 않는다 | `session-context.ps1:57` · `require-plan-for-write.ps1:141` · `require-evidence.ps1:259` 모두 *"체크박스(`- [ ] T<N>`)가 있는 파일만 plan으로 인정"* 주석 | 확인함 |
| P3 | 리뷰어에게 Bash는 필수다 | `spec-compliance-reviewer.md:46`·`code-quality-reviewer.md:47` Step 1/2가 `git diff <BASE_SHA> <HEAD_SHA>` 실행 · `spec:138` *"직접 빌드/테스트를 다시 실행 (Bash 도구)"* | 확인함 |
| P4 | 압축 감지 hook이 이미 존재하고 재읽기를 지시한다 | `session-context.ps1:45` `source -eq 'compact'` 일반 리마인더 · `:100` 미완료 task 있을 때 파일명 3종 지정 | 확인함 |
| P5 | `--porcelain`·`--stat`은 각 용도에 무손실이고 `--oneline`은 손실이다 | 위 실측 표(이번 세션 4회 실행) | 확인함(실측) |
| P6 | 선두 9,000B 재구성은 이미 두 번 실패했다 | `docs/prd.md:21` FR-5 REMOVED · `:56` *"plan-reviewer 4회차까지 BLOCKER를 해소하지 못했다"* | 확인함 |
| P7 | 대장에 완전 중복 항목이 실재한다 | `deferred.md:53` ↔ `:114` — 같은 등록일·제목·사고 | 확인함(실측) |
| P8 | `plan-feature` 세션은 압축 리마인더의 파일 지정 대상이 아니다 | `session-context.ps1:100`의 조건이 `$open -gt 0`(미완료 task) — 계획 수립 중에는 plan에 미완료 task가 없거나 plan 자체가 없다 | 확인함 |

## Decisions

- **D1. 대장 3분할 (T1)** — Options: ⓐ 종결+회고 분리 ⓑ 종결만 ⓒ 종결 삭제. **Chosen: ⓐ** — Source: Step 8 Q1 사용자 선택. 근거: 296KB → 144KB(51%)이고, ⓒ는 대장 규약이 *"기각만 `## 종결`로 이동"*이라 기각 이력의 조회처가 사라진다.
- **D2. 재확인 이력 압축 규약 (T2)** — Options: ⓐ 범위 밖 ⓑ 압축 규약 신설 ⓒ 항목당 상한. **Chosen: ⓑ** — Source: Step 8 Q2. 근거: `implement-task`의 **Progress Log 누적 압축**(`SKILL.md:500`)이 같은 구조의 선례다(최근 2-3개만 상세, 이전은 한 줄로 묶음).
- **D3. 소급 적용은 다음 batch에 위임 (T2)** — Source: 대장 규약 ①이 batch를 *"전건을 여는 정기 지점"*으로 정의. 이 plan에서 131건을 전건 편집하면 T1의 분할 diff와 뒤섞여 검증이 불가능해진다.
- **D4. `--oneline` 미채택 (T3)** — Source: 위 실측 표 + `phase-f-detail.md:53` F-4의 커밋 메시지 전수 스캔. 출력 예산 규약은 *"판정에 필요한 최소 형식"*을 요구하는데, F-4의 판정에는 본문이 필요하므로 `--oneline`은 그 형식이 아니다.
- **D5. 리뷰어 read-only는 정의 파일이 아니라 호출 지점에 (T5)** — Source: 대장 `[2026-08-17]` — *"호출 시점 주입이 정의 파일 하단 규칙보다 듣는다"*는 이 repo의 반복 관측. 같은 구조가 `plan-feature` Step 9·`implement-task` V-5의 판정문 요구에도 쓰였다.
- **D6. 위임 확장의 판정 축은 "탐색 vs 판정"이지 "대상 크기"가 아니다 (T6)** — Source: `implement-task/SKILL.md:237` *"위임 금지의 핵심은 **누가 판단하나**이지 **전건을 다 읽나**가 아니다."* 크기를 축으로 삼으면 「검증 목적 위임 금지」와 충돌하지만, 탐색/판정 축은 그 조항과 정합한다.
- **D7. T8은 리마인더 **커버리지** 확장이지 선두 재구성이 아니다** — Source: P6. 접근이 달라 과거 BLOCKER를 물려받지 않는다.

## 동반 변경 판정

| 구분 | 대상 | 처리 |
|---|---|---|
| **필수** | `check-harness-consistency.py:69`(`LEDGER_MD`)·`:640`(`check_deferred_stats`) — T1이 파일을 쪼개면 계수·불변식이 즉시 깨진다 | **T1에 편입** |
| **필수** | `check-harness-consistency.py:672`(`check_batch_number_sequence`)·`:902`(`ledger = read(LEDGER_MD)`)·`:922`(축 등록) — **같은 `ledger` 문자열을 쓰는 축이 하나 더 있다.** batch 회고를 옮기면 `nums`가 비고 `if not nums: return [], 0`으로 **무증상 통과**한다(실패가 아니라 0항목 통과라 `exit 0`이 잡지 못한다). 이 함수 docstring 자신이 *"v1.195.0이 10차 위에 같은 이름 블록을 얹었는데 이 대조기가 exit 0으로 통과시켰다"*는 사고를 적고 있다 | **T1에 편입** |
| **필수** | `AGENTS.md:41`(검증 매핑 `deferred.md` 수정 시)·`:86`(대장 설명) · `harness-conventions.md:125`(검증 매핑)·`:411`(스캔 제외 3종)·`:451`(디렉터리 트리) — 새 파일 2개가 어느 검증에 걸리는지 미기재면 규약이 실제와 어긋난다 | **T1에 편입** |
| **필수** | `phase-f-detail.md:102`(F-6.5 형식 정본)·`:222`(대장 짝 처리) · `plan-feature/SKILL.md:157`(Step 1 조회) · `plan-template.md:99` · `README.md:125` — 전부 「단일 대장」을 전제한 서술 | **T1에 편입** |
| **필수** | hook 골든 3시나리오(`require-evidence.ps1:162`·`require-plan-for-write.ps1:194`·`session-context.ps1:41`)가 `deferred.md`를 **오차단 방지 음성 케이스**로 쓴다 — 새 파일 2개도 같은 성질이라 케이스가 이를 덮는지 확인이 필요 | **T1에 편입 — 확인만.** P2가 성립하므로(세 hook 모두 체크박스 판정이고 현행 대장에 `- [ ] T<N>` 매치 0건) 케이스 추가는 예상하지 않는다. **다만 확인 결과 케이스 추가가 필요해지면 그 시나리오 파일과 `harness-conventions.md:312`의 케이스 수 기준선(692)을 같은 task에서 함께 갱신**한다(T1 Files에 미리 넣어 둔다) |
| **필수** | `plugin.json` 버전 + `README.md` 상단 버전 — 이 레포의 릴리즈 규약 | **T8 이후 F-6.5**(회차 단위) |
| **선택** | `rubric_eval.py:54` 주석 **+ `plugins/pjc/skills/evals/README.md:105`의 같은 서술** — 둘 다 *"`deferred.md` 같은 대장 문서를 plan으로 오인하지 않게"*라 적는데 대장이 3개가 된다. 코드는 파일명 규약(`YYYY-MM-DD-<slug>.md`) 기준이라 **동작은 불변**, 서술만 낡는다. **한쪽만 고치면 T1 acceptance ④(옛 표현 grep 잔존 0)와 어긋난다** | **T1에 포함**(주석 1줄 + README 1줄, 규칙 7-1의 stale 서술 축) |
| **무관** | `docs/.agents-presplit/2026-08-22/**` — 분할 전 스냅숏 아카이브. 과거 상태의 사본이라 갱신 대상이 아니다 | 건드리지 않음 |
| **무관** | `notes-archive/**` — 회차 기록 아카이브. 그 시점 사실이라 불변 | 건드리지 않음 |

## 4-D. 재사용 확인

| 신규 심볼 | 유사 기존 구현 검색 결과 | 재사용/신규 사유 |
|---|---|---|
| `check_deferred_stats()`의 다중 파일 읽기 | 같은 파일 `read()` 헬퍼 존재(`:902` `ledger = read(LEDGER_MD)`) | **재사용** — `read()`를 두 번 호출하고 구간 파싱 함수만 인자화 |
| 압축 리마인더의 `plan-feature` 분기 | `session-context.ps1:45`·`:100`에 동일 패턴 2개 존재 | **재사용** — 기존 `if ($source -eq 'compact')` 블록에 조건만 추가, 새 함수 없음 |
| 나머지 T2~T7 | 전부 기존 문서·정의 파일의 문면 추가·수정 | 신규 심볼 없음 |

## Tasks

- [x] **T1. `deferred.md`를 3분할하고 참조처 전수를 새 구조에 맞춘다** (Type D)
  - **Files**: `docs/plans/deferred.md` · `docs/plans/deferred-closed.md`(신규) · `docs/plans/deferred-history.md`(신규) · `plugins/pjc/evals/check-harness-consistency.py` · `AGENTS.md` · `docs/harness-conventions.md` · `plugins/pjc/skills/implement-task/references/phase-f-detail.md` · `plugins/pjc/skills/plan-feature/SKILL.md` · `plugins/pjc/skills/plan-feature/references/plan-template.md` · `README.md` · `plugins/pjc/skills/evals/rubric_eval.py`(주석 1줄) · `plugins/pjc/skills/evals/README.md`(1줄) · (조건부 — 위 「동반 변경 판정」 hook 골든 행이 케이스 추가로 판정되면) `plugins/pjc/hooks/evals/scenarios/require-evidence.ps1` · `plugins/pjc/hooks/evals/scenarios/require-plan-for-write.ps1` · `plugins/pjc/hooks/evals/scenarios/session-context.ps1`
  - **Design**: ① **배치** — 대장은 `## 대기`만 보유(머리말 + 앵커 1줄 유지), 종결 140건은 `deferred-closed.md`의 `## 종결`로, batch 회고 7블록은 `deferred-history.md`로 **문면 무변경 이동**한다. ② **신규 심볼과 책임** — 없음(파일 2개 신설, 함수 신설 없음). ③ **의존 방향** — 대장이 두 파일을 포인터로 가리키고 역방향 참조는 두지 않는다(한쪽만 고쳐지는 드리프트 방지). ④ **비추상화 선언** — 「대장 파일 집합」을 추상화하지 않는다. `check_deferred_stats()`는 두 경로를 인자로 받을 뿐 파일 목록을 일반화하지 않는다(3개가 4개가 될 계획이 없다).
  - **Acceptance**:
    - `python plugins/pjc/evals/check-harness-consistency.py` **exit 0** — 불변식 `대기 + 종결 + 정리 삭제 누계 == 총 등재 누계`가 두 파일 합산으로 성립하고 앵커 계수가 실측과 일치한다. **이 task 종료 시점의 값은 `대기 130 / 종결 140 / 정리 삭제 누계 56`** 이다 — 분할 자체는 계수를 바꾸지 않지만(131 불변), 아래 `[2026-08-22]` 반영 종결이 `대기 -1 · 정리 삭제 누계 +1`을 적용한다.
    - **`[2026-08-22] docs/plans/deferred.md에 크기 임계를 신설할지 — 신규 정책 판단 필요` 항목이 반영 종결된다** — 그 항목이 *"정하려면 '무엇을 어디로'를 사용자가 결정해야 한다"*고 적었고 Step 8 Q1이 그 결정을 받아 이 task가 수행하므로, 판정이 끝난 항목을 대기에 남기지 않는다(3분기 — 반영은 삭제, 근거는 커밋 본문). **회차 앵커 사슬은 T1 130 → T2 129 → T5 128**이며 각 task가 자기 몫만 반영한다.
    - **`batch 차수 수열` 축이 실제로 6항목을 세는 것을 검사기 출력으로 확인한다** *(계획 전제 정정 — plan 초안은 「11항목/11블록」이라 적었으나 실측은 **회고 7블록 · 정규식 `^> \*\*(\d+)차 (?:batch|판정)` 매치 6건**(6~11차)이었다. 「계획 결함 — 자체 해소 우선」 3조건을 판정해 실측값으로 진행했다 — plan 갱신만으로 해결되고, 외부 계약과 무관하며, **기준을 낮춘 것이 아니라 `N>0` 요구를 실측과 정확히 일치시킨 것**이라 carve-out ⓑ(acceptance 완화)에 해당하지 않는다. 11로 두면 acceptance가 원리상 영구 미충족이다.)* — `exit 0`만으로는 부족하다. 그 축은 대상이 0개면 `if not nums: return [], 0`으로 **통과**하므로, 회고를 옮긴 뒤 검사기가 `deferred-history.md`를 읽도록 배선되지 않으면 축이 무증상으로 사라진다. **`check_batch_number_sequence()`가 센 차수 개수를 검사기가 출력하게 하고**(예: `batch 차수 수열 6항목`) 그 값이 **6**임을 커밋 본문에 남긴다. **N=0이면 실패로 본다.**
      - **파일을 grep해 6건을 세는 것으로 갈음하지 않는다** — grep은 *"파일 안에 6건이 있다"*만 보이고 *"검사기가 그 파일을 읽는다"*는 증명하지 않는다. grep이 6을 반환하는데 검사기는 여전히 0인 상태가 바로 이 acceptance가 막으려는 것이다.
    - **이동은 문면 무변경이다** — 이동 전후로 `## 종결` 항목 140건·batch 회고 7블록의 본문이 byte-identical임을 대조한 결과를 커밋 본문에 남긴다(대장 규약이 *"그 시점의 기록이므로 갱신하지 않는다"*고 못박은 대상이다).
    - `deferred.md` 크기가 **150,000B 미만**(실측 기준선 296,450B → 목표 약 144,000B).
    - 위 「동반 변경 판정」의 **`필수` 전행**(현재 6행 — 행이 늘어도 이 문면은 어긋나지 않는다)의 지목 지점 **전건**이 새 구조를 반영한다 — 옛 표현(`docs/plans/deferred.md`가 유일 대장이라는 서술)으로 grep해 잔존 0을 확인한다.
    - **이 plan과 이번에 새로 쓰는 서술은 대장 항목을 `deferred.md:NN` 라인 번호로 지목하지 않는다**(대장 `[2026-08-15]` 관행 항목 — T1이 라인을 전부 이동시키므로 이번 회차가 그 사고를 직접 유발할 수 있다). 지목은 **등록일 + 제목**으로 한다. **적용 범위는 새로 쓰는 서술에 한정한다** — 이동되는 종결·회고 본문에 라인 지목이 들어 있어도(실재한다) **위 「문면 무변경」이 우선**이며 고치지 않는다. 두 acceptance가 같은 텍스트에 다른 것을 요구하지 않게 하는 경계다.
    - hook 골든 3시나리오가 새 파일 2개에 대해서도 오차단하지 않음을 확인한다 — 부분 실행(`-Filter require-evidence,require-plan-for-write,session-context`)으로 확인하고 **전체는 F-2가 판정한다**(「골든 부분 실행의 판정 자격」 ⓐ를 이 줄로 사전 기재).
  - **Edge Cases**: ⓐ `## 종결` 헤딩이 옮겨간 파일에 없으면 검사기가 `die()` — 두 파일 각각의 헤딩 존재를 먼저 검사한다 ⓑ 대기 구간 안의 batch 회고 인용(`>` 블록)과 **항목 본문의 인용**을 혼동하면 항목이 함께 딸려간다 — 이동 대상은 `## 대기` 헤딩 **직후의 연속 인용 블록**으로 한정한다 ⓒ 세 파일의 줄바꿈이 CRLF로 유지되는지 확인(`AGENTS.md` 규약 — `sed -i` 금지, Edit 도구 사용)
  - **Halt Forecast**: 불변식이 두 파일 합산으로도 안 맞으면(옛 계수가 이미 틀렸을 가능성) → 원인 연산을 찾아 대장 규약의 복구 규칙대로 고친다. 그래도 안 맞으면 Halt(누계를 서로 맞춰 green을 만드는 것은 규약이 금지).
  - **Depends on**: -

- [x] **T2. 대장의 중복 억제를 그룹 간으로 넓히고 재확인 이력 압축 규약을 신설한다** (Type C · 의존: T1)
  - **Files**: `plugins/pjc/skills/implement-task/references/phase-f-detail.md`(F-6.5 「중복 억제」·형식) · `docs/plans/deferred.md`(중복 1건 병합)
  - **Design**: ① **배치** — 두 규약 모두 F-6.5의 기존 「중복 억제」 불릿 인접에 둔다(대장 형식 정본이 그곳이다). ② **신규 심볼과 책임** — 없음(문면 추가). ③ **의존 방향** — `plan-feature` Step 1은 포인터만 두고 문면을 복제하지 않는다(기존 관례). ④ **비추상화 선언** — 압축 판정을 기계 검사로 만들지 않는다(형식이 자유 서술이라 정규식이 오탐을 낸다 — 검사기에 축을 더하지 않는다).
  - **Acceptance**:
    - F-6.5 「중복 억제」가 **그룹 경계를 넘는 대조**를 명시한다 — 현행 문면(*"같은 요지의 항목이 이미 `## 대기`에 있으면"*)이 실제로는 그룹 안에서만 적용된 것이 관측됐으므로(`:53` ↔ `:114`), *"그룹이 달라도"* 를 문면에 넣고 **그 관측을 근거로 인용**한다.
    - **재확인 이력 압축 규약**이 신설된다 — 최근 1건은 상세, 이전은 한 줄로 묶는다(`implement-task/SKILL.md:500` Progress Log 압축의 구조를 준용하고 그 선례를 근거로 인용). **소급 적용은 다음 소진 batch가 수행한다**는 위임이 규약 문면에 있다(D3).
    - `deferred.md`의 중복 2건(`[2026-08-17] 리뷰어가 워킹트리 줄바꿈을 바꿀 수 있다` — `### 대장 운영`과 `### 하니스 스킬`에 이중 등재)이 **1건으로 병합**된다 — 짧은 쪽의 고유 정보(*"메인 자체 검출"* 출처 표기)가 유실되지 않고 남은 항목에 흡수된다. 병합 흡수는 「대기에서 삭제」이므로 **T1이 확정한 130 위에 `대기 129` · `정리 삭제 누계 57`** 로 앵커를 갱신한다(절대값 기록 — T1 몫을 다시 빼지 않는다).
    - `python plugins/pjc/evals/check-harness-consistency.py` exit 0 (앵커 갱신 반영).
  - **Edge Cases**: ⓐ 두 항목의 본문이 완전히 같지 않다(`:53`이 후속 관측 2건을 더 담았다) — 긴 쪽을 남기고 짧은 쪽의 고유 표기만 흡수한다 ⓑ 압축 규약이 대장 규약 ③(*"삭제되는 항목도 근거 의무를 면제받지 않는다"*)과 충돌해 보일 수 있다 — 압축은 **삭제가 아니라 요약**이므로 근거는 남는다는 것을 문면에 1구로 구분한다
  - **Halt Forecast**: 없음(문서 수정 + 항목 1건 병합).
  - **Depends on**: T1 (같은 파일의 앵커를 건드리므로 분할 후에 한다)

- [x] **T3. 명령 출력 예산 규약을 신설하고 무손실이 실증된 플래그만 적용한다** (Type C)
  - **Files**: `docs/harness-conventions.md`(신설 절) · `plugins/pjc/skills/implement-task/SKILL.md:248` · `plugins/pjc/skills/pjc-systematic-debugging/SKILL.md:74~76` · `AGENTS.md`(포인터 1줄)
  - **Design**: ① **배치** — `harness-conventions.md`에 「명령 출력 예산」 절을 신설한다(`AGENTS.md`는 16KB 주입 상한이 있어 포인터만). ② **신규 심볼과 책임** — 없음. ③ **의존 방향** — 스킬 문서가 이 절을 가리키고, 절은 스킬을 가리키지 않는다. ④ **비추상화 선언** — "모든 명령에 상한을 건다"는 일반 규칙을 만들지 않는다. **판정용 명령의 형식**만 다루고, 출력을 정제 위임하는 경로는 열지 않는다(「검증 목적 위임 금지」와 충돌).
  - **Acceptance**:
    - 신설 절이 **원칙 + 실측 근거 표**를 담는다 — *"판정용 명령은 판정에 필요한 최소 형식으로 실행한다"*, 그리고 위 Investigation Log의 4행 실측 표(무플래그 바이트 ↔ 플래그 바이트 ↔ 손실 여부).
    - **`--oneline`을 채택하지 않는 이유가 같은 절에 명시**된다 — F-4의 커밋 메시지 전수 스캔이 본문을 요구한다(D4). 이 1구가 없으면 다음 회차가 "일관성"을 이유로 `--oneline`을 넣는다.
    - 플래그 교정 4곳: `implement-task/SKILL.md:248` `git status` → `git status --porcelain` · `pjc-systematic-debugging/SKILL.md:74` `git diff HEAD~N..HEAD -- <파일>`에 **`--stat` 선행 1구** · `:75` `git log --since` 에 **개수 제한**(`--oneline` 아님 — 본문 유지) · `:76` `git blame <라인>` → **`git blame -L <시작>,<끝> <파일>`**(현행은 인자로 라인을 받을 수 없어 파일 전체를 뽑는다).
    - `pwsh` 전 ps1 parse OK + JSON 3종 OK + `check-harness-consistency.py` exit 0.
  - **Edge Cases**: ⓐ `git status --porcelain`은 clean일 때 **0바이트**라 "명령이 실패한 것"과 구분되지 않을 수 있다 — 스킬 문면에 *"출력이 비면 clean"* 을 1구로 적는다 ⓑ `git blame -L`은 라인 범위를 알아야 쓸 수 있는데 디버깅 초기엔 모를 수 있다 — 그때는 `-L` 없이 쓰되 대상 파일을 좁히라는 조건을 함께 적는다
  - **Halt Forecast**: 없음.
  - **Depends on**: -

- [ ] **T4. 리뷰어 3종의 diff 수집에 `--stat` 선행을 넣는다** (Type C)
  - **Files**: `plugins/pjc/agents/spec-compliance-reviewer.md` · `plugins/pjc/agents/code-quality-reviewer.md` · `plugins/pjc/agents/spec-prefilter.md` · `docs/harness-conventions.md`(예산 표 갱신)
  - **Design**: ① **배치** — 각 정의의 Step 1/Step 2 「Diff 수집」 코드펜스 안. ② **신규 심볼과 책임** — 없음. ③ **의존 방향** — 세 파일이 같은 문면을 갖되 서로를 참조하지 않는다(리뷰어 4종 공통 규약과 같은 구조 — 각 subagent는 자기 정의만 로드하므로 단일 소스화가 불가능하다). ④ **비추상화 선언** — 임계 초과 시의 분할 방식을 규정하지 않는다(파일 그룹핑은 리뷰어 재량 — 규정하면 프로젝트마다 어긋난다).
  - **Acceptance**:
    - 세 정의 모두 `git diff --stat <BASE_SHA> <HEAD_SHA>`를 **먼저** 실행하고, 그 결과로 규모를 판단한 뒤 전문을 읽도록 문면이 바뀐다.
    - **`--stat`이 전문을 대체하지 않는다**는 것이 명시된다 — 규모 파악용이며 검토는 전문에 대해 한다(축약으로 오해되면 리뷰가 약해진다).
    - 세 파일에 **같은 문면**이 들어간다(한쪽만 고치면 갈린다 — `harness-conventions.md` 「리뷰어 4종 공통 규약」이 경계하는 형태).
    - `check-harness-consistency.py` exit 0 — 「문서 로드 예산 기준선」 표의 **해당 2행**(`spec-compliance-reviewer.md`·`code-quality-reviewer.md`)을 같은 task에서 갱신한다. **`spec-prefilter.md`는 그 표에 없으므로 갱신 대상이 아니다**(표는 9행이고 리뷰어는 4종만 담는다 — 3행이라 적으면 없는 행을 찾게 된다).
    - **다른 두 subagent를 제외한 사유를 1줄 남긴다** — `plan-completion-reviewer`는 diff를 grep 파이프로 좁혀 받고, `root-cause-analyzer`는 리뷰어가 아니라 진단자라 대상이 다르다. 사유가 없으면 다음 회차가 "일관성"을 이유로 네 파일로 넓힌다.
  - **Edge Cases**: ⓐ `--stat`은 바이너리 파일을 `Bin` 으로만 표시한다 — 규모 판정에는 충분하나 그 사실을 1구로 적는다 ⓑ 첫 커밋이라 BASE가 없으면 `--stat`도 실패한다 — 기존 「diff 가드」가 이미 `incomplete` 반환을 규정하므로 그 경로를 그대로 탄다
  - **Halt Forecast**: 없음.
  - **Depends on**: -

- [ ] **T5. 리뷰어 호출 지점 공통 프롬프트에 read-only 1구를 주입한다** (Type C)
  - **Files**: `plugins/pjc/skills/implement-task/SKILL.md`(V-5·V-6 호출 규정) · `plugins/pjc/skills/implement-task/references/phase-f-detail.md`(F-7 전달 목록) · `plugins/pjc/skills/plan-feature/SKILL.md`(Step 9 호출 규정) · `docs/harness-conventions.md`(예산 표 갱신) · `docs/plans/deferred.md`(항목 삭제 + 앵커) · `docs/plans/deferred-closed.md`(T1 후 — 반영 종결은 삭제이므로 종결 파일에 남기지 않으나, 앵커 계산에 두 파일이 걸린다)
  - **Design**: ① **배치** — 각 호출 지점의 프롬프트 요구 불릿 옆(판정문 요구 1줄과 같은 자리). ② **신규 심볼과 책임** — 없음. ③ **의존 방향** — 리뷰어 정의 파일은 고치지 않는다(read-only 규약이 이미 있다 — 중복 서술은 드리프트를 만든다). ④ **비추상화 선언** — 「호출 프롬프트 템플릿」을 공통 파일로 빼지 않는다. 두 스킬은 서로를 로드하지 않아 포인터가 호출 시점에 손에 없다(판정문 요구가 이미 같은 이유로 양쪽에 복제돼 있다).
  - **Acceptance**:
    - V-5·V-6·F-7·Step 9의 호출 규정에 *"조사 산출물은 레포가 아니라 임시 폴더에 만들 것 · 워킹트리를 편집하지 말 것"* 취지의 1구가 들어간다. 문면은 **대장 `[2026-08-17]`이 기록한 실사고 2건**(CRLF 변환 · 레포 루트에 6개 파일 생성 → 정합 검사 수치 85→95 오염)을 근거로 인용한다.
    - **네 지점의 문면이 같다** — 한쪽만 고치면 그 경로의 리뷰어만 규약 없이 돈다.
    - 대장 `[2026-08-17]` 항목이 **반영 종결**된다 — 그 항목이 지목한 해소안(*"리뷰어 정의 4종에 넣기보다 호출 지점의 공통 프롬프트에 넣는 쪽"*)을 그대로 수행했으므로. 대응 지점을 삭제 커밋 본문에 남긴다(대장 규약 ③).
    - **앵커는 T2가 확정한 129 위에 이 task 몫만 반영한다 — `대기 128` · `정리 삭제 누계 58`**(반영 종결은 「대기에서 삭제」). T1·T2의 삭제분을 다시 빼지 않는다(⚠ 앵커는 절대값 기록이지 증분 로그가 아니다 — 같은 갱신을 두 번 적용하면 값이 중복 가산된다).
    - `check-harness-consistency.py` exit 0 + 「문서 로드 예산 기준선」의 `implement-task/SKILL.md`·`plan-feature/SKILL.md` 행 갱신.
  - **Edge Cases**: ⓐ T2가 같은 항목군의 앵커를 건드리므로 계수 갱신이 겹친다 — T5가 마지막에 앵커를 확정한다(⚠ 앵커는 절대값 기록이지 증분 로그가 아니다) ⓑ `plan-reviewer`는 Bash가 없어 이 1구가 무의미해 보이나, Step 9 프롬프트는 **네 지점 문면 동일** 원칙을 따르므로 함께 넣는다(빼면 다음 회차가 "왜 여기만 없나"로 되돌린다)
  - **Halt Forecast**: 없음.
  - **Depends on**: T2 (대장 앵커 갱신이 겹친다)

- [ ] **T6. 위임 경계를 「탐색은 위임·판정은 메인」 축으로 넓힌다** (Type C)
  - **Files**: `plugins/pjc/agents/explorer.md` · `plugins/pjc/skills/plan-feature/SKILL.md`(Step 1 위임 규정) · `plugins/pjc/skills/implement-task/references/phase-f-detail.md`(F-3·F-4) · `docs/harness-conventions.md`(예산 표 갱신)
  - **Design**: ① **배치** — `explorer.md`의 역할 서술에 대상 확장 1구, `plan-feature` Step 1의 「위임 상한」에 판정 축 1구, F-3·F-4에 위임 가능 범위 1구씩. ② **신규 심볼과 책임** — 없음(새 에이전트를 만들지 않는다 — `explorer`가 이미 haiku·read-only·발췌 읽기라 성질이 같다). ③ **의존 방향** — 세 지점이 같은 판정 축을 쓰되 문면은 각 맥락에 맞춘다. ④ **비추상화 선언** — 「정제 에이전트」를 신설하지 않는다. 빌드·테스트 로그 정제는 「검증 목적 위임 금지」와 충돌하므로 열지 않는다.
  - **Acceptance**:
    - `explorer.md`의 대상이 **코드 locating에서 「대형 문서의 후보 추출」까지** 넓어진다 — 반환물이 **후보**이고 메인이 원본을 확인한다는 기존 규정(`plan-feature` 「explorer 결과 취급」)이 그대로 적용됨을 명시한다.
    - `plan-feature` 「위임 상한」 ②(소작업 다중 위임 금지)에 **판정 축이 추가**된다 — 현행은 *"읽을 대상이 몇 곳으로 특정돼 있는가"* 하나인데, **대상이 1개 파일이어도 그 파일이 대형이면 위임 이득이 있다**(대장 144KB·README 57KB). 축을 *"읽을 양이 큰가"* 로 보완하되, **판정을 위임하는 것은 여전히 금지**임을 같은 자리에 못박는다.
    - F-3(Impact 회귀 grep)·F-4(커밋 메시지 전수 스캔)에 **탐색부 위임 허용 + 판정부는 메인** 1구가 들어간다. F-1~F-6이 *"자체 검증"*으로 표기돼 있으므로, **「검증 목적 위임 금지」와의 경계**를 같은 자리에 적는다 — 위임하는 것은 *"어디에 흔적이 있나 / 어느 커밋이 무엇을 언급했나"*이고, *"그래서 통과인가"* 는 메인이 판정한다.
    - `check-harness-consistency.py` exit 0 + 예산 표 갱신.
  - **Edge Cases**: ⓐ 확장 문면이 「위임 금지 가드」(P-2·P-3·V-7)를 침식할 수 있다 — 그 세 단계는 **명시 제외**로 다시 열거한다 ⓑ "대형"의 기준을 수치로 박으면 파일이 자라며 낡는다 — 수치 대신 *"메인이 그 파일 전문을 담기 부담스러운가"* 라는 판정으로 적고, 근거 예시로 실측값을 든다
  - **Halt Forecast**: 없음.
  - **Depends on**: T1 (대장 크기 실측값이 확정된 뒤 근거 예시로 인용)

- [ ] **T7. `plan-reviewer`에 task 간 상호 간섭 검증 항목을 추가한다** (Type C)
  - **Files**: `plugins/pjc/agents/plan-reviewer.md` · `plugins/pjc/skills/plan-feature/SKILL.md`(Step 5 의존 관계 표기 + 통과 체크리스트) · `plugins/pjc/skills/plan-feature/references/plan-template.md`(`Depends on` 필드 설명) · `docs/harness-conventions.md`(예산 표 + 「메인 조합」 수치)
  - **Design**: ① **배치** — `plan-reviewer.md`의 검토 체크리스트에 **항목 18**로 추가(기존 17개 뒤). ② **신규 심볼과 책임** — 없음. ③ **의존 방향** — 항목 18이 `Depends on` 필드를 입력으로 쓰고, Step 5는 그 필드를 채우는 쪽이다. ④ **비추상화 선언** — task 의존 그래프를 기계 검사로 만들지 않는다(plan 형식에 의존하게 되고 형식이 바뀌면 조용히 깨진다 — `session-context.ps1`이 Phase 판정을 하지 않는 것과 같은 이유).
  - **Acceptance**:
    - 항목 18이 **두 방향을 모두** 본다 — ⓐ **선행 → 후행**: 앞 task의 변경이 뒤 task의 전제·Files·acceptance를 무효화하는가 ⓑ **후행 → 선행**: 뒤 task의 변경이 앞 task가 이미 만든 산출물을 되돌리는가. 심각도는 **MAJOR 후보**(BLOCKER는 `Depends on`이 명시된 순서를 실제로 위반할 때).
    - **기존 항목 3(Impact Coverage)과의 경계**가 명시된다 — 항목 3은 *"심볼의 호출자가 Files에 있는가"*(코드 축), 항목 18은 *"task A의 결과가 task B의 전제를 깨는가"*(계획 축). 같은 누락을 두 번 지적하지 않는다(항목 3 ↔ Step 4-A 경계 서술과 같은 형식).
    - Step 5의 *"의존 관계 표시"*(현행 1줄)가 **무엇을 적는지**로 확장된다 — 순서 의존만이 아니라 **전제 의존**(앞 task가 만든 것을 뒤가 전제하는가)을 포함한다.
    - **적용 범위가 셋에서 일치**한다 — 항목 18 · Step 5 · 통과 체크리스트. (`plan-feature` 4-E가 *"셋이 어긋나면 저자 의무와 검사 범위가 갈려 사각이 생긴다"*고 적은 그 형태.)
    - **항목 18이 실제로 발동하도록 같은 파일의 동반 지점 3곳을 함께 고친다** — 항목만 추가하면 죽은 문면이 된다: ⓐ **「Type-aware 적용」 표에 행 추가**(없으면 어느 Type에서도 발동하지 않는다) ⓑ **「검토 계층」 표에 배치**(없으면 예산 배분에서 누락된다 — **심화**에 둔다. plan 문면만 읽으면 판정 가능해 도구 호출이 싸다는 Edge ⓑ와 정합) ⓒ **`plan-reviewer.md`의 예산 서두가 적은 *"검토 항목 17개"* 수치 갱신**(그대로 두면 stale).
    - **`docs/harness-conventions.md`의 「메인 조합」 수치(`:145`·`:146`)를 재계산한다** — 그 줄이 스스로 *"이 세 수치는 표에서 옮겨 적은 것이라 기계 대조 대상이 아니다 — 표를 갱신하는 task가 이 줄도 함께 고친다"*고 적고, v1.186.0 전까지 실제로 어긋나 있었다. **이 회차에서 `implement-task/SKILL.md`(T3·T5)와 `plan-feature/SKILL.md`(T5·T6·T7)가 모두 바뀌므로 마지막 편집 task인 T7이 최종값으로 확정**한다. `check-harness-consistency.py`는 이 줄을 잡지 못하므로 exit 0으로 걸러지지 않는다.
    - `check-harness-consistency.py` exit 0 + 예산 표의 `plan-reviewer.md`·`plan-feature/SKILL.md` 행 갱신.
  - **Edge Cases**: ⓐ task가 1개인 plan에는 적용 대상이 없다 — *"task 2개 이상"* 을 발동 조건으로 적는다 ⓑ 항목 수가 18개가 되면 리뷰어 예산(maxTurns 40)이 더 쪼개진다 — 항목 18은 **plan 문면만 읽으면 판정 가능**해 grep·Read 비용이 낮다는 것을 항목 서두에 적어 예산 배분을 돕는다
  - **Halt Forecast**: 없음.
  - **Depends on**: T3 · T5 · T6 — 「메인 조합」 수치를 **최종값으로 확정**하려면 `implement-task/SKILL.md`(T3·T5)와 `plan-feature/SKILL.md`(T5·T6)의 편집이 모두 끝난 뒤여야 한다. 순서가 어긋나면 중간값이 박혀 v1.186.0 이전과 같은 드리프트가 재발한다.

- [ ] **T8. 압축 리마인더의 커버리지를 계획 세션까지 넓힌다** (Type C)
  - **Files**: `plugins/pjc/scripts/session-context.ps1` · `plugins/pjc/hooks/evals/scenarios/session-context.ps1`(골든 케이스) · `docs/harness-conventions.md`(케이스 수 기준선)
  - **Design**: ① **배치** — **`if ($planPath)` 블록 바깥**(=`$cwd` 블록 안, `$cwdBaseCount` 확정 이후)에 새 compact 분기를 둔다. `:100`의 기존 compact 분기는 `if ($planPath)` → `if ($planText)` → `if ($all -gt 0)` → `if ($open -gt 0)` **4중 중첩 안**이라, 그 자리의 형제 분기는 **plan이 없는 세션에 원리상 도달하지 못한다** — 이 task가 겨냥하는 것이 정확히 그 세션이다. 발동 조건은 `$source -eq 'compact'` **AND** (`$planPath`가 null **또는** `$all -eq 0`). ② **신규 심볼과 책임** — 없음(기존 `$lines.Add()` 패턴 재사용). ③ **의존 방향** — hook이 스킬 파일명을 문자열로 지정한다(현행과 동일 — 경로 존재 검사는 하지 않는다, 미설치 환경에서 무출력이 되면 리마인더 자체가 사라진다). ④ **비추상화 선언** — 어느 스킬이 활성인지 hook이 판정하지 않는다. **plan 상태로만** 분기하고(현행 `$open` 판정과 같은 축), 스킬 판정은 하지 않는다 — 활성 스킬을 알 방법이 없고 추측하면 틀린 파일을 지목한다.
  - **Acceptance**:
    - `source=compact` + **plan이 없거나 task가 0개**인 세션(= 계획 수립 중 압축된 경우)에 **`plan-feature/SKILL.md` 재읽기 지시**가 주입된다. 현행은 `$open -gt 0`(미완료 task 있음)일 때만 파일을 지목해, 계획 세션이 통째로 빠져 있다(전제 P8).
    - 지시 문면이 **"스킬 재invoke로는 복구되지 않는다"**는 기존 근거를 유지한다(`:97` 주석 — 동일 스킬 재invoke는 *"이미 로드됨"*만 반환).
    - 골든 케이스 **최소 2건** 추가 — ⓐ `compact` + plan 없음 → `plan-feature` 지시 **발화**(양성) ⓑ `compact` + 미완료 task 있음 → 기존 `implement-task` 지시만 발화, 계획 지시는 **미발화**(델타 음성 — 두 분기가 섞이지 않음을 고정). **두 케이스가 수정 전에 FAIL함을 실증**하고 그 결과를 커밋 본문에 남긴다.
    - `docs/harness-conventions.md`의 **케이스 수 기준선 692를 같은 task에서 갱신**한다(그 줄이 *"케이스를 추가·삭제하면 이 값을 함께 갱신할 것"*을 요구하고, 누락이 실제로 두 번 드리프트를 냈다).
    - `pwsh` parse OK + **hook 골든 회귀 전체 통과**(F-2가 판정).
  - **Edge Cases**: ⓐ 비 pjc 프로젝트의 압축 세션에 계획 지시가 붙으면 노이즈다 — 기존 게이팅(`$cwdBaseCount` 비교)이 vault 라인에만 걸려 있으므로, 새 분기도 **plan 또는 AGENTS.md가 있는 프로젝트**로 한정한다(`:16` 주석의 무출력 규칙과 정합) ⓑ plan이 없는 압축 세션은 계획 중일 수도, 아무 작업도 안 하는 중일 수도 있다 — 문면을 *"계획을 세우던 중이었다면"* 조건형으로 적어 오지시를 피한다
  - **Halt Forecast**: 골든 러너 전체 실행이 도구 시간 캡을 넘는다 → `harness-conventions.md` 「골든 러너 운용」의 분리 프로세스 + 래퍼 + `Monitor` 폴링 절차를 따른다(추측 실행 금지).
  - **Depends on**: -

## Phase F 확인 항목

- **F-2 전체 검증**: 전 ps1 parse · JSON 3종 · **hook 골든 무인자 전체**(T8이 케이스를 더하므로 기준선 갱신값과 대조) · `check-harness-consistency.py` 12축 exit 0
- **F-3**: 이 plan의 「동반 변경 판정」 **`필수` 전행**(현재 6행)이 전건 처리됐는지 grep 재확인
- **F-4**: 회차 커밋 본문에서 follow-up·TODO·MINOR 언급이 `## Deferred / Follow-up`에 반영됐는지
- **F-6.5**: README 갱신(대장 3분할 설명 — `README.md:125`) + 회차 서사 커밋 + 큐 소비
- **PRD 없음** → Phase G 미진입

## 사전 승인 항목 (일괄 승인 대상)

- **파일 신설 2개** — `docs/plans/deferred-closed.md` · `docs/plans/deferred-history.md`(T1, 구조 변경)
- **`check_deferred_stats()` 시그니처 변경** — 단일 경로 → 두 경로(T1, 내부 함수·호출부 1곳)
- **`plan-reviewer` 검토 항목 신설(18번)** — 리뷰 기준 확대(T7)

## 불가피한 Halt (위임 불가)

- **push · main 병합 · 태그 · 릴리즈** — 회차 종료 후 별도 승인
- **대장 항목의 삭제 — 3건.** 위 사전 승인에 **포함되지 않는다**. 대장은 커밋되는 유일 정본이므로 각 삭제 직전에 대상 항목을 보고하고 승인받는다.
  - **T1** — `[2026-08-22] docs/plans/deferred.md에 크기 임계를 신설할지` **반영 종결**(대기 131 → 130)
  - **T2** — `[2026-08-17] 리뷰어가 워킹트리 줄바꿈을 바꿀 수 있다` 중복 2건 중 1건 **병합 흡수**(130 → 129)
  - **T5** — 위 항목의 남은 1건 **반영 종결**(129 → 128)

## Open Questions

- [x] **Q1. `deferred.md` 분할 방식** → **종결 + batch 회고 둘 다 분리**(3파일). 근거: 296KB → 144KB(51%), 종결 삭제는 기각 이력의 조회처를 없앤다.
- [x] **Q2. 대기 구간 161KB의 항목 비대화** → **재확인 이력 압축 규약 신설**. 소급은 다음 batch 위임(D3).
- [x] **Q3. 전제가 반증된 2건(⑦·⑧)** → **둘 다 방향 수정해 포함**. ⑦은 선두 재구성 대신 압축 리마인더 커버리지 확장(T8), ⑧은 Bash 회수 대신 호출 지점 프롬프트 주입(T5).

## 리뷰 처리 기록 (Step 9)

**1R (plan-reviewer)** — Verdict: **BLOCKER 2 / MAJOR 5 / MINOR 4**. 전건을 실물로 대조해 **11건 모두 수용**했다(기각 0).

| 라운드 | 지적 | 심각도 | 반영 방식 |
|---|---|---|---|
| 1R | B1 `check_batch_number_sequence()`가 회고 이동으로 **0항목 무증상 통과** | BLOCKER | 동반 변경 필수행에 `:672`·`:902`·`:922` 추가 + T1 acceptance에 *"11항목을 센다, 0항목이면 실패"* 명시. 실물 확인: `if not nums: return [], 0` 실재 |
| 1R | B2 T4·T5·T6·T7 Files에 acceptance가 요구하는 편집 파일 누락 | BLOCKER | 네 task Files에 `docs/harness-conventions.md` 추가, T5에 대장 2파일 추가 |
| 1R | M1 T8 Design ①의 분기 위치가 4중 중첩 안이라 대상 세션에 **도달 불가** | MAJOR | Design ①을 `if ($planPath)` 바깥 + 발동 조건 명시로 교체. 실물 확인: `:82`~`:90` 중첩 구조 |
| 1R | M2 기계 대조 밖인 「메인 조합」 수치(`:145`·`:146`) 갱신이 어느 task에도 없음 | MAJOR | T7 acceptance에 편입 + **T7의 `Depends on`을 T3·T5·T6으로** 지정(최종값 확정 순서) |
| 1R | M3 항목 18의 발동 지점 3곳(Type-aware 표·검토 계층 표·`:18` 수치) 누락 | MAJOR | T7 acceptance에 ⓐⓑⓒ로 명시. 실물 확인: `:18` *"검토 항목 17개"* · `:42` · `:68` 실재 |
| 1R | M4 T1 acceptance ②(문면 무변경) ↔ ⑤(라인 지목 금지) 상호 모순 | MAJOR | ⑤의 적용 범위를 *"새로 쓰는 서술에 한정, 이동 본문은 무변경 우선"*으로 한정 |
| 1R | M5 `skills/evals/README.md:105`의 같은 서술 누락 | MAJOR | 동반 변경 선택행 + T1 Files에 추가. 실물 확인 |
| 1R | m1 예산 표에 `spec-prefilter.md`가 없어 *"리뷰어 3행"*이 틀림 | MINOR | T4 acceptance를 **2행**으로 정정 + 미포함 사유 1구. 실물 확인: 표 9행에 미존재 |
| 1R | m2 T5의 앵커 갱신이 T2와 이중 적용으로 읽힘 | MINOR | *"T2가 확정한 값 위에 T5 몫만"*으로 문면 축소 |
| 1R | m3 T4가 다른 두 subagent를 제외한 사유 부재 | MINOR | T4 acceptance에 제외 사유 1줄 요구 추가 |
| 1R | m4 hook 골든 3시나리오가 「T1 편입」인데 Files에 없음 | MINOR | 동반 변경 행을 *"확인만 — 케이스 추가 시 기준선 692도 함께"*로 명확화 + T1 Files에 조건부로 등재 |

**리뷰어가 확인해 통과시킨 것**: 전제 P1·P2(+ 실측 보강 — 현행 대장에 `- [ ] T<N>` 매치 0건)·P6·P8, Out of Scope 2건의 배제 근거, T8의 692 기준선, T1→T2→T5 사슬의 앵커 충돌 차단. **미검토 항목 없음**(필수 5 완주, 조건부 4 적용, 심화 7 훑음).

**2R (재호출 1회 — 상한 소진)** — Verdict: **BLOCKER 0 / MAJOR 3 / MINOR 0**. 1R 11건 중 **10건 실질 해소** 확인(B2·M1~M5·m1~m4). 리뷰어가 M1의 새 분기 위치가 `session-context.ps1:112` 지점에서 실제로 도달 가능함을, M2의 `Depends on: T3·T5·T6`이 **순환 없이** 최종값 확정 순서를 보장함을 코드·표 재대조로 확인했다(위상 정렬: T1→T2→T5, T1→T6, T3·T4·T8 자유, T7 최후).

| 라운드 | 지적 | 심각도 | 반영 방식 |
|---|---|---|---|
| 2R | M6 B1 반영의 **대체 실증 경로**(grep으로 11건 세기)가 결함을 닫지 못한다 — grep은 *"파일에 11건이 있다"*만 보이고 *"검사기가 그 파일을 읽는다"*는 증명하지 않는다 | MAJOR | **수용.** 선택지 ②를 삭제하고 **검사기 출력 단일 경로**로 고정 + 갈음 금지 사유를 하위 불릿으로 명시 |
| 2R | M7 `[2026-08-22]` 반영 종결이 **어느 task에도 미배정**이고 T1 acceptance의 앵커 131과 어긋난다. 「불가피한 Halt」의 대장 삭제 열거도 2건뿐이라 예고 없는 승인 정지가 한 번 더 발생 | MAJOR | **수용.** 종결을 **T1에 배정**하고 회차 앵커 사슬을 **T1 130 → T2 129 → T5 128**(정리 삭제 누계 56→57→58)로 각 task에 절대값으로 명시. 「불가피한 Halt」의 대장 삭제를 **3건 전건 열거**로 교체 |
| 2R | M8 1R 반영으로 필수행이 6개가 됐는데 T1 acceptance·F-3이 아직 *"필수 5행"* — 새로 붙은 행이 바로 B1 행이라 셈에서 빠지면 1R BLOCKER가 검증 단계에서 다시 샌다 | MAJOR | **수용.** 두 곳을 수치 대신 ***"「동반 변경 판정」의 `필수` 전행"*** 으로 교체(행이 늘어도 어긋나지 않는다) |

**종결 판정 — 「예산 소진 종결」(Step 9)**: 재호출 상한 2회를 **수렴이 아니라 소진**으로 끝냈다. **동일 지적 잔존 0**(M6은 B1의 잔여 구멍이나 지적 내용이 다르다 — B1은 *"축이 동반 변경에 없다"*였고 그 부분은 2R이 해소로 판정했다. M7·M8은 1R 반영 과정에서 드러나거나 생긴 것이다). 마지막 라운드 MAJOR 3건은 **메인이 근거를 실물에서 대조해 전건 수용**했고 기각은 0이다 — M6은 `check-harness-consistency.py:690-692`의 `if not nums: return [], 0`, M7은 대장 3분기 규약(반영은 삭제)과 「앵커는 절대값」 규약, M8은 「동반 변경 판정」 표의 실제 행 수 6으로 각각 확인했다. 라운드를 더 열지 않는 이유는 Step 9가 적은 그대로다 — 그 구간의 수정이 새 결함을 만든 사례가 이 repo에 3회 연속 관측됐고, 배출물이 늘면 대장으로 흘러 순증한다.

## Progress Log

- **T1 완료** — `deferred.md` 296,450B → **149,827B**(49.5%↓). 종결 140건 → `deferred-closed.md`(133,920B), batch 회고 7블록 → `deferred-history.md`(14,965B). 검사기 2축 배선(`check_deferred_stats(ledger, closed)`·`check_batch_number_sequence(hist)`) + 참조처 11곳 정합.
  - **계획 전제 정정 (자체 해소)**: plan이 「회고 11블록 / 매치 11건」이라 적었으나 실측 **7블록 / 매치 6건**. 「계획 결함 — 자체 해소 우선」 3조건 판정 후 실측값으로 진행 — 기준 완화가 아니라 실측 정합이라 carve-out ⓑ 비해당.
  - **자기 유발 결함 1건 즉시 수정 (규칙 4-1)**: 헤더 작성에 PowerShell 큰따옴표를 써서 **마크다운 백틱이 이스케이프 문자로 소비**됐다(`` `r ``→CR · `` `b ``→backspace · 나머지 백틱 전량 유실). quality 리뷰 M1이 CR/BS 2곳을 잡았고, 메인이 **백틱 전량 유실**까지 범위를 넓혀 세 파일 헤더를 Python으로 재작성했다. **교훈: `.md` 생성에 PowerShell 큰따옴표를 쓰지 않는다** — 백틱이 마크다운 인라인 코드와 PowerShell 이스케이프로 충돌한다.
  - 대장 항목 1건 반영 종결(승인받음) — `[2026-08-22]` 크기 임계 신설 판단. 앵커 131→130 / 누계 55→56.

- **T2 완료** — F-6.5 「중복 억제」에 그룹 경계 확장 + 「재확인 표기」에 누적 압축 규약 신설. 대장 중복 1건 병합 흡수(승인) — 앵커 130→129 / 누계 56→57. `deferred.md` 149,441B.
- **T3 완료** — `harness-conventions.md`에 「명령 출력 예산」 절 신설(원칙 + 실측 표 4행 + `--oneline` 미채택 근거) · git 플래그 4곳 교정 · `AGENTS.md` 포인터 1줄.
  - **9,000B 경계 이동 3조건 (규정 요구 — `harness-conventions.md` 「문서 로드 예산 기준선」)**:
    - **ⓐ 바이트 회계** — `pjc-systematic-debugging/SKILL.md` 26,873B → **27,320B**(**+447B**), 354행 → 357행.
    - **ⓑ 경계 재측정** — 9,000B 경계 행 **135 → 137**. **⚠ 이 두 수를 그대로 빼면 안 된다** — 이번 편집이 74~76행에 **+3행을 삽입**해 아래 전체를 밀었으므로, **원 위치로 정규화하면 137 − 3 = 134 < 135로 실질 1행 후퇴**다. 초안은 절대 행 번호만 보고 *"2행 늘었다"*로 오판했고 spec 리뷰 2R M2가 그것을 잡았다.
    - **ⓒ 밀려난 규칙 — 1줄(수용).** 실측: **`- git log에서 비슷한 수정 이력이 있는가?`** 한 줄이 BASE 134행(예산 안)에서 현재 137행(예산 밖)으로 밀렸다. 자명한 질문형 리마인더이고 절차 지시가 아니다.
      - **⚠ 두 리뷰어 판정이 상충해 메인이 코드로 판정했다 (CONFLICT 1회 — Retry Ledger 기록).** spec 2R M2는 *"BASE 135행의 「위키 참조 — 교차 프로젝트 검색」 불릿이 새로 유실됐다"*고 봤고, quality 2R은 *"그 줄은 BASE부터 예산 밖이었다 — 내 1R 서술이 틀렸다"*고 자기 정정했다. **정본은 `check-harness-consistency.py`의 `measure()`이며 `edge = i + 1`이 「초과를 만든 행」이라 경계 행 자체가 예산 밖 첫 행**이다. 그러므로 BASE는 134행까지 안, 135행(위키 참조)부터 밖 — **quality가 맞다.** 실측 대조(BASE 안쪽 집합 − 현재 안쪽 집합)에서 새로 빠진 것은 3줄인데 그중 둘(`git log --since=…`·`git blame <문제의 라인>`)은 **이 task가 교체한 명령 원문**이라 유실이 아니고, 남는 것이 위 1줄이다.
      - **위키 참조 절차는 이번 회차 탓이 아니다** — BASE부터 예산 밖이었다. 되찾으려면 그 줄까지 누적이 10,318B여야 하는데 9,000B를 **1,318B 초과**해, 이번 추가분 447B를 전부 되돌려도 871B가 모자란다. 회수하려면 이 task가 손대지 않은 절을 줄여야 하므로 규칙 4(요청 범위)를 넘는다.
      - **경위**: 초안은 노트가 440B라 경계가 **131행**이었고, 그때는 `### 2-A` **전체**(유사 사례 검색·위키 교차 검색·explorer 팬아웃)가 빠졌다 — quality 1R M1이 그 범위를 실측으로 특정했다(내 초안 판정 *"첫 세 불릿"*은 과소평가였다). 노트를 1줄로 축약해 **6줄 상당 손실 → 1불릿**으로 줄인 것이 이번 회차의 실제 결과다.
    - **교훈 둘**: ① 경계 안쪽 편집은 *"손실을 기록하고 감수한다"*가 아니라 **"손실이 0이 되게 줄인다"가 먼저**다 — 규정이 세 조건을 요구하는 것은 회계를 남기라는 뜻이지 손실을 정당화하라는 뜻이 아니다. ② **경계 이동은 절대 행 번호가 아니라 「삽입 행수를 뺀 정규화 값」으로 판정한다** — 아래에 줄을 넣으면 경계 번호는 저절로 커지므로, 그 증가를 개선으로 읽으면 후퇴를 전진으로 오판한다.
  - **자기 유발 결함 1건 (규칙 4-1 — spec B1이 잡음)**: 위 3조건을 *"Progress Log에 기록했다"*고 보고했으나 **실제로는 기록되지 않았다.** 원인은 append 스크립트가 `... if marker in s else s`로 조용히 통과하는데 `print`는 조건과 무관하게 찍히도록 짜여 **성공으로 오인**한 것이다. **교훈: 파일 편집 스크립트는 「변경 여부」를 판정해 출력한다** — 무조건 찍는 성공 메시지는 검증이 아니다.

- **T4 완료** — 리뷰어 3종 diff 수집에 `--stat` 선행. 설명은 코드펜스 주석(세 파일 동일)과 각 파일의 「검토 효율」·고유 불릿으로 나눠 담았다.
  - **9,000B 경계 3조건**: ⓐ 바이트 회계 — spec-compliance 30,796→31,131B · code-quality 33,399→33,734B ⓑ 경계 재측정 — **둘 다 BASE와 동일**(104 · 79) ⓒ 밀려난 규칙 — 각 1줄:
    - `spec-compliance-reviewer.md`: `| 인터페이스 메서드 추가/변경 | 모든 구현체 |`
    - `code-quality-reviewer.md`: `- 의심 케이스:`
  - **경위**: 초안은 설명 문단을 Step 1/2 본문에 넣어 경계가 4행·3행 밀렸고 **항목 G(Cross-File Caller Impact) 표**·**항목 B(환각 검출) 절차**가 압축 시야에서 빠졌다(quality M2 실측). 설명을 뒤로 옮기고 주석을 축약해 **BASE 경계를 회복**했다 — T3 교훈(손실 0이 먼저)을 이번엔 선제 적용했다.
  - **acceptance 3 위반 1건 (spec B1)**: `spec-prefilter`만 다른 문면을 넣었다가 지적받았다. 근거는 타당했으나 **plan이 acceptance와 Design ③ 양쪽에 「같은 문면」을 명시**했고 carve-out ⓑ(승인된 acceptance 완화)는 자체 해소 불가다. 리뷰어 제안대로 **공통 문면 + 고유 논리 부가**로 바꿔 둘 다 지켰다.
  - **범위 밖 서술 1건 되돌림 (quality M2)**: *"리뷰어 행의 9,000B 경계는 판정 의미가 없다"*를 표 서문에 넣었다가 제거했다 — ① plan이 요구하지 않았고 ② subagent 호출 내부의 auto-compact 발생 여부를 확인하지 않은 단정이며 ③ **그 주장이 T4 자신이 3조건을 면제받을 근거로 읽힌다**. spec은 그 판단을 「정확하다」고 봤으나 **범위·근거 축에서 quality가 옳다**고 메인이 판정했다(CONFLICT 2 — 지점이 달라 Halt 대상 아님).
  - **자기 유발 1건**: `plan.md`가 **LF로 회귀**해 있었다(CRLF 4 / LF 296). 대장 `[2026-08-16]` 항목이 경고한 형태이며 `git diff`로는 안 보인다(gitignore 파일이라 더욱). CRLF로 복구했다.
## Retry Ledger

- **T3 · `[CONFLICT]` 1회** — 지점: `pjc-systematic-debugging/SKILL.md`의 9,000B 경계 이동으로 무엇이 유실됐는가. spec 2R M2(*"위키 참조 불릿이 새로 유실"*) ↔ quality 2R(*"그 줄은 BASE부터 예산 밖 — 1R 자기 정정"*). **메인 판정: quality 채택** — 근거는 `check-harness-consistency.py`의 `measure()`가 `edge = i + 1`(초과를 만든 행)이라 경계 행이 예산 밖 첫 행이라는 것, 그리고 안쪽 집합 차분 실측(새 유실 1줄). 같은 지점 2회가 아니므로 Halt 대상 아님.

## Phase Ledger

<!-- implement-task가 채운다 -->
