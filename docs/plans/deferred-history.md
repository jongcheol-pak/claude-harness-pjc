# Deferred 대장 — 소진 batch 회고

> `docs/plans/deferred.md`에서 분리된 **회차 기록**이다. 소진 batch가 무엇을 어떤 근거로 판정했는지의 서사이며,
> **대기 항목 조회에는 필요하지 않다** — `plan-feature` Step 1은 대장만 열고 이 파일은 열지 않는다.
> 읽는 자리는 둘뿐이다: ① 다음 batch가 **직전 batch가 건너뛴 구간과 그 이유를 승계**할 때(F-6.5 「대장 소진 batch 규약」 ①)
> ② `check-harness-consistency.py`의 **batch 차수 수열** 축이 차수 연속·유일성을 대조할 때.

> **⚠ 각 블록의 「정리 직후 N건 / 현행 M건」은 그 시점의 기록이므로 갱신하지 않는다** — 고치면 과거 판정의 근거가 사라진다.
> 현행 잔량 앵커는 `deferred.md`의 `## 대기` 구간에 있다.

> **13차 판정 (v1.223.2 T1~T4)** — **구간 batch는 미실행이다**(규약 ① 말미). 날짜 구간을 3분류로 판정한 것이 아니라 `## 대기` **157건 전건을 문면 스캔**해 사용자가 지정한 4기준(① 착수 전제 붕괴 ② 이미 해소 ③ 외부 조건 대기 ④ 조치 없는 관측 기록) 후보를 뽑고 걸린 것만 전문 판정했다 — **v1.185.0 T2(게이트 소급 적용)와 같은 형태**이며, 다음 세션이 어느 날짜 구간을 처리된 것으로 오인하지 않게 명시한다. **착수 축은 셋 다 미발동이었고 트리거는 사용자 요청이다** — 착수 시점 실측으로 최고령 `2026-08-19`(15일) · 순증분 7 · 직전 batch(12차) 경과 5일로 ⓐⓑⓒ 전부 미달이었다(대장 `[2026-09-02]` 항목의 정정이 같은 값을 도출해 두었다).
>
> **결과: 스캔 157 → 기각 4 / 반영 0 / 유지 153.** 4기준별 환원 — ① 전제 붕괴 1 · ③ 외부 사건 트리거 1 · ④+③ 「이미 판단이 내려진 것」 2 · ② 이미 해소 **0건**(반영 종결이 0인 이유는 아래). 기각 4건은 전부 규약 ② ⚠의 기각 주력이다: `[2026-08-25]` **plan.md ↔ docs/plans 분할 사본 동기 축**(전제 소멸 — 분할 plan 자체가 폐지됐다. `plan-feature/SKILL.md:338`·`AGENTS.md:88` · `docs/plans/`에 남은 것은 대장 3파일뿐으로 동기할 `partN` 사본이 **0건** — 실측) · `[2026-09-02]` **구현 세션 압축 주입에도 큐 기록 규약**(본문이 *"관측되면 착수한다(외부 사건 트리거)"*로 스스로 적는다) · `[2026-08-28]` **hooks.json SessionEnd `resume` 값 정리**(*"매치 안 되면 무해 — 동작 결함 아님"*·*"단독 정리는 비경제"*로 결론이 났고 처리를 *"다음 hooks 회차 동반"*으로 넘겼다) · `[2026-07-13]` **`secret-patterns.ps1` 탐지 정밀도**(ⓐⓒⓓ는 v1.182.0 T1이 닫았고 남은 ⓑ는 `secret-patterns.ps1:49-52`가 *"트레이드오프(의도)"*로 못 박은 것이며, 항목 자신이 *"다시 「미탐 결함」으로 올리지 않도록 여기 못 박는다"*고 결론을 확정했다).
>
> **반영 종결이 0건인 것은 이 판정 형태의 귀결이다** — 기준 ②(이미 해소)은 항목마다 대응 코드·문서를 대조해야 확정되는데, 전건 스캔은 **문면**을 보는 방식이라 해소 여부를 실증하는 데 닿지 못한다. 실제로 해소가 의심돼 대조한 2건은 둘 다 **미해소**로 확인됐다 — `[2026-08-12]` Phase F 커밋 스테이징(`phase-f-detail.md:88`이 여전히 `git add` 없이 `--allow-empty`) · `[2026-08-25]` 분할 사본(해소가 아니라 **전제 소멸**이라 기각으로 갔다). 해소분 발굴은 구간 batch가 전건을 코드와 대조할 때 나오는 산물이고, 그것이 **이 형태가 구간 batch를 대체하지 않는 이유**다.
>
> **규약 ② ⚠의 「관측 경로 없음」 예외로 유지한 것은 3건**(전건): `[2026-07-08]` block-destructive heredoc 본문 미스캔 · `[2026-07-08]` block-destructive `-Include *.tmp,*` catch-all(5차가 기각했다가 F-7 지적으로 복귀시킨 항목이고 그 오판이 이 예외를 명문화한 계기다) · `[2026-08-07]` 포인터 도달성 축의 미탐 2종. 셋 다 **차단·검출의 미탐**이라 위험이 조용히 통과할 뿐 아무도 신고하지 않아 *"관측되면 재등재된다"*는 전제가 성립하지 않는다.
>
> **「대장 항목을 등록일로 참조하는 외부 문서」 축으로 유지 고정한 것이 65건이다** — 이 판정에서 새로 도입한 축이며, 종결 후보의 등록일이 그 집합에 들면 **전문 판정 없이 유지**했다. 참조는 두 층이다: **ⓐ 외부 21곳**(`docs/harness-conventions.md` 5 · `docs/golden-runner.md` · `docs/prd.md` 3 · `docs/prds/` 1 · `check-harness-consistency.py` 2(주석) · `session-context.ps1` 2 · hook 골든 시나리오 2 · `phase-f-detail.md` 3 · `implement-task/SKILL.md` 2) **ⓑ 대장 자신의 내부 교차 참조**(등록일 형태 11건 + `위 「…」`·`아래 「…」` 제목 형태 7건 — 제목 형태는 등록일 조건에 아예 걸리지 않아 별도로 훑는다). **역참조된 등록일 16종**: `2026-07-09 · 07-15 · 07-24 · 07-25 · 07-28 · 08-03 · 08-04 · 08-12 · 08-15 · 08-16 · 08-17 · 08-19 · 08-20 · 08-21 · 08-22 · 08-26`(전건 열거를 65줄로 늘리지 않고 **재도출 가능한 형태**로 남긴다 — 이 집합으로 grep하면 같은 65건이 나온다). **같은 사고의 선례**: `deferred-closed.md:26`이 *"종전 문면의 「위 ⓐ」는 그 항목이 `## 종결`로 이동해 가리킬 대상을 잃었다 — 2026-08-03 정정"*을 기록한다. **참조 문면을 함께 고치는 경로는 이번에 배제했다** — 그 파일들이 어느 task의 `Files`에도 없어 자율 루프가 무검증으로 코드를 고치게 되기 때문이다.
>
> **⚠ 다음 batch로 가는 승계 신호 (반드시 읽을 것)** — **유지 153건 중 스탬프를 받은 것은 33건뿐이다.** 이 판정은 「전건 스캔 + 후보만 전문 판정」이라(사용자 선택) **판정하지 않고 넘긴 항목에는 재확인 표기를 달지 않았다** — 규약이 *"대장 비대화의 주 원인"*으로 지목한 스탬프 100여 건을 만들지 않기 위해서다. 그 결과 **날짜 축은 대부분 갱신되지 않았다.** 다만 **판정일 최솟값 항목 전건(2026-08-19 · 22건)에는 후보로 걸리지 않아도 스탬프를 달았고**, 그래서 최고령이 **2026-08-19 → 2026-08-24**(51건)로 올라가 축 ②가 열리는 날이 `2026-09-18` → **`2026-09-23`**이 됐다. **다음 batch는 이번에 판정한 자리를 다시 열지 말 것** — 열 곳은 ① 위 65건(역참조로 전문 판정을 건너뛴 것) ② 최고령 `2026-08-24` 구간이다. 11차(v1.195.0)가 직전 batch의 판단을 승계하지 못해 소득 0으로 끝난 선례가 이 신호를 적는 이유다.
>
> **정리 직후 153건 / 현행 154건** — 「정리 직후」는 batch 판정분(대기 −4 · 종결 +4)만 반영한 값이고, 「현행」은 같은 세션 F-6.5의 신규 등재 1건(병합 후보 관측)까지 반영한 값이다(규약 ⑥ 분리 표기). 삭제 누계는 **불변(86)**이다 — 이번 종결이 전부 **기각(이동)**이라 삭제가 0건이기 때문이다. 불변식은 등재 후 `154 + 152 + 86 == 392`로 성립한다.

> **12차 batch (v1.212.0 T4)** — 구간 `2026-08-17 ~ 2026-08-23` **40건 전건 판정**(착수 시 재계수로 확정 — 계획 전제 40과 일치). 결과: **유지 30 / 기각 5 / 병합 4 / 반영 1**. **구간 선택은 11차의 실패를 승계해 정했다** — 11차(v1.195.0)가 `2026-07-08 ~ 07-30`을 다시 열어 **소득 0**이었고 그 기록이 *"규약 ①만 읽고 10차의 판단을 승계하지 못했다"*를 남겼으므로, 이번에는 처리된 자리(6차 07-08~07-25 · 7차 08-05~08-09 · 9차 07-08~07-30 · 10차 08-03~08-04, 08-12~08-16 · 11차 07-08~07-30)와 **게이트 소급 적용(v1.185.0 T2 — 날짜 구간이 아니라 `## 대기` 전건 스캔이라 구간 승계 대상이 아니다)**을 먼저 열거하고 **실질 미처리 구간 하나**만 열었다. **틈으로 보이는 `07-31~08-02`·`08-10~08-11`은 대기 항목이 각 0건**이라 열 대상이 없다(등록일 계수 실측).
>
> **기각 5건은 전부 규약 ②의 기각 주력이다** — 외부 사건 트리거 3(`자격증명 쌍 알고리즘 식별자`는 본문이 *"착수 조건은 **관측**이다"* + 실재 차단 0건 · `feat-llm-wiki 84.5%`는 임박 도달이 곧 새 등재 시점 · `_decision_body_span 멀티라인`은 *"실제 위반이 관측되면"*) · 액션 없음이 본문의 결론 1(`커널에 갇힌 find.exe` — *"재부팅 외에 수단이 없어 **코드가 할 일은 없다**"*) · 판정 유보 + 결론 실재 1(`budget_split 재판정 마진` — *"이번 변경이 만든 결함이 아니라 **고정된 판정**"*). **병합 4건은 착수 단위를 만든 것이다** — 축 ⑫(개념 정본 유일성) 관련 3건(대상 개념 확대 · 정본 내부 완전 중복 미탐 · AGENTS ↔ 위키 경계 등재)을 「리포트 전용 승격」에 흡수했다. 넷 다 **면제 입도를 ⑪처럼 조각 단위로 바꿀지**라는 하나의 선행 판정에 걸려 따로 착수할 수 없기 때문이다. `check_consistency.py` 축 신설 2건(§7-16 대조 · recipe 이중 등재)도 같은 파싱 설계를 공유해 하나로 합쳤다. **반영 1건**(`리뷰 판정문을 받기 전 외부 승인 금지`)은 항목 자신이 *"(implement-task/SKILL.md 권한 경계에 규정 반영됨)"*이라 적어 대응 지점이 이미 특정돼 있었다.
>
> **유지 30건의 스탬프에는 항목별로 다른 근거를 적었다** — 대장 `[2026-08-12]`가 요구한 *"조건 없이 지금 착수할 수 있는 액션인가"*의 답을 1구로 담았고, 보일러플레이트 재확인을 피했다. **그중 셋은 이번 회차가 실제로 표본을 더했다**: ⓐ `gitignore된 plan.md의 기록은 리뷰어가 diff로 검증할 수 없다`가 **T1 spec 1R에서 재발현**했다(acceptance ③을 BLOCKER로 냈고 리뷰어가 파일을 직접 읽어 2R에서 닫았다 — 처방 ⓐ·ⓑ 모두 미착수라 다음 회차도 같은 왕복을 한다) ⓑ `리뷰어 판정문 절단의 원인 확정`에 sonnet 8회 전건 판정문 수신·예산 최대 20/32가 더해졌다(haiku 호출이 없어 모델 가설은 가르지 못했다) ⓒ `리뷰 subagent가 골든 러너를 각자 띄워`의 처방 ⓐ가 5회 호출 전건에서 실효를 보였다(경합 재발 0 — 남은 착수 단위는 ⓑ 러너 lock 하나).
>
> **착수 축은 ⓐ(잔량 + 신규 등재분)다** — 대기 150 > 100 **AND** 직전 batch(11차 = 2026-08-24) 이후 등록일 기준 신규 등재 **31건 ≥ 30**(08-24 1 · 08-25 12 · 08-26 6 · 08-27 6 · 08-28 6). **ⓒ 절대 상한 축은 미달이다** — 150 > 130이나 직전 batch로부터 4일 < 14일. **정리 직후 140건 / 현행 140건** — batch 판정분(대기 −10 · 종결 +5 · 정리 삭제 누계 +5)만 반영한 값이고, 이 회차의 신규 등재분은 T6이 더한다(값 사슬 — T4는 자기 몫만 반영한다).

> **11차 batch (v1.195.0 T3)** — 구간 `2026-07-08 ~ 2026-07-30` **27건 전건 판정**(착수 시 재계수로 확정 — 계획 전제 27과 일치). 결과: **유지 27 / 종결 0 / 기각 0 / 병합 0**. **소득 0의 원인은 구간 선택이며, 그것이 이 batch의 관측이다** — 같은 구간을 **9차 batch(v1.193.0, 2026-08-23)가 이미 전건 판정**했고, **10차 기록(v1.194.0 T9)이 그 자리를 건너뛴 이유를 명시**해 두었다(*"규약 ①의 「가장 오래된 등록일부터」를 그대로 적용하면 어제 재확인한 27건을 다시 판정하게 된다"*). 이번 T3는 **규약 ①만 읽고 그 판단을 승계하지 못해** 같은 구간을 다시 열었다 — 규약 ①에는 「직전 batch가 건너뛴 구간 판단을 승계한다」는 문면이 없고, 승계 신호가 **batch 기록 본문에만** 있어 규약만 보는 경로에서는 닿지 않는다. **착수 축은 절대 상한(대기 131 > 130)이고 이는 2회 연속이다** — 10차도 같은 축(대기 135 > 130)으로 열렸다. 경과는 9차 정리 직후 **129** → v1.194.0 회차 등재로 **135** → 10차 batch −4로 **131** → 이번 착수다. 10차가 *"열린 구간에 소진할 것이 남아 있는지는 보지 않는다"*로 적은 관측을 이번이 **재현으로 보강**했고, 「직전 판정 이후 경과」·「구간 선택 승계」 두 축을 묶어 `### 대장 운영`에 등재했다. **정리 직후 131건 / 현행 133건** — batch 자신의 종결이 0이라 정리 효과가 없고, 이 회차의 등재 3건(T2 미이관 이관 1 + 이 batch 관측 1 + F-7 관측 「차수 기계 검사 부재」 1)과 T2의 반영 삭제 1건이 반영돼 133이다.
>
> **6차 batch (v1.169.0 T3)** — 구간 `2026-07-08 ~ 2026-07-25` **29건 전건 판정**. 결과: **유지 28 / 기각 1 / 재수용 0 / 병합 0**. 종결이 1건에 그친 이유는 그 구간이 **6일 전(2026-08-06) 5차 batch에서 이미 전건 재확인·병합을 마쳤고** v1.169.0 변경으로 바뀐 전제가 없기 때문이다 — 이 관측 자체를 「소진 batch 착수 조건이 '판정 밀림'과 '착수 밀림'을 구분하지 못한다」로 `### 대장 운영`에 등재했다. 기각 1건(`lint.py` 불릿 판별 오탐)은 **초안에서 유지로 처리했다가 V-5 리뷰 지적으로 바로잡은 것**이다 — 규약 ②의 ⚠ 예외(조용한 실패)를 오탐에까지 확장해 읽으면 *"관측되면"* 조건부 항목이 영구히 남는다. **정리 직후 104건 / 현행 107건** — 착수 시점 105건에서 batch 자신의 기각 1건이 빠져 104건이고, 같은 세션의 신규 등재 4건(T3 batch 중 1건 + F-6.5가 F-7 지적으로 등재한 2건 + F-2 골든 조기 종료 1건)이 반영돼 현행 108건이다. **두 연산을 "상쇄"로 뭉쳐 적으면 batch 고유의 −1 효과가 지워져** 다음 세션이 이 batch가 실제로 무엇을 종결했는지 이 줄만으로 재구성할 수 없다(규약 ⑥이 경계하는 "정리 직후 값만 적고 등재분 미반영"의 반대 방향 오류).
> **7차 batch (v1.181.0 T1)** — 구간 `2026-08-05 ~ 2026-08-09` **38건 전건 판정**(착수 시 재계수로 확정 — 계획 전제와 일치). 결과: **유지 27 / 기각 6 / 반영 2 / 병합 3**. 여기에 **구간 밖 1건**(「볼드 마커 홀수 줄 3개」 오탐 기각)을 더해 이 batch의 종결 이동은 기각 7건이다 — **구간 소진율은 38건 기준이고 구간 밖 처리는 모수에 넣지 않는다**(3차 batch가 세운 관례). **이번 batch의 성과는 「판정이 이미 끝난 것」을 걷어낸 데 있다** — 기각 6건 중 3건(`증분 재리뷰 기계화`·`references 이관 기계화`·`[CONFLICT]` 자기 인지)이 *"오판이 관측되면"* 조건부인데 그 오판들은 **리뷰 결과로 즉시 드러나** 조용하지 않다(규약 ② 기각 주력). 나머지는 전제 반증 1건, 정본 확립으로 중복이 된 추적 항목 1건, 이번 batch가 그 판단을 수행해 답이 나온 것 1건이다. **반영 2건은 삭제 전에 대응 지점을 지목했다** — ①「acceptance에 "특정 표현·리터럴의 부재"를 조건으로 걸면」은 그 항목이 제안한 *"acceptance를 판정 축으로 쓰는 관행"*이 `plugins/pjc/skills/plan-feature/SKILL.md:103`에 *"대상이 애매하면 안전하게 **판정 축**(그 기준을 채택했는가 · 그 값이 보존됐는가)으로 쓴다"*로 실재한다(절대 규칙 8) ②「2차 batch 이력 블록의 규약 재서술」은 그 항목이 *"다음 batch에서 각주를 붙일지 판단한다"*고 적은 대로 **이번 batch가 그 블록에 각주를 부착**했다(아래 2차 정리 블록). **두 건 다 「반영」이라 항목은 삭제되지만 근거는 여기와 삭제 커밋 본문에 남는다**(규약 ③ — 삭제되는 항목도 근거 의무를 면제받지 않는다). **병합 3건은 항목 자신이 *"같은 축이라 함께 판단"*이라 적어 둔 것**이라 흡수 판정에 재량이 들어가지 않았다. **유지 27건의 스탬프는 `[2026-08-12]` 항목이 요구한 형식을 따랐다** — *"조건 없이 지금 착수할 수 있는 액션인가"*의 답을 1구로 담아, 보일러플레이트 재확인이 아니라 판정 기록이 되게 했다. **정리 직후 149건 / 현행 134건**(규약 ⑥ 분리 표기 — 두 값이 벌어진 것은 같은 회차의 **다른 task들이 각자 해소분을 지웠기** 때문이다. batch 자신의 −12는 「정리 직후」가 담고, T2~T8의 해소 종결 −16과 F-6.5 신규 등재 +1이 「현행」에 반영됐다. **두 연산을 상쇄로 뭉쳐 적으면 batch 고유의 효과가 지워진다** — 6차 batch 기록이 경계한 그 오류다).
> **8차 판정 (v1.188.0 T3) — 구간 batch는 미실행이다.** 착수 조건 세 축을 다시 재니 **전부 미달**이라 구간을 열지 않았다(규약 ① 말미 — 다음 세션이 어느 구간을 이미 처리한 것으로 오인하지 않게 명시한다). **ⓐ 잔량 128 > 100이나 순증분이 미달**: raw −21(직전 batch 「정리 직후 149」 → 현재 128)은 음수라 정의상 「기대보다 낮은」 값이므로 규약 ⓪의 보정을 수행했다 — 직전 batch(v1.181.0 = 2026-08-19) 이후 개별 종결로 대기에서 빠진 29건(7차 회차 다른 task −16 · v1.185.0 T2 게이트 소급 기각 이동 −13)을 더해 **보정 순증분 8**이고, 등록일 기준 직접 측정(최대 15건)과도 30 미만으로 일관한다. **ⓑ 판정일 최솟값 2026-08-06**(15일). **ⓒ 128 < 130**. **이 회차의 출발점은 그 ⓑ가 잘못 열렸다는 것이었다** — 판정일 도출 규칙이 `(YYYY-MM-DD 재확인: 유지 …)` 표기만 인정해, 날짜 접두에 부기가 붙은 형식의 실제 판정일을 읽지 못했다. 그래서 **어제(v1.182.0 = 2026-08-20) 판정된 2건이 39일 방치로 잡혀** batch 착수 조건이 성립한 것처럼 보였다. 규약 ⓪이 막으려던 *"정리했는데 또 열린다"*가 **다른 경로로 재현된 것**이라, batch를 여는 대신 T1이 그 규칙을 「마지막으로 판정된 날」 의미 기준으로 고치고 T2가 복제 쌍 동기를 기계 축으로 고정했다. **부기 형식 3건은 전건 형식 교정**했다(부기를 제목 뒤로 — v1.185.0 T2가 세운 관례). 그중 「시크릿 탐지 오탐의 재귀 구조」는 **정식 재확인 표기를 이미 보유**해 새 표기를 부여하지 않았다 — v1.147.0(2026-07-30)로 찍으면 기존 2026-08-06보다 과거라 **판정일이 퇴행**해 이 회차가 고치려던 오판을 반대 방향으로 재현한다. 교정 전후로 **대기 판정일 최솟값은 2026-08-06으로 불변**이고 계수(대기 128)도 움직이지 않았다. **정리 직후 127건 / 현행 127건** — batch 판정이 아니라 T2가 해소한 항목 1건(「복제 쌍에 동기 안전장치가 없다」)을 **반영 종결(삭제)**한 것이 유일한 증감이다 — 대응 지점은 `plugins/pjc/evals/check-harness-consistency.py:215`(`check_batch_trigger_sync()` 신설)과 `:729`(axes 맨 뒤 등록)이고, 삭제 커밋 본문에도 같은 지목을 남겼다(규약 ③).
>
> **9차 batch (v1.193.0 T11)** — 구간 `2026-07-08 ~ 2026-07-30` **31건 전건 판정**(착수 시 재계수로 확정). 결과: **유지 27 / 병합 4 / 기각 0 / 반영 0**. **이 구간은 직전 8차 판정(v1.188.0)이 형식 교정만 하고 지나간 자리**라 전건에 이미 재확인 표기가 있었고, 그래서 이번 판정의 값은 「유효한가」가 아니라 **「착수 단위를 만들 수 있는가」**에 있었다. **병합 4건이 그 산물이다** — vault 세션에서만 수행 가능한 3건(wiki-schema §12 판정 문장 · 위키 오독 문장 ① · feature 136개 축소)을 「OKF v0.2 미러 갱신」에 흡수하고, rationale 분리 확장을 「이관 임계치」에 흡수했다. 흡수 근거는 **각 항목이 스스로 적어 둔 것**이다(*"같은 위키 세션에서 묶어 처리 가능"*·*"확장 여부 결정"*) — 흡수 판정에 재량이 들어가지 않았다. **기각 0의 이유**: 외부 사건 트리거로 걸린 2건(`-Include *.tmp,*` catch-all · 축약구 제거 D3 재검토)은 **둘 다 이미 유지로 판정된 선례**가 있다 — 전자는 *"차단 hook의 미탐이라 관측 전제가 성립하지 않는다"*(규약 ② ⚠ 예외), 후자는 *"D3 결정을 되돌릴 유일한 안전판"*이라 종결하면 근거가 사라진다. **전제 반증 1건을 실측으로 잡았다** — 「나머지 7개 프로젝트 위키 이전」의 7건 중 **2건이 이미 등록돼 있었다**(`ProjectDashboard`·`DevDashboard-WinUI`). 남은 5개로 목록을 정정했고, v1.193.0의 자동 등록이 각 프로젝트의 다음 계획 세션에서 일부를 처리한다. **정리 직후 129건 / 현행 129건** — 이 회차의 다른 task는 대장을 건드리지 않았다.
>
> **10차 batch (v1.194.0 T9)** — 구간 `2026-08-03 ~ 2026-08-04` + `2026-08-12 ~ 2026-08-16` **34건 전건 판정**(착수 시 재계수로 확정 — 10 + 24). 결과: **유지 30 / 기각 3 / 반영 1 / 병합 0**. **구간이 연속 날짜가 아닌 것은 의도다** — 7차(`2026-08-05 ~ 08-09`)와 9차(`2026-07-08 ~ 07-30`)가 이미 처리한 자리를 건너뛰고 **실질 미처리 구간**만 이었다(규약 ①의 「가장 오래된 등록일부터」를 그대로 적용하면 어제 재확인한 27건을 다시 판정하게 된다). 착수 축은 **ⓒ 절대 상한**이다(대기 135 > 130 — ⓐ는 순증분 미달, ⓑ는 9차가 08-23에 최고령 구간을 재확인해 미달). **반영 1건**: `[2026-08-03]` `sed -i` CRLF 손상 — 대책 ⓐ(md는 Edit 도구 우선)가 `AGENTS.md:55`에 실재한다. **기각 3건**은 전부 규약 ②의 기각 주력이다 — 기록성 관측 1(`budget_split` 예산 소비) · 외부 사건 트리거 2(`bibi/decisions.md` 2건은 *"다음 BIBI ingest가 판정"*, `session-context.ps1` 중첩은 *"다음에 크게 손볼 때"* + *"사용처 1곳이라 문턱 미달"*). **유지 30건에는 항목별로 다른 근거를 적었다** — `[2026-08-12]` 「유지 스탬프에 착수 가능성 판정을 요구하는 형식 규정」이 지적한 **동일 문면 스탬프**를 피한 것이고, 그 항목 자신은 명문화 미착수라 유지로 남는다. **정리 직후 131건 / 현행 131건**(이 회차의 신규 등재분은 F-6.5가 반영한다). **소진 효과가 4건에 그친 이유**: 이 구간은 08-19(7차)·08-20(게이트 소급 적용)에 이미 훑여 조건 없는 액션만 남아 있었다. **절대 상한 축은 잔량을 근거로 batch를 열지만, 열린 구간에 소진할 것이 남아 있는지는 보지 않는다** — 그래서 131건으로 여전히 상한 위다.
>
> **게이트 소급 적용 (v1.185.0 T2)** — **구간 batch는 미실행**이다(규약 ① 말미). 이번 회차는 날짜 구간을 3분류로 판정한 것이 아니라, T1이 신설한 **등재 게이트 ⓐ·ⓓ·ⓔ를 `## 대기` 전건에 소급 적용**한 것이다 — 다음 세션이 어느 날짜 구간을 이미 처리한 것으로 오인하지 않게 명시한다. **전건 138건 스캔 → 후보 34건 전문 판정 → 기각 이동 13건**(ⓓ 8 · ⓐ 4 · ⓔ 1). **삭제가 아니라 `## 종결`로 이동**했으므로 재확인 이력·병합 이력을 포함한 본문이 그대로 보존된다. **정리 직후 125건**(= T3의 절대 상한 도출 입력). **보존 규칙 발동 8건은 전건 열거한다** — **ⓓ 예외(관측 경로 없음) 3건**: `[2026-07-08]` block-destructive catch-all(차단 hook 미탐) · `[2026-08-03]` 규칙 4-1 커버리지 경계(기록조차 안 된 것은 어느 게이트에도 안 걸린다) · `[2026-08-06]` 포인터 도달성 미탐 2종(재확인이 이미 명기). **보존 규칙 ⓑ(판정이 갈림) 5건**: `[2026-08-03]` 동반 변경 판정 형식화(관측 경로가 반쪽 — 리뷰어가 근거의 존재는 잡고 참됨은 못 잡는다) · `[2026-07-30]` 축약구 제거 재검토 조건(종결로 옮기면 Step 1 조회에서 사라지는데 그 항목이 되돌림의 유일한 안전판이다) · `[2026-08-12]` Phase F 커밋 스테이징(대상이 특정되고 회피만 했을 뿐 규정은 안 고쳐졌다) · `[2026-08-19]` `git add -A` 이중 계산(*"결함이 아니라 낭비"* 는 심각도 판정이지 착수 보류 결론이 아니다) · `[2026-08-04]` tail 상한 6000(*"재평가할 여지"* 가 액션 함의로 읽힌다). **형식 이상 1건을 교정했다** — `[2026-08-02, **v1.154.0에서 범위 축소**]`처럼 **날짜 뒤에 부기가 붙은 항목**은 종결 패턴(`[등록일 → 종결일]`)에 맞출 수 없어 부기를 제목 뒤로 옮겼다. 「카운트 기준」이 경고한 그 형태이며, **같은 형태가 다시 생기면 이동 시 이 교정이 필요하다**.
>
