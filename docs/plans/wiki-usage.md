# 위키 사용 기록

> 회차마다 계획 단계의 위키 참조가 **무엇을 읽고 · 계획과 구현 중 어느 자리로 가고 · 각 시점에서 실제로 걸렸는가**를 1줄로 남기는 기록이다. `pjc:implement` 가 완료 시점에 append 한다(절차·수치 정의의 정본은 `plugins/pjc/skills/WIKI.md` 「회차별 사용 기록」).
> 왜 이 파일인가 — `plan.md` 는 gitignore 라 회차가 끝나면 그 Investigation Log 가 사라져, 위키 읽기가 도움이 되는지 회차를 넘겨 셀 자리가 없었다(2026-09-17 감사: 36회차 intent 어디에도 위키 참조 기록 0건).
> 행 형식: `- [YYYY-MM-DD] <intent 슬러그> · <성격> · 읽음 N절 · [계획] 반영 M1/걸림 K1 · [구현] 배분 M2/걸림 K2 · 폴백 J절 · 미반영: <절 이름|없음> · 없어서조사: <건수> — <비고 1구>`
> **성격**은 `조사`/`구현`/`디버깅`/`규약` 중 하나다(2026-09-19 신설). **없어서조사**는 위키에서 찾았는데 없어 코드·외부를 새로 조사한 건수다(같은 날 신설).
> 읽는 법 — **비율(`(M1+M2)/N`)은 2026-09-19 에 폐기했다.** N 이 분모라 **적게 읽으면 기계적으로 오르고**, 7절만 읽으면 100% 가 나오는데 그것이 개선인지 유실인지 구분되지 않는다(Goodhart). **목표는 「K 를 유지하며 N 을 줄이는 것」이고 K 절대값과 N 을 따로 본다.** **그리고 같은 성격 태그끼리만 견준다** — 조사 회차의 N=50 과 구현 회차의 N=7 은 다른 것을 재는 수다(2026-09-19 실측: N≥40 인 3행이 전부 `promoted-duplicate-*` 조사 계열이었고, 그것을 모르고 보면 「위키 구조가 나쁘다」로 오독된다). **K1 과 K2 는 갈라 본다** — K2 만 0이면 구현 트랙의 배분이 헛돈 것이고, **둘 다 0이 이어질 때만** 위키 읽기 자체가 값을 못 내는 것이다. **「미반영」 필드를 담은 행이 5개 쌓이면** 읽기 대상 축소를 판정한다. 그 판정은 Deferred 대장의 대기 항목으로 등재돼 있다. **뒤처짐 임계 조정(감사 선택지 3)은 2026-09-19 에 기각됐다** — 「회차」의 기계 경계가 이 레포 `intent/` 뿐이라 타 레포에서 셀 수단이 없고, 관측된 오경보의 원인이 임계가 아니라 hook 의 분할 조각 오식별이었다.
> **신설 필드(「미반영」·「성격」·「없어서조사」)는 그 이전 행에 소급하지 않는다** — 그 값은 각 회차의 `plan.md` Investigation Log 에만 있었고 그 파일은 gitignore 라 이미 사라졌다. 없는 것을 지금 복원하면 원 회차 실행자의 판정이 아니라 추측이 된다.
> **첫 행만 4수치 시기에 쓰였고 2026-09-17 에 6수치로 소급 변환했다** — 그때의 지표는 걸림을 「실행에서」로만 세어, 계획 단계에서 acceptance 설계를 바꾼 기여가 0으로 집계됐다. 변환은 기록된 N·J 를 그대로 두고 M·K 만 두 트랙으로 쪼갠 것이고, 근거는 그 회차 `plan.md` 의 Investigation Log 다(`[소급]` 표기가 그 행에 붙는다 — 원 회차 실행자의 판정이 아니다).
> 이 파일에 체크박스(`- [ ]`)를 두지 않는다 — `guard-write` 가 `docs/plans/*.md` 의 체크박스를 plan 으로 본다.

## 기록

- [2026-09-17] wiki-read-gate-and-usage-ledger · 읽음 14절 · [계획] 반영 2/걸림 2 · [구현] 배분 2/걸림 0 · 폴백 0절 — `[소급]` K2 0: 구현 배분 2건이 실행 경로에 없었다(실제 걸린 2건은 레포 문서 출처) · K1 2: acceptance 착수값 병기와 계수 방식이 그 함정 때문에 달라졌다
- [2026-09-17] wiki-circuit-two-tracks · 읽음 14절 · [계획] 반영 2/걸림 2 · [구현] 배분 2/걸림 2 · 폴백 0절 — 양 트랙 모두 실제로 걸렸다(구현: 병행 세션이 `pending.md` 에 넣어 둔 2건을 경로 한정 커밋이 막았다 · vault 워킹트리 선확인이 그것을 착수 전에 드러냈다)

- [2026-09-18] orphan-process-prevention · 읽음 9절 · [계획] 반영 5/걸림 5 · [구현] 배분 1/걸림 0 · 폴백 0절 — K2 0: 구현 배분 1건(`conventions.md` 절 2 — `-replace` 그룹 참조·두 셸 대조)이 실행 경로에 없었다(편집을 Edit·python 으로 해 `-replace` 를 쓰지 않았다). 실제로 걸린 둘은 위키 밖 출처다 — `AGENTS.md` 의 BOM·CRLF 규약, 그리고 Edit 이 dot-source 1줄을 함께 삼킨 사고. K1 5: 미완주 분기 재현 절차가 T1·T2 실증 설계를, `grep -o | wc -l` 이 acceptance 계수 형식을, 923 기준선이 G3 을, 「red 실험과 전체 스위트를 겹치지 말 것」이 T5 의 순서를 정했다

- [2026-09-18] skill-defects-1-5 · 읽음 11절 · [계획] 반영 4/걸림 4 · [구현] 배분 2/걸림 1 · 폴백 0절 — K2 1: 배분 2건 중 편집 도구의 LF 화만 실행에서 걸렸다(병행 세션 커밋은 staged 0이라 만나지 않았다)

- [2026-09-18] skill-defects-6-18 · 읽음 9절 · [계획] 반영 4/걸림 4 · [구현] 배분 2/걸림 1 · 폴백 0절 — K2 1: 배분 2건 중 편집 도구의 LF 화만 실행에서 걸렸다(병행 세션 커밋은 staged 0)

- [2026-09-18] skill-deletion-candidates · 읽음 13절 · [계획] 반영 3/걸림 3 · [구현] 배분 3/걸림 0 · 폴백 0절 — K2 0: 배분 3건(편집 도구의 LF 화 · 스크립트 편집 함정 · 병행 세션 커밋) 중 실행에서 만난 것이 없다(Edit 이 CRLF 를 보존해 되돌릴 것이 없었고, 병행 세션 staged 는 0이었다). K1 3: 교대 패턴 금지가 G1~G3 앵커 계수 방식을, acceptance 착수값 병기가 앵커 35개 전수 실행을, decisions.md 의 의도적 이중화 승격이 Out of Scope 의 「멈추는 넷」 유지를 정했다

- [2026-09-18] critical-literal-axis · 읽음 16절 · [계획] 반영 5/걸림 4 · [구현] 배분 2/걸림 2 · 폴백 1절 — K1 4: 「검사기의 서술 자리도 검사 대상인가」가 T3 를 독립 task 로 세웠고, 「검증 케이스의 축 분리」가 케이스 3건을 한 축씩으로 갈랐고, 자기참조 크기 표의 순환이 T3 acceptance 를 「수렴까지 재측정」으로 쓰게 했고, decisions.md 의 축 §7-37 번복(골든 PASS 만으로는 미검출을 못 거른다)이 G1 을 골든이 아니라 실 레포 실행에 걸었다. K2 2: python 텍스트 쓰기의 전면 LF 화가 red 확인 스크립트에 개행 보존 인자를 쓰게 했고 매 편집 뒤 PowerShell CRLF 복원을 강제했다. 폴백 1: feat-consistency-checkers.md 는 위키 뒤처짐(45커밋) 통지 때문에 지도로도 쓰지 않고 코드를 1차 출처로 읽었다

- [2026-09-19] budget-scaffold-exclusion · 읽음 7절 · [계획] 반영 1/걸림 1 · [구현] 배분 4/걸림 3 · 폴백 0절 — K1 1: 「예산·임계는 문자 수이고 `wc -c` 바이트로 읽으면 판정이 뒤집힌다」가 이 plan 의 전 수치를 python `len()` 측정으로 고정했다. K2 3: 편집 도구의 전면 LF 화가 매 편집 뒤 PowerShell CRLF 복원을 강제했고, 축 ⑪ 의 「넘」 오탐이 새 주석·rationale 3곳을 화이트리스트 대신 문면 수정으로 닫게 했고, 「검증 케이스의 축 분리」가 골든 2건을 비계 제외와 포인터 크기로 갈랐다. 배분 4건 중 「처방 스크립트 전 체크포인트 커밋」은 vault 워킹트리가 clean 이라 만나지 않았다
- [2026-09-19] wiki-usage-split-guard-marker · 읽음 7절 · [계획] 반영 3/걸림 2 · [구현] 배분 4/걸림 2 · 폴백 0절 · 미반영: 없음 — K2 2: 「골든 러너가 도는 동안 대상 파일을 편집하지 않는다」가 red 실험을 부분 실행으로 떼어 놓게 했고, vault 착수 전 `git status --porcelain` 이 남의 미커밋 변경(`pending.md`)을 실제로 드러내 커밋을 섞지 않게 했다. K1 2: 「새 축에는 red 실증이 붙는다」가 T3·T5 acceptance 를 양방향으로 고쳐 쓰게 했고, 「`updated`↔`synced_commit` 은 다른 축」이 T4 문면의 근거를 「부모와 맞춘다」에서 「이번 ingest 가 조각 본문도 다뤘다」로 바꿨다
- [2026-09-19] promoted-duplicate-32 · 읽음 46절 · [계획] 반영 9/걸림 7 · [구현] 배분 3/걸림 3 · 폴백 0절 · 미반영: conventions.md「크기·개수를 잴 때」·「규약 개정 요청을 받을 때」 — K1 7: 계획 함정 9행 중 7행이 위키 출처이고 측정 명령(`grep -o`)·acceptance 축(전수/비율)·조사 범위(허브 동명 절 제외)를 실제로 바꿨다. K2 3: 커밋을 전부 경로 한정으로 냈고(절4) vault 워킹트리를 선확인했으며(절5) T2 가 `DESIGN.md`§6 을 열어 부분중복을 갈랐다(-3.md [09-11]). N 이 46 인 것은 이번 회차가 `conventions-{2,3,release}.md` 32항목을 **편집 대상으로** 전건 열었기 때문이고, 평시 회차의 7절과는 성격이 다르다
- [2026-09-19] promoted-duplicate-golden · 읽음 41절 · [계획] 반영 6/걸림 6 · [구현] 배분 2/걸림 2 · 폴백 0절 · 미반영: conventions.md「규약 개정 요청을 받을 때」 — K1 6: 절 3「크기·개수를 잴 때」가 **G4 의 측정 명령이 죽어 있던 것을 드러냈다**(교대 패턴 `\|` 가 항상 0 — 파이프 없는 두 명령으로 교체) · `wc -c` 대신 python len() · 절 수는 awk 펜스 제외. 직전 회차 리뷰 지적 3건도 함정으로 옮겨 어간 검색·실해 비복사·커밋 메시지 파일 경유를 못박았다. K2 2: 커밋을 경로 한정 + `-F` 파일로 냈고(절 4·5) 그 덕에 백틱 치환이 재발하지 않았다. **직전 회차 미반영이던 절 3 이 이번에 반영됐다** — 그 절을 안 열었으면 G4 가 착수부터 통과했을 것이다
- [2026-09-19] promoted-duplicate-final40 · 읽음 50절 · [계획] 반영 3/걸림 3 · [구현] 배분 2/걸림 2 · 폴백 0절 · 미반영: conventions.md「규약 개정 요청을 받을 때」 — K1 3: 절 3「크기·개수를 잴 때」가 측정 명령 셋을 바꿨다(`wc -c` 대신 python len() · 교대 패턴 금지 · 절 수는 awk 펜스 제외). K2 2: 커밋을 경로 한정 + `-F` 로 냈다(절 4·5). **N 산식 주기** — 위키의 decisions.md·프로젝트 허브·pending.md 는 각각 1로 세는데, **허브를 0으로 둔 것은 「열지 않았다」이고 decisions.md 를 1로 센 것은 「열었으나 걸리는 것이 없었다」**이다. `WIKI.md` §1 이 그 구분을 문면으로 적지 않아 여기 남긴다
- [2026-09-19] wiki-queue-pruning · 규약 · 읽음 3절 · [계획] 반영 2/걸림 2 · [구현] 배분 2/걸림 2 · 폴백 0절 · 미반영: feat-llm-wiki-queues「관련 지식·레시피」 · 없어서조사: 0 — K1·K2 2: conventions 의 「스크립트로 파일을 고칠 때」(편집 도구 LF 화)가 전 task 검증에 `git ls-files --eol` 을 넣게 했고 구현 내내 python `newline=""` 편집으로 실제로 걸렸다 · 「병행 세션·커밋」은 경로 한정 커밋을 명시하게 했고 vault `pending.md` 가 실제로 M 상태여서 걸렸다
- [2026-09-20] convention-budget-exempt · 규약 · 읽음 21절 · [계획] 반영 6/걸림 5 · [구현] 배분 6/걸림 5 · 폴백 0절 · 미반영: conventions.md「하위 문서」·「다른 프로젝트의 파일을 하니스 세션에서 고칠 때」·「문서·검사기 구조와 용어」, conventions-verification-2「앵커·구간 파싱을 다룰 때」·「골든을 돌리거나 픽스처를 고칠 때」·「하위 문서」, conventions-verification-2-2「조건부 참조 문서 크기 임계」, feat-llm-wiki-lint「관련 지식·레시피」의 feat-llm-wiki·feat-hook-evals 링크 · 없어서조사: 2 — K1 5: **feature 페이지 §2 조회가 이 회차의 설계를 바꿨다**. `test-case-axis-isolation` 이 T3 을 「축 전수 열거 → 교체 → red 실증」 4단계로 다시 짜게 했고(그것이 없었으면 convention 제외 한 줄이 타입 무관 축 3개를 green 인 채로 껐다), `signal-remediation-predicate-parity` 가 D1 을 「상수 하나 + 파생 둘」로 바꾸고 델타 음성을 넣게 했다. K2 5: 축 ⑪ 「넘」 무조건 매치와 화이트리스트 앵커 조각 형식은 **실제로 걸려** 문면을 되쓰고 ⓒ 주석 3줄을 엔트리로 분리했다. 없어서조사 2 는 auto-split 골든 계수 기준과 「96%에서 여유 500자가 남는 타입」 — 둘 다 위키에 없어 코드·표에서 직접 쟀다.
- [2026-09-20] wiki-write-session-load · 규약 · 읽음 10절 · [계획] 반영 5/걸림 5 · [구현] 배분 6/걸림 6 · 폴백 0절 · 미반영: conventions.md「하위 문서」·「병행 세션·커밋」·「다른 프로젝트의 파일을 하니스 세션에서 고칠 때」·「문서·검사기 구조와 용어」 · 없어서조사: 0 — 쓰기 세션의 wiki-ops-rules 전량 Read 를 절 단위 라우팅 + 근거 분리로 갈랐다. 「스크립트로 파일을 고칠 때」의 CRLF 함정이 T3 에서 실제로 발화했다(w/mixed).
- [2026-09-20] ingest-procedure-rationale · 규약 · 읽음 8절 · [계획] 반영 6/걸림 4 · [구현] 배분 1/걸림 1 · 폴백 0절 · 미반영: conventions.md「하위 문서」·「병행 세션·커밋」·「다른 프로젝트의 파일을 하니스 세션에서 고칠 때」·「문서·검사기 구조와 용어」 · 없어서조사: 0 — 3회차의 근거 분리 수단을 규칙 문서에서 절차 본문(B-2)으로 옮겼다. K1 4: 「크기·개수를 잴 때」가 측정 명령을 python len() 블록 슬라이스로 고정하고 G4 를 교대 grep 대신 라벨 개별 확인으로 바꿨으며, 「규약 개정 요청을 받을 때」가 G1 하한을 표본 최솟값(32%)보다 낮은 −19% 로 잡게 했고, 「스크립트로 파일을 고칠 때」가 T2 함정에 앵커 10~20자를 못 박았다. 반영 6 중 2(코드펜스 헤딩·글로벌 지침 결합)는 배분했으나 해당 없음으로 닫혔다. K2 1: 그 짧은 앵커 규약이 실제로 걸렸다 — B-2 줄이 675~824자라 긴 앵커가 trailing space 로 한 번 실패했고 짧게 잡아 해소했다

- [2026-09-20] procedure-rationale-round5 · 규약 · 읽음 15절 · [계획] 반영 3/걸림 3 · [구현] 배분 2/걸림 1 · 폴백 0절 · 미반영: conventions.md 「하위 문서」, conventions.md 「스크립트로 파일을 고칠 때」, conventions.md 「위키 vault를 다룰 때」, conventions.md 「다른 프로젝트의 파일을 하니스 세션에서 고칠 때」, conventions.md 「규약 개정 요청을 받을 때」, conventions.md 「문서·검사기 구조와 용어」, conventions-verification-2.md 「정합 검사기·문서 대조」, conventions-verification-2.md 「골든을 돌리거나 픽스처를 고칠 때」, conventions-verification-2.md 「하위 문서」, decisions.md · 없어서조사: 0 — K2 1: 배분 2건 중 커밋 형식만 실제로 걸렸고(heredoc 이 본문 백틱 치환을 막았다) 이관 3원칙의 상대 참조는 걷은 조각에 0건이었다

- [2026-09-21] stale-docs-golden-lag-false-positive · 구현 · 읽음 22절 · [계획] 반영 6/걸림 4 · [구현] 배분 3/걸림 2 · 폴백 0절 · 미반영: conventions-2.md 「PowerShell return @()」, conventions-verification-3.md 「조기 return」·「부분 실행 판정 자격」·「결과 줄 판정」·「기록값 갈림」·「scope.txt 증명」·「10분 칩」, feat-consistency-checkers.md 「커밋 직전 자동 고지」, feat-hook-evals.md 「정본 문서」, feat-llm-wiki-lint.md 「기준일 구간」, feat-safety-hooks.md 「파일 목록」, decisions.md 조회(0건), pending.md 조회(0건) · 없어서조사: 2 — K2 2: 배분 3건 중 2건(안전 임계 hook 범위 · 러너 실행 중 편집 금지)이 실행에서 실제로 걸렸다
- [2026-09-22] lint-generated-dir-exclusion · 구현 · 읽음 21절 · [계획] 반영 5/걸림 3 · [구현] 배분 3/걸림 2 · 폴백 0절 · 미반영: conventions-3.md 「confidence 두 개념」, conventions.md 조회(0건), pending.md 조회(0건), feat-llm-wiki-lint.md 「동작(사용법)」 · 없어서조사: 3 — K1 3: `patterns/test-case-axis-isolation` 의 판정 순서가 골든을 1건에서 2건으로 가르고 red 실증을 Goal 기준으로 올렸다 · CRLF 함정이 픽스처(w/lf)와 정본(w/crlf)의 기대값을 acceptance 에서 갈라 적게 했다 · 「산문 정합은 셀프체크가 못 잡는다」가 T3 을 독립 task 로 세웠다. K2 2: intent 커밋에서 실제로 LF 평탄화 경고가 떠 복원했고, 픽스처를 둘로 가른 덕에 red 가 두 FAIL 형태(오탐 존재 / 미검출)를 갈라 보여줬다. 없어서조사 3: 제외 축의 구현 위치 · 검증 매핑 표 · 큐 태그 목록은 위키가 아니라 레포 문서·스킬 번들에 있었다
- [2026-09-23] opus55-loop-stop-guide · 규약 · 읽음 14절 · [계획] 반영 6/걸림 3 · [구현] 배분 0/걸림 0 · 폴백 0절 · 미반영: conventions-verification-2.md 「:41 여유 작은 문서」·「:43 예산 등급」·「:51 절 이동 참조」·「:65 AGENTS.md 이관」, conventions-verification-2-2.md 「:23 원문자 축 번호」, feat-implement.md 「:73 정지 서술」, decisions.md 조회(0건), pending.md 조회(0건) · 없어서조사: 0 — K2 0 은 배분 0 이라서다(문서 3파일 회차라 구현 함정이 CRLF 뿐이고 그것은 AGENTS.md 출처) · conventions 는 라우터 미경유 키워드 grep
- [2026-09-23] stop-hook-loop-continue · 구현 · 읽음 14절 · [계획] 반영 4/걸림 3 · [구현] 배분 5/걸림 4 · 폴백 0절 · 미반영: conventions.md 「하위 문서」 라우터, conventions-planning.md 나머지 항목, feat-safety-hooks.md 「:112 규칙 파일 로드 실패」, decisions.md 조회(0건), pending.md 조회(0건) · 없어서조사: 1 — 위키는 Stop 입력(stop_hook_active·last_assistant_message)까지만 담아 additionalContext 계속·UserPromptExpansion·Skill matcher 는 공식 문서를 새로 조사했다 · K2 미적중 1(conv-verification-3:27 — red 확인을 git show 복원이 아니라 한 줄 뒤집기로 했다)
- [2026-09-26] harness-dedup-cleanup · 규약 · 읽음 14절 · [계획] 반영 5/걸림 5 · [구현] 배분 2/걸림 1 · 폴백 1절 · 미반영: conventions.md 「하위 문서」 라우터·「위키 vault를 다룰 때」·「다른 프로젝트의 파일을 하니스 세션에서 고칠 때」·「규약 개정 요청을 받을 때」, conventions-planning.md 「Deferred 대장 판정」·「검증 범위·문서 이관 판정」, pending.md 조회(0건) · 없어서조사: 0 — K2 1: conventions-3 「지울 때 남은 조각이 red」가 T6 허용목록 MISMATCH 로 실제 발화
- [2026-09-26] harness-rules-relocation · 규약 · 읽음 14절 · [계획] 반영 2/걸림 2 · [구현] 배분 4/걸림 2 · 폴백 0절 · 미반영: conventions.md 「하위 문서」 라우터·「크기·개수를 잴 때」·「위키 vault를 다룰 때」·「다른 프로젝트의 파일을 하니스 세션에서 고칠 때」·「문서·검사기 구조와 용어」, conventions-planning.md 「Deferred 대장 판정」, decisions.md 조회(0건), pending.md 조회(0건) · 없어서조사: 0 — K2 2건은 CRLF(Edit 가 매 편집 LF 로 씀), 커밋 -F·검증 매핑 표 구조는 실행에서 만나지 않았다
