# Deferred 대장

> 미처리 Deferred의 추적 대장 — **`## 대기`만 담는다** (형식 정본: `plan/references/plan-template.md` 「Deferred / Follow-up」).
> **v1.198.0에서 셋으로 갈렸다**: 기각 종결은 `deferred-closed.md`, 소진 batch 회고는 `deferred-history.md`.
> **등재·조회는 이 파일 하나**이고 나머지 둘은 기각 이동·batch 착수 때만 연다 — 분할 전 296KB가 계획 1건마다 통째로 실렸다.
> 항목은 `- [등록일] {요지 1줄} (출처)`. **종결은 사유가 처리를 정한다 — 반영·병합은 항목을 삭제하고(근거는 커밋 본문) 기각만 `## 종결`로 이동한다**(3분기 표·판정 규칙 정본은 아래 「연산 규칙」).
> `pjc:plan` Step 1이 계획 때 `## 대기`를 조회하고, `pjc:implement`가 완료 때 신규분을 append한다.

## 대기

- [2026-09-05] **`--auto-split`의 index 계열 신설은 여전히 보고·`[SCHEMA]` 기록 밖이다** — 결함 4(재점검 몫 미기록)와 같은 계열인데 회차 16 범위 밖이라 남았다. `PRESCRIPTIONS`는 롤오버 3종 + `relocate_sections` 넷뿐이고 `_append_log_entries` 호출은 `auto_split` 안 두 곳뿐이라 **`index 분할` 기록을 낼 경로가 코드에 없다** — 그런데 `wiki-schema.md` §4 7번은 그 종류를 기록 형식에 열거하고 예시까지 든다(`index 분할 — index.md: index-work·…`). 실측: `split-recheck-log` 사본 실행에서 `index-personal.md`가 신설됐으나 `== --auto-split: 2건 수행 ==` 목록에도 `[SCHEMA]` 2줄에도 없다(파일은 3, 기록은 2). **v1.238.3이 세운 D8 논리가 그대로 적용되는 자리다** — §4 6번 사후 보고와 5번 원복이 둘 다 「무엇이 만들어졌는가」에 기대므로 기록에서 빠진 파일은 되돌릴 대상에서도 빠진다. **착수 조건 없음.** (출처: v1.238.3 완료 리뷰 MINOR 3)
- [2026-09-05] **`--auto-split`의 종료 코드(`ses.failed`) 경로를 어느 골든도 재지 않는다** — v1.238.3이 러너의 rc 판정을 `rc not in (0,1)` → `rc != 0`으로 좁혀 **경로 자체는 감시 대상에 들어왔으나**(27케이스 전부 rc 0 실측), `ses.failed`를 참으로 만드는 상황(사본 실패 = `OSError`)을 일으키는 픽스처가 없어 **그 값이 실제로 종료 코드로 나가는지는 여전히 미검증**이다. 완료 리뷰가 변이로 실증했다 — `ses.failed = ses.failed or recheck.failed` 한 줄을 지운 사본으로 `split-recheck-log`를 돌리니 PASS였다(좁히기 전 기준). 이 레포 자신의 규약(`AGENTS.md` 차단 경로 커버리지 — *"사유 문구가 골든에 없으면 그 경로는 코드를 지워도 green"*)이 성립하는 자리다. **처방 후보**: 사본 대상 경로를 읽기 전용으로 만들거나 `SplitSession.backup`에 주입 지점을 두어 실패를 유발하는 픽스처를 만든다(쓰기 권한 조작은 Windows에서 재현이 까다로워 후자가 유력하다). **착수 조건 없음.** (출처: v1.238.3 완료 리뷰 MAJOR 2)
- [2026-09-05] **`-presplit` 사본이 §4 7번 기록보다 늦게 떠 원복 후 기록만 남는다** — 롤오버가 `log.md`를 백업하는 시점이 **1회차 `[SCHEMA]` append 뒤**라, 사본에 그 기록이 이미 들어 있다. §4 5번대로 사본을 복원하면 **되돌려진 산문 분리의 기록만 남은** log.md가 된다(실측: `90_archive/backup/2026-09-05-presplit/log.md`에 1회차 기록 1줄). 선재 결함이며 v1.238.3이 만든 것이 아니다(재점검 백업 시점은 종전과 같다) — 다만 그 회차가 닫은 「파일은 있는데 기록이 없다」의 **거울상**(「기록은 있는데 파일이 없다」)이라 같은 축으로 묶인다. **처방 후보**: 기록 append를 사본 뒤로 미루거나, 원복이 `[SCHEMA]` 줄을 함께 되돌린다(후자는 §4 5번의 *"방금 append한 블록만 골라 지우기를 쓰지 않는다"*와 부딪히므로 전자가 유력). **착수 조건 없음.** (출처: v1.238.3 완료 리뷰 MINOR 6)
- [2026-09-05] **`## ` 하나가 예산보다 큰 페이지에는 자동 처방이 없다** — `--auto-split`의 이동 경계는 `## ` 섹션이고, 옮겼을 때 하위가 곧바로 발동할 섹션은 후보에서 빠진다(`lint.py`의 `_pick_relocatable`). 그래서 한 절이 예산보다 크면 그 절은 영영 안 옮겨지고 페이지는 `예산 초과` WARN만 낸 채 남는다(실측: 42,297자 `## UI·UX`). **v1.238.3 전에는 그 상태에서 남은 작은 절을 무한히 내보냈고**, 그 회차가 그것을 닫으면서 지금은 조용히 멈춘다 — §7-2 정지 가드가 *"그것까지만 옮기고 멈춘다"*로 이미 정상으로 규정하는 상태다. **남는 것은 처방 부재 자체**이고, 후보는 `### ` 단위 이동인데 그것은 §4가 이동 단위를 `## `로 고정한 결정론 근거를 바꾸는 **규약 개정**이라 별도 회차가 필요하다(그 단위를 내리면 「무엇을 옮길지」가 다시 판단이 되어 승인 불요 근거가 흔들린다). **착수 조건**: 실 vault에 그런 페이지가 실제로 생기면. 지금은 0건이다. (출처: v1.238.3 회차 16 Out of Scope)
- [2026-09-05] **분할 하위의 `index_label`·H1이 같은 절 제목 반복 시 누적된다** — `_sub_page_text`가 `{상위 index_label} — {절 제목}`으로 라벨을 만드는데, 하위가 다시 분할될 때 그 하위의 최대 절이 **같은 이름**이면 `A — T — T`가 된다(하위 파일의 본문 절 제목이 원본 절 제목을 그대로 물려받기 때문이다). 실 vault에 흔적 1건 — `conventions-verification-2.md`의 정본 포인터 라벨이 `conventions-verification — 정합 검사기·문서 대조 — 정합 검사기·문서 대조`인데 대상 파일 frontmatter는 사람이 ` (2)`로 고쳐 둬 **둘이 어긋나 있다**(포인터의 라벨과 대상의 라벨이 다르면 조회가 같은 문서를 둘로 읽는다). v1.238.3의 스텁 제외로 재생산 빈도는 크게 줄었으나 **공식 자체는 남는다**. **처방 후보**: 접미가 이미 그 제목으로 끝나면 다시 붙이지 않거나, 순번을 붙인다(` (2)`는 사람이 이미 그렇게 고친 형태다). **착수 조건 없음** — 실 vault의 어긋난 1건 정정이 함께 따라온다. (출처: v1.238.3 회차 16 Deferred)
- [2026-09-05] **위키 `index_label` 중복 검사(§7-33) 미신설** — 자동 분할이 만든 하위의 라벨이 같으면 조회가 그것을 가려낼 수 없다. 절차 K 2가 sub-index에서 **라벨 grep으로 행을 특정**하라고 규정하는데, 라벨이 같으면 그 판정 자체가 불가하고 `## 하위 문서`의 담당 범위 서술도 함께 같아져 선택적 읽기의 근거가 사라진다(실측: 결함 상태에서 index 행 13개·허브 표 13행·하위 목록 12행이 전부 같은 라벨이었다). **v1.238.3이 그 생산자를 없앴다** — 포인터 스텁을 이동 후보에서 빼면서 같은 라벨의 하위가 반복 생성되지 않는다. **남는 사각은 사람이 손으로 만든 하위**이고, 그 범위(같은 분할 계보의 형제 집합)는 실측 **0건**이다. **전역 중복 검사는 오탐한다** — 실 vault의 전역 중복 8건은 전부 다른 프로젝트의 동명 기능(`feat-tray-lifecycle` ×3 등)이라 정상이며, index 표에 프로젝트 열이 따로 있어 구분된다. **착수 조건**: 사람이 만든 하위에서 형제 범위 라벨 중복이 실제로 관측되면. **신설 시 묶음 4곳**(위키 규약 [2026-07-19]): `wiki-schema §7`에 §7-33 추가 · `lint.py` 검사 · `procedures-ops.md` F-1 인덱스에 같은 번호 등재 · `check_consistency.py`의 F-1↔§7 대조 32 → 33. (출처: v1.238.3 회차 16 — 사용자가 범위에서 명시적으로 제외)
- [2026-09-05] **축 순서상 UTF-8 손상 파일은 「줄바꿈 정합」 축까지 도달하지 못한다** — 회차 14 T3-3 실증 중 CR 을 UTF-8 문자 **중간**에 넣자 앞 축(포인터 도달성)의 `anchors_of()` 가 `UnicodeDecodeError` 로 죽어 줄바꿈 축이 **실행되지 않았다**(exit 1 은 났으나 `[MISMATCH]` 도 `결과` 도 없는 traceback). **조용한 실패는 아니다** — traceback 이 떴고, 대장 `[2026-08-25]` 가 기록한 현실적 bare CR 형태(정규식 인용 자리의 제어문자)는 ASCII 경계라 이 문제가 안 난다(ASCII 경계 주입으로 다시 재어 정상 검출을 확인했다). 다만 **손상 파일을 지목하는 진단은 줄바꿈 축 쪽이 낫다** — 처방은 축 순서 조정 또는 각 축의 파일 읽기 예외 처리이고 **설계 판단이 필요해 즉석 처리하지 않았다**. (출처: 회차 14 T3-3)
- [2026-09-05] **경고(exit 0) 경로에는 커버리지 축이 없다 — 차단 커버리지의 대칭 공백** — `check-block-coverage.py` 는 `exit 2` 지점마다 사유 문구를 재는 골든이 있는지 강제하며 근거를 *"그 경로를 재는 케이스가 없으면 코드를 지워도 green"* 으로 둔다. **같은 논리가 경고 경로에 그대로 성립하는데 축이 없다**. 실측(2026-09-05): `guard-write.ps1` 경고 출력 48곳 vs `exit 2` 3곳 · `guard-harness.ps1` 30 vs 3 · `guard-bash.ps1` 5 vs 3 — 경고가 압도적 다수다. **전수를 재면 안내 텍스트까지 걸려 소음이 되므로** 범위를 `additionalContext` 를 내는 지점이나 판정 결론을 담은 문구로 좁히는 설계가 선행돼야 한다. (출처: 회차 14 사전 조사 — 축 후보 B)
- [2026-09-05] **README 규약 2종이 기계 밖에 있다** — ① 상단 버전 ↔ `plugin.json` `version` 동기 ② 버전별 changelog 블록 기재 금지. `warn-version-drift.ps1` 은 **설치본↔레포와 릴리즈 누락만** 보고 README 를 전혀 언급하지 않는다(2026-09-05 실측: 해당 스크립트에 `README` 0회). 둘 다 정규식으로 판정 가능한 기계적 규약이다 — ①은 두 값 비교, ②은 `## v1.x` 헤딩·버전 태그 목록 탐지. 현재는 둘 다 충족(1.237.0 · 표기 1곳). (출처: 회차 14 사전 조사 — 축 후보 C)
- [2026-09-05] **우회 변수 문서↔코드 정합에 기계 대조가 없다** — `AGENTS.md` 「Conventions」이 *"우회 변수는 둘이며 서로 대체되지 않는다"* 며 **각각이 붙는 hook 이름까지 열거**하는데, 새 hook 이 변수를 읽거나 기존 hook 이 떼면 문서만 조용히 낡는다. 대조는 `.ps1` 의 `$env:CLAUDE_HARNESS_*` 실사용 ↔ 그 열거로 끝난다. 실측(2026-09-05): 현재 일치 — `QUICK` → guard-bash·guard-harness·guard-write · `ALLOW_SECRET` → guard-commit-secrets. **저빈도·저비용 축이라 다른 항목과 묶어 한 회차에 처리하는 것이 맞다**. (출처: 회차 14 사전 조사 — 축 후보 D)
- [2026-09-05] **분할한 문서를 가리키던 옛 참조는 기계로 잡히지 않는다** — 회차 13이 `write-gate-rationale.md` 의 §23 을 `plan-exempt-rationale.md` 로 분리하면서 남는 쪽에 **같은 이름의 포인터 절**을 뒀더니, 그 절을 가리키던 옛 참조(`docs/harness-conventions.md` 의 「코드 쪽 근거는 …」)가 `check_pointer_reachability` 를 **통과했다** — 헤딩이 실재하기 때문이다. 사용자 질문으로 발견했고 전수 검색으로 고쳤다. **처방 후보**: 분할 규약(`skills/DESIGN.md` 또는 `AGENTS-BOUNDARY.md`)에 「옛 경로+절 이름 조합으로 전수 검색」을 1구 추가하거나, 포인터 절의 헤딩에 표식(예: `(이관됨)`)을 넣어 도달성 축이 구분하게 한다. **함께 기록**: 분할 파일명에 숫자 접미(`-2`)를 쓰면 `check-comment-truncation.py:32` 의 인용 정규식 `` `rules/([a-z\-]+\.md)` `` 이 받지 못해 **주석↔근거 짝 검사에서 조용히 빠진다** — 용도명을 써야 한다. (출처: 회차 13 분할)
- [2026-09-05] **`session-context-rationale.md` 36,616 B 감량** — 회차 13이 `scripts/rules/*.md` 예산 축을 20,000 B 로 신설했는데(`DESIGN.md` 4절), 이 파일이 **상한의 1.83배**라 `BUDGET_EXEMPT` 에 등록한 채 시작했다. **면제이지 통과가 아니다** — 축을 세우는 회차와 감량 회차를 분리한 것이다. 감량 방향은 `§N` 단위 분할이 유력하다(같은 디렉터리의 다른 rationale 은 전부 상한 아래다 — 2위가 `destructive-rationale.md` 16,981 B — 회차 13이 `write-gate-rationale.md` 의 §23 을 `plan-exempt-rationale.md` 로 분리해 그 파일은 14,339 B 가 됐다). **착수 조건 없음.** (출처: 회차 13 T2-3)
- [2026-09-05] **`AGENTS.md` 11,581 B 감량** — 회차 13이 `AGENTS_TARGET_BYTES`(10,000 B) 판정을 구현하면서 **이미 초과 상태**임이 드러났다(`[NOTICE]` 로 상시 출력된다). 하드 게이트 16,384 B(`session-context.ps1` 정본)까지 여유 4,803 B 이고, 실측 증가율이 하루 약 +170B 라 방치하면 약 28일 뒤 주입이 목차 폴백으로 깨진다. 이관 경로는 `pjc:record-project-fact` Step 5 다. **상시 통지는 무뎌지므로**(대장 [2026-08-03] 「늘 '해당 없음'으로 채워지면 형식만 남는다」와 같은 축) 이 항목이 살아 있는 동안이 그 통지의 유효 기간이다. **착수 조건 없음.** (출처: 회차 13 T3)
- [2026-09-04] **`[PLAN-EXEMPT]` 표식의 수명이 세션 전체다** — transcript 는 append-only 라 세션 앞부분의 표식이 끝까지 유효하다. 파일 한정이 범위를 좁히지만, 같은 파일을 한참 뒤 **무관한 목적**으로 고칠 때도 열려 있다. 좁히려면 표식에 타임스탬프나 일련번호를 넣고 훅이 최근 N개만 보는 방식이 필요한데, transcript 줄 순서 파싱이 들어가 판정이 무거워진다(현재는 `Select-String` 단순 매치). **착수 조건 없음** — 다만 실제로 이 수명 때문에 샌 사례가 관측되기 전에는 비용이 이득보다 크다고 본다. (출처: 회차 12 plan Deferred) **(2026-09-05 부기 — 회차 13)**: 같은 설계(transcript 문자열 표식)에서 온 자매 항목 「산문 오통과」는 **닫혔다** — 마커가 텍스트 줄의 시작일 때만 표식으로 보게 좁혔다(§23). 그 변경은 이 항목의 수명 문제를 건드리지 않는다 — 줄 시작 조건은 「무엇이 표식인가」를 좁힐 뿐 「그 표식이 언제까지 유효한가」와 직교하기 때문이다. **다만 판정이 이미 정규식 전 매치 순회로 바뀌었으므로**, 수명 제한에 필요한 「줄 순서·타임스탬프 파싱」의 추가 비용은 등재 시점 추정보다 작다.
- [2026-09-04] **「전역 지침과 중복」 판정 축은 제3자 설치 환경에 이식되지 않는다** — 회차 9 가 `add-domain-service` 절대 규칙 6·7 을 지운 근거 전체가 *"전역 `CLAUDE.md` 는 매 세션 자동 주입된다"* 에 걸려 있는데, **그 파일은 `~/.claude/CLAUDE.md` — 이 PC 의 개인 파일**이다. 이 플러그인은 `README.md:11`·`marketplace.json` 대로 **공개 배포**되고(`github.com/jongcheol-pak/claude-harness-pjc`), 제3자 설치 환경에는 그 파일이 없어 지운 규칙이 **대체 없이 사라진다**. **회차 9 의 실질 피해는 작다** — 지운 두 줄은 회차 8 축(「모델이 이미 아는가」)으로도 지워질 일반 원칙이라 결론이 같다. 문제는 **기록된 근거가 그 축이 아니라 비이식적 축**이라는 것이고, 「같은 축을 나머지 스킬에」 항목이 이 축을 4개 스킬로 확대하라고 대기 중이라 **다음 회차가 같은 근거로 이식성이 걸리는 규칙을 지울 수 있다**. **처방**: 확대 적용 전에 각 규칙을 「모델이 이미 아는가」 축으로 **재판정**한다 — 그 축은 전역 파일의 존재에 의존하지 않는다. 같은 종류의 결합에 이 레포는 이미 전용 절을 갖는다(`docs/harness-conventions.md` 「llm-wiki ↔ 글로벌 지침 결합」 — *"어느 검사기도 잡지 못한다 … 판정이 사람 몫"*). (출처: 2026-09-04 회차 9 완료 리뷰 MINOR)
- [2026-09-04] **`add-viewmodel` Halt 조건 3(네비게이션 패턴 부재)에 화면 종류 한정이 없어 다이얼로그 요청에서 불필요한 루프 정지가 난다** — 회차 8 이 Step 5 를 「네비게이션 연결(**`Page` 인 경우**)」로 좁혔는데 Halt 3 과 사전 조건 `:25`(「상위 네비게이션과의 연결 방식」)는 좁히지 않았다. `ContentDialog` 은 호출부에서 직접 띄우므로 네비게이션 패턴이 애초에 없는데, Halt 3 은 그것을 「패턴 부재」로 읽어 **보고하고 중지**한다. **실재하는 경로다** — `plugins/pjc/skills/evals/trigger-cases.json:341` 의 `vm-pos-2`(*"WPF에서 파일 내보내기 다이얼로그 하나 추가하고 ViewModel도 같이 만들어줘"*)가 정확히 그 형태다. **BASE 부터 있던 비대칭이라 회차 8 의 회귀는 아니나**(그때 Step 5 는 「해당하는 경우」였다) 좁히면서 낙차가 커졌다. 처방 후보: Halt 3 앞에 「`Page` 요청에서」 한정을 붙인다. **절대 규칙 1 이 갖는 *"`pjc:implement` 안에서는 멈추지 말고"* 예외가 Halt 3 에는 없다는 것도 같이 본다.** (출처: 2026-09-04 회차 8 3차 완료 리뷰 MINOR)
- [2026-09-04] **같은 축을 나머지 스킬의 SKILL.md 본문에 적용할지 미판정** — 회차 8 은 `add-viewmodel` 본문만 봤다. `implement`·`plan`·`pjc-systematic-debugging` 의 규칙 중 「모델이 이미 아는 것」·「전역 지침과 중복인 것」이 있는지 판정하지 않았다. **`add-viewmodel` 절대 규칙 4(한글 주석)·5(인코딩)도 대상이다** — 회차 9 가 `add-domain-service` 의 같은 두 규칙(재매김 후 **5·6**)을 남긴 판단과 함께 봐야 한다. **대장의 「다른 스킬 references 가 같은 축인지 미판정」 항목과 대상이 다르다** — 그쪽은 `references/` 파일이고 이쪽은 SKILL.md 본문 산문이라, 두 항목을 한 회차에 묶으면 판정 대상이 섞인다. (출처: 2026-09-04 회차 8)
- [2026-09-04] **삭제한 변형 B(UserControl `DataContext` 덮어쓰기)는 「빌드가 못 잡는」 축이었다** — 회차 8 이 변형 A·B·C 를 지울 때 셋의 성격이 갈렸다: A 는 실행에서 드러나고 C 는 Step 4 수명 주석과 중복인데, **B 만은 틀렸을 때 「바인딩이 조용히 빈 값을 낸다」**(빌드·테스트가 못 잡는다). 지식 부족이 아니라 부주의로 틀리는 종류이며, **상시 로드되는 문면이 부주의를 얼마나 막는지는 실측된 적이 없다** — 이 하니스가 산문 규칙을 유지할지 판정할 때 반복해 부딪히는 축이다. **변형 C 의 두 번째 문장(「이벤트를 구독하면 화면을 닫아도 해제되지 않아 누수 — 해제 경로를 함께 만든다」)도 같은 축이다** — Step 4 수명 주석과 중복인 것은 C 의 첫 문장뿐이고 이쪽은 어디에도 없다. 사용자 판단으로 셋 다 지웠고, 실제로 문제가 관측되면 **안티패턴 표**(빌드가 못 잡는 것을 모으는 자리)에 한 행으로 되살린다. **B 의 근거 구 한 줄(「부모 View 가 `DataContext` 로 주입」)은 Step 3 의 절차 분기에 남아 있다** — 종류별로 Step 4·5 가 갈리는 이유를 대는 자리라 근거 없는 규칙을 금지한 `DESIGN.md` 1절 때문에 지울 수 없다. **되살아난 것은 근거 구뿐이고 하중 부분(「덮어쓰면 바인딩이 조용히 빈 값을 낸다」)은 아니다** — 다음 회차가 「왜 B 의 일부만 살아 있나」를 다시 판정하지 않도록 적어 둔다. (출처: 2026-09-04 회차 8)
- [2026-09-04] **리뷰 반영으로 산출물을 다시 고친 뒤 수치를 재측정하지 않아 회차 7 에서 2회 연속 재발** — 1차 리뷰 MAJOR(감량 오귀속)와 2차 리뷰 BLOCKER 2건이 **같은 원인**이다: 커밋 `813e307c` 시점에 잰 크기를 리뷰 반영 편집(`b4999c77`) 뒤 다시 재지 않고 `plan.md` Goal·커밋 본문·대장 세 곳에 그대로 옮겼다. 결과로 ⓐ G5(*"두 파일 모두 작성 시점보다 작다"*)가 거짓인 채 `[x]` 로 닫혔고 ⓑ `add-viewmodel` 여유 값이 대장 세 곳에서 **1,412 / 1,418 / 1,417** 로 각각 다르게 남았다(전부 오류). **검사기가 못 잡는다** — `check-harness-consistency.py` 의 문서 예산 축은 실제 파일 크기만 재고 **대장·plan 산문에 적힌 수치는 대조하지 않는다**. **3차 리뷰에서 또 났고, 회차 8 에서 네 번째로 났다**(회차 8 T2 커밋 `c7896801` 본문이 대장 등재 **전**에 잰 `Deferred 350` 을 등재 후 검증 기록으로 적었다 — 실측 353). **형태가 둘로 갈린다** — 이 항목이 담은 것은 **재측정 누락**이고(회차 8 의 `Deferred 350` 이 그 순수한 형태다: 등재 전에 잰 값을 등재 후 기록으로 적었다), **회차 7 의 3·4차 리뷰가 잡은 것은 복제 사본 동기 실패**다(같은 기준이 `plan.md` 5곳·대장 3곳에 복사돼 있는데 한 곳만 고쳤다). **후자에는 「옛 표현으로 검색한다」가 듣지 않는다** — 사본은 표현이 제각각이라(`11,173 B 미만` · `작성 시점보다 작다` · `포인터 8개만큼`) 옛 문자열 검색을 통과한다. 필요한 것은 **같은 사실의 사본을 세는 것**이다. **세는 기준: 영구 산출물(커밋된 파일·커밋 본문)에 불일치가 실제로 들어간 건만 센다** — 편집 중에 발견해 같은 커밋에서 고친 것은 정상 동기 작업이라 세지 않는다(회차 8 이 `9,870 → 10,169` 을 같은 커밋에서 갱신한 것이 그 예다). **회차 8 리뷰 반영 중 5회차** — 대장에 **예측값** 9,760 을 먼저 적었다가 커밋 전 재측정에서 실측 9,870 임을 발견해 고쳤다. 영구 산출물에 예측값을 적은 형태라 처방 후보 ⓐ 가 정확히 겨냥하는 사례다. **회차 9 에서 6회차** — 그 회차 plan 이 *"같은 사실의 사본을 센다 — 크기·개수를 적는 자리가 `plan.md`·커밋 본문·대장 셋이다"* 를 절로 적어 놓고도, **대장 안의 크기 기록처가 잔량 앵커가 아니라 아래 「포화 3파일」 항목이라는 것을 세지 못해** 옛 값 11,479 를 남겼다. **셋을 세는 것으로 부족하고 「그 문서 안에서 이 수치를 적은 자리」를 세야 한다.** 처방 후보: ⓐ 리뷰 반영 커밋 직전에 크기·집계를 전건 재측정하도록 `implement/SKILL.md` 「최종 리뷰」에 한 줄 · ⓑ 산문의 `N,NNN B` 패턴을 실제 크기와 대조하는 새 축 신설(**서수는 착수 시점에 다시 고른다** — 여덟째는 2026-09-05 「줄바꿈 정합」이 가져갔다)(어느 쪽도 규칙을 하나 더 얹는 것이라 `DESIGN.md` 1절 기준으로 먼저 재검토). (출처: 2026-09-04 회차 7 2차 완료 리뷰 MAJOR — `WIKI.md` 판정 ⓐ 「같은 원인 2회」) **(2026-09-05 7회차 — 회차 14 커밋 `df1db2a9` 본문이 한글 문면 교정을 「10곳」으로 적었는데 실제는 7곳이고 **같은 본문이 나열한 교정 쌍도 7개**라, 한 본문 안에서 수치와 열거가 서로 모순됐다. 리뷰 반영 중 대상이 줄었는데 수치만 남은 형태다. push 전이지만 amend 는 history rewrite 라 별도 승인 대상이어서 고치지 않고 여기 센다)**
- [2026-09-04] **자매 스킬 둘이 「진입점 호출 확인」을 두고 갈렸는데 그 사유가 문면에 없다** — 회차 7 이 `add-domain-service` Step 4 에는 *"진입점에서 그 등록 메서드가 실제로 호출되는지 확인한다"* 를 **넣고**, `add-viewmodel` Step 4 에서는 **걷어냈다**(그쪽은 절대 규칙 3 이 같은 판정 축을 이미 갖는다). 판단 자체는 각각 맞으나 **비대칭 사유가 커밋 본문에만 있다** — 두 파일 중 하나만 읽는 세션은 다른 쪽이 왜 다른지 알 길이 없고, 다음 근거 보강 회차가 「통일」이라는 이름으로 한쪽을 되돌릴 수 있다. 판정 축 후보: 「그 스킬의 절대 규칙이 이미 그 축을 갖는가」. (출처: 2026-09-04 회차 7 2차 완료 리뷰 MINOR)
- [2026-09-04] **두 스킬의 검증 절(Step 6·7)에는 경로·파일명이 없어 「전 Step 에 경로 1건」류 acceptance 가 문자 그대로 성립하지 않는다** — `add-domain-service` Step 6 은 `(csproj/import 확인)`, `add-viewmodel` Step 7 은 해석 경로 확인뿐이다. **BASE 부터 그랬고 회차 7 의 변경 대상이 아니었다** — 기준이 겨냥한 것은 **생성 절**(파일을 만드는 Step)이지 검증 절이 아니다. 그 범위를 acceptance 문면에 적지 않으면 다음 회차가 같은 판정을 다시 한다. (출처: 2026-09-04 회차 7 2차 완료 리뷰 MINOR)
- [2026-09-04] **코드 템플릿은 스킬 자산이 아니다 — 판단만 남긴다** — 회차 7이 `add-domain-service`·`add-viewmodel` 의 `references/templates.md` 2개(9,884 B)를 **전면 삭제**했다. **판정 근거**: ⓐ 코드 골격은 모델이 이미 쓸 수 있어 문서로 주면 `DESIGN.md` 3-1 이 경계한 축(모델이 이미 하는 일을 단계로 만들면 비용만 는다)과 같은 낭비다 ⓑ **Step 1(기존 파일 읽기)이 더 정확한 정보원**이라 템플릿을 따르면 오히려 그 프로젝트와 어긋난 코드가 나온다. **살린 것은 경로·배치 정보**(모델이 추측할 수 없는 이 스킬의 고유 자산)이고, 진입점 호출 확인은 `add-domain-service` 에만 남겼다(`add-viewmodel` 은 절대 규칙 3 과 중복이라 걷어냈다). **회차 6과 축이 다르다** — 그때 `references/` 로 옮긴 것은 예산 포화 해소(여유 5 B)였고 이번은 효용 판단이다. 회차 6이 틀린 것이 아니라 **옮기기 전에 지우기를 검토하지 않은 것이 놓친 선택지**였다. **감량은 `references/*.md` 예산 줄에서 빠졌고 `SKILL.md` 예산은 거의 그대로다** — BASE 11,173/10,588 → **11,479/10,727**(2차 리뷰 반영까지 포함해 오히려 +306 B·+139 B 늘었다 — 되살린 판단 규칙과 근거가 그만큼이다). `add-viewmodel` 의 포화 해소는 이번이 아니라 **회차 6 `a2053c99`** 가 한 것이다 — 여유 **23 → 1,412 B(회차 6)** → **1,273 B(회차 7 이 139 B 를 되먹었다)**. **이 판단의 자리는 `DESIGN.md` 3절인데 여유 120 B라 못 넣었다** — 아래 「3-5 신설」 항목과 함께 착수해야 다음 세션이 같은 것을 다시 만들지 않는다. (출처: 2026-09-04 회차 7)
- [2026-09-04] **다른 스킬의 `references/` 가 같은 축인지 미판정** — 회차 7은 「코드 템플릿」만 지웠다. `plan/references/plan-template.md`(산출물 서식) · `implement/references/loop-stop-patterns.md`(정지 형태 목록) · `pjc-systematic-debugging/references/*`(서식·근거)는 **모델이 추측할 수 없는 이 하니스 고유 규약**이라 성격이 다르나, **그 경계가 문면에 없다**. 판정 축 후보: 「모델이 이미 아는 것인가 / 이 하니스가 정한 것인가」. (출처: 2026-09-04 회차 7 Deferred)
- [2026-09-04] **규칙에 근거를 달 때 두 문장 이상으로 초안하면 12,000 B 상한을 넘긴다 — 회차 6에서 두 파일 연속 재발** — `DESIGN.md` 1절이 *"근거는 한 문장으로 쓴다"*를 이미 정했는데 초안이 두 문장 이상으로 나왔고, `add-domain-service` 는 11,152 B(재작성 후 9,799)·`add-viewmodel` 은 **12,613 B 로 상한을 초과**했다. **처방 순서**: ⓐ 처음부터 한 문장으로 쓴다 ⓑ 넘치면 문장을 줄이지 말고 **중복 항목을 뺀다**(`DESIGN.md` 4절) — 회차 6은 안티패턴 표에서 다른 규칙과 겹치는 3행을 뺐다 ⓒ 그래도 넘치면 **산문을 `references/` 로 내린다**(회차 6이 XAML 구성 산문 489 B 를 그렇게 처리해 10,588 B 가 됐다). **나머지 5파일(THIN+NONE 154개)에 착수하는 다음 작업자가 같은 순서로 다시 걸린다** — 크기 사실만 아는 것으로는 부족하고 작업 수단이 필요하다. (출처: 2026-09-04 회차 6 완료 리뷰 MAJOR — `WIKI.md` 판정 ⓐ 「같은 원인 2회」)
- [2026-09-04] **안티패턴 표의 중복 판정 기준이 재현 가능하지 않다** — 회차 6이 `add-domain-service` 에서 2행(「여러 Aggregate 트랜잭션」·「영문 XML doc」)을 절대 규칙과 중복이라 뺐는데, **같은 기준이면 「Domain 에서 `DbContext`」·「Domain 에서 `HttpClient`」 두 행이 더 문자 그대로의 중복**이다(절대 규칙 2가 `EF Core·HttpClient` 를 이름으로 금지한다). 그 둘은 「왜」 열이 각각 다른 사실을 줘서 유지했으나 **판정 기준이 문면에 없어 다음 회차가 같은 판정을 다시 한다.** 실제로 `add-viewmodel` 에서는 `HttpClient` 행을 뺐다가 **되살렸다** — 그쪽 규칙 2는 「비즈니스 로직 귀속」 축이라 중복이 아니었다. 처방 후보: 「규칙이 그 대상을 **이름으로** 금지하면 중복, 축만 겹치면 유지」. **절대 규칙 간 「전역 지침과 중복」 판정에도 같은 문제가 있다** — 회차 9 가 6(파일 단일 책임)·7(추측 API)은 지우고 5(한글 주석)는 남겼는데, 가른 축(**전역에 없는 스코프를 더하는가** — 규칙 5 의 「XML doc 포함」)이 커밋 본문에만 있고 문면에 없다. (출처: 2026-09-04 회차 6 완료 리뷰 MINOR)
- [2026-09-04] **두 스킬의 `evals/evals.json` 이 회차 6의 문면 변경을 반영하는지 미판정** — `add-domain-service`·`add-viewmodel` 의 SKILL.md 가 크게 바뀌었다(절대 규칙 5 → 8항목 분리 · 안티패턴 2열 → 3열 · 코드블록이 `references/templates.md` 로 이동). 루브릭이 옛 문면을 전제하면 eval 이 낡은 기준으로 채점한다. **판정에 실제 모델 호출이 필요해 회차 6 범위 밖이었다** — `description` 은 건드리지 않았으므로 트리거 케이스 10건은 안전하지만, 루브릭은 별개 축이다. (출처: 2026-09-04 회차 6 Deferred)
- [2026-09-04] **규칙 533개 중 38%가 「왜」를 갖지 못했다 — 전수 실측** — 스킬 6종·`AGENTS.md`·가이드 4종·리뷰어 2종·references 5종의 규칙 문장을 전수 판정했다(판정 기준: *이 규칙만 읽은 에이전트가 경계 사례에서 올바르게 판단할 수 있는가*). **OK 328(62%) · THIN 138(26%, 동어반복·부족) · NONE 67(13%, 근거 전무)**. **결함이 고르게 퍼진 것이 아니라 두 파일에 몰려 있다** — `add-domain-service` OK **19%** · `add-viewmodel` OK **35%**(나머지는 52~100%). 두 스킬은 「절대 규칙」·「안티패턴 표」·「Halt 조건」 세 형식으로 규칙을 쌓았는데 **세 형식 모두 근거 열이 없다** — 형식이 다른 세대의 문서로 보인다. 실제 오작동 형태: *"Aggregate 경계 존중 — 한 트랜잭션에서 여러 Aggregate를 수정하지 않는다"*(`:56`)는 「절대 규칙」인데 위반 시 처리도 이유도 없어, 명확히 두 Aggregate를 건드려야 하는 요구가 오면 규칙을 조용히 어기거나 요구를 축소하게 된다. **반복 형태 3종**: ① **자매 파일 비대칭** — 리뷰어 2종의 대응 규칙 5쌍이 한쪽에만 근거를 갖는다(서브에이전트는 서로의 정의 파일을 읽지 않아 런타임에 절반만 도착한다) ② **숫자 임계에 근거 없음** — `DESIGN.md` 예산 표 5행 중 4행 · `AUTHORING.md` 500줄 · "검증은 넷 이하". `SKILL.md 12,000 B`는 근거로 104,733·109,769 B 실측을 대는데 **임계와 9배 간극**이라 그 값의 출처를 설명하지 못한다 ③ **같은 전제에서 반대 결론** — `AGENTS-BOUNDARY.md`(독자가 에이전트다 → 근거를 빼라) vs `DESIGN.md`(같은 전제 → 근거를 붙여라). 실제 갈림선은 「매 세션 자동 주입인가」인데 그 축이 인용되지 않아 끊겼다. **착수 단위**: 205개 규칙에 근거를 다는 일이라 회차 하나가 필요하고, 두 스킬은 예산도 포화(`add-domain-service` 여유 5 B)라 `references/` 분리가 선행된다 — 대장의 「포화 3파일」 항목과 같은 착수 단위다. (출처: 2026-09-04 전수 조사 3그룹 병렬) **(2026-09-04 부분 해소 — 회차 6)**: 두 스킬분 51개를 처리했다(`add-domain-service` 절대 규칙 5 → 8항목 전부 근거 · 안티패턴 3열 · Halt 6/6 · 의존 방향 판정 축 · `add-viewmodel` 절대 규칙 6/6 · 안티패턴 3열 · Halt 5/5 · 변형 3종). **남은 것은 나머지 5파일의 THIN+NONE 154개**다. 회차 6이 확인한 것 — **근거를 다 달면 파일이 그만큼 커진다**: 두 스킬 모두 코드블록을 `references/`로 내리고도 11,322 B · 11,977 B 로 끝나 여유가 678 B · 23 B 뿐이었다(회차 6 말 `a2053c99` 의 산문 이관과 회차 7 삭제를 거쳐 **2026-09-04 현재 758 B · 1,609 B**(회차 8·9 를 거친 값 — 이 자리는 「현재」라 이력이 아니라 **현행 값**이며, 회차 8·9 가 연달아 갱신을 빠뜨린 자리다)). 나머지 5파일도 같은 제약을 받으므로 **착수 전에 감량 경로부터 잰다.**
- [2026-09-04] **`docs/harness-conventions.md`의 자기 참조·수치 2건이 어긋난다** — ① 「문서 로드 예산 기준선」을 `:65`·`:73`·`:101` 세 곳에서 자기 절로 지목하는데 **그 이름의 절이 없다**(현행은 「조건부 참조 문서 크기 임계」). `check-harness-consistency.py`의 안내 문자열도 같은 없는 절을 가리킨다. ② `:47`이 정합 셀프체크를 **「열다섯 축」**이라 적는데 실측 **7축**이다(`AGENTS.md`가 맞다). ③ `:157`이 `--oneline` 미채택 근거로 **소멸한 F-4**를 인용한다(정의: *"커밋 메시지 전체의 follow-up·TODO·MINOR 언급 ↔ plan Deferred 대조"*, `docs/.agents-presplit/` 사본에만 남음). **`AGENTS.md` 쪽 3건은 2026-09-04에 고쳤고 이 파일은 범위 밖이라 남겼다.** **검사기가 못 잡는 이유**: 포인터 축이 헤딩뿐 아니라 **굵은 텍스트도 앵커로 인정**하므로(볼드 앵커 관례가 실재해 의도적으로 넣은 것), 옛 이름이 본문 볼드로 남아 있으면 개명을 영원히 통과시킨다 — 실측: `문서 로드 예산 기준선`은 헤딩으로 없고 볼드로 있다. (출처: 2026-09-04 전수 조사)
- [2026-09-04] **고아 프로세스 회수의 폴백 2곳이 「모르면 죽이지 않는다」의 반대 방향이다** — `session-end-cleanup-lib.ps1` 은 4중 게이트(이름 화이트리스트 `more.com`·`find.exe` · 로그온 세션 일치 · 고아 판정 · unkillable 마커)로 전반적으로 안전하고 소멸 확인도 견고하나(`Stop-Process` 반환을 믿지 않고 CIM 재조회 + `CreationDate` 대조로 PID 재사용을 가른다), 판정 근거를 **못 얻었을 때**의 처리가 둘 다 회수 쪽으로 기운다. **F1 — 세션 ID 획득 실패 시 `$sessionId = 0` 폴백**: `Get-Process -Id $PID` 가 실패하면 회수 대상이 대화형 세션이 아니라 **Windows 서비스 세션(0)**이 된다. 실현 가능성은 낮으나(대상 2종이 서비스로 돌 일이 거의 없다) 방향이 반대다 — `-1` 같은 불가능한 값으로 폴백하면 그 자체로 전건 제외된다. **F2 — 부모 조회 실패를 고아로 판정하며 주석이 그것을 「보수적」이라 부른다**: `Get-CimInstance` 가 권한·타이밍으로 실패해도 `$null -eq $r.ParentStartTime` 분기로 들어와 **살아 있는 부모를 가진 프로세스가 고아가 된다**. 게이트 ①②가 피해를 좁혀 실해는 작지만 **주석의 자기 서술이 실제 동작과 반대라 다음 작업자를 오도한다**. **이연 사유**: 실해가 관측된 적이 없고, 이 파일 수정은 hook 골든 750+케이스 + 차단 경로 커버리지를 부른다(192~429초). F2는 주석 한 구 정정으로 끝나지만 같은 검증을 부르므로 함께 연다. **다음에 이 파일을 여는 회차에서 처리할 것.** (출처: 2026-09-04 세션 조사)
- [2026-09-04] **「압축 후 스킬 앞 5,000토큰만 재부착」은 미확인 사양인데 구조물 셋이 그 위에 서 있다** — `session-context.ps1`의 **압축 직후 원문 재주입 3종**(루프 제어·계획 규칙·큐 규약, 상한 20,000 B)과 `check-harness-consistency.py`의 **`HEAD_BUDGET_BYTES = 9000`**이 이 값에서 온다(그 주석이 *"사양에서 온 값"*이라 명시하되 실측 기록은 없다). **`AUTHORING.md`의 SKILL 500줄 권장은 여기 해당하지 않는다** — 그 줄은 *"5,000토큰 권장은 `skills/DESIGN.md` 4절의 문서 예산 표가 대체한다 — SKILL.md 12,000 B가 그 값이다"*로 **이미 갈아탄 상태**다(다음 작업자가 같은 오해를 하지 않도록 적어 둔다). **2026-09-04 조사**(공식 문서 4종 `code.claude.com/docs/en/{skills,prompt-caching,memory,settings-reference}.md` 확인 — **찾지 못했다**이지 부재의 증명은 아니다): 그 수치는 제3자 가이드에만 *"keeping the first 5,000 tokens of each. Re-attached skills share a combined budget of 25,000 tokens"*로 나온다. 같은 설계가 전제하는 **"동일 스킬 재invoke는 「이미 로드됨」만 반환한다"**는 **어느 문서에서도 확인되지 않았다**(공식 문서가 압축 후 동작을 기술하지 않는다). 확인된 것은 셋 중 **`SessionStart`가 `source=compact`로 재실행된다** 하나뿐이다(`hooks.md`). **새 단서 — 재부착 총예산 25,000토큰 공유**: 사실이라면 스킬을 여러 개 로드하는 세션에서 개별 스킬은 5,000보다 **더 적게** 남고, 재주입 필요성은 오히려 커진다. **판정 방법**: 압축을 실제로 일으킨 뒤 `implement/SKILL.md` 뒷부분(Halt 조건·재시도 카운터)이 컨텍스트에 남아 있는지 확인한다 — 의도적 압축이 어려워 이번 세션에서 못 했다. **틀렸다면 재주입 3종이 낭비이고, 맞다면 필수다 — 어느 쪽인지 모르는 채로 셋을 유지 중이다.** (출처: 2026-09-04 세션 조사)
- [2026-09-04] **「한 사실은 한 절에만」(신설)과 템플릿의 Out of Scope 요구가 구조적으로 겹친다** — 회차 5가 `plan-template.md`에 넣은 *"한 사실은 한 절에만 적는다"*와, 같은 템플릿이 Out of Scope에 요구하는 *"왜 빼는지 한 구"*가 부딪힌다. 빼는 사유는 거의 항상 Investigation Log 실측이라 **Out of Scope를 쓰는 순간 재기술이 되고**, 매 회차 「이것은 재기술인가 필수 사유인가」를 판정하게 된다. **회차 5 plan 자신이 그 형태였다** — 예산 여유 수치(58 / 857 / 120 B)가 Goal · Out of Scope · Investigation Log 세 절에 나왔다. `Deferred / Follow-up`은 신설 넷째 줄이 예외로 뺐으나 나머지 절은 아니다. 처방 후보: 「Out of Scope의 사유 한 구는 재기술로 보지 않는다」류 경계를 넷째 줄 옆에 두는 것 — 단 규칙을 하나 더 얹는 것이 `DESIGN.md` 1절이 경계한 수단이라, 형식으로 표현할 자리가 있는지 먼저 본다. (출처: 2026-09-04 회차 5 완료 리뷰 MINOR)
- [2026-09-04] **`pjc-systematic-debugging/SKILL.md` 여유 8 B — See also 한 구도 못 받는다** — 11,992 / 12,000. 회차 5가 `references/investigation-log.md`에 `## 분량` 절을 신설했으나 그 파일을 가리키는 See also 설명(*"조사 로그 서식·가설 작성 예·경계 로깅 예"*)에 신설 절을 더할 여유가 없다. 문면을 줄여 끼우는 것은 `DESIGN.md` 4절이 *"문장을 줄이지 말고 항목을 뺀다"*로 금지한 처방이라 하지 않았다. **포화 3파일을 한 축으로 본다** — 이 파일 8 B · `plan/SKILL.md` 58 B · `add-domain-service/SKILL.md` 3 B. 셋 다 `references/` 분리가 처방이고 착수 단위가 같다. (출처: 2026-09-04 회차 5 완료 리뷰 MINOR) **(2026-09-04 갱신 — 회차 6)**: `add-domain-service` 는 3 B → **678 B**, `add-viewmodel` 은 27 B → **23 B** 가 됐다(csharp 블록을 `references/templates.md` 로 분리한 뒤 근거를 달아 다시 찼다). **`add-viewmodel` 은 여전히 포화**이며 다음에 무엇을 넣든 그 자체로 예산을 넘긴다 — 남은 감량 경로는 `## 실행 단계` 산문(XAML 구성은 fenced 블록이 아니라 인라인 코드 스팬이라 이번 분리 대상이 아니었다)이다. `pjc-systematic-debugging` 8 B 는 그대로다. **(2026-09-04 회차 7 갱신 — 2차 리뷰 반영 후 재측정)**: 코드 템플릿을 전면 삭제해 `add-domain-service` **여유 521 B**(11,479 / 12,000) **(2026-09-04 회차 9 갱신: 11,242 / 여유 758)** · `add-viewmodel` **여유 1,273 B**(10,727 / 12,000) **(2026-09-04 회차 8 갱신: 10,391 / 여유 1,609 — 2·3차 리뷰가 Step 3~5 정합을 요구해 721 B 되먹었다)**가 됐다 — 등재 당시 3 B·23 B 였던 두 파일이 이 축에서 빠졌고, 남은 것은 `pjc-systematic-debugging` 8 B · **`record-project-fact/SKILL.md` 13 B**(11,987 — 이 축이 BASE 부터 세지 않던 파일이다. `plan` 보다 더 포화다) 다. **(2026-09-04 회차 10 갱신)**: `plan/SKILL.md` 는 Step 2 인터뷰 절차를 `references/interview.md` 로 이관해 **여유 1,813 B**(10,187 / 12,000)가 됐고 이 축에서 빠졌다 — 남은 것은 앞의 둘이다.
- [2026-09-04] **`DESIGN.md` 6절 적용 범위 목록이 회차 5의 D7 판정을 반영하지 않는다** — 회차 5가 `pjc-systematic-debugging`에 §1 규약 문면 형식을 적용하기로 판정했으나(커밋 `cdc2ccef` 본문), 6절은 여전히 `skills/plan/` · `skills/implement/` · `agents/`만 적고 *"나머지 스킬 6종은 아직 이 원칙으로 재작성되지 않았고"*로 남아 있다. **다음 작업자는 그 스킬을 미마이그레이션으로 읽고 같은 판정을 다시 한다.** 함께 볼 것 — 그 판정의 근거는 `SKILL.md`의 §1 형식 8줄이었는데 **편집 대상이던 `investigation-log.md`는 0줄**이었고, 그 결과 그 파일 안에 §1 형식(신설 2줄)과 비-§1 산문 규칙 2건이 공존한다. 목록 갱신 자체는 여유 120 B 안에서 가능(~45 B)하나 그 여유는 「3-5 신설」 항목과 경합하므로 두 건을 한 번에 판정한다. (출처: 2026-09-04 회차 5 완료 리뷰 MINOR)
- [2026-09-04] **`DESIGN.md` 3-5 「작성된 산출물의 길이」 신설** — 3절이 Opus 5 가이드 수용의 정본인데(3-1 검증 · 3-2 서브에이전트 · 3-3 작업 범위 · 3-4 진행 보고) 이 항목만 없다. 회차 5가 그 가이드를 산출물 3파일에 적용했으나 **왜 그 규칙들이 있는지**가 어디에도 남지 않았다. 실측 여유 120 B(11,880 / 12,000)라 최소 문면 ~430 B를 못 받는다. 착수하려면 4절 처방(*"문장을 줄이지 말고 항목을 뺀다"*)에 따라 **어느 항목을 뺄지**를 먼저 정하거나 상한 재판정이 선행된다 — `scripts/*.ps1`이 15,000 → 25,000으로 올린 선례가 있다. (출처: 2026-09-04 회차 5 실측) **(2026-09-05 — 회차 13: 선행 조건 해소)**: 그 회차가 `rules/*.md` 예산 축을 신설하며 「가이드 문서」 상한을 **12,000 → 13,000 B** 로 올렸다(사용자 승인). 현행 실측 **12,305 / 13,000 = 여유 695 B** 로 이 항목이 필요로 하던 ~430 B 를 받는다 — **상한 재판정 선행 조건은 더 이상 없다.**
- [2026-09-03] **「신규 생성 ↔ 기존 편집」 경계를 한 쌍으로 재던 트리거 케이스가 한쪽만 남았다** — v1.227.0 회차 4가 `boot-neg-2`(*"AGENTS.md에 빌드 명령 한 줄 추가해줘"* → 미발동)를 스킬과 함께 지웠고, 그 거울상인 `rec-neg-1`(*"AGENTS.md 새로 만들어줘"* → 미발동)만 남았다. `rec-pos-1·2·3·5`가 같은 방향(기존 파일 갱신)을 재고 있어 커버리지 공백은 아니지만, **두 방향을 한 쌍으로 재던 구조**는 사라졌다. 지운 질의를 `pjc:record-project-fact` **양성** 케이스로 되살리면 그 쌍이 복원된다 — 케이스 신설이라 이번 회차 범위(5건 삭제) 밖이었다. (출처: v1.227.0 T6)
- [2026-09-03] **`AGENTS.md` 신규 생성에 이제 어떤 게이트도 없다** — v1.227.0이 `guard-harness` 게이트 ②를 없애면서, 스킬을 거치지 않은 직접 Write 로도 `AGENTS.md` 가 생길 수 있게 됐다. 새 정책(「최소로 만들고 점진적으로 채운다」)에서는 그것이 정상 동작이라 의도된 결과지만, **`pjc:plan` Step 1의 승인 게이트를 우회하는 경로가 열린 것도 사실**이다. 실제로 문제가 되는지(대충 만든 `AGENTS.md` 가 다음 세션을 오도하는 사례가 나오는지) 몇 회차 관찰하고, 필요하면 **내용 기반 게이트**(Write 내용에 `## Build & Test` 가 있는가)로 복원할 것. 흔적 기반으로는 되돌릴 수 없다 — `pjc:plan` 흔적은 코드 세션에 상시 존재해 게이트가 무력화된다(v1.227.0 D1). (출처: v1.227.0 T4)
- [2026-09-03] **회차 2의 성공 기준 2개가 미충족으로 남았다 — G1 총 바이트 190,233(목표 150,000) · G3 파일 수 16(목표 12)** — 둘 다 계획 시점에 코드/주석 비중을 재지 않고 정한 값이다. 근거 주석은 `scripts/rules/*-rationale.md` 15파일로, 판정 데이터는 `rules/*.json` 3파일로 전부 내렸고 남은 것은 **판정 로직과 사용자에게 보여줄 차단 메시지**라 더 줄이려면 검사를 빼야 한다. G3는 「19개가 19개로 남지 않는다」를 재는 축을 hooks.json 진입점으로 옮겨 11 → 9로 충족했고(파일 16 중 7이 dot-source 헬퍼), 파일 수 축은 G2(단일 25,000 B)와 정면으로 맞물려 한쪽을 맞추면 다른 쪽이 깨진다. **다시 세우려면 「무엇을 뺄 것인가」부터 정해야 한다.** (v1.225.0 T8-7)
- [2026-09-03] **재작성된 hook 주석 34곳이 단어 중간에서 잘렸다** — 근거를 `rules/*.md`로 내리는 스크립트가 표제를 기계적으로 절단한 흔적이다(예: `guard-harness.ps1` 의 `$harnessHo`, `guard-write.ps1` 의 `우회한`). **실행에는 영향이 없고 전문은 대응 rationale 절에 전부 보존**돼 있으나, 남은 문면이 문장으로 성립하지 않아 후속 작업을 오도하고 잘린 식별자는 grep 대상이 못 된다. 재절단을 문장 경계 기준으로 한 번 돌리면 끝난다. (v1.225.0 완료 리뷰 3R)
- [2026-09-03] **G4의 델타 음성 축이 기준 문면보다 느슨하게 측정된다** — G4 문면은 *"모든 차단 **경로**에 양성과 델타 음성이 각각 1건 이상"*인데 `check-block-coverage.py` 는 양성만 경로별이고 **음성은 hook별**이며, 시나리오 집계는 *"파일 안에 그 hook 호출과 `-ExpectExit 0` 이 함께 있으면 1건"* 이라 파일당 1로 뭉갠다(출력의 `guard-write 경로 2 · 통과 케이스 1` 이 그 상태). 「전건 충족」은 검사기 기준으로 참이고 G4 문면 기준으로는 미측정 구간이 남는다. (v1.225.0 완료 리뷰 3R)
- [2026-09-03] **탐지면이 차단면을 따라가지 않았다** — `guard-harness` 는 설치본 `scripts/rules/*.json` 개조를 차단하게 됐는데 `post-write-checks` 의 H2 탐지는 `<name>.ps1`·`hooks/hooks.json` 만 본다. `rules/harness-hooks.json` 의 `_note` 가 내세우는 「탐지↔차단 대칭」이 이름 축에서만 성립한다. (v1.225.0 완료 리뷰 3R)
- [2026-09-03] **문서 위생 2건** — ⓐ 복원된 절이 파일 끝에 붙어 `rules/session-context-rationale.md` 의 절 순서가 §1~§11, §17~§22, §31~§35, §12~§16, §23~§30 이 됐다 ⓑ `README.md` 에 *"v1.225.0에서 …제거했습니다"* 형태의 회차 서술 2건이 남았다(사용자 규약은 README 에 버전별 변경 서술을 금지한다). (v1.225.0 완료 리뷰 3R)

- [2026-09-03] **dot-source 되는 PowerShell 파일은 최상위 실행 코드가 없어야 하고 자기가 쓰는 함수를 자기 안에 가져야 한다 — v1.225.0에서 이 경계를 두 번 밟았다** — ① 골든 프로브가 `guard-bash.ps1`(hook 본체)을 dot-source 하자 최상위 `[Console]::In.ReadToEnd()`가 실행돼 **stdin 을 무한 대기**했고 러너가 10분 넘게 매달렸다(구 `guard-bash.ps1`은 함수 전용이라 같은 자리에서 문제가 없었다). ② 대상을 `guard-commit-secrets.ps1`(함수 전용)으로 바꾸자 이번엔 `New-HookResult` 미정의 오류가 났다 — 그 함수가 `guard-bash.ps1`에 있고 헬퍼가 그것을 쓰는 **역방향 의존**이었다. 해소는 `New-HookResult`를 헬퍼 쪽으로 옮겨 자기완결적으로 만든 것. **증상이 「행」과 「미정의 오류」로 달라 같은 뿌리로 안 보인다** — 그래서 두 번 걸렸다. 골든 프로브·격리 실행처럼 스크립트를 dot-source 하는 자리를 만들 때 이 둘을 함께 확인한다. (v1.225.0 T3 — 하네스 레포 자신이라 `[PROJECT-FACT]` 큐가 아니라 여기 남긴다: `skills/WIKI.md` 「하네스 레포 자신에서 작업할 때」) **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**

- [2026-09-03] **`session-context`가 매 세션 주입하는 `AGENTS.md` 문면이 Anthropic 안전 분류기를 자극한다** — 2026-09-03 세션에서 Opus 5 safeguard 가 발화해 Opus 4.8 로 자동 전환됐다. 후보는 그 파일에 한 화면 안에 모인 조합이다: `-ExecutionPolicy Bypass`(24행) · **「우회 변수는 둘이며」**(63행) · *"차이가 곧 우회 경로"*(64행) · 「비밀번호·API 키·토큰·시크릿·DB 연결문자열」(78행) · **「평문 자격증명·`-WindowStyle Hidden`·과도한 `-ExecutionPolicy Bypass`(백신이 격리할 수 있음)」**(81행). PowerShell + 실행정책 우회 + 창 숨김 + 백신 회피 + 자격증명이 함께 오면 탐지 회피 도구를 만드는 세션과 문자열 수준에서 구분되지 않는다. **사용자 글로벌 `settings.json`의 base64 `-EncodedCommand` hook 도 같은 축의 후보**다(그쪽은 이 레포 밖). 해소는 81행의 회피 기법 명시를 의도 서술로 바꾸는 것이며 **규약 문면 변경이라 사용자 승인 사안**이다. (v1.225.0 관측)
- [2026-09-03] **`docs/golden-runner.md`가 T6 이후 남은 양을 재판정한다** — 분리 실행·`Monitor` 폴링·`EXIT=` 마커·우회 변수 오염 절이 대상 소멸해 36,369 → 33,527 B 가 됐다. 남은 것의 상당 부분이 회차별 케이스 수 이력(단일 줄 20KB+)이라, 그 이력을 어디까지 보존할지와 나머지를 `harness-conventions.md`로 되돌려 병합할지를 함께 판정한다. (v1.225.0 관측)

- [2026-09-03] **하니스 재작성 회차 2 — hooks 20개 스크립트 + 골든 스위트 전면 재작성** — 사용자가 「전부 재작성」을 선택했으나 hook 은 스킬 절차를 강제하는 장치라 새 절차가 확정된 뒤에야 설계할 수 있어 회차 1에서 미뤘다. 회차 1은 **깨진 참조 수리만** 했다(스킬 이름 치환·절 추출 대상 교체·골든 대조 문자열 교체). 대상: `scripts/*.ps1` 19개(약 375KB) · `hooks/evals/scenarios/*.ps1` 15개 · 골든 814케이스. (v1.224.0 회차 1 Deferred)
- [2026-09-02] **리뷰 판정문 대기 구간에 루프가 할 일을 잃는다** — subagent 호출은 항상 비동기라(v1.222.0 T1 실측: Agent 도구에 `run_in_background` 파라미터 자체가 없다) 판정문을 기다리는 동안 메인은 ⓐ 리뷰 대상 파일을 건드릴 수 없고 ⓑ 다음 task를 쓰면 커밋 단위가 엉키며(커밋 `6f4a02a`에 T6 픽스처 혼입 — 실측) ⓒ 텍스트만 내고 turn을 끝내면 그것이 곧 루프 정지다(구 `require-evidence` hook 이 그것을 기계로 잡았으나 v1.225.0에 제거돼 지금은 `implement/references/loop-stop-patterns.md` 문면이 유일한 방어선이다). 규약은 *"대기는 정지가 아니다"*라고만 적고 **그 구간에 무엇을 해도 되는지를 정하지 않는다**. 실무적으로 남는 것은 다음 task의 Phase P(읽기)뿐인데 그것도 유한하다. 대기 구간의 허용 행동을 규정하거나 그 구간을 정지로 보지 않는 신호를 두는 편이 낫다. **반영할 정본 문서가 특정되지 않아 등재한다.** (v1.222.0 F-6.5) **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-09-02] **실패 경험의 영구 보존·재참조 체계** — `## Retry Ledger`·기각된 가설·되돌린 수정은 `plan.md`에만 남는데 그 파일은 gitignore + 회차 교체라 **영구 유실**된다. 같은 실패를 다음 회차가 다시 밟아도 참조할 기록이 없다. 위키는 `wiki-schema.md:514`가 *"실패한 접근은 등재하지 않는다 — 증상 → 검증된 해법만 담는다"*로 **의도적으로 배제**하고 있어, 단순 추가가 아니라 그 방침을 뒤집는 설계 결정이 선행된다(미검증 실패가 위키 권위로 고착되는 것을 막으려던 규정이다). 사용자가 v1.222.0 범위에서 명시적으로 제외했다. (v1.222.0 F-6.5)
- [2026-09-02] **Claude Code 신기능 대비 규약 정리** — **셋 중 둘은 v1.225.0에서 판정했다(대체 불가).** ① **`claude plugin eval`은 hook 골든 러너를 대체하지 못한다** — `claude plugin eval --help`(CLI v2.1.259) 실측: LLM 채점기(`--judge-model`, 기본 haiku)와 `--max-cost-usd` 비용 상한을 갖는 **에이전트 실행 채점** 도구다. hook 골든은 stdin JSON → exit code·출력 문자열의 **결정적 대조**라 축이 다르다. ② **`permissions.deny`는 `block-destructive`를 대체하지 못한다** — 공식 문서(`code.claude.com/docs/en/permissions`)가 *"Bash permission patterns that try to constrain command arguments are fragile"*라며 그 처방으로 **"Use PreToolUse hooks"**를 명시한다. **다만 deny 는 hook 보다 먼저 평가되므로**(*"Hook decisions don't bypass permission rules"*) **1차 방어선으로 겹쳐 두는 안은 여전히 유효**하고 그 판정은 남는다. ③ `/code-review`가 리뷰어 호출을 대체하는가는 미판정 — 회차 1이 리뷰어를 2종으로 줄여 전제가 바뀌었다. (v1.222.0 F-6.5 · v1.225.0 D1·D2 판정)
- [2026-09-03] **위키 프로젝트 페이지의 hook 구성 서술이 낡았다** — `feat-safety-hooks.md` 가 `bash-hook-lib.ps1`·`orphan-process-cleanup.ps1` 을 현행 파일로 적는데 v1.225.0이 둘 다 없앴다. `AGENTS.md` 가 아키텍처 상세의 정본을 위키로 지정하므로 **정본이 낡은 상태**다. **v1.226.0이 그 낙차를 더 키웠다** — 스킬 5종 재작성·bootstrap 템플릿 11파일 폐기·`docs/prds/` 폐기가 모두 위키에 반영되지 않았다. ingest 는 위키 세션 사안이라 코드 회차에서 닫지 못한다. (v1.224.0 회차 1 등재분 · v1.226.0 T9에서 회차 3 항목 삭제 시 분리 승계)
- [2026-09-03] **`docs/harness-conventions.md` Repository Structure 절(260~282행)의 전면 갱신** — v1.226.0 T7-5는 낡은 2줄(`templates/`·`prd.md`)만 고쳤다. 그 절은 hook 진입점 9종·dot-source 헬퍼 7종·스킬 목록을 산문으로 담는데 회차 1~3이 셋 다 바꿨으므로 전수 대조가 필요하다. 기계 검사가 없어(포인터 축은 경로+절만 본다) 어긋나도 조용하다. (v1.226.0 계획 리뷰 MAJOR 3에서 파생)
- [2026-09-03] **`add-domain-service/SKILL.md` 는 11,997 B 로 예산 여유가 3 B다** — v1.226.0 T10-1이 신설한 예산 축이 한 줄만 늘어도 FAIL로 잡는다. 5종 중 유일하게 이번 회차가 감량하지 않은 파일이라(예산 안이었다) 근거 분리도 안 돼 있어 **줄일 여지를 아직 재지 않았다**. 다음에 이 스킬을 건드리는 회차가 `references/` 분리를 함께 판정할 것. (v1.226.0 T5)
- [2026-09-03] **`trigger_eval.py` 실측 미수행 — 본문 재작성이 발동에 미치는 영향이 0이라고 실증되지 않았다** — v1.226.0 D3이 `description:` frontmatter 를 재작성 대상에서 빼 트리거 축을 고정했으나(`trigger-cases.json` 58건이 그 문자열로 채점된다), **본문이 발동 판정에 전혀 관여하지 않는다는 것은 가정**이다. 그 러너는 실제 모델을 호출해 비용이 커 기본 검증 경로 밖이다(`skills/evals/README.md`). 스킬 문면을 다시 크게 바꾸는 회차에서 1회 측정해 가정을 닫을 것. (v1.226.0 D3)
- [2026-09-03] **`check-stale-refs.py` 의 `explorer` 는 일반명사라 단어 경계가 없다** — DEAD 21개 중 이 하나만 흔한 영단어여서 무관한 문맥(`file explorer` 등)을 잡을 수 있다. 현재 실측은 hit 0이라 드러나지 않았고, 다른 20개는 하이픈 복합어라 오탐 여지가 없다. 착수하면 그 이름만 `` 경계로 감싼다. (v1.226.0 T7-1)
- [2026-09-03] **`loop-stop-patterns.md` 의 형태 번호가 ②부터 시작한다** — 제목은 「루프를 멈추는 **네 형태**」이고 실제 항목도 ②③④⑤ 넷이라 **개수는 맞는데 ①이 없다**. 이 파일은 `pjc:implement` 가 「유일한 방어선」으로 지목하는 정본이고 압축 직후 주입 대상이기도 해서, ①을 찾다 없는 것을 「누락」으로 읽으면 그 자리에 없는 규칙을 추측하게 된다. 번호를 ①~④로 다시 매기거나 ①이 무엇이었는지 이력을 확인할 것. (v1.226.0 T9 부수 관측)
- [2026-09-03] **골든 케이스 2건이 회차 2의 통폐합으로 같은 것을 재게 됐다** — `scenarios/guard-harness.ps1` 의 「설치본 guard-bash Write 차단(v1.99.0 T6 집합 합류)」과 「설치본 guard-bash 헬퍼 Write 차단(v1.99.0 T6 등가 우회 봉쇄)」이 **둘 다 `scripts/guard-bash.ps1` 을 대상으로 한다** — 종전에는 `pre-bash-dispatch.ps1`·`bash-hook-lib.ps1` 두 파일이었는데 v1.225.0이 하나로 합쳤다. 이름만 다르고 판정이 같아 한쪽은 무회귀 케이스다. 지우려면 「어느 쪽이 무엇을 재던 것인가」를 먼저 가릴 것 — 골든 기준선도 함께 갱신된다. (v1.226.0 T7-3)
- [2026-09-03] **`llm-wiki/SKILL.md` 선두 9,000B 구간 재구성** — auto-compact 후 스킬은 **앞 5,000토큰(약 10,850 B)만 재부착**되므로(공식 skills 문서) 그보다 큰 SKILL.md 는 규칙·Halt·위치판정의 뒷부분이 통째로 유실된다. **실측 18,103 B로 1.67배**다. v1.226.0이 나머지 스킬 5종을 12,000 B 안으로 줄여 이 문제를 없앴으나 `llm-wiki` 는 회차 1~3의 Out of Scope 라 그대로다. **기계도 보지 않는다** — `check-harness-consistency.py` 의 문서 예산 축이 `BUDGET_EXEMPT_PREFIX` 로 그 트리를 면제한다(면제이지 통과가 아니다). 대장의 「분리된 `lookup-rules.md` 문면 감량」은 `references/` 쪽이라 **축이 다르다**. 착수하면 그 스킬의 「0. 시작 절차」·「사전 준수 사항」이 선두에 있어야 한다는 제약을 먼저 확인할 것. (구 「SKILL.md 선두 구간 재구성 (구 PRD FR-5)」에서 살아 있는 대상만 분리 승계 — v1.226.0 완료 리뷰 MAJOR 5)
- [2026-09-03] **`check-comment-truncation.py` 의 임계 50자가 같은 결함 13곳을 측정 밖에 둔다** — 「잘린 주석 0건」은 정확히는 **「50자 이상 잘린 주석 0건」**이다. 전수 스캔에서 임계 미만의 명백한 절단이 남는다(`session-context.ps1:128` 「…막는 절 하나만」 48자 · `session-wiki-signals.ps1:90` 「…앞서 다른 외부」 46자 · `session-context.ps1:125` 46자 · `:102` 43자 · `guard-commit-secrets.ps1:92` 41자 · `session-context.ps1:82` 40자 · `guard-bash.ps1:304` 39자 · `session-context.ps1:9` 32자 · `session-ledger-signal.ps1:17` 24자 등). 임계를 낮추면 오탐이 늘어 **종결부 판정을 먼저 정교화**해야 한다(현재는 종결어미 목록 + 동작명사 화이트리스트). (v1.226.0 완료 리뷰 MINOR 3-b)
- [2026-09-03] **`check-stale-refs.py --ledger` 가 예외 개수·사유를 출력하지 않는다** — 트리 경로는 `[예외] N건 — 사유` 블록을 내는데(v1.226.0 T7 acceptance 가 요구한 것) 대장 경로는 통과 건수만 낸다. 대기 146건 중 12건이 이력 예외, 1건이 검사기 자기언급 예외로 통과하는데 **출력만 보면 그 13건이 없는 것처럼 보인다**. 예외가 넓어져 0건이 되는 것과 실제로 0건인 것을 구별하지 못한다. (v1.226.0 완료 리뷰 MINOR 4)
- [2026-09-03] **기계 이름 치환은 「무엇이 그 이름에 붙어 있는가」를 보지 않아 없는 식별자를 만든다 — v1.226.0에서 이 경계를 두 번 밟았다** — ① T7의 치환 맵에 `bash-hook-lib` → `guard-bash 헬퍼` 가 있었는데 원문이 `bash-hook-lib.ps1` 이라 **`guard-bash 헬퍼.ps1`** 이라는 존재한 적 없는 파일명이 4곳 생겼다(완료 리뷰 BLOCKER). ② T6의 표제 복원이 여러 줄 주석의 첫 줄을 그대로 표제로 삼아 연결어미로 끝나는 문면을 남겼다. **둘 다 검사기는 green 이었다** — `check-stale-refs.py` 는 옛 이름의 부재만 보고 새 이름의 실재는 보지 않으며, `check-comment-truncation.py` 는 종결부 목록에 의존한다. **처방 후보**: ⓐ 치환 맵의 값이 실재하는 식별자인지 적용 후 대조하는 축 (파일명 형태 토큰이면 `ls` 로 확인) ⓑ 치환 전후 diff 를 사람이 훑는 것을 T 규약으로 명시. **옛 이름은 git 이력으로 추적되지만 발명된 이름은 어디서도 해소되지 않는다** — 「이름으로 검색하는 다음 세션을 오도하지 않는다」는 회차 목적과 정면으로 어긋난 사고였다. (v1.226.0 완료 리뷰 BLOCKER · MAJOR 3)

**▶ 현행 잔량(기계 대조 대상)**: 대기 178 / 종결 189 / 정리 삭제 누계 102 / 총 등재 누계 469

> 이 한 줄만 기계 대조 대상이다 — `plugins/pjc/evals/check-harness-consistency.py`가 파싱해 실측 집계와 대조한다. **아래 batch 기록 안의 "정리 직후 N건 / 현행 M건" 표기는 그 시점의 기록이므로 갱신하지 않는다** — 고치면 "정리 직후/현행 분리 표기" 관례가 깨지고 과거 판정의 근거가 사라진다. 대장을 편집한 task는 **이 줄만** 갱신한다.
>
> **카운트 기준 (두 구간의 표기 형식이 다르다 — 단일 정규식으로는 재현되지 않는다)**:
> - **대기** = `## 대기` 구간에서 **`^- \[\d{4}-\d{2}-\d{2}`** — 날짜 **접두**만 본다. 대부분은 `[등록일]`로 닫히지만 **`[등록일, **vN에서 부분 해소**]`처럼 부기가 붙는 형식이 실재**하므로(2026-08-05 실측 2건) `\]`로 닫는 패턴을 쓰면 그런 항목이 조용히 누락된다. 대장 운영 항목 자신도 포함된다.
> - **종결** = **`deferred-closed.md`의** `## 종결` 구간에서 **`^- \[\d{4}-\d{2}-\d{2} → \d{4}-\d{2}-\d{2}\]`** — `[등록일 → 종결일]` **날짜 범위** 형식. 대기 패턴을 여기 쓰면 0건이 나온다. **v1.198.0부터 파일이 다르다** — 대조기는 두 파일을 각각 읽어 합산하며, 종결 파일을 빠뜨리면 `done`이 0이 되어 불변식이 즉시 어긋난다.
> - **파일별로 위 두 패턴을 각각 적용하는 것이 정본**이다. 한쪽 패턴을 두 파일에 함께 쓰면 어느 쪽이든 값이 어긋난다.
> - **앵커(위 `▶ 현행 잔량` 줄)는 이 파일에만 둔다** — 계수의 정본이 한 곳이어야 세 파일이 갈리지 않는다. `deferred-closed.md`·`deferred-history.md`에는 앵커를 만들지 않는다.
> - **누계 2종은 실측 대조 대상이 아니라 기록값이다** — 삭제된 항목은 파일에 없어 셀 수 없으므로 `정리 삭제 누계`·`총 등재 누계`는 사람이 적는 값이다. 대조기는 이 둘을 실측과 맞춰보지 않고, 아래 **불변식**으로만 검사한다.
> - **불변식(대조기의 실검사)**: **`대기 + 종결 + 정리 삭제 누계 == 총 등재 누계`**. 종전의 「무손실 대조」(batch 전후 총수를 사람이 대조)를 대체한다 — 3분기 도입으로 삭제가 정상 경로가 되면서, 파일에 남지 않는 삭제분을 누계가 대신 센다.
> - **연산 규칙 (여기가 정본 — F-6.5는 이 표를 복제하지 않고 포인터만 둔다)**:
>
>   | 연산 | 갱신 |
>   |---|---|
>   | 등재 | `대기 +1` · `총 등재 누계 +1` |
>   | **대기에서 삭제**(반영·병합 종결 — **새 규약의 주 경로**) | `대기 -1` · `정리 삭제 누계 +1` |
>   | 종결에서 삭제(소급 정리) | `종결 -1` · `정리 삭제 누계 +1` |
>   | 대기→종결 이동(기각) | `대기 -1` · `종결 +1` |
>   | 종결→대기 복원(오종결 교정) | `종결 -1` · `대기 +1` |
>
>   대기 항목의 **병합 흡수**는 흡수된 항목이 `## 대기`에서 사라지는 것이라 「대기에서 삭제」에 포함된다(별도 연산 아님). 요지 수정·재확인 표기 추가·그룹 재배치는 개수를 바꾸지 않아 불변식과 무관하다.
> - **⚠ 앵커는 절대값 기록이지 증분 로그가 아니다** — 같은 갱신을 두 번 적용해 값이 중복 가산되지 않게 한다.
> - **⚠ 불변식이 깨졌을 때의 복구 규칙**: **대기·종결은 실측이 정본**, **정리 삭제 누계는 실제 삭제를 수행한 만큼만 가산**, **총 등재 누계는 등재할 때만 +1**이다. 식 하나에 사람이 적는 값이 둘(누계 2종)이라 **그 차만 구속**되므로, exit 1이 났을 때 누계를 서로 맞춰 green을 만들면 삭제 수가 영구히 과소 기록된다. **원인 연산을 찾아 그 연산 규칙대로 고친다.**
> - **총 등재 누계는 2026-08-12에 209로 세운 실측 리베이스값**이며(이후 등재분만큼 즍가한다) 역사적 총 등재 수가 아니다 — 이 필드 도입 이전에 이미 삭제된 항목들이 있어(5차 batch 등) 진짜 누적은 그보다 크다. 이 값은 도입 시점의 `대기 + 종결 + 0`으로 세운 기준선이다.
> - **형식 이상 1건은 2026-08-05 3차 batch에서 해소됐다** — `## 대기`에 종결 형식(`[날짜 → 날짜]`)으로 적혀 있던 「게이트 오점화」 항목(본문이 *"해소(v1.154.0 T6/D8)"*)을 `## 종결`로 옮겼다. **대기 패턴이 날짜 접두만 보므로 그때까지 대기로 계수되고 있었다** — 같은 형태가 다시 생기면 여기 적어 다음 batch가 집어가게 한다.

### 대장 운영

- [2026-09-02] **대장 「카운트 기준」의 부기 예시가 옛 형식만 담는다** — `:17`이 *"`[등록일, **vN에서 부분 해소**]`처럼 부기가 붙는 형식이 실재(2026-08-05 실측 2건)"*로 적는데, **v1.185.0 T2가 「부기를 제목 뒤로」 옮기는 관례를 세워 현행 대장의 실물은 전부 제목 뒤 괄호**다(2026-09-02 실측 — 제목 뒤 3건 / 대괄호 0건). v1.221.0 T2가 구 `phase-f-detail.md` ⓪·구 `SKILL.md` Step 1 ③의 같은 예시는 두 형식으로 정정했으나 **이 자리는 축이 달라 손대지 않았다** — 그 문장은 판정 규칙이 아니라 **계수 정규식의 근거이자 그 시점 실측 기록**이다(날짜 접두만 보고 `\]`로 닫지 말라는 지시). 착수하려면 「시점 기록을 갱신할 것인가 / 현행 관례를 부기로 덧붙일 것인가」를 먼저 정한다. **이연 사유는 「기존 문제」다** — 이번 변경이 유발한 것이 아니라 v1.185.0 이후 계속 있던 어긋남이다. (출처: v1.221.0 F-7 m5 판정 유보) **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-09-02] **다음 소진 batch는 12차가 건너뛴 구간을 승계해 구간을 정한다** — 12차 batch(v1.212.0)가 구간 `2026-08-17 ~ 08-23`을 판정하고 **건너뛴 구간이 있으므로 `deferred-history.md`에서 그것을 승계**한다(규약 ①). **등록일순을 기계적으로 열지 말 것** — 11차(v1.195.0)가 규약 ①만 읽고 그 판단을 승계하지 못해 같은 구간을 다시 열어 소득 0으로 끝낸 선례가 있다. **등재하는 이유**: 발동 여부 자체는 `pjc:plan` Step 1 ③이 매 계획마다 재계산하지만, **승계할 구간 정보는 재계산으로 나오지 않는다**. (출처: v1.220.0 F-7 M2 — 초안은 「기록성 관측」으로 미등재 판정했으나 문면에 액션 지시가 있어 게이트 ⓔ의 2조건 AND를 충족하지 않는다) **(2026-09-02 정정 — 이 항목의 착수 전제가 틀렸다)**: 등재 당시 제목은 *"축 ②가 두 회차 연속 발동 상태(최고령 「마지막 판정일」 2026-07-30 → 34일)"* 였으나 **그 값이 오산이다.** 정본 규칙(구 `phase-f-detail.md` ⓪ — 재확인 표기·부기·등록일 중 **가장 최신** 날짜. v1.224.0에 그 문서가 삭제돼 현재 정본은 `session-ledger-signal.ps1` 의 판정뿐이다)으로 대기 147건을 실측하면 최고령은 **2026-08-19(14일)** 로 30일 미달이고, 2026-07-30은 아래 「SessionStart hook의 위키 허브 컨텍스트 자동 주입」 항목의 **첫** 재확인 스탬프다(그 항목은 08-06·08-12·08-23을 더 갖는다). **세 축 전부 미발동이다** — ① 순증분 7건(147 − 12차 「정리 직후 140」) < 30 · ② 14일 < 30 · ③ 직전 batch 경과 5일 < 14. 축 ②가 실제로 열리는 날은 **2026-09-18**(08-19 + 30일)이다. 오산의 구조는 바로 아래 「최고령 「마지막 판정일」 계산에 기계 축이 없다」가 담는다. **(2026-09-03 재확인: 유지 — 13차 판정 전건 스캔. 착수 전제였던 「축 ② 발동」은 본문 정정이 이미 오산으로 무효화했으나, **승계할 구간 정보는 재계산으로 나오지 않는다**는 등재 사유가 그대로라 항목은 유효하다. 이번 13차는 구간 batch가 아니라 전건 스캔이어서 그 승계를 소비하지 않았다)** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-08-29] **`harness-conventions.md` 밖에서 예산 표 값을 인용하는 자리의 전수 발굴** — v1.215.0이 신설한 축 「파생 수치 동기」는 대조 범위를 그 문서의 **「메인 컨텍스트」 블록 6수치**로 한정했다(사용자 판정 — 근거 3건이 전부 그 블록이고, 그 블록 자신이 *"표를 갱신하는 task가 이 줄들도 함께 고친다"*는 대조 대상 선언을 이미 달고 있다). **같은 문서의 다른 자리·다른 문서(`SKILL.md` 등)가 예산 표 값을 인용하는지는 조사하지 않았다** — 발굴하면 그 축의 `checks` 튜플에 앵커를 더하는 것으로 흡수된다. 착수 조건 없음(지금 조사 가능). (출처: v1.215.0 plan Deferred 이관)
- [2026-08-27] **`## Progress Log` 갱신이 마지막 task 쌍에서 빠지는 사각** — `pjc:implement` 규칙은 *"2 task마다 갱신"*이라 **짝수 경계에서만 강제력이 있고**, 홀수로 끝나거나 마지막 쌍에서 건너뛰면 아무도 잡지 않는다. v1.208.0에서 실현됐다 — T7 acceptance ⓖ가 그 자리를 기록처로 지정했는데 T7·T8 구간이 갱신되지 않은 채 `[x]`로 닫혔고 **F-7이 BLOCKER로 잡았다**. **이 사각이 단순 기록 누락이 아닌 이유**: 같은 회차가 신설한 「완료 후 일괄 승인」(ii-c)의 **유일한 수집처가 그 절**이라(구 `halt-conditions.md`·구 `SKILL.md`·구 `final-report-template.md` 셋이 그렇게 지정했다 — 셋 다 v1.224.0에 삭제) 갱신이 끊기면 **미룬 승인 항목이 최종 보고에 닿지 못한다.** 후보 처방: Phase F 진입 시 「마지막 task 쌍의 Progress Log 존재」를 **F-2 자체 게이트에 1구 추가**(리뷰어 항목 신설이 아니라 메인 게이트 — F-7이 사후에 잡는 것보다 앞이다). (출처: v1.208.0 F-7 B1) **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-08-27] **「문서 로드 예산 기준선」의 상한이 크기를 억제하지 못한다** — 상한 열은 「현행 +10%」로 정해지므로 **파일이 커지면 상한이 따라 오른다.** `docs/harness-conventions.md:203`이 스스로 *"처방은 둘 중 하나다 — 본체를 줄이거나, **상한을 올릴 근거를 표 서문에 적거나**"* 라고 적어, 성실히 갱신하는 한 영원히 통과한다. **감시 축이지 제동 장치가 아니다.** `[2026-08-25]` 「SKILL.md 상한 초과분의 실제 해소」와 축이 다르다 — 그쪽은 *초과가 관측된 뒤의 처방*이고 이것은 *애초에 초과가 선언되지 않는 구조*다. 착수하려면 「+10% 자동 추종」을 절대선·증가율 예산 중 무엇으로 바꿀지부터 정해야 한다. (출처: v1.208.0 사용자 지적 — *"상향하면 다음에 또 문제가 생기지 않나"*) **(2026-09-01 실측 부기 — 별도 등재 대신 이 항목에 붙인다: 같은 처방에 걸린다)**: v1.216.0 시점 `pjc:plan/SKILL.md`는 **105,922B / 상한 106,000B로 여유가 78B**다. 다음 회차가 이 파일을 한 문단만 늘려도 상한에 걸리며, 현행 규약대로면 그때 「상한을 올릴 근거를 표 서문에 적는」 경로로 통과된다 — 이 항목이 지적한 구조가 **실제로 임박한 첫 사례**다. **(2026-09-02 실측 부기 — 같은 처방에 걸린다)**: v1.220.0이 `docs/harness-conventions.md`를 **상한 137,000을 827B 넘긴 상태로 만들었고**, 그 회차는 상한을 올리지 않고 서술 세 자리를 압축해 **136,912B(여유 88B)**로 맞췄다 — 이 항목이 요구하는 「본체를 줄이는」 처방을 실제로 택한 첫 사례이자, **여유가 88B라 다음 회차가 한 문단만 더해도 같은 선택을 다시 강요받는다**는 관측이다. **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-08-27] **실행 비용이 큰 러너를 acceptance로 거는 task는 `docs/harness-conventions.md` 갱신을 `Files`에 동반 표기해야 한다** — v1.208.0 T5가 신설한 A3 예외 ⓑ(*문서화된 기준선을 날짜 라벨과 함께 인용하고 착수 시 재측정*)를 T6이 쓰자, 재계수 결과(635 인용 → **769 실측**)를 「골든 러너 운용」 표에 기록해야 했고 그 파일이 **자기 크기를 자기 안에 기록**하는 탓에 조건부 임계 표까지 연쇄로 갱신됐다. 둘 다 `Files` 미선언이라 spec 리뷰가 MINOR로 잡았다(실질 침범 아님 — 자기 유발 동반 변경). **예외 ⓑ를 쓰면 그 갱신이 구조적으로 따라오므로** 관례로 못박는 안. (출처: v1.208.0 T6 spec M1)
- [2026-08-27] **[SUGGEST] `plan-reviewer` 비용 등급을 18곳 산문 대신 압축 표로 두는 안** — v1.208.0 T7이 18개 항목 서두에 등급 1구씩을 붙였는데, 등급이 바뀔 때마다 **18곳을 개별 갱신**해야 해 드리프트 위험이 있다(같은 문서가 「리뷰어 4종 공통 규약」에서 반복 지적하는 다중 소스 문제와 동형). 표로 모으면 바이트도 절약된다 — 그 파일이 상한 60,000까지 **여유 1,975B**라 유효한 고려다. 반대 근거: *"왜 비싼지"*의 맥락은 그 항목 안에 있는 편이 판독성에 유리하다. (출처: v1.208.0 T7 quality S1)
- [2026-08-27] **lint §7-20·§7-21·§7-26이 타 PC에서 조용히 꺼진다** — 허브의 「로컬 경로 힌트」가 그 PC와 안 맞으면 `repo_root_for_hub()`가 `None`을 반환해 세 검사(각주 경로 실존·관련 파일 실존·위키 뒤처짐)가 스킵된다. **현행 fail-open 설계 그대로**이며 `[2026-08-26]` 결정(식별 앵커를 `repo_url`로 이동)이 *"경로가 안 맞는 PC에서 그 셋이 조용히 스킵되는 것은 기존 설계"*라고 명시적으로 남긴 잔여분이다. **착수 조건**: 여러 PC에서 lint를 상시 돌려 그 사각이 실제 마찰로 관측되면 PC별 경로 매핑 파일을 재검토한다. (출처: v1.206.0 → v1.207.0 plan Deferred 이관) **(2026-09-03 재확인: 유지 — **규약 ② ⚠의 「관측 경로 없음」 예외**. 착수 조건이 *"실제 마찰로 관측되면"*이지만 `repo_root_for_hub()`가 `None`을 반환하면 세 검사가 **조용히 스킵**돼(fail-open) 아무도 신고하지 않는다 — *"관측되면 재등재된다"*는 전제가 성립하지 않는다)**
- [2026-08-26] **SSH remote 형식(`git@host:owner/repo.git`) 정규화가 실증되지 않았다** — v1.206.0이 `repo_url` 대조를 `scheme://…` 형태로 좁혔다. scp형 SSH를 받으면 허브의 https 값과 영영 일치하지 않아 경로로 폴백하던 세션이 「불일치」로 판정돼 조용히 침묵하기 때문이다(그 회귀를 리뷰가 잡았다). **등록 프로젝트 14개가 전부 https라 실증 대상이 없어** 상호 변환을 넣지 않았다. **착수 조건**: SSH remote를 쓰는 프로젝트가 위키에 등록되면 그때 `repo_url` 정규화를 넓힌다 — 대상은 `wiki-schema.md` §2.2 「대조 규칙」과 `session-context.ps1`의 `$cwdUrl` 판정 두 곳이다. (출처: v1.206.0 plan Deferred 이관) **(2026-09-03 재확인: 유지 — 착수 조건이 외부 사건(*"SSH remote를 쓰는 프로젝트가 위키에 등록되면"*)이나, 불일치 시 세션이 *"조용히 침묵"*한다고 본문이 적어 **관측 경로가 반쪽**이다. 판정이 갈려 규약 ②의 안전 방향(유지)을 따른다)**
- [2026-08-25] **「메인 조합」 파생 3행의 기계 대조 축** — `harness-conventions.md:145~147`의 min/max·합계·AGENTS.md 값을 표(「문서 로드 예산 기준선」)에서 도출해 대조. 현재 방어선은 *"표를 갱신하는 task가 이 줄도 함께 고친다"*는 사람 주의뿐이고 **네 번 뚫렸다**(v1.198.0 T3 → T7 → F-7 1R M1 → 2R B1). part2 T3(SKILL.md 크기 기계 회귀)과 같은 파일·성격. **(2026-08-25 보강: 범위에 「근거 서문의 나눗셈 결과」도 넣는다** — v1.200.0 F-7 M1이 `여유 ÷ 하루 증가율 = 경보 창 일수`를 틀리게 적은 것을 잡았다. 파생 3행과 같은 축이고 **둘 다 기계 대조 밖이라 사람 주의가 유일한 방어선**이다.)
- [2026-08-25] **§157(9,000B 경계 안쪽 편집 3조건)이 리뷰어 정의에도 적용되는지 문서상 불명확** — `AGENTS.md` 행에는 예외가 명시돼 있으나 `agents/*.md` 4종에는 없다. 닫으려면 **subagent 호출 내부에서도 auto-compact가 일어나는가**를 실측해야 한다(그 답이 없으면 어느 쪽으로도 단정할 수 없다). v1.198.0 T4가 *"리뷰어 행의 경계는 판정 의미가 없다"*를 표 서문에 넣었다가 **범위 밖·근거 미검증**으로 되돌렸고, 그 task는 보수적으로 3조건을 그대로 적용했다. 리뷰어 2종이 각각 *"T4가 만든 공백이 아니라 선재했던 것"*으로 판정했다. (출처: v1.198.0 T4 quality M2 · spec 2R)
- [2026-08-25] **`docs/plans/deferred.md` 항목당 크기 상한 — 신규 정책 판단 필요** — v1.198.0이 종결·회고를 분리해 296KB → 148KB로 줄였고 T2가 재확인 이력 압축 규약을 세웠으나, **항목 하나의 상한**은 여전히 없다. 분할 직후 실측으로 대기 128건 / 평균 1.2KB이고 최장은 3KB를 넘는다. 상한을 두려면 대장 규약 ③(*"삭제되는 항목도 근거 의무를 면제받지 않는다"*)와 경계를 그어야 한다 — 요지만 남기고 상세를 커밋으로 미는 방식이 그 의무와 충돌하는지가 판단 지점이다. (출처: v1.198.0 Step 8 Q2 미채택 안)
- [2026-08-21] **부분 반영 항목을 대장에서 어떻게 처리할지 규정이 없다 — 큐와 비대칭이다** — 3분기 표는 반영/병합/기각만 두고 **"처방 여러 개 중 일부만 구현된 항목"**을 다루지 않는다. v1.189.0이 그 공백을 실제로 밟았다: `[2026-08-19]`가 처방 ⓐ·ⓑ를 함께 담았는데 ⓐ만 구현하고 **「반영 종결」로 삭제한 뒤 ⓑ를 새 항목으로 재등재**했다 — 정보는 살았지만 **등록일이 08-19 → 08-21로 리셋**돼 소진 batch의 「판정일 최솟값 30일」 축에서 **체류가 초기화**된다. 같은 회차가 vault 큐(`skill-feedback.md`)에는 F-6.5의 *"부분 해소도 남긴다"*를 적용해 **항목을 유지하고 부기**했다 — **같은 성격에 두 처리가 갈린다.** 판정 필요: ⓐ 대장도 큐처럼 「항목 유지 + 부분 반영 부기」로 통일할지 ⓑ 삭제+재등재를 유지하되 재등재 항목에 **원 등록일을 승계**시킬지(체류 축 보존). (출처: v1.189.0 F-7 m1) **(2026-08-28 재확인: 유지 — ⓐ(항목 유지+부기)·ⓑ(등록일 승계) 중 무엇으로 통일할지가 선행 판정이라 조건 없는 액션이 아니다. 이번 회차가 그 자리를 또 밟았다 — 「리뷰 subagent가 골든 러너를 각자 띄워」 항목이 ⓐ만 반영된 채 ⓑ로 좁혀져 남아 있다)**

- [2026-08-17] **같은 규정이 한 파일 안에서도 여러 형태로 산다 — 판정 축이 여섯이다** — v1.177 회차에서 Phase F-7이 **6라운드 연속** 새 지점을 냈고 매번 다른 형태였다: ① 열거형(`index 분할.*산문`) ② **타입별 4칸 매트릭스**(정본 §2.x / SKILL 표 / templates 주석 / 실행 절차) ③ **전역 미러 쌍**(`SKILL.md:238` ↔ `schema:496` 같은 타입 무관 규정) ④ **코드 주석**(`.md` 전용 grep이 `lint.py:72`·`:73`의 규정 인용을 구조적으로 못 본다) ⑤ **eval 케이스 `rationale`·픽스처 서술** ⑥ **헤딩·라벨 ↔ 그 절 본문의 발동 판정 스텝**(4칸 매트릭스는 파일 단위 집계라 절 내부의 반쪽 수정을 못 본다). **규정 문면을 옮기는 작업은 이 여섯 축을 전부 돌려야 한다.** (2026-08-18 재확인: 유지 — v1.180.0 회차(위키 인덱스 파생화·스킬 파일 분할·git 자동 반영)가 이 영역을 건드리지 않았다.) **(2026-08-28 재확인: 유지 — 조건 없이 착수 가능한 것은 여섯 축의 체크리스트화뿐이고, 이번 회차는 규정 문면을 옮기지 않아 전제가 그대로다)**
- [2026-08-17] **트리거를 옮기면 「종료 조건·승급 게이트·인접 가드」를 같은 회차에 함께 옮긴다** — 진입(트리거)만 임박으로 옮기고 종료 조건(`예산 이내`)·승급 게이트(`그래도 초과면`)를 남기면 **신호가 영구 잔존**한다(v1.177.1 B2·M1이 그 사례). 인용원을 근거로 문면을 신설할 때는 **그 인용원의 인접 예외·가드까지, 그리고 그 예외의 동형 사례 전부에** 옮긴다 — §8의 「3000자까지 반복」만 가져오고 바로 다음 줄의 정지 가드를 두고 온 사고가 있었고, 그것을 고칠 때도 §2.8에만 적용해 §8·§4 5번을 또 남겼다. (2026-08-18 재확인: 유지 — v1.180.0 회차(위키 인덱스 파생화·스킬 파일 분할·git 자동 반영)가 이 영역을 건드리지 않았다.) **(2026-08-28 재확인: 유지 — 규정 이동 작업이 없는 회차라 전제 불변 — 착수는 다음 트리거 이동 회차에 그 절차를 적용하는 형태다)** **(2026-09-03 재확인: 유지 — *"다음 트리거 이동 회차"*는 착수 **시점**이지 외부 사건 트리거가 아니고, 여섯 축의 체크리스트화는 지금 가능한 조건 없는 액션이다)**
- [2026-08-17] **유보·Out of Scope 판정은 매 라운드 근거 생존을 재확인한다** — v1.177 회차에서 F-7 4R이 산문 4타입 트리거를 *"정본·실행 절차가 「초과」로 내부 정합"*을 근거로 유보했는데, **그 정합의 지붕(§4 표 헤더)을 같은 회차 3R이 임박으로 바꿔 이미 무너뜨린 상태**였다. 5R이 그것을 BLOCKER로 승격했다. 재분할 축의 Out of Scope도 같은 구조로 근거가 무너졌다(6R M2). **유보는 근거가 살아 있을 때만 유효하다.** **(2026-08-28 재확인: 유지 — 이번 회차의 계획 리뷰 2라운드에서 실제로 지켜졌다(1R 유보 없음·2R 지적이 전부 신규) — 규약화 대상은 여전히 미착수)**
- [2026-08-15] **글로벌 지침에 단위 혼동 경고가 있는데도 같은 오판이 재발했다** — 전역 CLAUDE.md 3단계 검증 절이 *"임계·상한은 그것이 정의된 단위로 잰다… 실제로 바이트로 잰 값을 문자 수 상한과 대조해 초과로 오판한 적이 있다"* 고 이미 적고 있는데, v1.173.0이 vault `conventions.md`를 `wc -c` 값(19,105)으로 재어 **"12,000자 대비 159% 초과"** 로 대장에 등재했다(실측 10,287자 = 86%, 초과 아님). 그 항목을 근거로 다음 회차가 분리 작업에 착수했다. 지침이 **"검증할 때"** 절에 있어 **측정하는 순간**에는 상기되지 않는 것이 원인으로 보인다. 후보: ⓐ 예산·임계를 다루는 문서(`harness-conventions.md` 예산 표 서문·llm-wiki 예산 절)에 *"이 표의 단위는 문자 수 — `wc -c`는 바이트"* 1구를 두어 **읽는 자리에서** 알리거나 ⓑ 값을 적을 때 단위 접미(`자`/`B`)를 의무화한다. (출처: v1.173.1 위키 ingest 세션 관측) (2026-08-18 재확인: 유지 — v1.180.0 회차(위키 인덱스 파생화·스킬 파일 분할·git 자동 반영)가 이 영역을 건드리지 않았다.) **(2026-08-24 재확인: 유지 — 지침의 배치를 조정하는 것이 조건 없는 액션)**
- [2026-08-15] **`deferred.md`와 `skill-feedback.md`가 같은 건을 이중 관리한다** — v1.173.0이 신설한 스킬 개선 큐(vault `skill-feedback.md`)의 이관 6건 중 **2건**(#5 소진 batch 잔량 축 · #7 스택 공통 지식 라우팅)이 이 대장 `## 대기`에도 있다(각각 `[2026-08-12]`·`[2026-08-13]` 등재분). 큐 항목에는 `[중복]` 병기로 표시했으나 **구조 자체는 그대로**다 — 두 대장이 같은 건을 담으면 한쪽만 처리하고 다른 쪽이 남는다. 판정 필요: ⓐ 큐는 "다른 프로젝트에서 관측한 것"만 담고 하네스 자체 관측은 이 대장으로 일원화할지 ⓑ 아니면 큐를 유입구로만 쓰고 대장으로 이관하는 경로를 둘지(사용자는 v1.173.0 계획에서 **자동 이관을 택하지 않았다** — 대장이 이미 착수 부족이라). (출처: v1.173.0 T10·F-7) **[2026-08-15 갱신]** 중복 2건 중 **`#7` 스택 공통 지식 라우팅은 해소됐다** — v1.174.0 T2가 `procedures-content.md` B-1 0에 스택 공통 라우팅 분기를 신설해 이 대장의 `[2026-08-13]` 짝을 반영 종결(삭제)했다. **[2026-08-18 갱신]** 남아 있던 중복 `#5`(소진 batch 잔량 축)도 **해소됐다** — v1.179.0 T8(`bdbae25` — 그 회차의 완료 커밋 `a5683c1`에는 이 파일이 담기지 않았다)이 `pjc:plan` Step 1 ③을 「100건 초과 **AND** 신규 30건 이상」으로 고쳐 대장 짝(`[2026-08-12]`)이 반영 종결됐고, **vault 큐 쪽 항목은 v1.180.0 T7이 신설한 F-6.5 「`[SKILL-IMPROVE]` 해소분 제거」로 이번에 제거**했다(vault 커밋 `102018c`). 즉 *"위키 세션 소관이라 제거되지 않는다"* 던 경로가 닫혔다. **다만 이 항목은 유지한다** — 이중 관리 **구조 자체**(같은 건이 두 대장에 등재될 수 있음)는 그대로이고, 판정 ⓐ/ⓑ도 미결이다. 바뀐 것은 *"한쪽만 처리되고 다른 쪽이 영영 남는다"* 는 결과 쪽이다. (2026-08-18 재확인: 유지 — v1.180.0 T7이 해소된 `[SKILL-IMPROVE]` 1건을 vault에서 제거해 **개별 사례는 닫았으나 구조는 그대로다**. 두 대장의 역할 분리 규약이 여전히 없다.) **(2026-08-24 재확인: 유지 — ⓐ/ⓑ 중 어느 구조로 갈지의 판정이 조건 없는 액션)** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-08-13] **`budget_split_chars` 기록값 자체를 검증하는 축이 없다** — 페이지가 자기 판정 문자 수를 **잘못된 기준으로**(예: 펜스 제외 타입에 파일 전체 길이를) 적어도 어떤 검사도 잡지 못한다. lint은 그 값으로 억제 여부만 계산할 뿐 값의 옳음을 검증하지 않고, 골든도 「코드가 `eff_chars`를 넘기는가」만 고정한다(v1.171.0 F-7 3R이 픽스처 값을 5,670→8,910으로 바꿔도 케이스가 PASS함을 실측). 결과는 **억제가 마진보다 넓게 유지되는데 아무도 모르는** 상태다. 해소하려면 lint에 「기록값이 현재 판정 기준값과 크게 어긋나면 INFO」 같은 축이 필요한데, 그 축은 정상적인 성장분과 오기를 구분해야 해 임계 설계가 별도 판단이다. (출처: v1.171.0 F-7 3R m1) **(2026-08-24 재확인: 유지 — lint에 기록값 검증을 넣는 해소안이 특정된 조건 없는 액션)**
- [2026-08-13] **bitleader-dashboard 위키를 레포까지 ingest** — 7커밋 미반영이고 그중 `04c0457`(help.md 우선 표시)이 허브의 「PJC Homepage 고유 추가 … help.md 문서 우선순위」 서술을 거짓으로 만들었다. 배너는 부착해 오독은 막았으나 내용 반영은 절차 B가 필요하다. (출처: v1.170.0 T7·F-7) **(2026-08-24 재확인: 유지 — 절차 B로 지금 착수할 수 있다)**
- [2026-08-13] **vault 루트 `CLAUDE.md`가 어떤 git 보호도 받지 못한다** — 글로벌 gitignore(`~/.gitignore_global:3` `CLAUDE.md`)에 걸려 vault·하니스 양쪽에서 커밋되지 않는다. 백업 삭제 대상을 조사하다 드러났다(그 1건 자체는 현행본과 동일해 손실 없음). 유실 대비가 없다. (출처: v1.170.0 T1) (2026-08-18 재확인: 유지 — v1.180.0 회차(위키 인덱스 파생화·스킬 파일 분할·git 자동 반영)가 이 영역을 건드리지 않았다.) **(2026-08-24 재확인: 유지 — 유실 대비 부재가 실재하고 대책 수립이 조건 없는 액션)**
- [2026-08-13] **BIBI `conventions.md` 27건의 개별 사실을 레포와 대조하지 않았다** — T6은 배치·예산·중복만 검증했고 소스 대조는 절차 B(ingest)의 축이다. 이 축의 결함 1건(AUMID 판정 가능 여부가 `feat-notifications`와 정반대)은 §2.9 중복 검사가 잡아 정정했으나 나머지는 미검증이다. (출처: v1.170.0 T6·F-7 미확인 ⓑ) (2026-08-18 재확인: 유지 — v1.180.0 회차(위키 인덱스 파생화·스킬 파일 분할·git 자동 반영)가 이 영역을 건드리지 않았다.) **(2026-08-24 재확인: 유지 — 절차 B ingest로 지금 착수할 수 있다)**
- [2026-08-12] **agent·skill·hook 신설 시 `validate.ps1` 화이트리스트 동반 갱신을 규약에 명문화** — v1.169.0 T1이 `root-cause-analyzer`를 신설하고도 `validate.ps1:133`의 `$agents` 하드코딩 배열에 등재하지 않아, 재설치 후 통합 검증이 `[WARN] validate 미등록 agent`를 적재하는 상태로 Phase F-7까지 갔다(F-7 BLOCKER로 검출 → T5에서 해소). **계획 단계에서 잡히지 않은 이유는 `docs/harness-conventions.md`의 동반 변경 축 ③(검증 자산)에 이 행이 없기 때문**이다 — 축 ③이 잡은 것은 「문서 로드 예산 기준선」 표뿐이었고, `validate.ps1`의 `$agents`/`$skills`도 **집합을 고정하는 검증 자산**인데 목록에 없었다. 그 배열은 이 repo가 정확히 이 누락을 잡으려고 심어둔 감지기라(`validate.ps1:120` 주석 *"skill 추가 후 validate 갱신 누락 포착"*), 감지기는 작동했으나 **계획이 그것을 예측하지 못한 것**이 문제다. 축 ③에 1구(`agents/`·`skills/`·`hooks/` 신설 시 `validate.ps1` 화이트리스트 동반 갱신)를 추가한다. (출처: v1.169.0 F-7 B1) (2026-08-18 재확인: 유지 — v1.180.0 회차(위키 인덱스 파생화·스킬 파일 분할·git 자동 반영)가 이 영역을 건드리지 않았다.) **(2026-08-24 재확인: 유지 — 동반 변경 축 ③에 행을 더하는 조건 없는 액션 — 이번 회차는 신설이 없어(NFR-3) 발현하지 않았다)** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-08-12] **소진 batch의 "유지" 스탬프에 착수 가능성 판정을 요구하는 형식 규정 (규약 ②)** — v1.169.0 T3에서 유지 28건에 **문자 그대로 동일한 스탬프**(*"직전 판정 근거 그대로 유효, 변경으로 바뀐 전제 없음"*)를 붙였는데, 그 문면은 규약 ②가 **금지한 질문 형태**(*"여전히 유효한가"*)이고 결과도 규약이 경고한 그대로였다(29건 중 종결 1건). F-7은 ⓐ 08-06 스탬프들이 올바른 형태로 적혀 있고 이번 스탬프가 그 판정을 명시 지목해 추적 가능하며 ⓑ 구간 선정·전건 판정이 실재하고 ⓒ 기각 1건이 백틱 근거를 갖췄으며 ⓓ 구조 결함으로 진단해 등재까지 했다는 이유로 **판정 회피는 아니라고** 결론했으나, 스탬프 형식이 규약과 어긋난다는 지적 자체는 유효하다. **규약 ②에 "유지 판정 스탬프는 *조건 없이 지금 착수할 수 있는 액션인가*에 대한 답을 1구로 담는다"는 형식 요구를 추가**할지 검토한다(보일러플레이트 재확인이 반복되면 대장은 판정 기록이 아니라 타임스탬프 갱신 로그가 된다). (출처: v1.169.0 F-7 m3) **(2026-08-24 재확인: 유지 — 이번 batch가 항목별 고유 근거로 스탬프를 적어 규정의 필요성을 실증했다 — 형식 명문화 자체는 미착수)**
- [2026-08-06] **트리거 eval 후속 4건 (직전 회차 이월)** — v1.161.0 plan의 미처리 `## Deferred / Follow-up`이며 `notes.md`(로컬 전용)에만 있어 유실 위험이 있었다. **네 건을 1건으로 묶어 등재한다** — 전부 트리거 eval 자산의 후속이고 착수하면 같은 러너·같은 케이스 파일을 함께 손대므로 따로 두면 개별 착수 비용이 내용보다 커진다(v1.162.0 batch가 확인한 병합 원리를 **등재 시점에** 적용한 첫 사례): ⓐ **비격리(`--isolation both`) 대조 측정** — 현행 기준선은 격리 단일이라 두 모드 차이가 관측되지 않는다 ⓑ **`impl-neg-1`의 "timeout + 다른 스킬 발동" 조합 관찰** ⓒ **오발동률 지표에 라우팅 축 추가 여부** — 지금은 "발동했는가"만 보고 "어느 스킬로 갔는가"를 세지 않아, 의도된 라우팅과 오발동이 같은 실패로 집계된다 ⓓ **`plan-pos-9`의 `why` 길이 아웃라이어**와 **description의 한글 완전 문장 관례 이탈**. (출처: v1.161.0 plan Deferred → v1.162.0 T6에서 A 게이트 판정 후 등재 — 비자기유발 ∧ 범위 밖) **(2026-08-19 재확인: 유지 — eval 러너·케이스 파일을 함께 손대는 조건 없는 액션 4건이고 착수 단위가 확정돼 있다)** **(2026-09-03 재확인: 유지 — 13차 판정 전건 스캔. 네 건이 여전히 같은 eval 러너·케이스 파일을 공유하는 조건 없는 액션이고, 병합 등재로 확정한 착수 단위도 그대로다)**
- [2026-08-12] **종결 109건의 소급 정리 (반영·병합 삭제 — 3분기 규약 적용)** — v1.168.0이 F-6.5에 「종결 처리 3분기」를 세워 앞으로는 반영·병합이 삭제되지만, **그 이전에 쌓인 109건은 종전 규약으로 이동된 것**이라 그대로 남아 있다(101,206B — 대장의 절반). 다음 소진 batch가 F-6.5 「종결 처리 3분기」의 판정 규칙 ①~④로 대상을 확정해 처리한다 — **사유 문자열은 상호배타가 아니다**(포함 기준 52/34/26 = 112 ≠ 109, 겹침 8건 · 사유 필드 기준 32/27/19로 31건은 사유 표기 자체가 없다)므로 기계 일괄 삭제 금지. 삭제 시 삭제 누계가 0에서 올라간다. (출처: v1.168.0 plan Deferred) **(2026-08-24 재확인: 유지 — 이번 batch의 범위는 구간 판정이고 109건(101KB)은 별도 task 규모라 다음 회차로 넘긴다 — 조건 없는 액션임은 그대로다)**
- [2026-08-12] **재확인 이력 보유 종결 9건의 회차 궤적이 삭제로 유실되는가 (미확인 전제)** — v1.168.0은 「반영 종결분의 실질 내용은 커밋에 있다」를 **표본 n=1**로만 확인했고(그래서 항목별 커밋 지목 의무로 대체했다), **`(YYYY-MM-DD 재확인: 유지 …)` 표기가 누적된 9건**은 여러 회차에 걸친 관측이라 어느 단일 커밋도 그 궤적을 담지 않는다(최장 2,618자·차장 1,196자가 둘 다 이 유형). 사용자가 예외 없는 원안을 선택해 **수용된 리스크**이나, 소급 정리 때 실제로 무엇이 사라지는지 1회 확인할 가치가 있다. (출처: v1.168.0 plan Deferred) **(2026-08-24 재확인: 유지 — 위 「종결 109건의 소급 정리」에 종속된 확인 항목이라 그것과 함께 착수한다)**
- [2026-08-12] **새 정본 절로 가는 포인터가 기계 검사 밖에 있다** — 대장 서두가 F-6.5의 종결 처리 절을 가리키는데, 포인터 도달성 축의 정규식은 경로 직후 짧은 범위의 낟표만 보므로 경로와 인용이 다른 줄에 있으면 등록되지 않는다(v1.168.0 전후 포인터 건수가 44로 동일한 것이 방증). 절 이름이 바뀌면 조용히 끊긴다 — 포인터 축의 탐지 범위를 넓힐지, 서두 표기를 검사 형식에 맞출지 판단한다. (출처: v1.168.0 F-7 m3) **(2026-08-24 재확인: 유지 — 포인터 축을 넓힐지 서두 표기를 검사 형식에 맞출지의 판단이 조건 없는 액션)**
- [2026-09-03] **같은 뿌리의 두 항목이 병합되지 않은 채 나란히 남아 있다 — 「구 PRD FR-5 선두 재구성」 1쌍** — `[2026-07-29]` 「SKILL.md 선두 구간 재구성 (구 PRD FR-5) — 별도 plan으로 재수립」과 `[2026-08-05]` 「선두 9,000B 재구성(구 PRD FR-5) — 별도 plan 대상」은 **같은 뿌리**(구 PRD FR-5의 선두 배치 재구성)이고 착수하면 같은 파일·같은 예산 표를 함께 손댄다 — 규약 ② ⚠가 정한 **병합**(착수 단위 만들기)의 정면 대상이다. 13차 판정이 수행하지 않은 이유는 **사용자가 지정한 4기준(전제 붕괴·이미 해소·조건 대기·관측 기록) 밖**이기 때문이며, 판정 자체는 이번에 끝나 있다. **착수 조건 없음** — 다음 소진 batch가 둘을 하나로 합치면 된다(흡수처는 자산을 담은 `[2026-07-29]` 쪽이 자연스럽다). (출처: v1.223.2 T3 관측 — 13차 판정 F-6.5)

### hook · 안전망 (차단·경고·골든 러너)

- [2026-08-24] **골든 픽스처가 `## Repository Structure`를 이관 대상 절 이름으로 쓴다** — `record-project-fact/SKILL.md`가 그 이름을 **AGENTS.md에 두지 않을 절**로 규정하는데, `relocate-agents.py` 골든 픽스처(`near-96` 등)는 그것을 이관 **대상** 절 이름으로 쓴다. 합성 테스트 데이터라 실동작에는 무관하고 그 픽스처는 v1.194.0 이전부터 그 이름을 썼다(**기존 문제** — 규칙 4-1 ⓐ). 픽스처 절 이름을 중립적인 것(`## Module Layout` 등)으로 바꿀지는 별도 판단이며, 바꾸면 그 이름을 기대값으로 쓰는 케이스도 함께 손봐야 한다. (출처: v1.194.0 T2 spec 리뷰 M2 → v1.195.0 T2에서 대장 이관)
- [2026-08-21] **`AGENTS.md` 목차 폴백이 실제로 켜졌을 때 알리는 신호가 없다** — v1.189.0이 `check_doc_budget()`에 상한 대비 검사를 넣어 **예방**은 닫았다(초과하면 정합 검사가 FAIL). 그러나 그것은 **워킹트리**를 재는 것이라 **재설치 전 설치본이 낡아 폴백이 켜진 구간**은 잡지 못한다 — 그 세션은 빌드·테스트 명령과 `## DO NOT`이 빠진 채로 도는데 아무도 모른다. 원 등재(`[2026-08-19]`)가 처방 후보 ⓐ(예산 표 행)·ⓑ(폴백 시 stderr 알림)를 함께 적고 *"배타적이지 않다"*고 했는데 **ⓐ만 채택**됐으므로(사용자 선택) ⓑ가 유실되지 않게 여기 남긴다. 착수하면 `session-context.ps1`의 폴백 분기에서 stderr 1줄을 내면 되고, 그 hook은 골든 회귀 대상이라 케이스도 함께 든다. **원 등록일 `2026-08-19` — 체류 축(소진 batch 「판정일 최솟값」)은 그 날짜로 잰다**(삭제+재등재로 등록일이 리셋됐다. 대장 처리 규정 자체는 같은 회차가 `### 대장 운영`에 등재한 항목이 판정한다). (출처: v1.189.0 — 원 항목은 T4가 반영 종결) **(2026-08-28 재확인: 유지 — 처방 ⓑ(폴백 분기 stderr 1줄)는 조건 없이 착수 가능하고 대상 파일이 이번 회차가 고친 `session-context.ps1`이다 — 그 자리를 열었으나 범위(compact 주입) 밖이라 넣지 않았다. 다음 hook 회차의 1순위)**

- [2026-08-15] **인위 발화로 검증한 실행은 산출물이 지워져 사후 대조가 불가능하다** — v1.173.1 T3 ⑦이 골든 러너의 미완주 분기를 `-StateDir` 격리 + 자식 `Stop-Process`로 결정적 발화시켜 실증했는데, 절차가 임시 StateDir 삭제로 끝나 **"인위 종료가 실제로 있었다"는 근거가 커밋 본문 진술뿐**이 됐다. 같은 회차의 정상 경로(⑥)는 `scope.txt`의 자산 해시로 F-7이 독립 대조에 성공한 것과 대비된다. 후보: 이런 실증은 삭제 전에 결과 줄 3~5줄을 **파일 산출물로 남기거나**, `scope.txt`처럼 사후 대조 가능한 흔적을 커밋 본문에 지목한다. (출처: v1.173.1 F-7 m1) **(2026-08-24 재확인: 유지 — 산출물 보존 후보가 특정된 조건 없는 액션)**

- [2026-07-30] **`last_assistant_message`의 실환경 도착 경로 확인 (낮은 우선순위)** — v1.148.0 T2의 차단 조건 ④가 이 stdin 필드를 쓰는데 실환경 제공 여부가 미실증이다. **다만 위험은 해소됐다** — quality BLOCKER 지적을 받아 **transcript 폴백**(마지막 assistant 텍스트 직접 추출)을 넣었고 폴백 경로 단독 BLOCK 발화를 pwsh 7·PS 5.1 양쪽에서 실측했다. 즉 필드가 안 와도 검사는 동작한다. 남은 것은 **어느 경로가 실제로 쓰이는지** 확인뿐이며, 릴리즈 후 hook 이벤트 로그로 보면 된다. (출처: v1.148.0 T2) **(최종 2026-09-03 재확인: 유지 — 13차 판정 전건 스캔. 위험은 이미 해소됐고(transcript 폴백 실측) 남은 것은 *"어느 경로가 실제로 쓰이는지"* 확인뿐이라 외부 사건 트리거로 읽힐 여지가 있으나, 그 확인이 hook 정비 회차에 **조건 없이** 가능해 규약 ②의 안전 방향(유지)을 따른다 · 이전 3회 동일)**

- [2026-07-30] **시크릿 탐지 오탐의 재귀 구조** (v1.147.0에서 부분 해소) — **추적 파일은 해소됐다**(T3: 편집 시점 검사가 `git diff HEAD`의 추가 라인만 스캔 → 이력에 이미 있는 예시는 재신고 안 함. 탐지 패턴은 무수정이라 미탐 위험 0). **남은 것은 셋이다**: ⓐ `plan.md`·`notes.md` 같은 gitignore 파일과 신규 파일 — `git diff`가 성립하지 않아 전재 폴백이므로 오탐이 그대로다(**오탐 사례를 그 문서에 기록하면 그 기록이 다시 오탐을 만드는 재귀**도 여기에 속한다) ⓑ **추적 파일도 커밋 전까지는 반복 경고된다** — 스캔 범위가 `git diff HEAD`(HEAD 대비 **미커밋 추가분 전체**)라 "이번 편집분"만이 아니어서, 시크릿 형태를 새로 적은 파일은 **커밋할 때까지 매 저장마다 같은 경고**가 난다(F-7 m3 지적 — 줄어든 것은 *이력에 이미 있는* 내용의 재신고뿐이다) ⓒ 커밋 경로의 동일 클래스 결함(**위** 별도 항목 — 단 그 성질은 오탐이 아니라 **미탐**이다). 완화 후보는 `Test-WarnOnce`(세션·파일당 디듑) 확대이며, 탐지 규칙에 "설명 문맥 제외"를 넣는 방향은 **기각**했다(미탐 위험 + `decisions.md:32` 차단 완화 기각 선례). 원 서술: 시크릿 규칙·예시를 *서술하는* 문서가 저장할 때마다 자기 자신을 오탐으로 잡는다(v1.146.0에서 `plan-template.md`·`deferred.md`·`run-hook-evals.ps1` 3파일 관측). 더 나쁜 것은 **그 오탐 사례를 문서에 기록하면 그 기록이 다시 오탐을 만든다**는 점이다(plan.md에서 실측). 탐지 규칙에 "설명 문맥(❌/✅ 대비·코드펜스 안 예시) 제외"가 없는 한 반복된다. (출처: v1.146.0 T2·T5 · notes 기존 항목의 확장) **(2026-08-05 재확인: 유지 — 잔여 ⓐ(gitignore·신규 파일 전재 폴백)·ⓑ(커밋 전까지 반복 경고) 그대로이고 완화 후보 `Test-WarnOnce` 확대는 미착수)** **(2026-08-05 병합 흡수: 「`deferred.md` 자체가 시크릿 경고를 상시 유발」 항목에서 — 완화 후보에 "규칙 문서·대장 **경로 예외**"를 더한다. 기각한 "설명 문맥 판별"과 달리 경로 예외는 탐지 규칙을 건드리지 않아 미탐 위험이 없다)** **(2026-08-06 재확인: 유지 — 3구간 batch. **이번 세션에서 ⓐ가 실제로 3회 발화**했다: 이 대장 파일을 편집할 때마다 post-write-checks가 `password 값`을 신고했고(대장이 탐지 패턴을 *서술*하는 파일이라 원 항목이 말한 재귀 그대로), 커밋 게이트에서도 같은 경고가 한 번 더 났다. 완화 후보 `Test-WarnOnce` 확대가 여전히 유효하며 관측 근거가 이번에 보강됐다)** **(2026-08-23 재확인: 유지 — 조건 없이 착수 가능한 hook 정비이고, v1.193.0은 신규 게이트를 더했을 뿐 이 판정 지점을 건드리지 않았다)** **(2026-08-24 재확인: 유지 — 9차 batch(절대 상한 축). 전건이 2026-08-23 재확인 상태였고 v1.195.0(인용 인자 스트립)이 이 항목의 전제를 바꾸지 않았다)**
- [2026-07-14] **표기만 보는 정규식의 두 발현 — 상위 탈출은 닫혔고 `$env:` 별칭 값은 남는다** (v1.182.0에서 부분 해소) — 원 항목은 「문자열 형태의 상위 탈출(`..`)·`/.*` 미탐」이고 **[2026-08-06 병합]** 으로 「`$env:` 별칭 시스템 루트의 깊이 cap 미탐」을 흡수했다(둘 다 **정규식이 값이 아니라 표기만 본다**는 한 한계의 두 발현 — `..` 상대 상승 / `$env:NAME`이 가리키는 실제 값). **v1.182.0 T2가 상위 탈출 쪽을 닫았다** — `$dangerTarget`에 ⓐ 루트 dotfile 글롭 `/\.\*` ⓑ **루트로 상승만 하다 끝나는 `..` 연쇄**(`..`가 2회 이상 연속되고 경로가 거기서 끝나는 형태) 두 성분을 넣었다. 임의 상대 경로는 대상 밖이라 `../build` 정리·`../../build`·되돌아오는 `..`는 그대로 통과하며, 같은 변수를 재사용하는 chmod 경로까지 델타 음성으로 실증했다. **남은 것은 `$env:` 별칭 쪽이다** — `$env:SystemRoot`처럼 **변수가 가리키는 값**이 시스템 루트인 경우는 표기만으로 판정할 수 없어 hook이 환경변수를 실제로 해석해야 하고, 그것은 이번 확대의 한정 범위(표기가 스스로 루트 상승을 드러내는 형태) 밖이다. 착수하려면 「값 해석을 도입할 것인가」를 먼저 정해야 한다 — 해석은 세션 환경에 따라 판정이 갈리므로 골든으로 고정하기 어렵다는 반대 근거가 있다. (출처: v1.121.0 plan Deferred / plan-reviewer 2회차 M1 · 2026-08-06 병합 · v1.182.0 T2) (2026-08-20 재확인: 유지 — v1.182.0 T2가 상위 탈출을 닫았고, 남은 `$env:` 별칭 값은 「값 해석을 도입할 것인가」를 먼저 정해야 착수할 수 있다. **v1.188.0은 날짜 표기 형식만 교정했다**.) **(2026-08-23 재확인: 유지 — 조건 없이 착수 가능한 hook 정비이고, v1.193.0은 신규 게이트를 더했을 뿐 이 판정 지점을 건드리지 않았다)** **(2026-08-24 재확인: 유지 — 9차 batch(절대 상한 축). 전건이 2026-08-23 재확인 상태였고 v1.195.0(인용 인자 스트립)이 이 항목의 전제를 바꾸지 않았다)**
- [2026-07-08] block-destructive: `cat <<EOF > file`(데이터 싱크 스트립) 후 같은 Bash 호출에서 즉시 실행 시 위험 본문 미스캔 — "파일 작성+동일 호출 실행" 조합 감지 개선 (출처: harness-quality-part1) **[2026-08-06 병합]** 「[2026-07-13] 한 호출 안에서 파일 생성 + 즉시 커밋은 여전히 통과」를 이 항목에 흡수했다 — 둘 다 **PreToolUse 시점에 파일이 아직 없다**는 같은 구조적 한계이고, 흡수된 항목이 제시한 해법(*"heredoc 본문이 command 문자열에 그대로 있으므로 명령 텍스트 자체를 `Get-SecretMatches`에 통과"*)이 이 항목에도 그대로 적용된다. **(최종 2026-09-03 재확인: 유지 — **규약 ② ⚠의 「관측 경로 없음」 예외**. 차단 hook의 **미탐**이라 위험 명령이 조용히 성공할 뿐 아무도 신고하지 않는다 — *"관측되면 다시 등재된다"*는 전제가 성립하지 않는다(08-06 재확인이 *"보안 미탐이라 기각 불가"*로 이미 못 박았다) · 이전 4회 동일)**
- [2026-08-03] **러너 분리 후속 정리 5건 (순수 이동 원칙 때문에 이번에 미룬 것들)** — v1.152.0 T1이 "이동만 하고 아무것도 고치지 않는다"를 판정 기준으로 삼아, 눈에 띈 정리를 전부 다음으로 넘겼다. ⓐ `§4`의 `foreach ($ph in $phraseList)` 루프 변수가 `§2b`의 디렉터리 변수와 **이름이 같다**(dot-source로 스코프 공유 — 현재는 실행 순서상 무해하나 파일이 갈려 충돌이 눈에 덜 띈다) → `$phrase`로 rename. ⓑ 러너의 실행 배너·부분 실행 경고 헤더가 블록 #1 범위에 있어 **`scenarios/stateless.ps1`에 종속**됐다 → 본체 헤더로 승격. ⓒ 본체 헤더 9행 *"② 이 파일의 시나리오 섹션"* 이 분리 후 문면상 어긋난다(바로 아래 ※ 2줄로 정정해 뒀다) → 9행 자체를 `② 시나리오 섹션(scenarios/*.ps1)`으로 정리. ⓓ 시나리오 헤더의 경계 표식 1줄은 재조립 등가 검사를 자동화하려고 넣은 것인데 규약으로 명문화돼 있지 않다(블록 본문도 `#`로 시작할 수 있어 주석 개수로는 헤더 끝을 못 잡는다). ⓔ `New-TranscriptLine`에 `-IsMeta`·`-ToolUseSkill`을 추가해 `$metaPayload`·`$skillEntry` 같은 나머지 리터럴 픽스처도 전환. (출처: v1.152.0 T1·T3) **(2026-08-05 재확인: 유지 — **부분 해소** — ⓐ(루프 변수 rename)는 해소(`foreach ($ph in $phraseList)` 0건)이나 ⓒ(본체 헤더 9행 *"② 이 파일의 시나리오 섹션"*)와 ⓔ(`New-TranscriptLine`의 `-IsMeta`·`-ToolUseSkill`)는 무변경(실측))** **(2026-08-06 재확인: 유지 — ⓐ(`$ph` 이름 충돌)는 v1.159.0 T6의 프로세스 분리로 구조적으로 해소됐다(시나리오가 각자 다른 프로세스에서 돌아 스코프를 공유하지 않는다). 다만 등재된 나머지 4건은 미확인이라 항목을 유지한다 — 다음 batch가 ⓑ~ⓔ를 개별 판정할 것)** **(2026-08-24 재확인: 유지 — ⓐ~ⓔ가 전부 코드 지점으로 특정된 조건 없는 액션 — 이번 회차가 손댄 `scenarios/suggest-agents-record.ps1`은 그 다섯과 다른 지점이다)**
- [2026-08-03] **`warn-commit-secrets`의 "이력에 이미 있는 내용" 면제가 파일 이동에는 적용되지 않는다** — 추적 파일은 `git diff HEAD` 추가 라인만 스캔해 자기참조 오탐을 막지만, **같은 내용을 신규 파일로 옮기면 untracked라 워킹트리 전체가 스캔**돼 기존 골든 시크릿 픽스처가 "신규 유입"으로 차단된다. v1.152.0 T1에서 실현됐고(차단 지점 `scenarios/post-write-checks.ps1:28-29` = 구 `run-hook-evals.ps1:1153-1154`), 사용자가 `CLAUDE_HARNESS_ALLOW_SECRET=1`로 우회해야 커밋이 됐다. **이 레포처럼 탐지 픽스처를 보유한 곳에서는 파일 분리·이동 작업마다 재발한다.** 대책 후보 ⓐ 신규 파일이어도 `git log -S`로 이력 실재를 확인해 면제 ⓑ 스캔 제외 경로(`hooks/evals/`) 도입 — 둘 다 차단 게이트 완화라 별도 계획·승인이 필요하다. (출처: v1.152.0 T1) **(2026-08-05 재확인: 유지 — `guard-bash.ps1`의 `diff HEAD` 호출 9지점 무변경)** (2026-08-18 재확인: 유지 — v1.180.0 회차(위키 인덱스 파생화·스킬 파일 분할·git 자동 반영)가 이 영역을 건드리지 않았다.) **(2026-08-24 재확인: 유지 — 차단 게이트 완화라 승인이 선행되나 그 승인 요청 자체는 지금 할 수 있는 액션이다)** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-08-03] **검증 자산이 자기 결함을 못 보는 두 형태 (v1.152.0에서 각각 실현)** — ⓐ **골든 green인데 픽스처가 깨져 있다**: T3에서 `New-TranscriptLine` 초안이 단일 요소 배열을 언랩해 `content`를 `[{...}]`가 아니라 `{...}`로 직렬화했는데, hook이 `@($mc)`로 감싸 읽는 덕에 **골든은 96/96 green**이었다. 검출한 것은 전환 전 리터럴과의 문자열 대조뿐 → **픽스처 생성 방식을 바꾸는 작업은 골든 통과를 등가의 근거로 쓸 수 없다.** ⓑ **판정 로직을 "재현"하는 도구가 과탐한다**: T4에서 판정 재현 도구(v1.225.0에 hook 과 함께 제거)의 초안이 그 hook 의 구조 필터(`if (-not $isUser -and -not $isAsst) { continue }`)를 빠뜨려, skip 4종은 정확히 옮겼는데도 예산을 과소비해 **없는 DRIFT 5건을 "발견"으로 보고**했다(최댓값 200 → 필터 추가 후 1~2). → **pin 검사 대상은 "skip"이 아니라 "예산에 관여하는 판정식 전부"** 여야 하고, 재현형 도구의 첫 결과는 **원본 주석의 기존 실측치와 자릿수를 대조**해야 한다. (출처: v1.152.0 T3·T4) **(2026-08-05 재확인: 유지 — 이번에 신설한 정합 검사는 **문서 축**이라 이 항목이 지목하는 픽스처·재현 도구 축과 다르다 — 전제 불변)** (2026-08-18 재확인: 유지 — v1.180.0 회차(위키 인덱스 파생화·스킬 파일 분할·git 자동 반영)가 이 영역을 건드리지 않았다.) **(2026-08-24 재확인: 유지 — 두 교훈이 규약화 대상 문서를 지목하지 않아 기록성으로도 읽히나 *"pin 검사 대상은 예산에 관여하는 판정식 전부"*는 실제 수정 대상이라 갈린다 — 규약 ②의 「애매하면 유지」)** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-08-06] **[SUGGEST] 골든 병렬: 미완주 그룹의 부분 취합에서 직접 `$results.Add()` 판정이 빠진다** — 자식이 죽으면 증분 라인만 남는데 그것은 `Assert-Case` 경유분뿐이라, 4개 시나리오(`warn-commit-secrets`·`session-context`·`hook-event-log` 등)의 직접 호출분 29건이 부분 취합에 포함되지 않는다. 그룹 전체가 `[FAIL] 완주하지 못했다`로 표시되므로 조용한 통과는 아니지만, "판정 N건까지 기록됨" 수치가 실제보다 작아 커버리지를 과소평가하게 만든다. **해소안**: 그 4개의 직접 호출을 `Add-EvalResult`로 교체(완주 시 전량 덤프 정책은 유지). (출처: v1.159.0 T6 quality m1 — 판정 유보) **(2026-08-19 재확인: 유지 — 해소안이 코드 지점으로 특정됨(4개 시나리오의 직접 호출을 `Add-EvalResult`로 교체))** **(2026-09-03 재확인: 유지 — 13차 판정 — 해소안이 코드 지점으로 특정된 조건 없는 액션(4개 시나리오의 직접 호출을 `Add-EvalResult`로 교체))** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-07-08] **(저신뢰 audit) block-destructive 열거 `.` 제외의 이름필터 판정에서 `-Include *.tmp,*` 콤마 배열 catch-all 원소 통과 여지** — 차기 hook 정비 때 감사 케이스로 검토 (출처: harness-quality-part1 F-7) 차단 hook의 **미탐**이라 위험 명령이 조용히 성공할 뿐 아무도 신고하지 않는다 — *"관측되면 다시 등재된다"* 는 전제가 성립하지 않는다. **(최종 2026-09-03 재확인: 유지 — **규약 ② ⚠의 「관측 경로 없음」 예외**(차단 hook 미탐). 5차 batch가 기각했다가 F-7 지적으로 복귀시킨 항목이고 그 오판이 규약 ②의 미탐 예외를 명문화한 계기라 같은 오판을 반복하지 않는다 · 이전 5회 동일)**
- [2026-08-19] **고아 콘솔 프로세스가 쌓여 골든 러너를 5배 느리게 만든다 — 발생원 미상** — v1.182.0 회차에서 전체 실행이 37분 → 2시간 이상으로 늘었고 원인은 **고아 `more.com` 54개**였다(각 누적 CPU 20,000초+). **처방 ⓐ(그물)는 해소됐다** — v1.183.0이 `session-end-cleanup-lib.ps1`을 신설해 **Stop·SessionStart·SessionEnd** 세 시점에서 부모가 죽은 `more.com`·`find.exe`를 회수한다(회수 사실은 `hook-events`에 `decision='cleanup'`으로 적재). **처방 ⓑ(발생원 차단)는 원 가설이 반증됐다** — *"`--no-pager`·`GIT_PAGER=cat` 미지정이 유력"*이라 적었으나 **`git var GIT_PAGER`가 `less`를 반환하고 `core.pager`·`PAGER`·`GIT_PAGER`가 전부 미설정**임을 2026-08-20에 실측했다(git은 발생원이 아니다). `more.com`의 발생원은 여전히 미상이며, 적재된 이벤트의 **시각대·건수 분포**가 쌓이면 그것으로 좁힌다. **한편 같은 회차에 `find.exe` 축은 원인이 특정됐다** — `find /` 류 전역 탐색이 Bash 도구 10분 캡을 넘겨 셸만 죽고 자식이 고아로 남는 구조이며, v1.183.0의 `warn-global-find` 경고가 실행 전에 이를 막는다. **회수로 닫히지 않는 경우도 실측됐다** — 커널에 갇힌 프로세스는 `Stop-Process`·`taskkill`이 무효라 `Unkillable`로 계상하고 마커로 재시도만 멈춘다. (출처: v1.182.0 F-6.5/T8 관측 → v1.183.0 T1·T2·T7에서 ⓐ 해소·ⓑ 가설 반증) **(2026-08-28 재확인: 유지 — 발생원 특정이 이벤트 누적에 달려 있어 조건 없는 액션이 아니다 — 이번 회차의 골든 전체 실행 1회분이 그 표본에 더해진다)** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-08-19] **[SUGGEST] `git add -A && git commit -am`이 같은 `git diff HEAD`를 두 번 계산한다** — v1.182.0 T5가 `$autoStage` **오판정**을 없앴지만, 이 조합에서는 두 분기가 **각각 정당하게** 발화해 전체 스코프 스캔이 두 번 돈다. 결과는 `Select-Object -Unique`로 합쳐져 사용자 노출 중복은 없고 차단 판정도 같으므로 **결함이 아니라 낭비**다(서브프로세스 1회). 한 호출 안에서 「전체 스코프를 이미 스캔했다」 플래그를 캐시하면 사라진다. (출처: v1.182.0 T5 quality 2R S1) **(2026-08-20 소급 적용: 유지 — 보존 규칙 ⓑ 애매)** *"결함이 아니라 낭비다"* 는 **심각도 판정**이지 ⓓ가 말하는 *착수 보류 결론*(비용>효과·의도적 유지)이 아니고, 해소안(플래그 캐시)과 대상이 함께 특정돼 있다. **(2026-08-28 재확인: 유지 — 해소안(플래그 캐시)과 대상이 특정돼 조건 없이 착수 가능하나, 낭비가 서브프로세스 1회분이라 우선순위가 낮다)**
- [2026-08-19] **`.state` 마커 30일 TTL이 세 hook에 손으로 복제돼 있다** — `post-write-checks.ps1`·`guard-write.ps1`·`suggest-agents-record.ps1`이 각자 `AddDays(-30)` 블록을 갖는다(v1.182.0 T3이 뒤 둘을 채우며 3회가 됐다). 공통 함수로 빼지 않은 근거는 **세 hook 모두 dot-source가 전무해** 공통 모듈이 도구 호출마다 뜨는 hook에 새 로드 의존을 만든다는 것이고(D3 정정본), 그 대가가 **값이 갈려도 아무도 모른다**는 것이다. 처방은 둘 — ⓐ `check-harness-consistency.py`에 「세 곳의 TTL 상수 일치」 축을 넣어 기계로 묶거나 ⓑ 로드 비용을 실측해 공통 헬퍼 추출이 감당 가능한지 판정한다. **ⓐ가 먼저다** — 추출 없이도 드리프트를 막고, 로드 비용 실측 없이 ⓑ를 고르면 상시 비용을 근거 없이 늘린다. (출처: v1.182.0 T3 quality 리뷰 m1) **(2026-08-28 재확인: 유지 — 처방 ⓐ(정합 검사 축으로 묶기)는 조건 없이 착수 가능하며, 이번 회차 T3이 그 검사기에 축을 하나 더한 직후라 같은 자리에 넣기 쉬워졌다)** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**

### 하니스 스킬 (plan · implement · 리뷰어 · 템플릿)
- [2026-08-28] **Deferred 마커를 대장 항목 형식에도 적용할지** — v1.214.0이 plan `## Deferred / Follow-up` 항목에 등재 판정 마커 3종(`[등재]`/`[미등재:<게이트 기호>]`/`[미판정]`)을 도입했으나, 대장(`docs/plans/deferred.md`) 자신의 항목 형식(`- [등록일] …`)은 건드리지 않았다. 대장 쪽에 대응 표시가 필요한지는 별도 판단이며, 필요하다면 「카운트 기준」의 날짜 접두 정규식과 충돌하지 않는 형태여야 한다(현행 대기 패턴은 `^- \[\d{4}-\d{2}-\d{2}`). (출처: v1.214.0 plan Deferred 이관)
- [2026-08-28] **마커 3종·Ledger 리터럴의 복제 쌍에 지속 동기 축이 없다** — v1.214.0은 형식 정본을 구 `phase-f-detail.md` 한 곳에 두었고(v1.226.0 T7이 그 정본을 `plan/references/plan-template.md` 로 옮겼다) `plan-template.md`·`SKILL.md`에는 리터럴과 포인터만 남겨 복제 표면을 최소화했으나, 리터럴 자체(`- [등재]`·`- [미등재:<게이트 기호>]`·`- [미판정]`·`등재 N / 미등재 M`)는 여전히 여러 파일에 산다. **실제로 이 회차에서 한 번 갈렸다** — T1 초안이 `<기호>`와 `<게이트 기호>`로 어긋난 것을 수동 대조로 잡았고(T1 커밋), T4에서도 `게이트 제외 M`↔`미등재 M` 용어 갈림을 리뷰가 지적했다. 같은 성격의 의도적 복제(Deferred 착수 조건)는 `check-harness-consistency.py` 축 ⑬(`check_batch_trigger_sync`)으로 기계화한 선례가 있다. 축 신설은 검사기 수정 + 골든 설계가 따라붙어 그 회차 범위를 넘어 미뤘다. **같은 뿌리의 두 번째 실측이 F-7에서 나왔다** — `docs/harness-conventions.md:172` 「메인 컨텍스트 합계」의 성분 `pjc:plan`가 표 행과 어긋난 채 남았고(그 줄은 **기계 대조 밖**이라 검사기 13축이 exit 0을 내면서도 못 잡았다), **커밋 본문은 「합계를 재산출했다」고 적고 있었다**. v1.186.0에 이어 두 번째 드리프트다. 대상 범위가 「같은 규정의 복제 리터럴」에서 「표에서 옮겨 적은 파생 수치」까지 넓어진다. **세 번째 근거(F-7 2R m2)**: 같은 문서 `:174` 「리뷰어」 행의 범위 수치 `29,628 ~ 58,025B`가 표 실측(`completion-reviewer` 28,777 / `plan-reviewer` 57,695)과 어긋난 채 남아 있다. `:172`·`:173`에는 *"표를 갱신하는 task가 이 줄도 함께 고친다"* 규칙이 붙어 있는데 이 행에는 없어 **기계도 규약도 못 잡는다**(v1.214.0 이전부터 어긋남 — 그 회차가 유발한 것이 아니다). (출처: v1.214.0 plan Deferred 이관 · plan 리뷰 1R m3 · F-7 B1 · F-7 2R m2) **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)** **(2026-09-04 실측 — 이 드리프트가 실현됐다)**: 회차 5가 자기 `plan.md` 마커를 ``- `[등재]` ``(백틱 포함)로 적어 `session-context.ps1` 의 `^- \[등재\]` 에 매치되지 않았고, 미판정 2건으로 계수될 뻔했다(완료 재리뷰가 검출). **원인은 형식 정본 `plan-template.md` 의 표 셀이 마커를 코드스팬으로 표기**해 그대로 옮기면 hook 이 못 읽는 형태가 나오는 것이다. 골든 픽스처(`hooks/evals/scenarios/session-context.ps1`)는 백틱 없는 형태를 쓴다. **`[미판정]` 은 백틱이 있어도 계수돼 우연히 맞으므로 깨지는 것은 면제 마커 2종뿐이고, 그래서 겉으로는 정상으로 보인다.**

- [2026-08-28] **폐기 회차 승계 ⓐ — `writing-for-agents` 개념 4개를 `AUTHORING.md`에 도입** — no-op 테스트(모델 기준 판정·문장 통째 삭제)·부정 대신 긍정 서술(하드 가드레일 예외)·분기 기준 progressive disclosure·완료 기준 clarity/demand. 직전 감량 처방 회차(미착수 폐기, 2026-08-28)의 구 T1. 사용자 결정: 전부 Deferred 등재.
- [2026-08-28] **폐기 회차 승계 ⓑ — 감량 처방을 삭제·압축·이관 3종으로 규약 확장** — `docs/harness-conventions.md` 「rationale 이관 임계」를 「본문 감량 처방」으로 개정(선택 순서 ⓐ삭제→ⓑ압축→ⓒ이관, 손실 검증 의무, 구명 앵커·파생 지목 5곳·검사기 안내 문자열 2곳 동반). 폐기 plan의 구 T2 — 그 plan의 §2-1~§2-3 실측(처방이 이관 하나로 좁혀져 대장 감량 항목 3건이 정체)이 근거로 유효하다. F1~F7 회차(v1.211.0)가 압축·이관을 acceptance 손실 검증으로 직접 걸어 부분 실증했다. **(2026-09-02 관측 — 이 규약의 난이도가 실증됐다)**: v1.218.0 후속 회차가 `llm-wiki` 절차 K를 줄이려고 이 개정을 실제로 설계했는데, 계획 리뷰 2R이 **개정안 자신의 판정 기준(「기본 경로 규칙 ↔ 저빈도 규칙」)으로 6줄 중 5줄이 기본 경로로 판정**돼 옮길 수 없음을 보였다(121·124·126·128·130 — 전부 K 2 구간). 다툼 없는 최대 감량 4,305B < 필요량 5,468B라 **이관만으로는 목표에 못 닿았고**, 그 회차는 주입 구간 축소로 우회했다. **규약을 세우려면 「그 절차의 기본 경로 단계 목록」을 산출물로 먼저 요구하는 조건이 필요하다** — 포인터 잔존·기계 검사·손실 검증 3조건은 전부 **옮긴 뒤의 형식 요건**이라 무엇을 옮길 수 있는지를 제한하지 못한다.
- [2026-08-28] **폐기 회차 승계 ⓒ — 과길이·중복·충돌 세 축 전수 실측·대장 정비** — 15파일(SKILL 8+agents 7) 재부착 커버율 표·「정본」 선언 27종 vs 축 ⑫ 커버 4종 차집합 열거·38파일 충돌 조사 3종(정본 지목·수치 임계·방향). 폐기 plan의 구 T3.
- [2026-08-20] **`pjc:plan`·`pjc-systematic-debugging`에 진행 중 출력 규약이 없다** — v1.186.0이 `llm-wiki`(절차당 1줄)와 `pjc:implement`(task당 두 줄 + 루프 밖 단계당 한 줄) 양쪽에 출력 예산을 두었으나, 나머지 두 스킬은 **전수 grep 0건**이다(`pjc:plan/SKILL.md` 88,834B · `pjc-systematic-debugging/SKILL.md` 26,873B). 착수하려면 먼저 **"무엇이 산출물이고 무엇이 내레이션인가"의 경계**를 정해야 한다 — 두 스킬은 USER-INTERACTIVE라 질문·선택지·추천 근거·승인 프롬프트·진단 가설이 **억제 대상이 아니라 산출물**이고, 조사 과정 서술·읽은 파일 요약만 내레이션이다. 그 경계 정의가 그 회차의 본체다. **등재 게이트 판정**: ⓐ 판정 유보 아님(사용자가 범위에서 명시 제외) · ⓑ·ⓒ 수정 대상·원인 모두 이번 회차 `Files` 밖 · ⓓ 미해당(외부 사건 트리거 문구 없음 — 조건 없는 액션) · ⓔ 미해당(대상 두 파일과 동작이 함께 적혀 있다) → **등재**. (출처: v1.186.0 사용자 범위 선택 + F-7 m1) **(2026-08-28 재확인: 유지 — 경계 정의(무엇이 산출물이고 무엇이 내레이션인가)가 그 회차의 본체라 선행 판정이 필요하다)** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-08-20] **등재 게이트 ⓐ(판정 유보)에 「관측 경로가 없는 것」 예외가 없다 — ⓓ에만 걸려 있다** — ⓐ의 논거는 *"정말 결함이면 다음 회차에 다시 관측된다"* 인데, **fail-open 미탐처럼 실패가 조용한 것에는 그 전제가 성립하지 않는다**(ⓓ는 그 예외를 갖는다). v1.185.0 T2 소급 적용에서 실제로 갈렸다 — `[2026-08-06]` 포인터 도달성 미탐 2종은 출처가 *"판정 유보"* 라 규정대로면 ⓐ 기각인데, **미탐이라 관측 경로가 없어** ⓓ 예외를 우선해 유지했다(`deferred.md`의 그 항목 · 안전 방향 이탈이라 오종결은 아니다). 후보: ⓐ에 P3 예외 1구를 걸거나, 두 범주가 겹칠 때의 우선순위를 명시한다. (출처: v1.185.0 F-7 m4) **(2026-08-28 재확인: 유지 — ⓐ에 P3 예외를 걸지 두 범주 우선순위를 명시할지가 선행 판정 — 이번 batch에서 ⓐ 기각 1건을 냈으나 관측 경로가 있는 형태라 이 사각에 닿지 않았다)**
- [2026-08-19] **차단 범위 확대의 「델타 음성」 요구가 경계 단위로 강제되지 않는다** — `AGENTS.md` `## DO NOT` ②는 *"새 경계가 실제로 발화하는 델타 음성으로 오차단 0을 실증할 것"*이라 적지만, **한 task가 경계를 둘 넓히면 그중 하나에만 델타 음성을 걸어도 규정을 지킨 것처럼 보인다.** v1.182.0 T1이 실제로 그랬다 — DB 앵커 경계에는 3종을 걸었고 자격증명 쌍 경계에는 0건이었으며, 그 경계가 평범한 문서 표기를 차단하는 것을 **F-7이 함수 직접 실행으로 잡아냈다**(T8로 해소). 다음 회차가 같은 사각을 되풀이하지 않으려면 ⓐ 규정 문면을 「확대한 **성분마다**」로 좁히거나 ⓑ 리뷰어 체크리스트에 「확대 성분 수 == 델타 음성이 닿는 경계 수」 대조를 넣는다. **ⓑ가 기계적이지만 성분 수를 자동으로 셀 수 없어 ⓐ가 선행한다.** (출처: v1.182.0 F-7 B1) **(2026-08-28 재확인: 유지 — ⓐ(규정 문면을 「확대한 성분마다」로 좁히기)는 조건 없이 착수 가능 — 이번 회차는 차단 hook을 건드리지 않아 전제 불변)**

- [2026-08-19] **리뷰 subagent가 골든 러너를 각자 띄워 메인 실행과 경합한다** — v1.182.0 T2에서 spec·quality 리뷰어가 각각 `run-hook-evals.ps1`을 돌렸고 메인 실행까지 셋이 같은 상태 디렉터리를 두고 겹쳐 **세 실행이 모두 정체**했다(1시간 소진 후 프로세스를 정리하고 단독 재실행해 해소). 리뷰어 정의에도 `pjc:implement`의 V-5/V-6 호출 규약에도 「무거운 러너를 직접 실행하지 말 것」이 없다 — 이번 회차는 T3부터 호출 프롬프트에 그 문장을 손으로 붙여 막았다. 처방 후보는 ⓐ V-5/V-6 호출 프롬프트 규약에 한 줄 추가(Bash 쓰기 금지·판정문 요구와 같은 자리) ⓑ 러너에 실행 중 lock을 두어 두 번째 실행이 즉시 「이미 실행 중」으로 끝나게 하기. **ⓑ가 근본이지만 ⓐ가 즉시 효과가 있고 둘은 배타적이지 않다.** (출처: v1.182.0 T2 관측) **(2026-08-27 재확인: 유지 — 처방 ⓐ는 이미 반영돼 있다.** `pjc:implement/SKILL.md`의 V-5·V-6 두 자리가 *"메인이 돌리는 러너·무거운 검증 명령을 직접 실행하지 말 것"*을 호출 시점 주입 네 축의 하나로 규정하고(`grep -c "러너 직접 실행 금지"` = 2), 위키 `feat-review-subagents.md`도 그 네 축을 서술한다. **삭제하지 않는 이유는 ⓑ(러너 실행 lock)가 미착수**여서 항목 전체가 해소되지 않았기 때문이며, 이 상태는 대장 `[2026-08-21]` 「부분 반영 항목을 대장에서 어떻게 처리할지 규정이 없다」가 가리키는 바로 그 자리다. **남은 착수 단위는 ⓑ 하나로 좁혀졌다** — 다음 회차는 ⓐ를 다시 검토하지 않는다.**)** **(2026-08-28 재확인: 유지 — 남은 착수 단위는 ⓑ(러너 실행 lock) 하나이며 조건 없이 착수 가능하다. **이번 회차가 ⓐ의 실효를 재확인했다** — 리뷰어 5회 호출 전건에 러너 금지 1구를 주입했고 경합이 재발하지 않았다)** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**

- [2026-08-05] **리뷰어 effort를 `xhigh`까지 올릴지** — v1.155.0이 코드 리뷰 2종(`spec-compliance`·`code-quality`)을 `medium`→`high`로 올렸다(Opus 5 공식 권장 기본값). 공식은 **코딩·에이전틱에 `xhigh`**를 권하지만 Sonnet·`maxTurns: 30` 조합에서 실익이 불확실해 한 단계만 올렸다. **효과는 리뷰 라운드 수로 측정**해 판단한다. (출처: v1.155.0 plan Deferred) **(2026-08-18 재확인: 유지 — 전제가 바뀌었다. v1.179.0이 `maxTurns`를 30 → 40/36으로 올렸으므로 *"Sonnet·`maxTurns: 30` 조합에서 실익 불확실"*이라는 보류 근거가 더는 성립하지 않는다. 다만 같은 회차가 예산 자기인지를 함께 넣어 실효 예산이 달라졌으므로, 라운드 수 관측을 한 회차 더 쌓은 뒤 판정한다)** **(2026-08-19 재확인: 유지 — 라운드 수 관측 한 회차 축적이 아직 미충족(08-18 재확인이 건 조건))** **(2026-09-03 재확인: 유지 — 13차 판정 — 효과 측정이 착수 조건으로 읽힐 여지가 있으나 *"리뷰 라운드 수로 측정"*은 이미 쌓이는 데이터라 외부 사건 대기가 아니다 — 판정 갈림에 안전 방향(유지))**
- [2026-08-04] **tail 상한(6000)을 넘는 초장기 루프는 여전히 fail-open** — 실측 최대가 2817줄이라 6000은 2배 여유지만 상한이 사라진 것은 아니다. 넘으면 루프 시작 지점이 창 밖으로 밀려 `$userFound`·게이트를 함께 잃는다. `$loopSkill`처럼 전 파일 스캔으로 올리는 안은 hook 10초 예산에서 `ConvertFrom-Json` 비용이 위험해 채택하지 않았다 — 다만 v1.154.0의 `Get-TranscriptTail`(`-Raw` 기반)로 읽기 비용이 90배 낮아졌으므로 **재평가할 여지가 생겼다**(전 파일 로드 자체는 47ms). (출처: v1.154.0 plan Deferred + D7) **(2026-08-05 재확인: 유지 — 상한 6000 그대로 — 초과 시 fail-open 구조 불변)** (2026-08-18 재확인: 유지 — v1.180.0 회차(위키 인덱스 파생화·스킬 파일 분할·git 자동 반영)가 이 영역을 건드리지 않았다.) **(2026-08-20 소급 적용: 유지 — 보존 규칙 ⓑ 애매)** *"재평가할 여지가 생겼다"* 가 액션 함의로 읽히고 대상(tail 상한 6000)도 특정된다 — 순수 관측이라 단정하기 어려워 유지한다. **(2026-08-24 재확인: 유지 — 08-20 소급 적용의 보존 규칙 ⓑ 판정을 승계한다)**
- [2026-08-04] **[구조] "신규 N건 등재" 형태의 acceptance가 누락을 못 잡는다** — v1.154.0에서 **같은 뿌리의 사고가 두 번** 났다: ⓐ T5가 `AGENTS.md` 기준선을 513→523으로 옮겼는데 **T6가 케이스를 늘리고도 재적용하지 않았다**(F-7 1회차 B1) ⓑ T5 acceptance가 *"신규 4건 등재"*로 **숫자를 고정**해 둔 사이 T3·T4가 5번째 Deferred를 plan에만 추가해 대장·notes에서 빠졌다(F-7 2회차 M1'). 둘 다 **"조건이 나중에 재성립했는데 고정된 목록이 안 늘어난"** 패턴이다. 대안: acceptance를 숫자 대신 **"plan `## Deferred / Follow-up`의 미해소 전 항목 ↔ 대장 항목 단위 대조"**로 쓰면 등재 단계에서 걸린다. 같은 원리로 기준선류 수치는 **"변경 후 실측값과 문서값 일치"**로 표현한다. (출처: v1.154.0 F-7 1·2회차) **(2026-08-05 재확인: 유지 — 규칙화 미착수 — 다만 v1.156.0 T8이 **착수 시 재계수**(F-6.5 소진 규약 ①)로 같은 유형을 한 번 잡아냈다(구간 예상 28건 → 실측 29건). 부분 대응이지 일반 규칙은 아니다)** (2026-08-18 재확인: 유지 — v1.180.0 회차(위키 인덱스 파생화·스킬 파일 분할·git 자동 반영)가 이 영역을 건드리지 않았다.) **(2026-08-24 재확인: 유지 — 대안(숫자 대신 항목 단위 대조)이 문면으로 특정된 조건 없는 액션)**

- [2026-08-03] **`## 동반 변경 판정`이 늘 "해당 없음"으로 채워지면 형식만 남는다** — v1.153.0 T1이 신설한 이 섹션은 판정 대상이 없을 때 `해당 없음 — <확인한 축 1줄>`을 허용한다(과잉 방지). 그래서 **실제로 확인 축 5개를 훑지 않고 그 한 줄만 적는 회피**가 가능하다. `plan-reviewer` 항목 16이 "근거 없는 해당 없음 → MAJOR"로 받지만, 리뷰어가 판정할 수 있는 것은 **근거의 존재**이지 **근거의 참됨**이 아니다. 몇 회 실사용 후 회피가 관측되면 "무관 판정에도 훑은 축을 명시" 같은 강화를 검토한다. (출처: v1.153.0 plan Deferred) **(2026-08-05 재확인: 유지 — `plan-template.md`의 `해당 없음` 3건 그대로 — 형식화 방지 장치 미도입)** (2026-08-18 재확인: 유지 — v1.180.0 회차(위키 인덱스 파생화·스킬 파일 분할·git 자동 반영)가 이 영역을 건드리지 않았다.) **(2026-08-20 소급 적용: 유지 — 보존 규칙 ⓑ 애매)** ⓓ 트리거(*"회피가 관측되면"*)는 있으나 **관측 경로가 반쪽이다** — `plan-reviewer` 항목 16이 근거의 *존재*는 잡지만 *참됨*은 못 잡는다고 본문이 스스로 적는다. 어느 쪽인지 갈려 유지한다. **(2026-08-24 재확인: 유지 — 08-20 소급 적용의 보존 규칙 ⓑ 판정을 승계한다 — 관측 경로가 반쪽이다)**
- [2026-08-03] **규칙 4-1의 커버리지 경계 — "기록조차 되지 않은" 자기 유발 결함** — F-4와 `completion-reviewer` 항목 8은 **Deferred/Follow-up에 기록된 항목의 분류**를 검사하므로, 아예 적지 않고 넘어간 자기 유발 결함은 두 게이트에 걸리지 않는다. task 단위의 V-8 체크박스가 그 층을 받지만 **자기 점검**이라는 한계가 있다(T3 quality 리뷰어가 관찰로 지목했으나 "이번 diff가 만든 결함이 아니다"며 SUGGEST로도 올리지 않았다). 실사용에서 그 형태가 관측되면 완료 리뷰어에 **"diff 대비 미기록 결함 탐색"** 축을 추가할지 검토한다. (출처: v1.153.0 T3 V-6) **(2026-08-05 재확인: 유지 — F-4·`completion-reviewer` 항목 8 문면 무변경 — 경계 명문화 미착수)** (2026-08-18 재확인: 유지 — v1.180.0 회차(위키 인덱스 파생화·스킬 파일 분할·git 자동 반영)가 이 영역을 건드리지 않았다.) **(2026-08-20 소급 적용: 유지 — ⓓ 예외 ⓐ)** *"아예 적지 않고 넘어간"* 자기 유발 결함은 F-4·완료 리뷰어 항목 8 어느 쪽에도 걸리지 않아 **관측 경로가 없다**. **(2026-08-24 재확인: 유지 — 08-20 소급 적용의 ⓓ 예외(관측 경로 없음)를 승계한다)** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-07-29] **요구 원본 ↔ FR 매핑이 산출물에 없다** — Opus 5 대응 plan의 `## 요구 이해`가 "P0~P10 개선안 전부 반영"을 요구로 세웠으나 **P 항목 목록과 P→FR 매핑이 커밋된 산출물 어디에도 없다**(plan·docs·notes 전수 grep 0건). PRD 15개 FR로 흡수된 것이 사실이어도 **감사할 수단이 없어**, P 하나가 PRD 작성 때 조용히 빠졌다면 PRD 대조도 요구 이해 대조도 잡지 못한다. 다음 유사 작업에서는 요구 원본 번호 ↔ FR 매핑 표를 PRD 또는 plan에 남길 것. (출처: Opus 5 대응 plan F-7 m1) **(2026-08-06 재확인: 유지 — 요구 원본 ↔ FR 매핑 부재는 실제 산출물 공백)** **(2026-08-23 재확인: 유지 — 조건 없이 지금 착수할 수 있는 액션이고, v1.193.0 변경으로 바뀐 전제가 없다)** **(2026-08-24 재확인: 유지 — 9차 batch(절대 상한 축). 전건이 2026-08-23 재확인 상태였고 v1.195.0(인용 인자 스트립)이 이 항목의 전제를 바꾸지 않았다)**
- [2026-07-28] **`⏳` 금지 축의 플랫폼 확장 검토** — v1.144.0의 정적 판정 축 규정은 XAML(WinUI 3/WPF/MAUI)처럼 **선언적 마크업으로 컨트롤 타입·문구가 소스에 드러나는** 스택을 전제한다. 코드로 UI를 조립하는 스택(C# Markup·Compose·SwiftUI 등)에서도 같은 강도로 판정 가능한지 실사용 후 확인. (출처: v1.144.0 plan Deferred) **(2026-08-06 재확인: 유지 — `⏳` 금지 축의 플랫폼 확장은 범위 결정이 곧 액션)** **(2026-08-23 재확인: 유지 — 문서·규약 정비라 착수 단위가 명확하고, 이번 회차의 경계 재정의가 그 대상 절을 바꾸지 않았다)** **(2026-08-24 재확인: 유지 — 9차 batch(절대 상한 축). 전건이 2026-08-23 재확인 상태였고 v1.195.0(인용 인자 스트립)이 이 항목의 전제를 바꾸지 않았다)**
- [2026-07-28] **[SUGGEST] `completion-reviewer`(F-7)에 인벤토리 축이 없다** — plan 전체 관점의 인벤토리 미달 검출이 **F-8 단일 방어**에 걸려 있다. v1.144.0 작업에서 F-8 절차 2가 "인벤토리 미달만 남은 상태"를 🎉로 보내는 사슬 끊김이 실제로 있었고 **F-7이 그것을 잡았지만, F-7 자신은 인벤토리 대조 항목 없이 잡았다** — 같은 유형이 재발하면 놓칠 수 있다. 이번 세션이 그 필요성을 실증한 사례이므로 다음 하니스 정비 때 우선 검토. (출처: v1.144.0 F-7 SUGGEST) **(2026-08-06 재확인: 유지 — [SUGGEST]이지만 F-7에 인벤토리 축이 없다는 것은 실제 검증 공백)** **(2026-08-23 재확인: 유지 — 검증 자산 확장이라 조건 없이 착수 가능하고, 이번 회차가 그 축을 대체하지 않았다)** **(2026-08-24 재확인: 유지 — 9차 batch(절대 상한 축). 전건이 2026-08-23 재확인 상태였고 v1.195.0(인용 인자 스트립)이 이 항목의 전제를 바꾸지 않았다)** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-07-28] **[SUGGEST] 지목한 `파일:라인`의 실재 확인** — V-9가 인벤토리 표의 `원본 근거`·`구현 근거`에 **존재하지 않는 파일:라인을 적는 환각**은 v1.144.0 변경과 무관한 일반 위험이나, 지목 의무를 강화할수록 "형식만 채우는" 유인이 생긴다. V-8 자기점검에 "지목한 파일:라인이 실제로 존재하는지 재확인" 항목을 넣을지 검토. (출처: v1.144.0 T2 V-6 참고 관찰) **(2026-08-06 재확인: 유지 — [SUGGEST]이지만 지목한 `파일:라인`의 실재 확인은 리뷰 신뢰도에 직결)** **(2026-08-23 재확인: 유지 — 검증 자산 확장이라 조건 없이 착수 가능하고, 이번 회차가 그 축을 대체하지 않았다)** **(2026-08-24 재확인: 유지 — 9차 batch(절대 상한 축). 전건이 2026-08-23 재확인 상태였고 v1.195.0(인용 인자 스트립)이 이 항목의 전제를 바꾸지 않았다)**
- [2026-07-25] **모델·effort 티어 재검토 (Opus 5 신설분)** — Claude 5 세대에 `fable` alias가 추가돼 리뷰어의 haiku(4.5 세대) 대안이 생겼고(v1.224.0이 `spec-prefilter`·`explorer`를 삭제해 대상은 `plan-reviewer`·`completion-reviewer` 둘이다)(deferred [2026-07-08] explorer 상향 기각의 재검토 조건 변화), effort에 `xhigh`/`max`가 추가돼 [2026-07-15] "품질 게이트 모델 티어 조건부 상향"을 **모델 상향이 아니라 effort 상향**으로 더 싸게 해결할 수 있다. 단 agent frontmatter가 이 값들을 실제로 수용하는지 **미검증** — 바꾸기 전 agent 1종으로 스모크 테스트 선행. (출처: Opus 5 하니스 점검) **(2026-08-26 실적 계측 — 유지, 단 판정 방향이 좁혀졌다)**: v1.202.0 T2가 `plugins/pjc/scripts/report-reviewer-usage.ps1`을 신설해 커밋 trailer에서 `spec-prefilter` 실적을 집계했다. **기준 SHA `c7ce084` 기준 표본 34 = PASS 32 + ESCALATE 1 + 미분류 1**(스크립트 출력 라벨이 「미분류」이고, 이번 1건의 실체는 incomplete다 — 재측정 시 출력과 그대로 대조된다), 그와 직교하는 **실패경로(빈 응답·무응답·incomplete·D 분기 재요청) 6건(17.6%)**. **관측의 요지는 둘이다** — ⓐ 절감은 실재한다(PASS 32회만큼 Sonnet 호출을 안 했다) ⓑ **prefilter가 스스로 「trivial 아님」을 판정해 격상시킨 실적은 0건**이다(ESCALATE 1건 `dac9f97`은 *"무응답 → spec-compliance로 escalate"*, 판정 없음 1건 `d6218ad`은 *"결과 없이 소진 → 메인이 직접 대조"* 로 **둘 다 판정 실패에 따른 대체**다). 즉 관측된 결함은 「유지비」가 아니라 **haiku의 판정 실패율**이며, 그렇다면 처방 후보는 「제거」가 아니라 이 항목이 적어 둔 **haiku → fable 상향**이다. **착수 전 표본을 더 쌓는다** — 34건은 방향을 가리키기엔 충분해도 상향의 효과를 전후 비교하기엔 적다. 재측정은 위 스크립트를 같은 `-Since`로 돌려 한다. **(2026-08-06 재확인: 유지 — Claude 5 세대 alias·effort 신설로 재검토 조건이 실제로 바뀜, 스모크 1종이 선행 단계)** **(2026-08-12 재확인: 유지 — 직전 판정(2026-08-06) 근거 그대로 유효, v1.169.0 변경으로 바뀐 전제 없음)** **(2026-08-23 재확인: 유지 — 검증 자산 확장이라 조건 없이 착수 가능하고, 이번 회차가 그 축을 대체하지 않았다)** **(2026-08-24 재확인: 유지 — 9차 batch(절대 상한 축). 전건이 2026-08-23 재확인 상태였고 v1.195.0(인용 인자 스트립)이 이 항목의 전제를 바꾸지 않았다)** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-07-24] **SKILL 방향 재편(규칙 폐기·구조 재편)** — 통합의 실익이 미미함(v1.138.1)이 확인됐으므로, hook 이벤트 로그 근거로 저효용 규칙 폐기(위험 감수) 또는 최빈/희소 경로 분리·스킬 분할을 재고. 사용자 "토큰 과다" 지적의 실질 해결은 이 방향에 남아 있으나 안전·설계 재검토가 선행. (출처: v1.138.1 Q3-C) **(2026-08-06 재확인: 유지 — 사용자 "토큰 과다" 지적의 실질 해결이 이 방향에만 남아 있음)** **(2026-08-12 재확인: 유지 — 직전 판정(2026-08-06) 근거 그대로 유효, v1.169.0 변경으로 바뀐 전제 없음)** **(2026-08-23 재확인: 유지 — 조건 없이 착수 가능한 hook 정비이고, v1.193.0은 신규 게이트를 더했을 뿐 이 판정 지점을 건드리지 않았다)** **(2026-08-24 재확인: 유지 — 9차 batch(절대 상한 축). 전건이 2026-08-23 재확인 상태였고 v1.195.0(인용 인자 스트립)이 이 항목의 전제를 바꾸지 않았다)**
- [2026-07-24] **D6 "재서술 금지 ↔ 참조 축약" 경계 명문화** — 부연 삭제로 남는 골격의 조사·어순 재구성이 "재서술 금지" 위반인지 삭제의 부수효과인지 경계가 불명(T1 V-5 MINOR). 다음 유사 감량 task 전 "골격의 문법적 재구성은 허용, 규칙 내용 변경은 불허" 기준을 D6에 명문화 검토. (출처: v1.138.1 T1 V-5) **(2026-08-06 재확인: 유지 — D6에 판정 기준 1구 명문화로 끝남)** **(2026-08-12 재확인: 유지 — 직전 판정(2026-08-06) 근거 그대로 유효, v1.169.0 변경으로 바뀐 전제 없음)** **(2026-08-23 재확인: 유지 — 문서·규약 정비라 착수 단위가 명확하고, 이번 회차의 경계 재정의가 그 대상 절을 바꾸지 않았다)** **(2026-08-24 재확인: 유지 — 9차 batch(절대 상한 축). 전건이 2026-08-23 재확인 상태였고 v1.195.0(인용 인자 스트립)이 이 항목의 전제를 바꾸지 않았다)**
- [2026-07-15] **직접 수정(Trivial Bypass) 누적 품질 드리프트 점검 장치** — plan 없는 직접 수정(3줄 이내·순수 값 치환)은 리뷰 0회로 커밋되며, 여러 세션 누적돼도 어떤 게이트도 재점검하지 않는다(F-7 7.5는 plan 단위 작업에만 발동). 트리거 설계 필요(누적 횟수 기준? 다음 pjc:plan Step 1에서 경량 훑기?). (출처: v1.123.0 plan Deferred) **(2026-08-06 재확인: 유지 — 실제 검증 공백(직접 수정 경로에 재점검 게이트 0))** **(2026-08-12 재확인: 유지 — 직전 판정(2026-08-06) 근거 그대로 유효, v1.169.0 변경으로 바뀐 전제 없음)** **(2026-08-23 재확인: 유지 — 검증 자산 확장이라 조건 없이 착수 가능하고, 이번 회차가 그 축을 대체하지 않았다)** **(2026-08-24 재확인: 유지 — 9차 batch(절대 상한 축). 전건이 2026-08-23 재확인 상태였고 v1.195.0(인용 인자 스트립)이 이 항목의 전제를 바꾸지 않았다)** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)** **(2026-09-04 영향 부기 — 회차 12)**: `guard-write` 에 `[PLAN-EXEMPT]` 면제 경로가 생겨 **리뷰 0회로 통과하는 입구가 하나 늘었다**. 다만 그 통과는 ⓐ 사용자 승인을 전제하고 ⓑ 표식에 적힌 파일로 한정되며 ⓒ 표식이 transcript 에 남아 **사후에 무엇이 면제됐는지 셀 수 있다** — 종전 trivial bypass(3줄 이내·값 치환)가 아무 흔적을 남기지 않는 것과 다르다. 이 항목이 찾는 「트리거 설계」의 후보로 **`guard-write` 이벤트 로그의 `allow`·`PLAN-EXEMPT 면제` 레코드 계수**가 추가됐다. **단 그 계수를 낼 도구가 지금 없다** — `report-hook-events.ps1` 은 v1.225.0 에 삭제됐고, 현재 수단은 이벤트 jsonl 을 직접 읽는 것뿐이다. 트리거를 설계할 때 집계 도구 복원이 함께 필요하다.
- [2026-07-15] **Phase F에 전체-diff(BASE..HEAD) 품질 패스 추가** — task별 diff 리뷰는 task 간 누적 중복·패턴 불일치·여러 task에 걸쳐 비대해진 파일을 원리적으로 못 본다. 전 task 완료 후 전체 diff에 1회 품질 리뷰를 수행하는 F-단계 신설 검토. v1.122.0 3레버(Design 필드·V-6 기본화·SUGGEST)의 효과 관찰 후. v1.123.0 분석에서 재확인: 4-D 재사용 조사는 계획된 심볼만 대상이고 V-6 항목 J②(중복)는 non-blocking이라, task 간 누적 중복의 실효 검출 수단은 이 패스뿐이다. (출처: v1.122.0 plan Deferred) **[2026-08-06 병합]** 「[2026-07-15] simplify(정리) 패스」를 이 항목에 흡수했다 — 흡수된 항목이 스스로 *"우리 대장의 'Phase F 전체-diff 품질 패스' 항목과 이 항목의 경계도 같은 축(전체 vs diff)이라 **둘을 함께 설계**하는 편이 낫다"* 고 적었다. ponytail 선행 사례(삭제 후보 목록만 반환해 churn 회피 · diff 대상과 전체 대상을 별도 명령으로 분리)도 이 설계에 함께 쓴다. **(2026-08-06 재확인: 유지 — 누적 중복 검출 수단이 이 패스뿐, 병합 후 착수 단위 1건)** **(2026-08-12 재확인: 유지 — 직전 판정(2026-08-06) 근거 그대로 유효, v1.169.0 변경으로 바뀐 전제 없음)** **[2026-08-13 전제 갱신]** v1.172.0이 `code-quality-reviewer` 항목 H에 **장황함·추적 거리** 2축을 신설했다 — 다만 그 둘도 **task 단위 diff**만 보므로 이 항목이 겨냥하는 *task 간 누적 중복*은 여전히 검출 수단이 없다. 즉 신설 축은 이 항목을 대체하지 않으며, 착수 시 항목 H의 두 축과 **역할이 겹치지 않게** 설계할 것(전체-diff 패스는 "여러 task에 걸쳐 같은 일을 두 번 만들었는가"를 본다). **(2026-08-23 재확인: 유지 — 검증 자산 확장이라 조건 없이 착수 가능하고, 이번 회차가 그 축을 대체하지 않았다)** **(2026-08-24 재확인: 유지 — 9차 batch(절대 상한 축). 전건이 2026-08-23 재확인 상태였고 v1.195.0(인용 인자 스트립)이 이 항목의 전제를 바꾸지 않았다)** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-07-13] `pjc-systematic-debugging` 단독 세션이 **plan.md가 없는 레포**에서 조사 로그를 plan.md로 **신규 Write**하면 plan 작성 게이트에 차단된다(통과 흔적 목록에 debug 스킬 없음 — D3). 실해는 작다(SKILL.md:281이 `debug-<날짜>.md` 대안을 이미 제시, 차단 메시지도 그리로 유도). SKILL.md:281에 "plan.md가 없으면 `debug-<날짜>.md`" 1구 명문화 또는 흔적 허용 여부 재검토 (출처: v1.118.0 F-7 m2) **(2026-08-06 재확인: 유지 — SKILL.md 1구 명문화로 끝나는 명확한 작업)** **(2026-08-12 재확인: 유지 — 직전 판정(2026-08-06) 근거 그대로 유효, v1.169.0 변경으로 바뀐 전제 없음)** **(2026-08-23 재확인: 유지 — 문서·규약 정비라 착수 단위가 명확하고, 이번 회차의 경계 재정의가 그 대상 절을 바꾸지 않았다)** **(2026-08-24 재확인: 유지 — 9차 batch(절대 상한 축). 전건이 2026-08-23 재확인 상태였고 v1.195.0(인용 인자 스트립)이 이 항목의 전제를 바꾸지 않았다)**
- [2026-08-05] **선두 9,000B 재구성(구 PRD FR-5) — 별도 plan 대상** — 이번 이관은 본체 총량을 줄였을 뿐 **선두 배치는 텍스트 무변경**(D4)이다. 이관 후 실측값(선두 바이트·가용 예산·전체 행수)은 **위 「대장 운영」 그룹의 「가용 예산」 불릿이 정본**이며 여기 옮겨 적지 않는다 — 같은 수치를 두 곳에 두는 것이 이 대장이 이미 두 번 겪은 드리프트 원인이다. 파일별 총바이트·행·경계 행은 `docs/harness-conventions.md` 「문서 로드 예산 기준선」 표가 기계 대조한다. (출처: 하니스 다이어트 plan v1.156.0) (2026-08-18 재확인: 유지 — **오히려 악화됐다.** v1.180.0 T8이 `llm-wiki/SKILL.md` 선두에 라우팅 행·지연 로드 안내를 더해 「출처 우선순위」 1불릿이 재부착 구간 밖으로 밀렸다. 되돌리려면 §0을 줄여야 하는데 그 task의 범위가 아니었다. 다음 llm-wiki 편집 회차의 우선 검토 대상이다.) **(2026-08-19 재확인: 유지 — 별도 plan 대상이고 08-18 재확인에서 오히려 악화가 관측됐다)** **(2026-08-28 재확인: 유지 — v1.211.0 T1·T2가 llm-wiki 선두 편집에 회계 3종을 수행(경계 81→80, 지연 로드 지시 추가분). 재구성 자체는 여전히 별도 plan 대상)**
- [2026-08-06] **포인터 도달성 검사가 "산문 정본 참조"를 못 잡는다** — `"정본 SKILL.md V-5 …"` 처럼 `「」` 없이 위치를 지목하는 참조는 검사 패턴(`` `경로` `` 직후 12자 내 `「절」`) 밖이다. 어휘(`정본`·`참조`) + 파일명 패턴으로 잡으면 **오탐이 커지고, 오탐이 늘면 검사 신뢰도가 떨어져 결국 무시된다**. 오탐 억제 설계(예: `[NOTE]` 등급 비차단 노출)를 찾으면 재검토. (출처: v1.156.0 T4 quality M1 → v1.159.0에서 사용자가 "표기 정정만" 선택해 보류) **(2026-08-19 재확인: 유지 — 오탐 억제 설계가 아직 없다 — 그것을 찾는 것이 착수 조건)** **(2026-09-03 재확인: 유지 — 13차 판정 — 검사기 축 확대가 조건 없는 액션 — 전제 불변)**
- [2026-08-06] **포인터 도달성 축의 미탐 2종** — ⓐ 도달 대상을 헤딩 ∪ 굵은 텍스트로 잡고 `startswith` 부분 일치를 허용해, 대상 파일에 우연히 같은 접두의 굵은 문구가 있으면 끊긴 포인터가 "도달함"으로 오판정될 수 있다(오탐↓/미탐↑ 트레이드오프) ⓑ 경로를 해석하지 못한 건은 검사에서 빠진다(`[NOTE]` 노출). 오판정 실례가 관측되면 굵은 앵커를 줄 시작으로 좁히거나 짧은 이름에 완전 일치만 허용하는 안을 검토. (출처: v1.156.0 T3 quality m1·m2 — 판정 유보) **(2026-08-19 재확인: 유지 — 미탐이라 관측 경로가 없다(규약 ② ⚠ 예외 — 오판정이 조용히 통과한다))** **(2026-08-20 소급 적용: 유지 — ⓓ 예외 ⓐ)** 재확인 표기가 이미 *"미탐이라 관측 경로가 없다"* 로 명기하고 있다. **출처가 「판정 유보」라 게이트 ⓐ 기각 대상으로도 읽히나, ⓓ 예외(관측 경로 없음)를 우선했다** — 그 규정 갭 자체는 `[2026-08-20]` 항목이 추적한다. **(2026-09-03 재확인: 유지 — 13차 판정 — 미탐 축이라 관측 경로가 없다(규약 ② ⚠ 예외) — v1.185.0 T2가 같은 근거로 보존한 항목이다)**
- [2026-08-06] **2차 이관 후보 — `### V-5. Spec Compliance Review` 내부 조건부 하위 블록** — 12,698B 안에 재리뷰 규약·incomplete 처리 등 조건부 블록이 있다. 1차 이관으로 −10% 목표를 이미 달성해 보류. (출처: v1.156.0 plan Deferred) **(2026-08-19 재확인: 유지 — 이관 대상 블록이 특정된 조건 없는 액션)** **(2026-09-03 재확인: 유지 — 13차 판정 — 이관 대상이 절 단위로 특정된 조건 없는 액션 — 전제 불변)**
- [2026-08-06] **문서 이관 회차는 "정보 항목 전수 대조"를 먼저 만들 것 (교훈 자산화 후보)** — v1.159.0 T7에서 이관 손실 3건이 났고 **셋 다 사후 리뷰가 각각 찾아냈다**(파이프 버퍼링 경고·케이스 수 갱신 의무·`run_in_background` 캡). 전부 원문의 **조건·경고**인데 요약하며 뭉개진 것이고 바이트 여유는 2,400B로 넉넉했다 — 압박이 아니라 **요약 행위 자체의 실패 유형**이다. 사후 대응으로 구 블록(1,809자)의 정보 항목을 22개로 열거해 `git show <BASE>:<파일>`로 뜬 원문과 1:1 기계 대조했고(누락 0 확인) 그 방식이 통했다. **다음 이관 회차는 그 대조를 착수 시점에 만들 것.** 검사 키는 문면 리터럴이 아니라 **검증하려는 사실**을 가리켜야 한다(수치 `600000ms`를 키로 잡았다가 문장을 다듬자 정보가 있는데도 누락으로 잡혔다). 이 규약을 `pjc:implement` 어딘가에 명문화할지는 다음 회차 판단. (출처: v1.159.0 T7 spec M1·M2 + quality M1) **(2026-08-19 재확인: 유지 — 명문화는 조건 없는 액션이고 넣을 자리만 미정이다)** **(2026-09-03 재확인: 유지 — 13차 판정 — 교훈 자산화가 조건 없는 액션 — 이번 회차도 대장 항목 전수 대조를 손으로 했다(같은 마찰 재현))** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-08-06] **D6의 "재교정 1회 상한"이 진단성 재실행을 커버하지 않는다** — 케이스를 **수정하지 않고** 같은 필터를 다시 돌려 원인을 확인하는 것(v1.160.0에서 `plan-pos-6` error를 2회 재실행)은 "수정 → 재측정" 루프가 아니라 진단인데, plan 규정 문면에 그 구분이 없어 상한 위반으로 읽힐 여지가 있다. 다음 회차에 문면을 나누거나(수정 루프 vs 진단 반복) 진단 반복에도 상한을 명시할 것. (출처: v1.160.0 T3 spec M1, 판정 유보) **(2026-08-19 재확인: 유지 — 문면 분리는 조건 없는 액션)** **(2026-09-03 재확인: 유지 — 13차 판정 — 상한 문면 개정이 조건 없는 액션 — 전제 불변)**
- [2026-08-06] **`stale_agents_md` 픽스처가 정답을 주석으로 알려준다** — AGENTS.md에 `- Test: python -m pytest tests/ (tests/ 디렉터리 없음 — 옛 명령)`이라 적어 두었는데 **실제 stale 명령은 스스로 stale이라고 알리지 않는다**. `rec-pos-3`의 발동 난이도를 실사용보다 낮췄을 여지가 있다(D7이 막는 복창의 픽스처판). 주석 없이도 stale임이 드러나는 형태(예: `tests/` 부재를 픽스처 구조로만 표현)를 검토. (출처: v1.160.0 F-7 m2, 판정 유보) **(2026-08-19 재확인: 유지 — 픽스처 수정은 조건 없는 액션)** **(2026-09-03 재확인: 유지 — 13차 판정 — 픽스처 주석 제거가 조건 없는 액션 — 전제 불변)**
- [2026-08-06] **[SUGGEST] `build_workspace`의 파일 쓰기 보일러플레이트 공통화** — `open(path, "w", encoding="utf-8", newline=" **(2026-08-19 재확인: 유지 — 수정 지점이 특정된 조건 없는 액션(SUGGEST 등급))** **(2026-09-03 재확인: 유지 — 13차 판정 — 공통화 대상이 특정된 SUGGEST이고 등재 게이트 ⓕ의 대상 지목을 갖췄다 — 전제 불변)**
")` 패턴이 10회 이상 반복되고 v1.160.0의 `multi_file`이 4회를 더했다. `_write(path, content)` 헬퍼로 묶으면 각 kind 블록이 내용에만 집중해 읽기 쉬워진다. Design ④의 *"템플릿·데이터 주도로 일반화하지 않는다"* 는 **kind별 분기 나열 구조**를 지키라는 선언이지 I/O 보일러플레이트 공통화를 막는 취지가 아니다(리뷰어 판정). (출처: v1.160.0 T2 quality S1)

- [2026-08-18] **gitignore된 `plan.md`의 기록은 리뷰어가 diff로 검증할 수 없다** — acceptance가 *"…를 Progress Log에 기록한다"* 형태이면 그 산출물이 커밋 diff에 나타나지 않아 리뷰어의 **diff 가드**(빈 diff면 즉시 incomplete)가 먼저 발동한다. 메인이 *"파일을 직접 Read하라"*고 안내해야 회수되는데 그 안내가 규약에 없다. v1.179.0에서 **T4 spec 2R이 실제로 이 공백에 걸렸다**. 후보: ⓐ 리뷰어 입력 계약에 *"plan은 gitignore일 수 있으니 diff가 아니라 파일을 읽는다"* 1구 ⓑ plan 기록형 acceptance를 쓸 때 호출 프롬프트에 그 사실을 명시하는 규약. (출처: v1.179.0 T4 2R) **(2026-08-28 재확인: 유지 — **이번 회차에 재발현했다** — T1 spec 1R이 acceptance ③(폴백 실증을 Progress Log에 기록)을 BLOCKER로 냈고, 메인이 기록한 뒤 리뷰어가 diff가 아니라 파일을 직접 읽어 2R에서 닫았다. 처방 ⓐ(리뷰어 입력 계약 1구)·ⓑ(호출 프롬프트 명시) 모두 미착수라 다음 회차도 같은 왕복을 한다)**
- [2026-08-18] **리뷰어 판정문 절단의 원인 확정(P1) — 관측 대기** — v1.179.0이 「예산 소진」으로 가정하고 T1·T2를 넣었으나(4회 관측 전부 33·31·31·31회 = `maxTurns: 30` 초과 지점), 「출력 순서(판정문을 마지막에 쓴다)」와의 분리는 아직 안 됐다. T1이 넣은 **사용량 계측 1줄**이 쌓이면 갈린다 — 예산 미달인데 판정문이 없으면 순서 문제다. 다음 회차에서 판정. (출처: v1.179.0 T1 D4) (2026-08-18 재확인: 유지 — **관측 2건 확보, 가설이 흔들린다.** v1.180.0 회차에서 `spec-compliance`·`code-quality`(sonnet)는 15회 호출 **전부** 판정문을 냈고 예산도 최대 26/32였다. 반면 **`spec-prefilter`(haiku)는 두 번 연속(T15·T16) 판정문 없이 조사 서술 도중 종료**했다(도구 9회·15회 — 예산 32회에 한참 못 미침). **예산 소진으로는 설명되지 않는다** — 모델(haiku)·프롬프트 구조 쪽으로 가설을 옮길 근거다. 두 경우 모두 메인이 자체 검증으로 대체했다.) **(2026-08-28 재확인: 유지 — **표본이 늘었다** — 이번 회차 sonnet 리뷰어 8회 호출이 전건 판정문을 냈고 예산도 최대 20/32였다. haiku(`spec-prefilter`) 호출은 없어 모델 가설을 가르지는 못했다)** **(2026-09-03 재확인: 유지 — 13차 판정 — 제목이 *"관측 대기"*라 외부 사건 트리거 후보였으나 유지 — 판정문 부재는 **즉시 드러나고**(메인이 D 분기로 재요청) 계측 표본이 회차마다 자동으로 쌓인다(12차가 sonnet 8회분을 더했다). 조용한 실패가 아니라 진행 중인 관측이다)** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-08-25] **SKILL.md 상한 초과분의 실제 해소** — v1.199.0 T3이 예산 표에 `상한` 열을 세웠으나 그것은 **감시 축**이고, 값이 「현행 +10%」라 착수 시점에는 전 행이 통과한다. 실제 감축은 **초과가 관측되는 회차**의 몫이다. (출처: v1.199.0 plan Deferred 이관) **(2026-08-28 재확인: 유지·부분 대응 — v1.211.0이 세 SKILL.md를 no-op 판정·압축으로 총 1,955B 감량(107,198+102,883+59,859 → 106,916+102,482+58,682). 상한 초과 전 능동 감축의 첫 실행이나 구조적 해소는 잔존)**
- [2026-09-02] **분리 경계를 넘는 상대 참조는 어느 기계 검사도 잡지 못한다** — 절을 다른 파일로 이관하면 원문의 *"아래 「X」"*·*"위 「Y」"* 같은 **상대 지시어 참조**가 가리키는 대상을 잃는데, `check-harness-consistency.py`의 포인터 도달성 축은 **백틱 경로가 앞에 붙은 형태만** 인식해 이 산문형을 보지 못한다(v1.223.0 T1 실측 — quality 리뷰가 2건을 잡았고 그 축을 전수로 세어 **총 4건**이 나왔다: 새 파일 → 본체 3 · 본체 → 새 파일 1). **plan 단계의 사전 grep도 못 잡는다** — 그쪽은 절 *이름*으로 레포를 훑으므로 같은 파일 안에 있던 참조는 애초에 후보가 아니다. 처방 후보: ⓐ 이관 task의 acceptance에 「상대 지시어 + 낟표가 상대 파일 헤딩과 일치하는 것 0건」을 표준으로 넣는다 ⓑ 포인터 도달성 축에 *경로 없는 낟표 참조* 판정을 더한다(오탐이 커 스코프 설계가 선행된다). (출처: v1.223.0 T1 V-6 M1·M2 + 전수 조사)
- [2026-09-02] **`docs/harness-conventions.md`에 남은 큰 절 둘이 다음 감량 후보다** — v1.223.0이 「골든 러너 운용」(31,838B · 23.2%)을 떼어 여유를 20B → 30,882B로 되돌렸으나, 본체에는 여전히 **「문서 로드 예산 기준선」 11,648B(8.5%)** 가 있다(2026-09-02 절별 실측). 상한이 다시 좁아지면 **전자가 1순위**다 — 후자는 검사기가 파싱하는 표 2종을 담아 「남기는 것 셋」의 ⓑ(검사기 파싱 자리)에 정면으로 걸린다. **이 실측을 등재하는 이유는 재계산으로 나오지 않기 때문이다**(절별 바이트를 다시 재야 한다). (출처: v1.223.0 plan Deferred) **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**

### llm-wiki · vault

- [2026-09-05] **`origin`·`confidence` 두 필드의 갱신 주체가 실제로 도는지 관측** — v1.238.0 T2가 승격 문을 F-1 10(코드 정합 샘플링)에 연결했으나, 그 샘플링은 매 lint에서 소수 표본만 돈다(367 페이지 중). **개정 전 40일간 신규 `human-validated`가 0건**이었던 것이 이 개정의 근거였으므로, 개정 후 실제로 승격이 일어나는지가 검증되지 않은 채 남는다. 다음 몇 회 lint에서 관측하고 **여전히 0건이면 두 필드도 폐지 후보**다(같은 회차가 60일 confidence 하락을 그 근거로 폐지했다). `confidence`는 §7-3 폐지 이후 §11의 「코드 재대조 없이 유지된 페이지는 medium 이하」 한 줄만이 값을 정한다. (출처: v1.238.0 회차 15 T2 · plan Deferred 이관)
- [2026-09-02] **분리된 `lookup-rules.md`(21,321B)의 문면 감량** — v1.220.0이 절차 K를 이관하며 **문면은 한 글자도 바꾸지 않았다**(D3 — 이관과 개정을 섞으면 축 ⑪ 화이트리스트 앵커가 깨지고 어느 변경이 무엇을 깼는지 갈리지 않는다). 그 결과 이 파일이 **계획·디버깅 세션의 최빈 로드 문서**가 됐고 크기는 그대로다. 1차 후보는 **K 2**(8,432B · 구간의 55%)의 인덱스 조회 순서 서술이다. **단 계획 2R이 「기본 경로 규칙」으로 판정해 이관 불가로 결론낸 5줄이 전부 K 2 안에 있으므로**(`[2026-08-28]` 폐기 회차 승계 ⓑ 참조) 이관이 아니라 **압축·삭제** 축으로 접근해야 한다. **함께 볼 축 하나 — 「자동 호출」 서술**: 이관된 `:11`·`:13`이 *"이 절차 K는 `pjc:pjc:plan`·`pjc:pjc:implement`가 **자동 호출**해"*로 남아 있다(D3의 무변경 이관 결과). 같은 파일 `:4` 헤더가 *"스킬을 발동하지 않고 이 파일 하나를 Read하는 것이 정상 경로"*로 덮지만 「자동 호출」이 스킬 발동으로 읽히는 여지는 남는다. (출처: v1.220.0 D3 · plan Deferred · F-7 m1) **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-09-02] **구현 세션은 절차 M이 발동하면 조회 분리의 이득이 없다** — v1.220.0이 호출측 3곳을 경량 Read로 바꿔 계획·디버깅 세션의 위키 참조 로드를 37,815B → 약 21KB로 줄였으나, **구현 세션은 F-6.5 ⓒ가 `pjc:llm-wiki`를 Skill 도구로 발동해 큐를 소비**하므로(구 F-6.5 — v1.224.0 폐기) 그 세션은 이득이 없다. **로드 총량은 `lookup-rules.md` 21.3KB + `SKILL.md` 18.0KB + `procedures-ops.md` 38.5KB ≈ 77.8KB**이고 이관 전(37.8 + 38.5 = 76.3KB)보다 **1.5KB 크다** — F-6.5 ⓒ가 *"정본은 `references/procedures-ops.md`의 「M. 큐 소비 (경량)」"* 이라 명시해 그 파일이 로드에서 빠지지 않는다(**초안은 이 항을 빼고 ≈39KB로 적었다 — 2026-09-02 실측 정정**). 절차 M도 Read화할지는 별도 판단이다 — **M은 쓰기 절차라 `procedures-ops.md`까지 필요해 축이 다르다**(조회 하나로 완결되는 K와 성격이 갈린다). **이 레포에서는 재현되지 않는다** — F-6.5 ⓕ가 하네스 레포 비적용을 규정하므로 관측은 다른 프로젝트의 구현 세션에서만 가능하다. 발동 자체도 조건부다(vault 설정 + 대상 프로젝트 큐 1건 이상). (출처: v1.220.0 계획 리뷰 1R M4) **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-08-28] **위키 `feat-safety-hooks-advisory.md`의 「압축 직후 재읽기 대상 경로 주입」 항목 갱신** — v1.212.0이 그 동작을 **경로 지시 → 원문 발췌 주입**으로 바꿨다(`session-context.ps1`의 `Get-SkillSection`이 `pjc:implement/SKILL.md` 「🚨 자율 루프의 절대 규칙」과 구 references/halt-conditions.md(v1.224.0 삭제)의 「중단 조건 표」~「위임 경계」 구간을 잘라 온다). 그 페이지는 이 hook 8종의 판정 상세가 정본이라 서술이 실제와 어긋난 채 남는다. **vault 파일이라 하니스 레포 task가 아니고 별도 위키 세션**이다(대장 `[2026-08-26]` `pending.md` 헤더 항목과 같은 축). (출처: v1.212.0 plan Deferred 이관) **v1.214.0이 같은 hook에 축을 하나 더 얹었다** — plan 상태 라인 세 분기 뒤에 「Deferred 미판정 N건」 부기가 붙고 compact 리마인더에 「아직 plan.md에 적지 않은 발견을 지금 적으라」 1구가 추가됐다(`session-context.ps1`의 `$defUnjudged`·`$defNote`, 골든 SC40~SC40j 11 어서션). 같은 페이지의 같은 서술이 낡는 축이라 별도 등재하지 않고 여기 흡수한다. **이연 사유는 「범위 밖」이다**(규칙 4-1의 이연 가능 3종 중 하나) — 대상이 vault라는 **별도 저장소**여서 하니스 레포 커밋에 담을 수 없고, 고치려면 위키 세션이 필요하다. 자기 유발이지만 그 수정이 이 회차의 범위 밖이라는 판정이며, 근거를 남기지 않으면 다음 회차가 재구성할 수 없다(v1.214.0 F-7 m2 지적). (출처: v1.214.0 F-6.5) **[2026-09-02 흡수]** v1.220.0이 절차 K를 `references/lookup-rules.md`로 분리하고 압축 주입을 경로 지시로 되돌리면서 **`feat-llm-wiki.md:22`**(*"절차 K는 본체만으로 완결(추가 Read 0회)"*)**와 `:63~64`**(주입 구간 축소·K 3~4 감수)**도 낡았다** — 같은 vault·같은 성격(코드 세션이 고칠 수 없는 별도 저장소)이라 여기 흡수한다. 이연 사유는 이 항목과 같은 「범위 밖」이다. **[2026-09-02 흡수 2]** v1.221.0 T1이 `session-context.ps1`에 **Deferred 대장 최고령 라인**을 더해 그 페이지의 라인 목록·게이팅 서술이 또 낡았다 — 같은 vault·같은 성격이라 여기 흡수한다(이연 사유도 같은 「범위 밖」). **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**

- [2026-08-26] **`pending.md` 헤더에 hook 언급 1줄이 없다 — `skill-feedback.md`와 관례가 갈린다** — `skill-feedback.md` 헤더는 *"`session-context` hook이 하네스 세션 시작 시 잔량·최고령을 1줄 알린다"*를 이미 담는데 `pending.md`에는 그 형태가 없다. v1.204.0으로 hook이 `pending.md`도 읽게 됐으므로(K-DRIFT 잔량) 같은 관례를 맞추는 편이 일관된다. **vault 파일이라 하니스 레포 task가 아니고**(별도 위키 세션) 없어도 동작이 성립해 그 회차 범위에서 뺐다. (출처: v1.204.0 plan Deferred 이관)
- [2026-08-17] `[SUGGEST]` **wiki-schema §7-2 도입 문장이 예고와 소불릿 사이에 긴 산문을 끼운다** — *"네 조건을 아래 소불릿에서만 정의한다"*고 예고한 뒤 guide_kind·펜스 판정·INFO 계층 폐지 경위를 지나서야 소불릿에 닿는다. 조건 4종을 앞으로 당기거나 산문 일부를 접는 여지가 있다. 산문-먼저·요약-나중은 §2.8 등 파일 전반의 기존 관례라 결함은 아니다. (출처: v1.178.0 T2 quality 1R S1) **(2026-08-28 재확인: 유지 — vault 문서 편집이라 위키 세션 대상 — 결함이 아닌 가독성 제안이라 우선순위가 낮다)**

- [2026-07-28] **`wiki-schema.md` frontmatter(`version`·`updated`) 관리 부재** — 최근 6개 수정 커밋에서 `version: "2.40"`·`updated: 2026-07-12` 어느 쪽도 갱신되지 않았다(실측). 규칙 진실원천의 버전 표기가 사실상 방치 상태인데, 단독 근거 없이 관례를 바꾸는 셈이라 그 작업에서는 손대지 않았다. 갱신 트리거를 정할지 별도 판단 필요. (출처: OKF v0.2 갱신 plan Deferred) **(2026-08-06 재확인: 유지 — `wiki-schema.md` frontmatter 관리 부재는 조건 없는 실제 공백)** **(2026-08-23 재확인: 유지 — vault 세션에서만 수행 가능한 작업이고 대상이 특정돼 있다)** **(2026-08-24 재확인: 유지 — 9차 batch(절대 상한 축). 전건이 2026-08-23 재확인 상태였고 v1.195.0(인용 인자 스트립)이 이 항목의 전제를 바꾸지 않았다)**
- [2026-07-09] lint.py `section()` 헬퍼 Match 반환형 확장으로 `section_span()`과 단일화 — 호출부 리팩터링 동반, 다음 lint.py 정비 때 (출처: harness-wiki-part2 T2 리뷰 m2) **(2026-08-06 재확인: 유지 — 리팩터링 범위가 특정됨(`section()` 반환형 + 호출부))** **(2026-08-12 재확인: 유지 — 직전 판정(2026-08-06) 근거 그대로 유효, v1.169.0 변경으로 바뀐 전제 없음)** **(2026-08-23 재확인: 유지 — 조건 없이 지금 착수할 수 있는 액션이고, v1.193.0 변경으로 바뀐 전제가 없다)** **(2026-08-24 재확인: 유지 — 9차 batch(절대 상한 축). 전건이 2026-08-23 재확인 상태였고 v1.195.0(인용 인자 스트립)이 이 항목의 전제를 바꾸지 않았다)**
- [2026-07-09] 나머지 7개 프로젝트 메모리의 위키 순차 이전(AI-Agents·neighborhood-walk-rpg·Obsidian-Vault-WIKI·bitleader-dev-HomePage·Web-HomePage·ProjectDashboard·DevDashboard-WinUI) — 각 프로젝트 위키 절차 B 세션에서 (출처: v1.102.0) **(2026-08-06 재확인: 유지 — 남은 7개 프로젝트가 명시된 실제 작업 목록)** **(2026-08-12 재확인: 유지 — 직전 판정(2026-08-06) 근거 그대로 유효, v1.169.0 변경으로 바뀐 전제 없음)** **[2026-08-23 전제 갱신]** **7건 중 2건이 이미 완료됐다**(실측) — `ProjectDashboard`는 `20_projects/work/projectdashboard.md`, `DevDashboard-WinUI`는 `20_projects/personal/devdashboard-winui.md`로 등록돼 있다. **남은 것은 5개**(AI-Agents·neighborhood-walk-rpg·Obsidian-Vault-WIKI·bitleader-dev-HomePage·Web-HomePage)다. **(2026-08-23 재확인: 유지 — 남은 5개는 조건 없는 실제 작업 목록이고, v1.193.0의 자동 등록(pjc:plan Step 1)이 각 프로젝트의 다음 계획 세션에서 일부를 자연히 처리한다)** **(2026-08-24 재확인: 유지 — 9차 batch(절대 상한 축). 전건이 2026-08-23 재확인 상태였고 v1.195.0(인용 인자 스트립)이 이 항목의 전제를 바꾸지 않았다)** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-07-09] SessionStart hook의 위키 허브 컨텍스트 자동 주입 — v1.102.0 미채택(지침 방식 선택), 지침 방식 실효성이 낮다고 관찰되면 재검토 (출처: v1.102.0) **→ [2026-07-30] 부분 재수용 (v1.146.0)**: 재검토 조건이 **실제 사고로 충족**됐다 — 설정·실재하는 vault를 "미설정"으로 단정해 절차 K를 건너뛴 세션이 관측됐고, 지침만으로는 확인을 강제하지 못한다는 것이 실증됐다. 다만 이번에 채택한 것은 **vault 설정 상태 1줄 주입만**이며(사용자 확정), **허브 본문·`## 작업 규약·주의사항` 요약 주입은 이 항목에 그대로 잔존**한다(컨텍스트 비용이 크고 절차 K의 역할과 겹침). **(2026-07-30 재확인: 유지 — 잔존분(허브 본문·규약 요약 주입) 기준, 미도입 확인)** **(2026-08-06 재확인: 유지 — 잔존 범위(허브 본문·규약 요약 주입)가 명시됨)** **(2026-08-12 재확인: 유지 — 직전 판정(2026-08-06) 근거 그대로 유효, v1.169.0 변경으로 바뀐 전제 없음)** **(2026-08-23 재확인: 유지 — 조건 없이 착수 가능한 hook 정비이고, v1.193.0은 신규 게이트를 더했을 뿐 이 판정 지점을 건드리지 않았다)** **(2026-08-24 재확인: 유지 — 9차 batch(절대 상한 축). 전건이 2026-08-23 재확인 상태였고 v1.195.0(인용 인자 스트립)이 이 항목의 전제를 바꾸지 않았다)**
- [2026-08-07] **기존 위키 허브 12개의 conventions.md 마이그레이션** — v1.164.0이 `## 작업 규약·주의사항`을 `conventions.md`(§2.9)로 분리했으나 **코드 세션은 vault를 수정하지 않으므로**(절차 K read-only) 기존 허브는 그대로다. 위험 2개(`claude-harness-pjc` 12,996자/99% · `projectdashboard` 11,187자/86%)를 **별도 `pjc:llm-wiki` 세션(절차 B)** 에서 우선 이전하고 `## 최근 주요 변경` 초과분을 `changes.md`로 롤오버한다. 나머지 10개(21~59%)는 각 프로젝트 ingest 때 자연 적용 — lint §7-31 INFO가 매 실행 대상을 알린다. **(최종 2026-09-03 재확인: 유지 — 13차 판정 — vault 세션에서 조건 없이 착수 가능한 마이그레이션이고 대상 12개가 확정돼 있다 · 이전 3회 동일)**
- [2026-08-07] **타입 열거 검사의 잔여 자리 — C안 4자리 + 파일 범위 밖 복제본 3곳** — v1.165.0이 축 ⑩을 **B안(10자리/11항목)** 으로 신설하면서 남긴 것. ⓐ **C안 잔여 4자리**: §3 파일 네이밍 표(`wiki-schema.md:308-310` + `SKILL.md:192`, 두 표 상호 대조) · §3 `category` 대상 목록(`:332`) · §4 `:364` ↔ §8 `:529` 재분할 적용 대상 목록. 넣지 않은 이유는 **코드에 매핑 선언이 필요**하기 때문이다(네이밍 표는 행 라벨이 한글이라 `{한글 라벨 → 타입}` 매핑, category는 `templates.md`의 `category:` 보유 타입에서 도출) — 그 선언 자체가 새 드리프트 지점이 된다. ⓑ **검사 대상 3파일(`wiki-schema.md`·`templates.md`·`lint.py`) 밖의 복제본 3곳**: `procedures-content.md:132`(§12 권장 5타입 복제) · `SKILL.md:214`(§4/§8 분리 처방 타입의 3번째 사본) · `procedures-ops.md:49`(F-1 28번 — A5 `RELEASE_MARKER_EXEMPT_TYPES`의 3번째 사본). 파일을 넓히면 **"어느 것이 정본인지 검사기가 판정해야 하는 문제"** 가 새로 생겨(정본은 schema, 나머지는 재서술) 이번엔 3파일로 한정했다. **착수 조건**: 축 ⑩의 앵커 취약성이 실제로 어느 정도인지(exit 2 발생 빈도) 한두 회차 관측한 뒤 판단한다. (출처: v1.165.0 plan Out of Scope·Deferred) **(2026-08-19 재확인: 유지 — 축 ⑩ 앵커 취약성 관측(한두 회차)이 아직 미충족)** **(2026-09-03 재확인: 유지 — 13차 판정 — C안 4자리·복제본 3곳이 경로:라인으로 특정된 조건 없는 액션)**
- [2026-08-07] **`INFRA_TYPES`의 문서 짝 부재** — `lint.py:108`의 `INFRA_TYPES = {index, log, dashboard, schema}`는 `wiki-schema.md`에 대응하는 **열거 서술이 없다**(§12 `:630`이 `dashboard` 1종만 언급). v1.165.0의 축 ⑩ ⓐ 그룹은 "lint 상수 ↔ 문서 산문" 대조라 문서 자리가 없으면 대상이 될 수 없어 제외했다. **[2026-09-05 전제 갱신]** v1.238.0이 `INFRA_TYPES`에서 `dashboard`를 빼(`{index, log, schema}`) 이 항목이 인용하던 §12의 dashboard 언급도 함께 삭제했다 — **문서 짝이 부재한 것이 아니라 열거 대상이 하나 줄었다**. *"인프라 타입을 문서에 열거할 것인가"* 라는 별도 판단은 그대로 남는다. 역으로 *"인프라 타입을 문서에 열거할 것인가"* 는 별도 판단이며, 열거를 만들면 그때 축 ⑩에 자리를 하나 더할 수 있다. (출처: v1.165.0 plan Deferred) (2026-08-18 재확인: 유지 — v1.180.0 회차(위키 인덱스 파생화·스킬 파일 분할·git 자동 반영)가 이 영역을 건드리지 않았다.) **(2026-08-19 재확인: 유지 — 「인프라 타입을 문서에 열거할 것인가」 판단 자체가 조건 없는 액션)** **(2026-09-03 재확인: 유지 — 13차 판정 — 문서 자리를 만드는 것이 선행이나 그 자체가 조건 없는 액션 — 전제 불변)**
- [2026-08-07] **하네스 레포 큐잉 예외 때문에 큐 자동 소비를 자기 레포에서 검증할 수 없다** — 절차 K 5-2·5-3이 하네스 레포에서 큐 대신 plan/notes 직접 기록을 규정하므로 **소비할 큐가 없고**, v1.166.0은 실행 경로를 한 번도 돌지 못한 채 배포됐다(검증은 문서 정합 + 포인터 도달성 기계 대조까지). 닫는 길은 둘 — ⓐ 그 예외를 푸는 것(단 예외의 근거인 *"자기 레포 개선은 그 레포 세션에서 일어난다"* 가 여전히 유효한지 재판정 필요) ⓑ 큐 픽스처 + vault 사본으로 소비 경로를 도는 eval 신설(범위가 크다). **다른 프로젝트에서 첫 실사용 관측이 나오면 그것이 1차 실증**이므로 그 결과를 먼저 보는 것도 선택지다. (출처: v1.166.0 위험 표 1행) (2026-08-18 재확인: 유지 — v1.180.0 회차(위키 인덱스 파생화·스킬 파일 분할·git 자동 반영)가 이 영역을 건드리지 않았다.) **(2026-08-19 재확인: 유지 — 두 해소안(예외 해제·큐 픽스처 eval)이 모두 조건 없는 액션)** **(2026-09-03 재확인: 유지 — 13차 판정 — 닫는 길 두 갈래가 본문에 특정된 조건 없는 액션 — 이번 회차도 이 레포라 큐 소비 경로를 돌지 못했다(같은 공백 재현))**
- [2026-08-09] **절차 K 폴백 기록처가 `plan.md` 단일점이 됐다** — notes 규약 폐지(v1.167.0)로 5-1~5-5의 vault 부재 폴백이 "plan.md 또는 notes.md"에서 **plan.md 단일**로 좁아졌는데, **plan.md가 없는 세션**(질문·조사만 한 세션, 계획 없이 진행하는 trivial 작업)에는 폴백 자리가 아예 없다. 현행 문면은 "plan.md가 있으면 그 섹션에, 없으면 보고로 갈음"이라 **유실 2차 방지가 그 경우에만 꺼진다**. 후보는 대장 `## 대기`를 폴백으로 승격하는 것인데, 대장은 레포 내 작업 추적용이고 큐는 크로스 프로젝트용이라 목적이 달라 즉시 채택하기 어렵다. (출처: v1.167.0 plan Deferred) **(2026-08-19 재확인: 유지 — 폴백 승격 판단이 조건 없는 액션)** **(2026-09-03 재확인: 유지 — 13차 판정 — plan.md 없는 세션의 폴백 자리 부재가 그대로다 — 문면 1구 추가로 닫히는 조건 없는 액션)**
- [2026-08-09] **`wiki-schema` §5-2b·`procedures-content` A-2의 30일 임계가 유도 근거를 잃었다** — 그 값은 원래 "notes.md 1개월 보존 규칙"에서 유도된 것인데 notes 규약이 사라져 근거가 없어졌다. v1.167.0은 값을 유지한 채 근거만 "위키 신선도 관례"로 재서술했으므로 **숫자 자체는 재산정되지 않은 상태**다(바꿀 실측 근거가 그 회차에 없었다). ingest 델타 신뢰도 실측이 쌓이면 30일이 적정한지 판정한다. (출처: v1.167.0 plan Deferred) **(2026-08-19 재확인: 유지 — 실측 축적이 조건이나 「근거를 잃은 숫자가 남아 있다」는 것 자체가 실재 결함이라 유지(애매하면 유지))** **(2026-09-03 재확인: 유지 — 13차 판정 — 임계 재산정이 조건 없는 액션(다만 바꿀 실측 근거가 아직 없다는 본문 단서도 그대로다))**
- [2026-08-21] **`check_consistency.py`에 「§7-16 대상 조건 ↔ §3 행 형식 서술」 대조 축 신설 검토** — v1.190.0 T11이 `feat_row_name`의 판정을 넓혔을 때 `wiki-schema.md` §3·§7-16·§7-27·§2.6의 서술이 옛 동작(첫 컬럼 평문 한정)에 그대로 남았는데 **186항목 어느 축도 잡지 못했다**(F-7 2R MAJOR로 사람이 발견). 코드 상수와 문서 산문을 잇는 축은 이미 있으나(예산 14키·통제 어휘) **행 인식 조건**은 대조 대상이 아니다. 축을 만들려면 조건을 문서에서 기계로 뽑을 수 있는 형태로 고정해야 하므로 비용이 작지 않다. (출처: v1.190.0 F-7 2R Follow-ups) **(2026-08-28 12차 batch 병합 — `[2026-08-21]` 「마커 없는 vault에서 recipe의 이중 등재를 강제하는 기계 검사가 없다」를 흡수한다. 둘 다 `check_consistency.py`에 **§4·§3의 행 배치 규정을 기계로 잇는 축**을 새로 만드는 일이고, 조건을 문서에서 기계로 뽑을 수 있는 형태로 고정하는 선행 작업을 공유한다 — 따로 열면 같은 파싱 설계를 두 번 한다)** **(2026-08-28 재확인: 유지 — 조건을 문서에서 기계로 뽑을 수 있는 형태로 고정하는 선행 설계가 필요하고, 병합으로 recipe 이중 등재 축까지 범위가 넓어졌다)**
- [2026-09-02] **위키 절차 B-2에 「커밋 본문에서 결정·사실을 직접 기록하는 단계」가 명시돼 있지 않다** — v1.222.0 T10이 신설한 B-2 0-0은 *"이 절차가 커밋 본문·레포 문서에서 읽어 낸 결정·작업 사실을 기록하기 전에"*를 전제하는데, **B-2의 어느 단계도 그 기록 행위를 정의하지 않는다**(`wiki-schema.md` §2.8도 소비 주체를 「ingest 세션 B-1 0 · lint · F-6.5」 셋으로만 열거한다). 실무적으로는 실행자가 ingest 중 발견한 사실을 판단해 기록해 왔고 그래서 이중 유입이 실제로 생겼지만, **그 행위가 절차에 없어** 0-0이 무엇을 막는지가 B-2만 읽어서는 추적되지 않는다. 기록 단계를 명시하거나 0-0의 대상을 B-1 0 소비로 한정하는 판단이 필요하다. (출처: v1.222.0 T10 quality m1 — 그 회차가 판정 유보로 남긴 것을 다음 계획이 게이트 판정해 등재)
- [2026-09-02] **`--auto-split`의 형제 하위 수집이 §7-30ⓔ보다 좁은 범위를 본다** — ⓔ 본검사는 파일명과 무관하게 복귀 링크만 보는데, `--auto-split`의 형제 수집은 여전히 `{stem}-*.md` **glob**에 의존한다(`plugins/pjc/skills/llm-wiki/scripts/lint.py:1927` 부근). 그래서 상위와 **전혀 다른 이름**의 수기 하위(복귀 링크만 있는 것)는 린트가 잡아도 자동 재등재는 못 한다. v1.222.0 T6이 숫자 접미 요구는 없앴으나 **접두 요구가 glob 자체에 남아 있다** — 두 자리를 같은 신호로 만들려면 형제 수집도 전체 스캔 + 복귀 링크 판정으로 바꿔야 하는데 비용이 다르다. 실무 영향은 제한적이다(`{stem}-` 접두를 공유하는 흔한 형태는 커버되고 `--fix`는 §7-30을 대상으로 하지 않는다). (출처: v1.222.0 T6 quality m2 — 그 회차가 판정 유보로 남긴 것을 다음 계획이 게이트 판정해 등재)
- [2026-09-02] **위키 `conventions-verification-2.md`의 릴리즈 마커 7건** — 본문에 v접두 3필드 semver가 7곳 있어 lint §7-28(§5 changelog 미러링 금지 — *"이력 정본은 레포 git·릴리즈, 위키 서술은 현재형·이유 중심"*)에 걸린다. v1.223.0 T3이 같은 파일을 구역화했으나 **처방이 문면 재서술**이라 「이동만 하고 개정하지 않는다」는 그 회차 원칙과 성격이 달라 사용자가 범위에서 뺐다(Q1). 다음 위키 세션(ingest 또는 lint 소비)이 현재형으로 고쳐 쓴다. 근거: 2026-09-02 lint 실측 `릴리즈 마커 7건`. (출처: v1.223.0 Q1)

### eval · 측정

- [2026-07-15] **품질 루브릭 eval(Opus judge 채점)** — 고정 task 세트를 돌려 아키텍처 품질을 1-10 루브릭으로 채점, 파이프라인 변경 효과를 수치로 측정(eval 기준선은 Opus 실작업). (출처: v1.122.0 plan Deferred) **(2026-08-06 재확인: 유지 — 설계가 구체적이고 조건부가 아님(고정 task 세트 + Opus judge 루브릭))** **[2026-08-06 병합]** 「[2026-07-29] eval 기준선의 2회차(전후 비교) 측정」의 **잔여분을 이 항목에 흡수**했다 — 그 항목의 **트리거 축은 해소됐다**(`plugins/pjc/skills/evals/README.md:60`이 현행 격리 기준선을 `2026-08-06 run 20260806-153530 · 56건 · 발동률 0.906 · 오발동률 0.000`으로 기록하고, v1.160.0·v1.161.0이 전량 재측정을 두 차례 수행했다). 남은 것은 **루브릭 축의 전후 비교**뿐인데 그 러너·루브릭을 세우는 일이 곧 이 항목이라, 둘을 따로 두면 같은 작업을 두 번 등재한 셈이 된다. **(2026-08-06 재확인: 유지 — 루브릭 eval 설계가 구체적이고 2회차 비교 잔여분까지 흡수해 착수 단위가 1건으로 모임)** **(2026-08-12 재확인: 유지 — 직전 판정(2026-08-06) 근거 그대로 유효, v1.169.0 변경으로 바뀐 전제 없음)** **(2026-08-23 재확인: 유지 — 검증 자산 확장이라 조건 없이 착수 가능하고, 이번 회차가 그 축을 대체하지 않았다)** **(2026-08-24 재확인: 유지 — 9차 batch(절대 상한 축). 전건이 2026-08-23 재확인 상태였고 v1.195.0(인용 인자 스트립)이 이 항목의 전제를 바꾸지 않았다)**
- [2026-08-06] **`plan-pos-9`: 값 치환 + 동작 변경이 섞인 요청을 `pjc:plan`가 잡지 못한다** — v1.161.0 T4가 축을 복원했는데도(포맷 문자열 치환 → **조건 분기 신설**: *"로그 레벨 DEBUG로 낮추고, 합계가 음수면 0으로 처리하게"*) 모델이 `tool_calls=[Read,Glob,Read,**Edit**]`로 **직접 수정**을 택했다. 즉 순수 값 치환이 아닌데도 Trivial Bypass 쪽으로 판정한다. description의 Do NOT 절(*"multi-element pure value substitution … Claude edits directly"*)과 *"Ambiguous scope — ask A/B"* 사이의 경계가 모델에게 충분히 갈리지 않는 것으로 보인다. **주의**: 이 예외를 좁히면 `plan-neg-2`(단건 값 치환)·`plan-neg-7`(다건)이 깨져 오발동률 0.000이 무너진다 — 문구를 넓히는 방향이 아니라 **경계를 더 선명히 하는 방향**이어야 한다. (출처: v1.161.0 T4·T5) **(2026-08-19 재확인: 유지 — 경계 선명화는 조건 없는 액션이고 회귀 가드(neg 2건)가 명시돼 있다)** **(2026-09-03 재확인: 유지 — 13차 판정 — 경계 선명화가 조건 없는 액션이고 회귀 가드(neg 2건)도 그대로다)** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-08-06] **`dsvc-pos-2`: `ddd_project` 픽스처에 재고 개념이 없어 질의 전제가 성립하지 않는다** — 질의는 *"재고 차감 use case를 애플리케이션 레이어에 추가하고 DI 등록까지"* 인데 픽스처는 **`Order.cs`(Id·Amount 2필드)뿐이고 Application·Infrastructure는 빈 디렉터리**다. v1.161.0 T5에서 모델이 그것을 확인하고 *"차감할 재고가 도메인에 없습니다"* 로 되물어 **발동 → 미발동 회귀**로 나타났다(T2에선 그냥 발동했다 — 런 편차가 기존 결함을 드러낸 것). **v1.160.0 `dbg-pos-1`과 같은 유형**(질의 전제가 픽스처와 어긋남)이며 해소안도 같다 — 픽스처에 재고·품목 개념을 넣거나 질의를 픽스처에서 성립하는 것으로 바꾼다. `add-domain-service` description·`ddd_project` 픽스처는 v1.161.0 diff에 없어 **개정과 인과가 없다**. (출처: v1.161.0 T5) **(2026-08-19 재확인: 유지 — 픽스처 보강·질의 교체 중 택1로 닫히는 조건 없는 액션)** **(2026-09-03 재확인: 유지 — 13차 판정 — 픽스처 보강·질의 교체 중 택1로 닫히는 조건 없는 액션)**

- [2026-08-06] **description을 케이스 문면 쪽으로 쓰는 방향에는 복창 규정이 없다** — `evals/README.md`의 복창 규칙은 **케이스가 description을 베끼는 방향만** 금지하는데(*"그러면 발동률이 아니라 복창률을 재게 된다"*), v1.161.0의 전환 3건은 모두 반대 방향이다 — `pjc:plan`에 `…해야 해`를 넣자 `plan-pos-2`(*"…바꿔**야 해**"*)가, `bootstrap`에 *"빌드·실행·테스트 방법…문서"* 를 넣자 `boot-pos-2`(*"빌드랑 테스트 방법을…문서 하나"*)가, `add-viewmodel`에 *"전역 상태(테마·언어·환경설정)"* 를 넣자 `vm-pos-1`(*"테마 토글이랑 언어 선택"*)이 발동했다. **그래서 0.871 → 0.906(+0.035)이 일반화인지 그 3케이스 적합인지 구분할 장치가 없다.** 부수로 `boot-pos-2`의 `why`(*"description 예시를 베끼지 않은 실사용 발화"*)도 이제 stale이다. **해소안**: ① `evals/README.md` 복창 문단에 반대 방향 1구 추가 ② 각 축별로 **미복창 신규 pos 케이스**를 넣어 일반화를 확인. (출처: v1.161.0 F-7 M1) **(2026-08-19 재확인: 유지 — 해소안 2개(README 1구 + 미복창 pos 케이스)가 모두 조건 없는 액션)** **(2026-09-03 재확인: 유지 — 13차 판정 — 해소안 ①②이 특정된 조건 없는 액션)** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)** **(2026-09-03 재판정: 유지 — 해소안 ①②는 그대로 유효하다. 단 `bootstrap` 축의 케이스는 v1.227.0 회차 4가 스킬과 함께 폐기했고 `evals/README.md` 의 그 반증 문단도 케이스 이름 의존을 걷었으므로, 남은 대상은 `pjc:plan`·`add-viewmodel` 두 축이다.)**
- [2026-08-06] **오발동 게이트 ②-b가 `timeout` 케이스를 통째로 제외한다 — 진짜 오발동이 숨을 수 있다** — ②-b는 `triggered` 집합을 보는 축인데 제외 기준을 ②-a(`status` 기반 판정 집합)에서 그대로 상속했다. **`timeout`에도 `triggered`는 채워지므로**(`trigger_eval.py:399-402`) *"발동했는데 180초를 넘겨 timeout"* 인 케이스는 대조에서 빠진다. v1.161.0 T5에서 `impl-neg-1`이 정확히 그 형태였고(그 건은 개정 이전 run에도 같은 발동이 있어 귀속 근거가 없었지만) **다음 회차엔 진짜 오발동이 이 뒤에 숨을 수 있다**. **해소안**: D6 ⓑ를 *"`triggered`가 비어 있지 않은 timeout은 대조에 포함한다"* 로 개정. (출처: v1.161.0 F-7 m5) **(2026-08-19 재확인: 유지 — 해소안이 1구 개정으로 특정됨(D6 ⓑ))** **(2026-09-03 재확인: 유지 — 13차 판정 — 해소안이 1구 개정(D6 ⓑ)으로 특정됨)**

- [2026-08-15] **절차 M(큐 자동 소비)의 보류 8종에 「프로젝트 아님(스택 공통)」 축이 없다** — v1.174.0 T2가 `procedures-content.md` B-1 0에 스택 공통 라우팅 분기를 신설했으나, `references/procedures-ops.md`의 자동 소비 경로는 *"이 목록이 B-1 0 상속에 우선한다"*고 규정한 **보류 8종**에 그 축을 갖지 않는다. **실해는 확인되지 않았다** — 절차 M은 그 세션 *대상 프로젝트의* 큐만 소비하므로 스택 항목(프로젝트가 아님)은 매칭되지 않아 보고 경로로 빠진다. 다만 문면상 축이 비어 있어 자동 경로가 스택 항목을 `(등록보류 …)`로 오분류할 여지가 남고, 그것은 T2 acceptance ④가 막으려던 *"gpui를 프로젝트로 등록할까요"* 혼동의 자동 판본이다. 후보: 보류 8종에 「프로젝트 아님 → `(스택보류 …)` 표식·보고」 1행 추가. (출처: v1.174.0 F-7 m2 — 판정 유보였으나 메인이 실해 없음으로 판정하고 문면 공백만 등재) (2026-08-18 재확인: 유지 — v1.180.0 회차(위키 인덱스 파생화·스킬 파일 분할·git 자동 반영)가 이 영역을 건드리지 않았다.) **(2026-08-24 재확인: 유지 — 실해는 미확인이나 문면 보완 자체는 조건 없는 액션)**
- [2026-08-15] **acceptance가 산출물을 요구할 때 그 기록의 영구 저장 위치 규약이 없다** — v1.174.0 T2 acceptance ⑤가 *"큐 12건 대조 결과를 `## Progress Log`에 표로 남긴다"*를 요구했는데 `plan.md`는 `.gitignore`(로컬 전용)라 **다음 계획 때 교체되면 사라진다**. 이번 회차는 회차 서사 커밋 본문에 12행 요약을 옮겨 해소했으나, pjc:plan가 acceptance에 산출물 기록을 요구할 때 *"어디에 남기면 영구인가"*(커밋 본문 / 대장 / 레포 문서)를 규약으로 둘지는 미정이다. 같은 계열: `[2026-08-15]` vault 예산 단위 오판 항목(기록처가 갈려 값이 어긋난 사례). (출처: v1.174.0 F-7 m3) (2026-08-18 재확인: 유지 — v1.180.0 회차(위키 인덱스 파생화·스킬 파일 분할·git 자동 반영)가 이 영역을 건드리지 않았다.) **(2026-08-24 재확인: 유지 — *"어디에 남기면 영구인가"*를 규약으로 둘지의 판단이 조건 없는 액션)** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-08-15] **대장 항목을 라인 번호로 지목하는 관행이 stale 사고를 낸다** — v1.174.0 계획에서 대장 항목을 `:43`·`:52`로 지목했는데 실제 내용은 무관한 미해결 항목이었다(원인: `sed`로 `## 대기` 구간만 추출한 뒤의 **상대 라인**을 파일 라인으로 옮겨 적음). 그 지목이 **사전 승인된 삭제 대상**이었기 때문에 그대로 실행됐다면 살아 있는 항목을 지우고 정작 해소된 항목은 남았을 것이다(plan-reviewer 1R이 BLOCKER로 차단). 이 대장은 **상단 등재라 매 회차 라인이 밀리므로** 라인 참조가 구조적으로 썩는다. 후보: 대장 안팎에서 항목을 가리킬 때 **`[등재일] + 요지 인용`**을 쓰도록 규약화(그 plan은 자기 문서에만 적용했다). (출처: v1.174.0 plan Deferred) **(2026-08-24 재확인: 유지 — 규약 ③이 **병합에 한해** 등록일+제목 지목을 정했을 뿐 전면 규약은 아니다 — 남은 자리가 그대로다)**
- [2026-08-15] **§7-29 제외에서 `question` 축이 빠진 것은 의도된 비대칭이지만 조건부다** — v1.174.0 T1이 `decision-log`만 타입 제외한 근거 셋 중 하나가 *"기존 `emoji-bad` 픽스처의 두 제외 실증(`strip_code`·`is_lint_report`)이 `type: question`에 실려 있어 question을 넣으면 두 축이 통째로 가려진다"*였다(축 겹침). 즉 **픽스처를 question 아닌 타입으로 옮기면 그 근거가 해소되고 question 제외도 검토 가능해진다.** §7-28은 이미 question을 제외하고 있어 두 검사의 기준이 여전히 갈린 상태다. 착수 조건: 픽스처 재배치가 선행되어야 하며, 그 자체가 「테스트 케이스의 축 분리」(위키 patterns) 적용 사례가 된다. (출처: v1.174.0 plan Deferred·D1) **(2026-08-24 재확인: 유지 — 픽스처를 question 아닌 타입으로 옮기는 것이 조건 없는 액션)**

- [2026-08-18] **검출 범위를 축소할 때 「무엇을 못 잡게 되는가」 1줄 의무가 일부 지점에만 걸려 있다** — T9 acceptance ③은 *검증 지점 축소*에 그 기록을 의무화했는데, **가드·검사기의 스코프 조정**에는 같은 요구가 없다. v1.179.0 T3이 축 ⑫ 스코프에서 `llm-wiki`를 뺐을 때(위반 17 → 14) 오탐 3건이라는 축소 근거는 적었으나 **손실 쪽 1줄은 없었다**. 의무를 「검출 능력을 줄이는 모든 변경」으로 일반화할지 검토한다. (출처: v1.179.0 F-7 m3) **(2026-08-28 재확인: 유지 — 의무를 「검출 능력을 줄이는 모든 변경」으로 일반화할지가 선행 판정 — **이번 회차 T3이 자발적으로 그 형식을 따랐다**(신규 축 docstring에 「못 잡는 것」 명시))**
- [2026-08-19] **`--build-index`가 페이지당 1행만 만들어, 한 feature 페이지를 두 기능으로 등재하던 구조가 소실된다** — v1.180.0 vault 적용에서 실측: `maid/feat-app-shell.md`가 「앱 껍데기」와 「정보 창」 두 행으로 등재돼 있었는데 생성 후 1행이 됐다(라벨 병합으로 검색은 살렸으나 **행은 하나다**). 생성기는 `index_label` 1개만 읽는다. 수요가 실재하므로 **`index_label`을 리스트로 받아 여러 행을 만드는 안**을 검토한다 — 다만 그러면 §7-16 병기 검사·역이관 스크립트·충돌 판정이 모두 다중 값을 다뤄야 하므로 비용이 작지 않다. 대안은 페이지 분리(위키 내용 변경). 현재는 1건뿐이라 병합으로 견딘다. (출처: v1.180.0 vault 적용 회차) **(2026-08-28 재확인: 유지 — `index_label` 다중화가 §7-16 병기 검사·역이관·충돌 판정까지 번져 비용이 크고, 실측 수요가 1건이라 병합으로 견디는 중)**
- [2026-08-20] **골든 `Unkillable` 케이스가 실기계 PID 4에 `Stop-Process`를 건다** — System은 보호 프로세스라 실해가 없고 시나리오 주석이 선택 근거를 밝히지만, D15가 `-DryRun`을 만든 취지(목 PID가 실재와 겹치는 것 회피)와 T3의 「실기계 상태에 의존하지 않는다」에서 유일하게 벗어난 지점이다. 죽지 않는 대상을 목으로 쓰지 않고 **소멸 판정을 주입하는 방식**이 있는지 검토할 것. (출처: v1.183.0 F-7 m4 → v1.184.0 T7 이관) **(2026-08-28 재확인: 유지 — 소멸 판정 주입 방식의 존재 여부 확인이 선행 — 이번 회차는 골든을 2회 돌렸고 그 케이스가 실해 없이 통과했다)**
- [2026-08-20] **⏳ HUMAN-VERIFY 2건 — 재설치 이후에만 확인 가능** — ① SessionEnd hook이 실제로 발화하는지(`hook-events` jsonl에 `hook='session-end-cleanup'` 엔트리가 생기는지) ② `pwsh ./validate.ps1` 신규 경고 0건. **hooks는 세션 시작 시 로드되고 플러그인은 설치 캐시에서 읽히므로 자율 루프 안에서는 원리상 확인할 수 없다.** v1.183.0이 최종 보고로 인계했고 v1.184.0도 릴리즈 없이 끝나면 같은 자리에 남는다 — `install.ps1 -Uninstall` → 재설치 뒤 그 두 가지를 확인할 것. (출처: v1.183.0 plan Deferred → v1.184.0 F-7 m2로 대장 이관) **(2026-08-28 재확인: 유지 — 재설치가 선행 조건이라 자율 루프 안에서 원리상 확인 불가 — **이번 회차도 릴리즈 없이 끝나면 같은 자리에 남는다**)**
- [2026-08-22] **[SUGGEST] `lint.py` 파일 응집도 재검토** — 2,327줄이며 §7 검사 엔진 · `--fix` · `--build-index` · `--auto-split`/`SplitSession`이 한 파일에 공존한다(v1.191.0 T2 quality S1). 그 회차가 계속 키웠으므로 별도 판단이 필요하다. **단 `pjc:implement` 규칙 8의 네 질문으로 판정할 것** — 줄 수 자체는 분할 사유가 아니다. (출처: v1.191.0 plan Deferred 이관) **(2026-08-28 재확인: 유지 — 규칙 8의 네 질문 판정이 선행이며 vault 세션이 아니라 하니스 레포에서 할 수 있다 — 조건 없이 착수 가능)** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)**
- [2026-08-22] **임계 초과 나머지 9페이지의 `### ` 구역화** — v1.191.0 T11의 §7-32 INFO가 각 ingest 시점에 알린다(그 회차 사용자 선택: 대상 2건만 소급). 나머지는 ingest 때 처리하거나 별도 회차에서 일괄한다. (출처: v1.191.0 plan Deferred 이관) **(2026-08-28 재확인: 유지 — vault 세션 대상 — 각 ingest 시점의 §7-32 INFO가 알리므로 일괄 회차를 따로 열 필요가 낮다)**

- [2026-08-22] **하니스 축 ⑫(개념 정본 유일성)의 「위반」·「면제 잔여」가 리포트 전용이다** — `plugins/pjc/evals/check-harness-consistency.py`의 `check_concept_locality()`는 다섯 구획(정본·화이트리스트·위반·차집합·면제 잔여)을 버킷으로 나누지만 `issues`에 넣는 것은 **정본 파일 부재와 앵커 매치 수뿐**이고, 위반·잔여는 `--concept-report`에만 나온다(2026-08-22 실측 — `:421` 잔여 판정은 `line.count(word) > len(hit_ref)` 개수 비교다). `docs/harness-conventions.md:212`가 *"T3 시점에는 리포트 전용이고 기본 실행에는 편입돼 있지 않다(T11에서 승격 — **축 ⑪의 전례대로** 소진이 끝난 뒤 편입한다)"*고 예고했는데 그 승격이 아직 없다. **v1.192.0이 축 ⑪에서 그 전례를 완성했으므로**(면제 입도를 조각 단위로 바꿔 잔여를 0으로 만든 뒤 편입) 같은 경로를 축 ⑫에 적용할 수 있다. **단 두 축은 면제 형태가 다르다** — ⑪은 「앵커 조각이 조건어를 덮는가」이고 ⑫는 「개념어 출현 수 ≤ 정본 식별자 수」라 개수 비교다. 개수만 보는 판정은 **개수가 보존된 채 문면이 치환되면 미탐**이므로, 승급 전에 ⑪처럼 입도를 바꿀지부터 판정해야 한다. (출처: v1.192.0 계획 조사) **(2026-08-28 12차 batch 병합 — 같은 축 ⑫의 착수 단위 3건을 흡수한다: `[2026-08-18]` 「대상 개념 확대」(계측 I 나머지 — `Halt` 16파일·`압축` 10파일, 오탐 선측정 필요) · `[2026-08-18]` 「정본 앵커 **내부**의 완전 중복 줄을 못 잡는다」(v1.179.0 T8이 동일 문단 3회, F-7이 실물 대조로 발견) · `[2026-08-23]` 「AGENTS.md ↔ 위키 소속 경계 등재」(같은 문면 16자리 복제, F-7 3라운드 연속 지적). 넷 다 **축 ⑫를 어떻게 승급·확대할지**라는 하나의 판정에 걸려 있어 따로 착수할 수 없다 — 입도를 ⑪처럼 조각 단위로 바꿀지가 선행 판정이고, 그 답이 대상 확대·내부 중복·경계 등재의 형태를 함께 정한다)** **(2026-08-28 재확인: 유지 — 승급 전에 면제 입도를 ⑪처럼 조각 단위로 바꿀지가 선행 판정이며, 병합으로 대상 확대·내부 중복·경계 등재 3건이 이 판정에 함께 걸렸다)** **(2026-09-02 재확인: 유지)** — 이 항목이 인용한 `harness-conventions.md` 문면은 v1.222.0이 *"v1.179.0 T11에서 `main()`에 편입돼 exit code에 영향을 주는 gate"*로 정정했다(그 문서가 stale이었다). **이 항목의 주장은 그와 양립한다** — 축은 편입됐으나 `issues`에 담기는 것은 정본 파일 부재·앵커 매치 수뿐이고 **위반·면제 잔여 구획은 여전히 리포트 전용**이다. 두 층을 섞지 말 것.

- [2026-08-23] **FR-11(Could) — 주입 상한 임박 경고가 절 단위 크기를 지목하도록** — `session-context.ps1`의 경고 메시지에 상위 절 크기를 넣는 것. v1.193.0 T9가 `check-harness-consistency.py`에 같은 계산(`top_sections()`)을 넣었으므로 그 함수를 재사용할 수 있게 되면 착수 비용이 준다. PRD에서 Could라 이번 범위 밖이었다. (출처: v1.193.0 PRD FR-11) **(2026-08-28 재확인: 유지 — `top_sections()` 재사용 경로가 이미 있어 조건 없이 착수 가능하고, 대상이 이번 회차가 고친 `session-context.ps1`이다 — 같은 파일의 「목차 폴백 신호」 항목과 함께 열면 hook 회차 하나로 닫힌다)**
- [2026-09-02] **「볼드 마커 짝」 축이 중첩을 보지 못한다** — `check-harness-consistency.py`의 그 축은 `**` 개수만 세므로, 볼드 안에 볼드가 겹친 형태를 통과시킨다(여는 쪽과 닫는 쪽의 개수가 맞아떨어져 짝이 성립한 것처럼 보인다). v1.218.0 후속 F-7 2R이 1R 수정 과정에서 실제로 그 형태를 만들어 M3로 잡혔는데, 같은 회차 plan의 T2 Edge Cases가 그것을 이름으로 금지해 두고도 기계는 보지 못했다. (출처: v1.218.0 후속 F-7 2R 권고)

---

> **분리된 두 파일** — 이 대장은 `## 대기`만 담는다(v1.198.0 분할).
> - **종결 항목**: `docs/plans/deferred-closed.md` — 기각 종결분. 계수는 위 앵커가 정본이다.
> - **소진 batch 회고**: `docs/plans/deferred-history.md` — 회차 서사. `batch 차수 수열` 축의 대조 대상이다.
