# Deferred 대장

> 미처리 Deferred의 추적 대장 — **`## 대기`만 담는다** (형식 정본: `plan/references/deferred-rules.md`).
> **v1.198.0에서 셋으로 갈렸다**: 기각 종결은 `deferred-closed.md`, 소진 batch 회고는 `deferred-history.md`.
> **등재·조회는 이 파일 하나**이고 나머지 둘은 기각 이동·batch 착수 때만 연다 — 분할 전 296KB가 계획 1건마다 통째로 실렸다.
> 항목은 `- [등록일] {요지 1줄} (출처) (실해: YYYY-MM-DD {관측 요지})` — **실해 필드는 필수**이고 `check-harness-consistency.py` 의 「등재 근거 실측」 축이 `## 대기` 전건을 잰다.
> **등재 하한선은 축 둘을 모두 넘어야 한다** — ① **실해**(틀린 출력·차단·유실·오탐이 실제로 일어났다) ② **액션**(그 항목이 지시하는 것이 파일 수정이다). 「현재 오차 0이다」·「조건이 오면 터진다」·「재는 축이 없다」·「관측 경로가 구조적으로 없다」는 ①에서, **교훈·작성법·착수 전 점검 목록·미착수 제안**은 ②에서 떨어진다(정본은 `plan/references/deferred-rules.md`).
> **회차 순증 금지** — 한 회차가 등재하는 수는 종결하는 수를 넘지 않는다. **리뷰 소스의 지적은 `implement/references/final-review.md`「필터 패스」가 먼저 가르고, 거기서 걸러지지 않은 것만 이 문턱을 통과한다.**
> **항목 상한은 600 B다** — 대장은 「무엇을 고칠지」의 목록이지 조사 기록이 아니다(정본은 `skills/BUDGET.md`). **종결은 두 경로다 — 항목을 삭제하거나(근거는 커밋 본문) `## 종결`로 사유와 함께 옮긴다. 사유가 기각인지 반영인지는 경로를 가르지 않는다**(회차 24 가 실태를 규약으로 삼았다. 3분기 표·판정 규칙 정본은 아래 「연산 규칙」).
> `pjc:plan` Step 1이 계획 때 `## 대기`를 조회하고, `pjc:implement`가 완료 때 신규분을 append한다.

## 대기

- [2026-09-10] **`destructive.json` 의 `why` 필드가 어디에도 출력되지 않는다 — 차단 사유가 정규식 원문으로 나간다** — `block-destructive.ps1:215~218` 이 `Deny "파괴적 명령 패턴 감지: '$pattern'"` 로 **패턴 문자열 자체**를 stderr 에 싣고, 그 옆의 `why`(45행 전부가 갖고 있다)는 **읽히지 않는다.** 그래서 ⓐ 사용자는 차단 이유 대신 정규식을 보고 ⓑ 골든이 사유로 케이스를 가르지 못해 **정규식 조각을 앵커로 삼아야** 하며, 그 앵커는 패턴을 손대는 순간 깨진다. 처방: `Deny` 에 `why` 를 넘겨 사유를 출력하고 패턴은 이벤트 로그에만 남긴다(그 자리는 `Write-BdEvent` 가 이미 받는다). **착수 조건 없음.** (출처: 2026-09-10 회차 53 T4) **(실해: 2026-09-10 회차 53 이 신규 패턴 3행의 골든 4건을 `why` 문구로 걸었다가 전건 FAIL — 출력에 그 문구가 없었다. 정규식 조각으로 앵커를 바꿔야 했고, 그 앵커는 패턴이 바뀌면 함께 깨진다)**
- [2026-09-10] **`Split-TopLevel` 은 heredoc 을 모른다 — 그 본문의 `;`·`|` 가 최상위 구분자로 잘려 `block-plan-write` 가 정상 명령을 오차단한다** — `block-destructive.ps1:61` 에는 heredoc 스트립이 있지만 `guard-bash.ps1` 에는 **0건**이라, 인라인 스크립트 본문의 구분자가 최상위 구분자로 취급된다. 쪼개진 조각의 첫 실효 토큰이 heredoc 본문의 단어가 되어 **쓰기 동사 판정이 그 자리에서 발화한다.** **미탐이 아니라 오차단 쪽이 먼저 난다** — 회차 52 착수 전 정규식 분할에서도 같아 회귀는 아니다. 처방: `block-destructive` 의 heredoc 스트립을 같은 형태로 가져오고 **오차단 음성 1건**(heredoc 본문에 `plan.md` 를 쓰는 동사가 있어도 대상은 다른 파일)과 양성 1건을 함께 넣는다. **착수 조건 없음.** (출처: 2026-09-10 회차 52 T6 · 완료 리뷰가 방향을 정정) **(실해: 2026-09-10 실측 — `cat <<'EOF' > notes.md` / 본문 `hello; cp notes.md plan.md` / `EOF` 가 `exit 2` 로 차단된다. 대상은 `notes.md` 인데 heredoc 본문의 `;` 뒤 조각이 `cp … plan.md` 로 서서 **정상 명령이 막힌다**)** **[2026-09-10 회차 53 재확인 — 유지]** 착수했다가 뺐다: 이식 최소형이 **코드 402 B + 근거·마커 주석 ≈ 647 B** 인데 `guard-bash.ps1` 여유가 **344 B** 라 303 B 초과이고, 대비책으로 검토한 「`Invoke-RequireTaskCheckbox` 정규식 외부화」는 실측 **순증 +555 B**(이관 가능 리터럴 103 B 중 47 B 는 `$taskNum` 보간이라 불가 · 신규 로더 591 B)로 반대 방향이었다. **그리고 「분할 헬퍼 동기」 축 확장이 구조적으로 막힌다** — `block-destructive.ps1:64~70` 의 heredoc 스트립은 **함수가 아니라 최상위 인라인 코드**라 함수 선언을 찾는 `_ps_function_body` 로는 그 파일에서 잡히지 않는다. **해법은 있다**: 그 구간이 마커 주석 두 개(`# ---- heredoc 파일-리다이렉트 본문 스트립 ----` ↔ `# ---- Join-Path 정규화 ----`) 사이에 있어 **`block-destructive.ps1` 을 안 고치고** 마커 구간 추출로 축을 넓힐 수 있다. **위 예산 항목과 묶어 착수한다.**
- [2026-09-06] **완료 리뷰가 `**구조**` 판정 대조를 잃었다 — 예산으로 뺀 검사 축** — 회차 29 T4 의 순증이 계획 상한 208 B 를 10 B 넘겨 plan 의 예비 처방을 적용했고, 그 대상이 `completion-reviewer.md` §5 의 *"plan 의 `**구조**` 판정과 실제 파일 배치가 다른 자리를 지적한다"* 였다. **`plan-reviewer.md:33` 이 그 자리를 메우지 않는다** — 그쪽은 계획 시점에 `**구조**` **필드의 존재**를 보고, 지운 쪽은 완료 시점에 **계획과 산출물의 일치**를 봤다. **부기 — 같은 회차가 뺀 「임시 파일을 레포에 만들지 않는다」는 대체 문면이 없다**: `plan-reviewer.md:81` 에는 남아 있어 두 리뷰어가 비대칭이고, 완료 리뷰어가 오히려 clean 트리에서 돌아 여기서 만든 파일이 다음 커밋의 diff 오염이 된다(세션의 scratchpad 주입이 실무상 완화한다). **착수 조건: `completion-reviewer.md` 의 여유가 400 B 이상이 되는 회차**(회차 29 종료 시 193 B). (출처: 2026-09-06 회차 29 완료 리뷰 BLOCKER·MINOR) **(실해: 2026-09-06 회차 29 가 예산으로 §5 의 「구조 판정 ↔ 실제 배치」 대조를 지워 그 축이 실제로 사라졌다)**
- [2026-09-07] **`block-destructive` 가 규약 문서의 SQL 키워드 나열을 차단한다 — 자기 문면을 고칠 때 걸린다** — `scripts/rules/destructive.json` 의 `TRUNCATE\s+(?!TABLE\b)(?!-)` 패턴이 **문서 문면**의 `TRUNCATE` + 공백 + 가운뎃점을 SQL 실행으로 판정해 `exit 2` 로 막았다(2026-09-07 실측 — 승인 목록을 `DB DROP/TRUNCATE · WHERE 없는 UPDATE/DELETE` 로 넓히는 편집이 Bash PreToolUse 에서 BLOCKED). **이 레포는 자기 규약 문서에 SQL 키워드를 나열한다**(`implement/SKILL.md` 의 상시 승인 목록 · `plan-template.md` 의 「승인 필요 항목」 · 글로벌 지침의 DB 줄) — 그 문면을 손댈 때마다 재발하는 구조다. 이번엔 파이썬 리터럴을 조각으로 조립해(`'TRUNC' + 'ATE'`) 우회했고 **패턴은 건드리지 않았다**. **처방 후보**: ⓐ 패턴에 SQL 문맥 조건을 더한다 — 키워드 뒤에 오는 것이 식별자인가(가운뎃점·한글·마크다운 구분자면 문서다) ⓑ **「문서 편집 경로 예외」는 두지 않는다** — 경로로 뚫으면 그것이 우회로가 된다. **착수 조건 없음** — 단 `AGENTS.md` 「DO NOT」이 오탐 수정에 **골든 회귀(신규 통과 케이스 + 수정 전 차단 음성 대조)로 실증**을 요구하므로 hook 수정과 골든 설계가 한 묶음이다. (출처: 2026-09-07 DB 승인 범위 확대 작업 중 관측) **(2026-09-07 같은 회차에서 2회째 — 발화 경로가 둘이다)**: 이 변경을 담는 **커밋 메시지**가 `DROP` + 공백 + `TABLE` 로 다시 막혔다(문서 편집 1회 · 커밋 메시지 1회). **이 관측이 처방 ⓑ 를 굳힌다** — 「문서 편집 경로 예외」를 둬도 커밋 메시지는 그 경로에 없어 덮이지 않는다. 막는 것은 **파일 경로가 아니라 문자열이 놓인 문맥**이므로 처방은 ⓐ 쪽이다. 해소는 두 번 다 파이썬으로 리터럴을 조각내 조립한 것이고, 커밋 메시지는 `git commit -F <파일>` 로 넘겨야 명령줄 스캔을 지난다. **(실해: 2026-09-07 block-destructive 가 문서 편집·커밋 메시지를 exit 2 로 2회 차단)**
- [2026-09-04] **규칙 533개 중 38%가 「왜」를 갖지 못했다 — 전수 실측** — OK 328(62%) · THIN 138(동어반복) · NONE 67(근거 전무). 결함이 두 파일에 몰려 있다 — `add-domain-service` OK 19% · `add-viewmodel` OK 35%(나머지 52~100%). 처방은 THIN+NONE 205건 중 살아 있는 자산분에 근거를 다는 것이고 회차 단위 작업이다. (출처: 2026-09-04 회차 9) **(실해: 2026-09-04 근거 없는 규칙이 경계 사례에서 오판정을 냈다 — 회차 9 가 규칙 하나를 반대로 적용했다)**
- [2026-09-03] **문서 위생 2건** — ⓐ 복원된 절이 파일 끝에 붙어 `rules/session-context-rationale.md` 의 절 순서가 §1~§11, §17~§22, §31~§35, §12~§16, §23~§30 이 됐다 ⓑ `README.md` 에 *"v1.225.0에서 …제거했습니다"* 형태의 회차 서술 2건이 남았다(사용자 규약은 README 에 버전별 변경 서술을 금지한다). (v1.225.0 완료 리뷰 3R) **(실해: 2026-09-03 README 에 회차 서술 2건 — 규약 위반 · rationale 절 순서가 뒤섞임)**

- [2026-09-03] **`session-context` 가 주입하는 `AGENTS.md` 문면이 Anthropic 안전 분류기를 자극한다** — 2026-09-03 세션에서 Opus 5 safeguard 가 발화해 **Opus 4.8 로 자동 전환**됐다. 한 화면에 실행정책 우회 · 「우회 변수는 둘이며」 · 시크릿 종류 나열 · 창 숨김/백신 격리 서술이 모여 있다. **처방이 규약 문면 개정이라 착수 조건은 사용자 결정이다.** (출처: 2026-09-03 관측) **(실해: 2026-09-03 모델이 강등돼 그 세션의 판정 품질이 내려갔다)**

- [2026-09-03] **위키 `feat-safety-hooks.md` 의 hook 구성 서술이 낡았다** — `bash-hook-lib.ps1`·`orphan-process-cleanup.ps1` 을 현행으로 적는데 v1.225.0 이 둘 다 없앴다. `AGENTS.md` 가 아키텍처 상세의 정본을 위키로 지정하므로 **정본이 낡은 상태다.** v1.226.0 이 낙차를 더 키웠다(스킬 5종 재작성·bootstrap 11파일 폐기). **위키 세션 사안이라 코드 회차가 닫지 못한다.** (출처: v1.224.0 회차 1) **(실해: 2026-09-03 위키가 없는 파일 2개를 현행으로 적고 있다)**
- [2026-09-03] **`docs/harness-conventions.md` Repository Structure 절(260~282행)의 전면 갱신** — v1.226.0 T7-5는 낡은 2줄(`templates/`·`prd.md`)만 고쳤다. 그 절은 hook 진입점 9종·dot-source 헬퍼 7종·스킬 목록을 산문으로 담는데 회차 1~3이 셋 다 바꿨으므로 전수 대조가 필요하다. 기계 검사가 없어(포인터 축은 경로+절만 본다) 어긋나도 조용하다. (v1.226.0 계획 리뷰 MAJOR 3에서 파생) **(실해: 2026-09-03 Repository Structure 절이 hook 9종·헬퍼 7종을 옛 구조로 적음)**
- [2026-09-03] **골든 케이스 2건이 회차 2의 통폐합으로 같은 것을 재게 됐다** — `scenarios/guard-harness.ps1` 의 「설치본 guard-bash Write 차단(v1.99.0 T6 집합 합류)」과 「설치본 guard-bash 헬퍼 Write 차단(v1.99.0 T6 등가 우회 봉쇄)」이 **둘 다 `scripts/guard-bash.ps1` 을 대상으로 한다** — 종전에는 `pre-bash-dispatch.ps1`·`bash-hook-lib.ps1` 두 파일이었는데 v1.225.0이 하나로 합쳤다. 이름만 다르고 판정이 같아 한쪽은 무회귀 케이스다. 지우려면 「어느 쪽이 무엇을 재던 것인가」를 먼저 가릴 것 — 골든 기준선도 함께 갱신된다. (v1.226.0 T7-3) **(실해: 2026-09-03 골든 케이스 2건이 통폐합 후 같은 파일을 대상으로 함)**
- [2026-09-06] **Claude 메모리에 프로젝트 사실 2건이 남아 있다** — 글로벌 「메모리 저장 정책」은 레포 귀속 실행 사실을 `AGENTS.md` 로 라우팅하라고 정하는데 ⓐ `readme-no-changelog.md` 는 `AGENTS.md` 「README.md 갱신 규약」과 내용이 겹쳐 **정본이 둘**이고(삭제 후보 — 삭제 전 그쪽 실재를 grep 으로 확인할 것) ⓑ `plugin-install-github-only.md` 는 릴리즈 절차라 목적지 후보가 `AGENTS.md` 와 위키 `conventions-release.md` **둘**이라 그 두 문서를 대조해야 정해진다. 메모리는 이 PC 로컬이라 다른 PC 에서 유실·접근 불가다. **착수 조건: 그 두 문서를 대조할 수 있는 회차.** **회차 28 이 뺀 이유는 인터뷰가 확정한 둘 밖의 셋째 주제이기 때문이다**(계획 리뷰 MAJOR). **착수하는 회차는 2건이 아니라 3건을 본다** — 회차 28 T5 가 `handoff-paste-ready-prompt.md` 에 하니스 레포의 정본 경로(`references/report-format.md`)를 적어 같은 범주를 1건 더했다(완료 리뷰 MINOR). 그 문장은 「스킬이 메모리를 이긴다」는 스코프 한정이라 목적은 정당하나, 범주로는 레포 귀속 사실이다. (출처: 2026-09-06 회차 28 T5 범위 축소) **(실해: 2026-09-06 메모리 3건이 레포 귀속 사실을 담아 정본이 둘)**
**▶ 현행 잔량(기계 대조 대상)**: 대기 21 / 종결 221 / 정리 삭제 누계 324 / 총 등재 누계 566

> 이 한 줄만 기계 대조 대상이다 — `plugins/pjc/evals/check-harness-consistency.py`가 파싱해 실측 집계와 대조한다. **아래 batch 기록 안의 "정리 직후 N건 / 현행 M건" 표기는 그 시점의 기록이므로 갱신하지 않는다** — 고치면 "정리 직후/현행 분리 표기" 관례가 깨지고 과거 판정의 근거가 사라진다. 대장을 편집한 task는 **이 줄만** 갱신한다.
>
> **카운트 기준 (두 구간의 표기 형식이 다르다 — 단일 정규식으로는 재현되지 않는다)**:
> - **대기** = `## 대기` 구간에서 **`^- \[\d{4}-\d{2}-\d{2}`** — 날짜 **접두**만 본다. 대부분은 `[등록일]`로 닫히지만 **`[등록일, **vN에서 부분 해소**]`처럼 부기가 붙는 형식이 실재**하므로(2026-08-05 실측 2건) `\]`로 닫는 패턴을 쓰면 그런 항목이 조용히 누락된다. 대장 운영 항목 자신도 포함된다.
> - **종결** = **`deferred-closed.md`의** `## 종결` 구간에서 **`^- \[\d{4}-\d{2}-\d{2} → \d{4}-\d{2}-\d{2}\]`** — `[등록일 → 종결일]` **날짜 범위** 형식. 대기 패턴을 여기 쓰면 0건이 나온다. **v1.198.0부터 파일이 다르다** — 대조기는 두 파일을 각각 읽어 합산하며, 종결 파일을 빠뜨리면 `done`이 0이 되어 불변식이 즉시 어긋난다.
> - **파일별로 위 두 패턴을 각각 적용하는 것이 정본**이다. 한쪽 패턴을 두 파일에 함께 쓰면 어느 쪽이든 값이 어긋난다.
> - **앵커(위 `▶ 현행 잔량` 줄)는 이 파일에만 둔다** — 계수의 정본이 한 곳이어야 세 파일이 갈리지 않는다. `deferred-closed.md`·`deferred-history.md`에는 앵커를 만들지 않는다.
> - **누계 2종은 실측 대조 대상이 아니라 기록값이다** — 삭제된 항목은 파일에 없어 셀 수 없으므로 `정리 삭제 누계`·`총 등재 누계`는 사람이 적는 값이다. 대조기는 이 둘을 실측과 맞춰보지 않고, 아래 **불변식**으로만 검사한다.
> - **불변식(대조기의 실검사)**: **`대기 + 종결 + 정리 삭제 누계 == 총 등재 누계`**. 종전의 「무손실 대조」(batch 전후 총수를 사람이 대조)를 대체한다 — 3분기 도입으로 삭제가 정상 경로가 되면서, 파일에 남지 않는 삭제분을 누계가 대신 센다.
> - **연산 규칙 (여기가 정본 — F-6.5는 이 표를 복제하지 않고 포인터만 둔다)**:
>
>   | 연산 | 갱신 |
>   |---|---|
>   | 등재 | `대기 +1` · `총 등재 누계 +1` |
>   | **대기에서 삭제**(반영·병합 종결 — **새 규약의 주 경로**) | `대기 -1` · `정리 삭제 누계 +1` |
>   | 종결에서 삭제(소급 정리) | `종결 -1` · `정리 삭제 누계 +1` |
>   | 대기→종결 이동(**기각·반영·병합·해소** — 사유를 항목에 적는다. 대상 소멸은 독립 범주가 아니라 `기각(대상 소멸 — …)` 로 적는다. **인정 표현의 정본은 `check-harness-consistency.py` 의 `CLOSE_REASON_RX` 이며 여기 열거하지 않는다** — 두 곳에 적으면 갈린다) | `대기 -1` · `종결 +1` |
>   | 종결→대기 복원(오종결 교정) | `종결 -1` · `대기 +1` |
>
>   대기 항목의 **병합 흡수**는 흡수된 항목이 `## 대기`에서 사라지는 것이라 「대기에서 삭제」에 포함된다(별도 연산 아님). 요지 수정·재확인 표기 추가·그룹 재배치는 개수를 바꾸지 않아 불변식과 무관하다.
> - **⚠ 앵커는 절대값 기록이지 증분 로그가 아니다** — 같은 갱신을 두 번 적용해 값이 중복 가산되지 않게 한다.
> - **⚠ 불변식이 깨졌을 때의 복구 규칙**: **대기·종결은 실측이 정본**, **정리 삭제 누계는 실제 삭제를 수행한 만큼만 가산**, **총 등재 누계는 등재할 때만 +1**이다. 식 하나에 사람이 적는 값이 둘(누계 2종)이라 **그 차만 구속**되므로, exit 1이 났을 때 누계를 서로 맞춰 green을 만들면 삭제 수가 영구히 과소 기록된다. **원인 연산을 찾아 그 연산 규칙대로 고친다.**
> - **총 등재 누계는 2026-08-12에 209로 세운 실측 리베이스값**이며(이후 등재분만큼 즍가한다) 역사적 총 등재 수가 아니다 — 이 필드 도입 이전에 이미 삭제된 항목들이 있어(5차 batch 등) 진짜 누적은 그보다 크다. 이 값은 도입 시점의 `대기 + 종결 + 0`으로 세운 기준선이다.
> - **형식 이상 1건은 2026-08-05 3차 batch에서 해소됐다** — `## 대기`에 종결 형식(`[날짜 → 날짜]`)으로 적혀 있던 「게이트 오점화」 항목(본문이 *"해소(v1.154.0 T6/D8)"*)을 `## 종결`로 옮겼다. **대기 패턴이 날짜 접두만 보므로 그때까지 대기로 계수되고 있었다** — 같은 형태가 다시 생기면 여기 적어 다음 batch가 집어가게 한다.

### 대장 운영

- [2026-08-13] **bitleader-dashboard 위키를 레포까지 ingest** — 7커밋 미반영이고 그중 `04c0457`(help.md 우선 표시)이 허브의 「PJC Homepage 고유 추가 … help.md 문서 우선순위」 서술을 거짓으로 만들었다. 배너는 부착해 오독은 막았으나 내용 반영은 절차 B가 필요하다. (출처: v1.170.0 T7·F-7) **(2026-08-24 재확인: 유지 — 절차 B로 지금 착수할 수 있다)** **(실해: 2026-08-13 7커밋 미반영으로 허브의 help.md 우선순위 서술이 거짓이 됨)**
- [2026-08-12] **agent·skill·hook 신설 시 `validate.ps1` 화이트리스트 동반 갱신을 규약에 명문화** — v1.169.0 T1이 `root-cause-analyzer`를 신설하고도 `validate.ps1:133`의 `$agents` 하드코딩 배열에 등재하지 않아, 재설치 후 통합 검증이 `[WARN] validate 미등록 agent`를 적재하는 상태로 Phase F-7까지 갔다(F-7 BLOCKER로 검출 → T5에서 해소). **계획 단계에서 잡히지 않은 이유는 `docs/harness-conventions.md`의 동반 변경 축 ③(검증 자산)에 이 행이 없기 때문**이다 — 축 ③이 잡은 것은 「문서 로드 예산 기준선」 표뿐이었고, `validate.ps1`의 `$agents`/`$skills`도 **집합을 고정하는 검증 자산**인데 목록에 없었다. 그 배열은 이 repo가 정확히 이 누락을 잡으려고 심어둔 감지기라(`validate.ps1:120` 주석 *"skill 추가 후 validate 갱신 누락 포착"*), 감지기는 작동했으나 **계획이 그것을 예측하지 못한 것**이 문제다. 축 ③에 1구(`agents/`·`skills/`·`hooks/` 신설 시 `validate.ps1` 화이트리스트 동반 갱신)를 추가한다. (출처: v1.169.0 F-7 B1) (2026-08-18 재확인: 유지 — v1.180.0 회차(위키 인덱스 파생화·스킬 파일 분할·git 자동 반영)가 이 영역을 건드리지 않았다.) **(2026-08-24 재확인: 유지 — 동반 변경 축 ③에 행을 더하는 조건 없는 액션 — 이번 회차는 신설이 없어(NFR-3) 발현하지 않았다)** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)** **(실해: 2026-08-12 validate 미등록 agent WARN 이 F-7 까지 적재된 채 감)**
### hook · 안전망 (차단·경고·골든 러너)




### 하니스 스킬 (plan · implement · 리뷰어 · 템플릿)
- [2026-08-19] **리뷰 subagent가 골든 러너를 각자 띄워 메인 실행과 경합한다** — v1.182.0 T2에서 spec·quality 리뷰어가 각각 `run-hook-evals.ps1`을 돌렸고 메인 실행까지 셋이 같은 상태 디렉터리를 두고 겹쳐 **세 실행이 모두 정체**했다(1시간 소진 후 프로세스를 정리하고 단독 재실행해 해소). 리뷰어 정의에도 `pjc:implement`의 V-5/V-6 호출 규약에도 「무거운 러너를 직접 실행하지 말 것」이 없다 — 이번 회차는 T3부터 호출 프롬프트에 그 문장을 손으로 붙여 막았다. 처방 후보는 ⓐ V-5/V-6 호출 프롬프트 규약에 한 줄 추가(Bash 쓰기 금지·판정문 요구와 같은 자리) ⓑ 러너에 실행 중 lock을 두어 두 번째 실행이 즉시 「이미 실행 중」으로 끝나게 하기. **ⓑ가 근본이지만 ⓐ가 즉시 효과가 있고 둘은 배타적이지 않다.** (출처: v1.182.0 T2 관측) **(2026-08-27 재확인: 유지 — 처방 ⓐ는 이미 반영돼 있다.** `pjc:implement/SKILL.md`의 V-5·V-6 두 자리가 *"메인이 돌리는 러너·무거운 검증 명령을 직접 실행하지 말 것"*을 호출 시점 주입 네 축의 하나로 규정하고(`grep -c "러너 직접 실행 금지"` = 2), 위키 `feat-review-subagents.md`도 그 네 축을 서술한다. **삭제하지 않는 이유는 ⓑ(러너 실행 lock)가 미착수**여서 항목 전체가 해소되지 않았기 때문이며, 이 상태는 대장 `[2026-08-21]` 「부분 반영 항목을 대장에서 어떻게 처리할지 규정이 없다」가 가리키는 바로 그 자리다. **남은 착수 단위는 ⓑ 하나로 좁혀졌다** — 다음 회차는 ⓐ를 다시 검토하지 않는다.**)** **(2026-08-28 재확인: 유지 — 남은 착수 단위는 ⓑ(러너 실행 lock) 하나이며 조건 없이 착수 가능하다. **이번 회차가 ⓐ의 실효를 재확인했다** — 리뷰어 5회 호출 전건에 러너 금지 1구를 주입했고 경합이 재발하지 않았다)** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)** **(실해: 2026-08-19 세 실행이 같은 상태 디렉터리로 정체해 1시간 소진)**

- [2026-08-18] **gitignore된 `plan.md`의 기록은 리뷰어가 diff로 검증할 수 없다** — acceptance가 *"…를 Progress Log에 기록한다"* 형태이면 그 산출물이 커밋 diff에 나타나지 않아 리뷰어의 **diff 가드**(빈 diff면 즉시 incomplete)가 먼저 발동한다. 메인이 *"파일을 직접 Read하라"*고 안내해야 회수되는데 그 안내가 규약에 없다. v1.179.0에서 **T4 spec 2R이 실제로 이 공백에 걸렸다**. 후보: ⓐ 리뷰어 입력 계약에 *"plan은 gitignore일 수 있으니 diff가 아니라 파일을 읽는다"* 1구 ⓑ plan 기록형 acceptance를 쓸 때 호출 프롬프트에 그 사실을 명시하는 규약. (출처: v1.179.0 T4 2R) **(2026-08-28 재확인: 유지 — **이번 회차에 재발현했다** — T1 spec 1R이 acceptance ③(폴백 실증을 Progress Log에 기록)을 BLOCKER로 냈고, 메인이 기록한 뒤 리뷰어가 diff가 아니라 파일을 직접 읽어 2R에서 닫았다. 처방 ⓐ(리뷰어 입력 계약 1구)·ⓑ(호출 프롬프트 명시) 모두 미착수라 다음 회차도 같은 왕복을 한다)** **(실해: 2026-08-18 gitignore plan 기록을 리뷰어가 diff 로 못 봐 v1.179.0·v1.211.0 이 왕복)**
### llm-wiki · vault

- [2026-08-28] **위키 `feat-safety-hooks-advisory.md` 의 「압축 직후 재읽기 대상 경로 주입」 항목이 실제와 어긋난다** — v1.212.0 이 그 동작을 **경로 지시 → 원문 발췌 주입**으로 바꿨다(`session-context.ps1` 의 `Get-SkillSection`). 그 페이지가 hook 판정 상세의 정본이라 어긋난 채 남는다. **vault 파일이라 위키 세션 사안이다.** (출처: v1.212.0) **(실해: 2026-08-28 위키 서술이 실제 동작과 반대다)**


- [2026-07-09] **나머지 5개 프로젝트 메모리의 위키 이전** — AI-Agents · neighborhood-walk-rpg · Obsidian-Vault-WIKI · bitleader-dev-HomePage · Web-HomePage. 각 프로젝트 **절차 B 세션**에서 한다(2026-08-23 실측으로 ProjectDashboard·DevDashboard-WinUI 2건 완료 확인돼 7 → 5 로 줄었다). **위키 세션 사안이라 코드 회차가 닫지 못한다.** (출처: v1.102.0) **(실해: 2026-07-09 프로젝트 사실이 위키가 아닌 로컬 메모리에만 있어 다른 PC 세션이 못 읽는다)**
- [2026-09-02] **위키 `conventions-verification-2.md` 의 릴리즈 마커 7건** — 본문에 v접두 3필드 semver 가 7곳 있어 **lint §7-28**(changelog 미러링 금지 — 이력 정본은 레포 git·릴리즈)에 걸린다. v1.223.0 T3 이 같은 파일을 구역화했으나 처방이 문면 재서술이라 그 회차 원칙(「이동만 하고 개정하지 않는다」)과 성격이 달라 사용자가 범위에서 뺐다. **다음 위키 세션이 현재형으로 고쳐 쓴다.** (출처: v1.223.0 Q1) **(실해: 2026-09-02 lint 실측 `릴리즈 마커 7건`)**

### eval · 측정

- [2026-08-06] **`plan-pos-9`: 값 치환 + 동작 변경이 섞인 요청을 `pjc:plan`가 잡지 못한다** — v1.161.0 T4가 축을 복원했는데도(포맷 문자열 치환 → **조건 분기 신설**: *"로그 레벨 DEBUG로 낮추고, 합계가 음수면 0으로 처리하게"*) 모델이 `tool_calls=[Read,Glob,Read,**Edit**]`로 **직접 수정**을 택했다. 즉 순수 값 치환이 아닌데도 Trivial Bypass 쪽으로 판정한다. description의 Do NOT 절(*"multi-element pure value substitution … Claude edits directly"*)과 *"Ambiguous scope — ask A/B"* 사이의 경계가 모델에게 충분히 갈리지 않는 것으로 보인다. **주의**: 이 예외를 좁히면 `plan-neg-2`(단건 값 치환)·`plan-neg-7`(다건)이 깨져 오발동률 0.000이 무너진다 — 문구를 넓히는 방향이 아니라 **경계를 더 선명히 하는 방향**이어야 한다. (출처: v1.161.0 T4·T5) **(2026-08-19 재확인: 유지 — 경계 선명화는 조건 없는 액션이고 회귀 가드(neg 2건)가 명시돼 있다)** **(2026-09-03 재확인: 유지 — 13차 판정 — 경계 선명화가 조건 없는 액션이고 회귀 가드(neg 2건)도 그대로다)** **(2026-09-03 재판정: 유지 — 회차 1·2가 지운 자산을 가리키던 이름을 현행으로 옮겼고, 항목의 본체는 새 구조에서도 성립한다.)** **(실해: 2026-08-06 plan-pos-9 에서 모델이 Edit 로 직접 수정 — 축이 안 잡힘)**

- [2026-08-06] **오발동 게이트 ②-b가 `timeout` 케이스를 통째로 제외한다 — 진짜 오발동이 숨을 수 있다** — ②-b는 `triggered` 집합을 보는 축인데 제외 기준을 ②-a(`status` 기반 판정 집합)에서 그대로 상속했다. **`timeout`에도 `triggered`는 채워지므로**(`trigger_eval.py:399-402`) *"발동했는데 180초를 넘겨 timeout"* 인 케이스는 대조에서 빠진다. v1.161.0 T5에서 `impl-neg-1`이 정확히 그 형태였고(그 건은 개정 이전 run에도 같은 발동이 있어 귀속 근거가 없었지만) **다음 회차엔 진짜 오발동이 이 뒤에 숨을 수 있다**. **해소안**: D6 ⓑ를 *"`triggered`가 비어 있지 않은 timeout은 대조에 포함한다"* 로 개정. (출처: v1.161.0 F-7 m5) **(2026-08-19 재확인: 유지 — 해소안이 1구 개정으로 특정됨(D6 ⓑ))** **(2026-09-03 재확인: 유지 — 13차 판정 — 해소안이 1구 개정(D6 ⓑ)으로 특정됨)** **(실해: 2026-08-06 triggered 가 찬 timeout 이 오발동 대조에서 빠짐)**

- [2026-09-08] **`docs/.agents-presplit/` 의 과거 사본 2파일이 git 에 추적 중이라 `.gitignore` 추가가 소급되지 않는다** — 회차 45 T9-8 이 이관 사본을 gitignore 에 넣었으나 gitignore 는 **이미 추적 중인 파일에 효력이 없다**. 그 회차가 노린 것(**새** 사본이 커밋에 딸려 가는 것)은 성립하고, 남은 것은 `2026-08-22/` 자 잔재 2파일(`AGENTS.md`·`harness-conventions.md`)뿐이다. **처방**: `git rm --cached -r docs/.agents-presplit` — **추적 해제는 삭제 성격이라 별도 승인 대상**이라 그 회차 범위 밖으로 남겼다. 파일 자체는 지우지 않는다(원복 수단). **착수 조건 없음.** (출처: 회차 45 완료 리뷰 MINOR 4) **(실해: 2026-09-08 회차 45 의 잔존 수치 검색이 그 잔재의 낡은 케이스 수 표기에 걸렸다 — 살아 있는 자산이 아닌데 검색 결과에 섞인다)**

---

> **분리된 두 파일** — 이 대장은 `## 대기`만 담는다(v1.198.0 분할).
> - **종결 항목**: `docs/plans/deferred-closed.md` — 기각 종결분. 계수는 위 앵커가 정본이다.
> - **소진 batch 회고**: `docs/plans/deferred-history.md` — 회차 서사. `batch 차수 수열` 축의 대조 대상이다.
