# Deferred 대장

> 미처리 Deferred의 단일 추적 대장 (형식 정본: implement-task `references/phase-f-detail.md` F-6.5).
> 항목은 `- [등록일] {요지 1줄} (출처)`, 종결 시 삭제가 아니라 `## 종결`로 이동한다.
> plan-feature Step 1이 계획 때 `## 대기`를 조회하고, implement-task F-6.5가 완료 때 신규분을 append한다.

## 대기

- [2026-07-30] **`bash-hook-lib.ps1`의 `diff HEAD` 경로에 같은 클래스의 결함이 있을 수 있다 (실증 필요)** — 커밋 시점 검사(`warn-commit-secrets`, **조건부 차단** hook)도 `git diff HEAD --unified=0`을 개별 exit code 검사 없이 쓴다(`:215`·`232`·`247`·`256`). v1.147.0 T3이 고친 것과 동일 패턴이다. **다만 실질 위험은 낮아 보인다** — 기본 경로는 `git diff --cached`(`:207`)이고 그것은 HEAD가 없어도 빈 트리와 비교해 정상 동작하므로 초기 커밋 전에도 staged 시크릿을 잡는다. `diff HEAD`는 `-a/-am` 자동 스테이징분·`git add` 동반 경로의 **보완 스캔**이라 그 경로만 조용히 빠질 수 있다. **차단 hook이라 미탐 비용이 편집 시점보다 크므로 실증이 필요하다** — 초기 커밋 전 저장소에서 `git commit -am`으로 재현되는지 확인하고, 재현되면 T3과 같은 방식으로 고친다.
  - **⚠ F-7이 실증의 절반을 끝냈고, 결론은 "위험이 낮다"가 아니라 "재현 가능성이 높다"로 상향됐다**: 임시 저장소 실측 — HEAD 없는 repo에서 `git diff --cached`는 **exit 0**(빈 트리 비교로 staged 내용을 출력)이고 `git diff HEAD`는 **exit 128**이다. 그리고 **staged 이후의 미스테이징 추가분은 `--cached` 출력에 없다** → `fresh repo + 기존 staged 파일 + 미스테이징 시크릿 추가 + git commit -am` 경로에서 보완 스캔이 조용히 0줄을 기여한다. 남은 것은 **hook 레벨 재현**뿐이다. (출처: v1.147.0 T3 quality 리뷰의 범위 밖 관찰 → F-7이 git 원시 동작을 실측 확인)
- [2026-07-30] **`run-hook-evals.ps1`이 1516라인 — 분리 "검토" 신호** — v1.147.0 T3의 델타 케이스 3건 추가로 1500을 넘겼다. AGENTS.md는 이것을 **강제 분리선이 아니라 검토 신호**로 규정하며, 이 파일은 단일 책임(hook 골든 러너)이고 hook별 섹션(§1~§13)으로 이미 구조화돼 억지 분리는 지역성을 해친다. 분리한다면 "hook별 파일 + 공용 헬퍼(`Assert-Case`·`Invoke-Hook`·격리 홈)" 단위가 자연스러우나 그때 **공용 헬퍼의 위치·로드 방식**을 정해야 한다. 케이스 추가로 계속 늘어나면 그 시점에 검토.
- [2026-07-30] **축약구 제거(D3)의 재검토 조건 — 결정을 되돌릴 유일한 안전판** — v1.147.0 T2가 판정 게이트 축약구를 7지점에서 제거해 각 지점에서 **"무엇을 확인해야 하는지"가 즉시 보이지 않게** 됐다(K 1을 여는 한 단계가 늘었다). 이 결정은 **직전 세션 quality 리뷰어의 "필요한 반복(문서들이 개별 세션에 로드되므로)" 판정을 뒤집은 것**이며, 사용자 선택으로 진행했다. **재검토 트리거**: 실사용에서 *게이트 내용을 몰라 확인을 건너뛰는* 사례가 관측되면 D3(제거)을 재검토한다 — 그때는 축약구를 되살리는 대신 K 1 포인터 옆에 **한 단어 요약**을 붙이는 중간안도 후보다. (출처: v1.147.0 T2·D3 / F-7 M1 — plan.md는 `.gitignore`라 이 한 줄이 없으면 되돌림 근거가 감사 경로에서 유실된다)
- [2026-07-30] **판정 게이트 포인터 문구의 문체 불통일 (선택)** — v1.147.0 T2가 축약구를 제거한 뒤 남은 포인터 표현이 지점마다 다르다("거친 확인 결과여야 한다" 4곳 · "따르고" 1곳 · "다.**" 1곳 · "거치지 않으면 고를 수 없다" 1곳). **의미론적 drift 위험은 아니다** — 포인터는 K 1의 내용을 담지 않아 K 1이 어떻게 바뀌어도 항상 참이다(plan-reviewer 판정). 순수 문체 다듬기라 우선순위 낮음.
- [2026-07-30, **v1.147.0에서 부분 해소**] **시크릿 탐지 오탐의 재귀 구조** — **추적 파일은 해소됐다**(T3: 편집 시점 검사가 `git diff HEAD`의 추가 라인만 스캔 → 이력에 이미 있는 예시는 재신고 안 함. 탐지 패턴은 무수정이라 미탐 위험 0). **남은 것은 셋이다**: ⓐ `plan.md`·`notes.md` 같은 gitignore 파일과 신규 파일 — `git diff`가 성립하지 않아 전재 폴백이므로 오탐이 그대로다 ⓑ **추적 파일도 커밋 전까지는 반복 경고된다** — 스캔 범위가 `git diff HEAD`(HEAD 대비 **미커밋 추가분 전체**)라 "이번 편집분"만이 아니어서, 시크릿 형태를 새로 적은 파일은 **커밋할 때까지 매 저장마다 같은 경고**가 난다(F-7 m3 지적 — 줄어든 것은 *이력에 이미 있는* 내용의 재신고뿐이다) ⓒ 커밋 경로의 동일 클래스 결함(아래 별도 항목)(오탐 사례를 그 문서에 기록하면 그 기록이 다시 오탐을 만드는 재귀도 포함). 완화 후보는 `Test-WarnOnce`(세션·파일당 디듑) 확대이며, 탐지 규칙에 "설명 문맥 제외"를 넣는 방향은 **기각**했다(미탐 위험 + `decisions.md:32` 차단 완화 기각 선례). 원 서술: 시크릿 규칙·예시를 *서술하는* 문서가 저장할 때마다 자기 자신을 오탐으로 잡는다(v1.146.0에서 `plan-template.md`·`deferred.md`·`run-hook-evals.ps1` 3파일 관측). 더 나쁜 것은 **그 오탐 사례를 문서에 기록하면 그 기록이 다시 오탐을 만든다**는 점이다(plan.md에서 실측). 탐지 규칙에 "설명 문맥(❌/✅ 대비·코드펜스 안 예시) 제외"가 없는 한 반복된다. (출처: v1.146.0 T2·T5 · notes 기존 항목의 확장)
- [2026-07-29] **요구 원본 ↔ FR 매핑이 산출물에 없다** — Opus 5 대응 plan의 `## 요구 이해`가 "P0~P10 개선안 전부 반영"을 요구로 세웠으나 **P 항목 목록과 P→FR 매핑이 커밋된 산출물 어디에도 없다**(plan·docs·notes 전수 grep 0건). PRD 15개 FR로 흡수된 것이 사실이어도 **감사할 수단이 없어**, P 하나가 PRD 작성 때 조용히 빠졌다면 PRD 대조도 요구 이해 대조도 잡지 못한다. 다음 유사 작업에서는 요구 원본 번호 ↔ FR 매핑 표를 PRD 또는 plan에 남길 것. (출처: Opus 5 대응 plan F-7 m1)
- [2026-07-29] **eval 기준선의 2회차(전후 비교) 측정** — T1·T2가 낸 수치는 변경 *전* 기준선이다(트리거 격리 0.6/비격리 0.9, 루브릭 평균 7.79·편차 0.25). 이번 plan의 변경이 실제로 효과가 있었는지는 **같은 러너를 다시 돌려 `compare_evals.py`로 대조**해야 나온다. 격리·비격리는 다른 것을 재므로 같은 모드끼리만 비교할 것. (출처: Opus 5 대응 plan T1·T2)
- [2026-07-29] **`plan-feature` 트리거 갭 후보 — "여러 파일에 걸쳐 …를 바꿔야 해" 형태** — T1 기준선에서 **격리·비격리 양쪽 모두 미발동**한 유일한 케이스라 환경 요인이 아니다. description은 "여러 곳"·"전체"를 트리거로 적고 있으나 실제로는 발동하지 않았다. description 문구를 조정한 뒤 이 케이스로 A/B 재측정하면 효과가 즉시 보인다. (출처: Opus 5 대응 plan T1 기준선)
- [2026-07-29] **`antipatterns.md`의 "Review subagent 생략 금지" 예외 표에 C 분기(거절) 미언급** — 표는 B(호출 불가 환경의 대체)만 예외로 인정한다. C의 "범위를 좁힌 재의뢰"는 생략이 아니라 재시도이고 두 번째 거절 시 B로 수렴하므로 실질 모순은 없으나 문면상 명시적이지 않다. 표가 안전 임계 문구라 별도 검토. (출처: Opus 5 대응 plan T8 quality m1)
- [2026-07-29] **리뷰어 판정 표의 두 MINOR 행 경계** — "결함 여부가 해석에 달림"(MINOR 강등)과 신설 "결함인지 확신이 낮음"(MINOR + `(판정 유보)`)이 서술만으로는 경계가 모호해 태깅이 흔들릴 수 있다. **두 행의 종착지가 모두 MINOR라 오분류 비용은 없다.** `(판정 유보)` 운용 실적이 쌓인 뒤 두 행을 합칠지 판단. (출처: Opus 5 대응 plan T7 quality m1)
- [2026-07-29] **`plan-feature` 위임 상한 ②의 경계값 정밀도** — "파일 한둘"(직접)과 "두어 곳으로 좁혀진 뒤"(직접)의 숫자 경계가 겹쳐 몇 개부터 위임 가능한지 애매하다. 판정 축(대상이 특정됐는가)은 이미 관측 가능해 실행에 지장은 없다. 실사용에서 애매함이 관측되면 "파일 3개 이상 또는 대상 디렉터리 미특정" 같은 수치로 다듬는다. (출처: Opus 5 대응 plan T6 quality m1·m2)
- [2026-07-29] **`rubric_eval.py` `extract_json`의 관대한 파싱** — 첫 `{`~마지막 `}` 절단이라, judge가 JSON 앞뒤 설명에 별도 `{...}` 예시를 넣으면 잘못된 구간을 파싱할 수 있다. 프롬프트가 "JSON 한 덩어리만"을 지시하고 실패 시 재시도·`error`로 이어져 현재는 실해가 없다. 오파싱이 실제로 관측되면 좁힌다. (출처: Opus 5 대응 plan T2 quality m1)
- [2026-07-29] **[SUGGEST] `compare_evals.py` `main()`의 회귀/개선 출력부 삼항식 단순화** — `for line in regressions or [...]` 루프가 매 반복 같은 조건을 재평가한다. 접두사를 루프 밖에서 정하면 결과는 같고 더 단순하다. (출처: Opus 5 대응 plan T3 quality S1)
- [2026-07-29] **SKILL.md 선두 구간 재구성 (구 PRD FR-5) — 별도 plan으로 재수립** — auto-compact 후 스킬은 **앞 5,000토큰만 재부착**되므로(공식 skills 문서) 34k 토큰급 SKILL.md의 규칙·Halt·위치판정이 통째로 유실된다. Opus 5 대응 plan에서 다뤘으나 **plan-reviewer 4회차까지 BLOCKER 미해소**로 분리했다(나머지 요구는 2회차 이후 지적 0건). 압축 대응 자체는 그 plan의 hook 주입(FR-4)이 담당한다. **후속 plan이 재조사 없이 쓸 자산**:
  - **선두 9,000B 경계 실측**: `implement-task/SKILL.md` **75행** / `plan-feature/SKILL.md` **84행** / `llm-wiki/SKILL.md` **79행**— **경계 행은 실측 불변**. ⚠️ **전체 행수는 이 불릿이 아니라 아래 「가용 예산」이 정본**이다(분리 시점 기록값 512·521·200행은 낡았고 실측은 **538·521·199행** — 같은 수치를 두 불릿에 둔 것이 이번 드리프트의 원인이라 한쪽만 정본으로 남긴다). 환산: 74,663B = 34,445토큰 → 1토큰 ≈ 2.17B → 5,000토큰 ≈ 10,850B(게이트는 여유 두어 9,000B).
  - **가용 예산 ⚠️ (Opus 5 대응 plan 완료 후 실측 갱신 — 착수 시 재측정 필수)**: `## 절대 규칙` 헤딩까지의 선두가 **3,131B**(9,000B 게이트 기준 아래 가용 **5,869B**). 전체 **538행**.
    - **분리 시점 기록값은 1,945B / 가용 7,055B였다** — 그 뒤 **같은 plan의 T9가 `## 절대 규칙` 바로 앞에** 질문 도구 제거 블록쿼트를 삽입해 선두가 **+1,214B** 늘었다(가용 -17%). 즉 "T5·T6·T9 편집은 L91 이후라 선두 측정에 영향 없다"고 적었던 종전 판단은 **틀렸다**(T9만 선두 안쪽을 건드렸다).
    - **재측정 대상은 포인터 건수만이 아니다** — **선두 바이트·가용 예산·전체 행수**도 함께 재측정할 것. 이 값들로 설계를 시작하면 17% 좁은 예산을 원래 값으로 착각한다.
  - **절대 규칙 항목 수**: `^[0-9]+(-[0-9]+)?\. ` 기준 **17항목**(하위 `5-1`·`6-1`·`7-1`·`7-2`·`7-3` 포함 — "1~12"로 세면 5개가 조용히 빠진다).
  - **`llm-wiki` 사전 준수 사항**: 16항목이 **전부 `- ` 불릿**이라 번호 기반 기계 검사가 그 스킬에서 무효(`0 == 0` 통과).
  - **`규칙 [0-9]` 포인터**: **42건 / 15파일**(2026-07-29 측정). `record-project-fact`(5건)·`add-viewmodel`은 **그 스킬 자체의 규칙**이라 귀속 분류 없이 전수 대조하면 게이트가 항상 실패. ⚠️ **건수는 착수 시 재측정할 것** — Opus 5 대응 plan의 T5·T6·T9가 `implement-task/SKILL.md`를 편집하므로 값이 달라진다(귀속 분류 **방식**은 그대로 재사용). **실측 확인된 불변 자산**: 선두 경계 행(75/84/79행)·절대 규칙 17항목·`llm-wiki` 불릿 16항목. **바뀐 자산은 위 「가용 예산」 항목 참조** — T9가 `## 절대 규칙` 앞을 건드려 선두 바이트·가용 예산·전체 행수가 달라졌다("L91 이후라 불변"이라던 종전 판단은 틀렸다).
  - **밀어내면 안 되는 기존 선두 내용**: `plan-feature` `## 절대 규칙`(L64) · `llm-wiki` `## 0. 시작 절차`(L47)·`## 사전 준수 사항`(L73) · `implement-task` `🚦 Phase 0`(L102, 현재 이미 선두 밖).
  - **앵커로 쓰면 안 되는 문자열**(무변경 상태에서 이미 통과): `절차 목차`(L20)·`Trivial Bypass`(L11)·`K. 작업 참조`(L35).
  - **검증된 설계 방향**: 3단(원문 축자 이관 → 판정선 압축본 신설 → 항목 1:1 대조표) + 게이트 4축(앵커 존재·바이트 상한·대조표 행 수 == 항목 매치 수·포인터 도달성). **단 ③축은 스킬별 정규식이 필요하고 ④축은 귀속 분류가 필요하다.**
  - **리뷰어가 제시한 두 봉쇄책 — 후속 plan의 설계 전제로 삼을 것**: ① **게이트 축마다 "무변경 상태에서 반드시 실패하는가" 셀프테스트**(AGENTS.md가 hook 차단 확대에 요구하는 red-green 실증과 동종) ② **plan 내 수치·목록의 단일 출처화**(사본 드리프트가 4회차 결함의 절반을 만들었다).
  (출처: Opus 5 대응 plan 분리 — plan-reviewer 1~4회차)

- [2026-07-28] **인벤토리 전수 추출의 토큰·시간 비용 관측** — 원본 화면 항목이 30~50개면 인벤토리 표가 그만큼 길어져 plan 작성·V-9 대조·reviewer 입력이 모두 커진다. 사용자가 지적한 "시간이 오래 걸린다"와 상충할 여지가 있으므로 실제 이식 작업 1~2회 후 상한(화면당 행 수·그룹 축약)을 둘지 판단. **이번엔 상한을 두지 않았다** — 누락 0이 요구의 핵심이고 상한을 먼저 두면 개선이 무력해진다. **관측 대상에 `spec-compliance-reviewer` I-2의 20행 읽기 예산도 포함** — V-9 본 대조는 전수(상한 없음)라 D4를 뒤집지 않으나, 21행 이상 화면에서는 2차 검증 커버리지가 상위 20행으로 제한된다. (출처: v1.144.0 parity 인벤토리 plan Deferred + F-7 m3)
- [2026-07-28] **`⏳` 금지 축의 플랫폼 확장 검토** — v1.144.0의 정적 판정 축 규정은 XAML(WinUI 3/WPF/MAUI)처럼 **선언적 마크업으로 컨트롤 타입·문구가 소스에 드러나는** 스택을 전제한다. 코드로 UI를 조립하는 스택(C# Markup·Compose·SwiftUI 등)에서도 같은 강도로 판정 가능한지 실사용 후 확인. (출처: v1.144.0 plan Deferred)
- [2026-07-28] **[SUGGEST] `plan-completion-reviewer`(F-7)에 인벤토리 축이 없다** — plan 전체 관점의 인벤토리 미달 검출이 **F-8 단일 방어**에 걸려 있다. v1.144.0 작업에서 F-8 절차 2가 "인벤토리 미달만 남은 상태"를 🎉로 보내는 사슬 끊김이 실제로 있었고 **F-7이 그것을 잡았지만, F-7 자신은 인벤토리 대조 항목 없이 잡았다** — 같은 유형이 재발하면 놓칠 수 있다. 이번 세션이 그 필요성을 실증한 사례이므로 다음 하니스 정비 때 우선 검토. (출처: v1.144.0 F-7 SUGGEST)
- [2026-07-28] **[SUGGEST] 지목한 `파일:라인`의 실재 확인** — V-9가 인벤토리 표의 `원본 근거`·`구현 근거`에 **존재하지 않는 파일:라인을 적는 환각**은 v1.144.0 변경과 무관한 일반 위험이나, 지목 의무를 강화할수록 "형식만 채우는" 유인이 생긴다. V-8 자기점검에 "지목한 파일:라인이 실제로 존재하는지 재확인" 항목을 넣을지 검토. (출처: v1.144.0 T2 V-6 참고 관찰)
- [2026-07-28] **`plan-feature/SKILL.md` 분량이 AUTHORING.md 권장 상한 초과** — v1.144.0 T1 반영으로 460→516줄이 되어 "SKILL.md 본문 500줄 미만 권장" 초과(`implement-task`는 대형 스킬 예외 목록에 있으나 `plan-feature`는 없음). T1 Design이 "표 하나 규격에 파일을 신설하면 포인터만 늘고 Step 2.5를 읽는 흐름이 끊긴다"며 분리를 명시적으로 거부해 인라인 유지했다. 다음 정비 때 **Step 2.5 전체를 묶음 단위로** 뽑을지 재검토 — 부분 분리는 포인터만 늘어 이득이 없다. (출처: v1.144.0 T1 V-6 MAJOR)
- [2026-07-28] **caller 갱신 기록에 부수 변경까지 적기** — v1.144.0 T1의 V-7 caller 갱신 주석이 "문단명 참조 갱신"만 적었으나 실제로는 해소책 예시 1건도 함께 확장됐다. 판정 로직 영향은 없으나 다음 유사 작업에서는 부수 변경도 함께 적어 추적성 유지. (출처: v1.144.0 T1 V-5 재리뷰 MINOR)
- [2026-07-28] **llm-wiki vault의 OKF v0.2 미러 갱신 (별도 `pjc:llm-wiki` 세션 — 절차 F/B)** — ① vault 루트 `index.md` frontmatter의 `okf_version: "0.1"` → `"0.2"` ② 위키 페이지 `20_projects/personal/claude-harness-pjc/feat-llm-wiki.md:63`의 OKF 정합 미러 서술을 v0.2 기준으로 갱신. 코드 세션에서 위키 쓰기 금지 규약(SKILL 절차 K — 예외는 `pending.md` 1줄 append뿐)이라 하니스 세션 범위 밖. [2026-07-25] "병렬 = 결과를 안 기다림 오독 문장 ①"과 같은 위키 세션에서 묶어 처리 가능. (출처: OKF v0.2 갱신 plan Deferred)
- [2026-07-28] **wiki-schema §12 미적합 잔여 ②③의 적합성 판정 문장 부재** — ①(wikilink)에만 "적합성 3요건 위반은 아님"이 붙어 있고 ②(index.md 표)·③(log.md 평면 목록)은 판정 문장이 없다. okf-spec §11 Conformance 요건 ③(예약 파일이 §8·§9 구조 준수)에 정면으로 닿아, §12만 읽고는 이 vault가 conformant인지 결정할 수 없다. v0.1 시절부터 이어진 공백이라 회귀는 아님 — 다음 §12 손질 때 1줄 판정 추가. (출처: OKF v0.2 갱신 F-7 m4)
- [2026-07-28] **`confidence` 행 해석의 뉘앙스 보강** — §12 매핑 표의 "OKF가 배제하는 것은 소스별 credibility 점수"는 okf-spec §5.1 문면상 정확하고 결론(§4.1·§11의 확장 키 보장)도 뒷받침되나, §5.1이 든 배제 사유("subjective, unportable, goes stale")는 페이지 단위 점수에도 성립할 여지가 있다. 그 뉘앙스를 한 구절로 덧붙일지 검토. (출처: OKF v0.2 갱신 F-7 Goal Assessment)
- [2026-07-28] **`wiki-schema.md` frontmatter(`version`·`updated`) 관리 부재** — 최근 6개 수정 커밋에서 `version: "2.40"`·`updated: 2026-07-12` 어느 쪽도 갱신되지 않았다(실측). 규칙 진실원천의 버전 표기가 사실상 방치 상태인데, 단독 근거 없이 관례를 바꾸는 셈이라 그 작업에서는 손대지 않았다. 갱신 트리거를 정할지 별도 판단 필요. (출처: OKF v0.2 갱신 plan Deferred)
- [2026-07-25] **`B-1` 라벨 이름 충돌** — `implement-task/references/recovery.md`의 B-1/B-2(Reviewer 실패 대응 B 분기의 하위 유형)와 `llm-wiki` 절차 B의 B-1(변경분 파악)·B-1a가 같은 라벨을 쓴다. 문서·스코프가 달라 실무 혼동은 낮으나 **하니스 전체에 `B-1` 일괄 치환·검색을 걸면 위키 절차가 오염**된다(`confidence` 이중 개념과 같은 계열). 필요해지면 recovery 쪽을 `B-정책`/`B-물리` 등으로 개명 검토. (출처: B 분기 분리 작업)
- [2026-07-25] **"병렬 = 결과를 안 기다림" 오독 문장 2곳 정비** — ① 위키 `feat-review-subagents.md` L35 "V-5와 병렬 실행이라 spec 판정을 기다리지 않는다"(별도 `pjc:llm-wiki` 절차 F 세션) ② 레포 내 `plugins/pjc/agents/code-quality-reviewer.md:15` "V-6은 V-5와 병렬 실행되어 spec 판정을 기다리지 않습니다". 둘 다 실제로는 "두 리뷰 간 의존 없음"을 뜻하나 백그라운드 실행으로 오독될 여지. ②는 callee-side 정의라 호출 지시가 아니고 말미에 자체 완화구가 있어 실해는 낮음. 한 건으로 묶어 처리. (출처: 동기 호출 규약 명문화 — plan Deferred + F-7 m2) **[2026-07-29 부분 반영 — Opus 5 대응 plan T8]** ②(`code-quality-reviewer.md`)는 "V-6은 V-5와 **동시에 호출**되므로 spec 판정 결과를 입력으로 받지 않습니다(결과를 기다리지 않고 진행한다는 뜻이 아닙니다)"로 정정 완료. **①(위키 `feat-review-subagents.md` L35)만 남음** — 코드 세션의 위키 쓰기 금지 규약상 별도 `pjc:llm-wiki` 세션 필요.
- [2026-07-25] **T1 커밋 메시지 계수 오기** — `fbdc0aa` 본문이 "implement-task/SKILL.md 6지점"이라 적었으나 실제 열거는 7개(264·294·308·314·322·473·497). 제목의 "11지점"은 정확. 이미 완료된 커밋이라 rebase 없이는 못 고침 — 이력 정정 가치가 낮아 기록만. (출처: F-7 m5)
- [2026-07-25] **`implement-task/SKILL.md:316` 중첩 볼드** — `**여기서 "병렬"은 **동기 호출**(...) …이다**`가 한 문장에 `**` 4개라 편집 시 짝 착각 위험(CommonMark 파싱 자체는 정상). 단일 볼드 쌍으로 정리 검토. (출처: 동기 호출 규약 명문화 T1 V-6 MINOR m1)
- [2026-07-25] **Opus 5 신규 도구 표면의 hook 커버리지** — `Artifact`(파일 내용을 claude.ai에 게시 = 외부 전송)·MCP 도구가 `hooks.json` matcher(`Write|Edit|MultiEdit|NotebookEdit`, `Bash|PowerShell`) 어디에도 안 걸려 시크릿 유출 경로가 무방비. `Artifact`는 tool_input 구조가 달라 기존 hook 재활용이 안 되고 얇은 hook이 하나 더 필요 — 실제로 Artifact를 쓰는 워크플로가 생길 때 착수. (출처: Opus 5 하니스 점검)
- [2026-07-25] **모델·effort 티어 재검토 (Opus 5 신설분)** — Claude 5 세대에 `fable` alias가 추가돼 `spec-prefilter`·`explorer`의 haiku(4.5 세대) 대안이 생겼고(deferred [2026-07-08] explorer 상향 기각의 재검토 조건 변화), effort에 `xhigh`/`max`가 추가돼 [2026-07-15] "품질 게이트 모델 티어 조건부 상향"을 **모델 상향이 아니라 effort 상향**으로 더 싸게 해결할 수 있다. 단 agent frontmatter가 이 값들을 실제로 수용하는지 **미검증** — 바꾸기 전 agent 1종으로 스모크 테스트 선행. (출처: Opus 5 하니스 점검)

- [2026-07-24] **lint.py `mentioned` 정규식 경계 부재** — `index-[a-z]+(?:-\d+)?`가 `index-work-1abc`·`myindex-work-1` 근접 문자열을 `index-work-1`로 매치할 이론적 여지(§7-15 미등록 검사 정밀도). 실사용 wikilink는 정확 파일명이라 실무 영향 낮음. `(?<![\w-])…(?![\w-])` 경계 추가 검토. (출처: sub-index 3단계 자동 분할 V-6 MINOR)
- [2026-07-24] **wiki-schema §4 분할 수행 절차 L342 '1단계 소제목 구역화' 스코프 문구** — 하드 임계 초과 후 실행 스텝 문맥이 근접 단계 1단계 정의(L329)와 시점이 달라 '1단계는 비분할 섹션에만'으로 오독 여지. '이 절차는 초과 후 스텝이라 근접 1단계와 무관' 한 구절 보강 검토. (출처: sub-index 3단계 자동 분할 V-6 MINOR)
- [2026-07-24] **위키 feature 136개 `## 구현 방법` 섹션 축소** — T6이 규칙(코드 답하는 '어떻게'는 빼고 '왜 이 구조인가' 3~5줄)만 바꿔 신규·갱신 페이지부터 적용된다. 기존 136개(구현 방법 섹션 합 236KB)의 소급 정리는 vault 작업이라 하니스 세션 권한 밖 — 후속 `pjc:llm-wiki` 절차 F/B 세션에서 수행. (출처: v1.138.0 T6·Q4)
- [2026-07-24] **references 9개 전수 정밀 조사로 통합 후보 최대 도출** — v1.138.1은 "확실히 안전한 통합"만 하고 전수 조사(Q3-B)는 미채택. implement-task references 9개를 메인이 전부 정독해 "통합 여지 작음"을 추측이 아닌 전수로 확정. 조사 비용이 크고 효과 불확실이라 보류. (출처: v1.138.1 Q3)
- [2026-07-24] **SKILL 방향 재편(규칙 폐기·구조 재편)** — 통합의 실익이 미미함(v1.138.1)이 확인됐으므로, hook 이벤트 로그 근거로 저효용 규칙 폐기(위험 감수) 또는 최빈/희소 경로 분리·스킬 분할을 재고. 사용자 "토큰 과다" 지적의 실질 해결은 이 방향에 남아 있으나 안전·설계 재검토가 선행. (출처: v1.138.1 Q3-C)
- [2026-07-24] **D6 "재서술 금지 ↔ 참조 축약" 경계 명문화** — 부연 삭제로 남는 골격의 조사·어순 재구성이 "재서술 금지" 위반인지 삭제의 부수효과인지 경계가 불명(T1 V-5 MINOR). 다음 유사 감량 task 전 "골격의 문법적 재구성은 허용, 규칙 내용 변경은 불허" 기준을 D6에 명문화 검토. (출처: v1.138.1 T1 V-5)
- [2026-07-24] **Hook 골든 러너 실행 시간(전체 19분/413케이스)** — 각 케이스가 pwsh를 콜드스타트해 케이스 수에 선형 비례. 3줄 변경에도 전체를 돌려야 한다(부분 실행 판정 불가 규정). pwsh 프로세스 재사용·병렬 실행·변경 파일 기반 선택 실행으로 검증 비용을 변경 규모에 비례시킬 것. (출처: v1.138.0 T5)
- [2026-07-24] **severity 상한 규칙 4개 reviewer 파일 리터럴 복제** — 한 곳만 수정되는 드리프트 위험. 각 agent가 자기 파일만 읽어 단일 소스화는 불가하나, "이 규칙은 4개 reviewer에 동일 존재" 상호 참조 각주를 4곳에 추가하면 편집 시 자기 점검 신호가 된다. (출처: v1.138.0 T2 quality SUGGEST)
- [2026-07-24] **rationale 이관 임계치 미설정** — T3에서 소량(0.7%) 이관에도 파일 2개를 신설해 "신설 오버헤드 > 감축 이득"이 될 뻔했다. 다음 유사 task 전에 "항목당 N자 이상만 이관" 같은 임계를 plan Design에 미리 못박을 것. (출처: v1.138.0 T3 quality SUGGEST)
- [2026-07-24] **규칙 유실 측정(집합 grep)의 구조적 한계** — 리터럴 매치라 ⓐ 재서술된 판정 문장("~해야 한다") 증감을 못 잡고 ⓑ "카운트 불변"이 "같은 문장 잔존"을 보장 못 함(삭제+신규 상쇄). 순수 삭제 작업엔 유효하나 재작성 동반 작업엔 이 측정을 신뢰 금지. (출처: v1.138.0 T3 spec)
- [2026-07-24] **`references/*.md`에도 rationale 분리 확장 검토** — T4에서 `phase-f-detail.md`에 "이 점검만 남긴 이유(v1.138.0)" 블록쿼트(버전 태그+과거형 이력 서사)가 생겼다. T3의 rationale 분리가 두 SKILL.md에만 적용돼 references 파일엔 이력이 본문에 쌓인다. 확장 여부 결정 필요. (출처: v1.138.0 T4 quality MINOR)
- [2026-07-22] **pending 형식 위반 검출의 불릿 판별 오탐 여지** — `lint.py`의 위반 검출이 불릿을 `^\s*-\s*`로 보므로, `-`로 시작하되 불릿이 아닌 줄(`-- 구분선 …`·`-5 …`)에 태그 문자열이 **우연히** 포함되면 위반으로 잡힐 이론적 여지가 있다. 실측 오탐 0(현행 vault + eval 34/34)이라 결함으로 단정할 근거는 없어 관측 시 대응. 좁히려면 `^\s*-\s*\[`로. **단 공백 폭을 `\s`로 되돌리면 안 된다** — `-[TAG]` 줄이 정상에도 위반에도 안 잡히는 2차 사각지대가 재발한다(v1.132.0 T1 quality m1이 잡은 바로 그 케이스, 픽스처 `pending-malformed`가 고정). (출처: v1.132.0 T1 quality m1)
- [2026-07-22] **post-write-checks가 "시크릿 금지"를 가르치는 문서 자체를 신고하는 오탐** — `plan-template.md`를 편집하자 `DB/서비스 URI 인증정보` 경고가 발생했으나, 원인은 편집분이 아니라 그 템플릿 30행의 **금지 예시**(`❌` 마커가 붙은 DB 연결 URI 예시 → `✅ .env에 정의`)였다(diff에 URI 0건, grep 실증). **이 항목을 처음 쓸 때 그 예시 문자열을 인용했다가 같은 경고를 재발시켰다** — 오탐 기록조차 오탐을 낳는 자기참조 구조라, 리터럴 대신 서술로 남긴다. 42행의 "`warn-commit-secrets`가 탐지기 자신의 패턴 정의를 신고" 오탐과 **동일 계열**(보안 위생을 다루는 파일이 스캔 대상이 되는 구조적 부작용) — `❌`/`WRONG` 마커가 붙은 예시 줄 배제 등을 이벤트 로그 축적 후 함께 정비. (출처: v1.131.0 T1 편집 중 실관찰)
- [2026-07-15] **Fast-Path 표 B행에 ESCALATE→C격상 각주 추가** — 표는 PASS 경로 기준이라 무변경이 옳으나(v1.123.0 plan 확정), 표만 읽는 독자가 ESCALATE에서도 V-3/V-6 생략으로 오독할 여지. §267 포인터 각주 1구로 가독 개선. 다음 implement-task SKILL 정비 때. (출처: v1.123.0 T2 quality m1)
- [2026-07-15] **직접 수정(Trivial Bypass) 누적 품질 드리프트 점검 장치** — plan 없는 직접 수정(3줄 이내·순수 값 치환)은 리뷰 0회로 커밋되며, 여러 세션 누적돼도 어떤 게이트도 재점검하지 않는다(F-7 7.5는 plan 단위 작업에만 발동). 트리거 설계 필요(누적 횟수 기준? 다음 plan-feature Step 1에서 경량 훑기?). (출처: v1.123.0 plan Deferred)
- [2026-07-15] **품질 게이트 모델 티어 조건부 상향** — 계획 검토(plan-reviewer)·전체 검증(F-7)은 Opus인데 아키텍처 판단이 가장 어려운 Type D 대형 diff의 V-6은 Sonnet/effort medium 고정. Type D 시 effort 상향 등 조건부 에스컬레이션 검토 — v1.122.0 Out of Scope "reviewer 모델 상향(비용 관찰 후 별도 검토)"와 동일 사안. (출처: v1.123.0 plan Deferred)
- [2026-07-15] **Phase F에 전체-diff(BASE..HEAD) 품질 패스 추가** — task별 diff 리뷰(V-6)는 task 간 누적 중복·패턴 불일치·여러 task에 걸쳐 비대해진 파일을 원리적으로 못 본다. 전 task 완료 후 전체 diff에 1회 품질 리뷰를 수행하는 F-단계 신설 검토. v1.122.0 3레버(Design 필드·V-6 기본화·SUGGEST)의 효과 관찰 후. v1.123.0 분석에서 재확인: 4-D 재사용 조사는 계획된 심볼만 대상이고 V-6 항목 J②(중복)는 non-blocking이라, task 간 누적 중복의 실효 검출 수단은 이 패스뿐이다. (출처: v1.122.0 plan Deferred)
- [2026-07-15] **simplify(정리) 패스** — 리뷰 통과 후 diff를 더 단순하게 정리하는 단계. 자율 루프 churn(수정→재리뷰 반복) 위험이 있어 범위·트리거 별도 설계 필요. (출처: v1.122.0 plan Deferred)
- [2026-07-15] **품질 루브릭 eval(Opus judge 채점)** — 고정 task 세트를 돌려 아키텍처 품질을 1-10 루브릭으로 채점, 파이프라인 변경 효과를 수치로 측정(eval 기준선은 Opus 실작업). (출처: v1.122.0 plan Deferred)
- [2026-07-14] **문자열 형태의 상위 탈출(`..`)·`/.*` 미탐** — `Remove-Item -Recurse -Force "$env:APPDATA\..\..\.."`(드라이브 루트로 상승)·`rm -rf "/.*"`이 현행에서 통과한다(실측 PASS). v1.121.0의 `Join-Path` 정규화가 만든 구멍이 **아니다** — 조인 조건 ②③이 이런 자식을 걸러 `Join-Path` 경로에서는 차단 유지되며, 남은 것은 원래부터 뚫려 있던 문자열 형태다. `$dangerTarget`에 `\.\.`·`/\.?\*` 성분을 추가하는 강화는 정상 상대 경로(`../build` 정리) 오탐과 트레이드오프가 있어 별도 검토 필요. (출처: v1.121.0 plan Deferred / plan-reviewer 2회차 M1)
- [2026-07-14] **`$env:` 별칭 시스템 루트의 깊이 cap 미탐** — `$env:SystemRoot`·`$env:SystemDrive`·`$env:ProgramFiles`·`$env:windir`처럼 **환경변수가 시스템 경로를 가리키는 경우**, 그 하위 삭제(`"$env:SystemDrive\Windows"`·`(Join-Path $env:SystemDrive "Windows")`)가 통과한다. `$dangerTarget`이 `$env:NAME`을 리터럴 토큰으로만 보고 **값이 무엇인지 모르기 때문**(깊이 cap의 구조적 한계 — v1.98.0 승인 설계). **v1.121.0이 만든 구멍이 아니다**: 등가 문자열 형태가 수정 전에도 이미 통과했고(실측), Join-Path 형태가 그와 대칭이 된 것뿐이라 실질 공격면 증가는 0이다. 다만 37케이스 매트릭스·골든 어디에도 이 축이 없어 의식적 판단 없이 통과했다. 위 `..` 미탐 항목과 **같은 성격**(정규식이 값이 아니라 표기만 본다)이라 함께 검토. (출처: v1.121.0 F-7 m1 — 리뷰어 독립 프로브)
- [2026-07-14] **`Join-Path` 3인자·중첩 괄호·역순 명명 인자의 오탐 잔존** — `(Join-Path $env:TEMP "claude" "x")`(PS6+ AdditionalChildPath)·`(Join-Path $env:TEMP (Get-Date -F yyyy))`·`(Join-Path -ChildPath "x" -Path $env:TEMP)`는 정규화 정규식에 매치되지 않아 무변경 → 현행 차단이 유지된다(정상 작업이 막히는 오탐이지만 **오차단 방향이라 안전**). 골든에 `[알려진 오탐 — 3인자 미커버]` 라벨로 현행 동작만 고정해 뒀다. 실제 오차단이 관측되면 커버 확대. (출처: v1.121.0 Out of Scope / D5)
- [2026-07-13] **DB 연결 문자열 패턴이 흔한 실제 형태를 미탐** — `secret-patterns.ps1`의 `(Server|Data Source)=[^;]+;\s*(User|Uid|Password|Pwd)=`는 `Server=` **바로 뒤에** 자격증명 키가 와야 매치한다. 실제로 흔한 `Server=x;Database=y;User Id=sa;Password=z;`(중간 키 + `User Id=` 공백 표기)는 **불매치**(저신뢰 `password 값` 경고만). 이 라벨이 고신뢰 **차단** 등급이라 실효성이 걸린 문제. `[^;]*(;[^;]+)*` 류로 중간 키 허용 + `User\s*Id` 표기 수용 검토 — 오탐 회귀(정상 설정이 연결 문자열로 오인)를 골든으로 함께 확인할 것. (출처: v1.119.0 T2 골든 FAIL 조사 / F-7 M1)
- [2026-07-13] **도메인형 비밀번호 미탐** — `Test-CredentialPairToken`의 파일명 배제(마지막 `.` 뒤 2~5 영문자)가 pw 토큰에도 적용돼 `Secret1.io`·`Pw123.dev` 류를 놓친다. **의도적 트레이드오프**(pw에서 배제를 빼면 `계정: admin / config2.yml` 류 파일명 쌍이 오탐되고, 이 라벨은 커밋 차단 기준이라 오탐 1건이 자율 루프를 세운다). 엔트로피·사전 대조 등 더 정밀한 판별 근거가 생기면 재검토. (출처: v1.119.0 T1 quality M1)
- [2026-07-13] **자격증명 쌍의 나머지 미탐 형태** — 마크다운 **표 형태**(`| admin | pw |`)는 라벨 0건(경고조차 없음), **줄 분리형**(`- 아이디: admin` / 다음 줄 `- 비밀번호: pw`)은 `password 값` 경고만. 표·줄분리는 문서에서 흔한 표기라 차단 등급 확장 시 오탐 위험이 크므로, 경고 계층(post-write)에서 먼저 데이터를 모은 뒤 결정할 것. (출처: v1.119.0 F-7 M1 실측)
- [2026-07-13] **비인용 자격증명 쌍(경고 라벨)의 숫자 접미 열거형 오탐** — `Account 상태: active / suspended_30d`·`Login 실패 코드: AUTH_401 / AUTH_403` 같은 **상태·에러코드 열거**가 pw 토큰 요건(6자 + 숫자)을 만족해 `자격증명 쌍(비인용)` 경고를 받는다. 차단이 아니라 경고라 루프는 안 서지만, 이 라벨은 **편집 시점 hook이 모든 파일 쓰기마다 발동**하므로 늑대소년화 대상이다(D9가 경계한 바로 그 실패 모드). pw 토큰 요건을 "숫자만으로는 부족, 특수문자 필요" 쪽으로 좁히거나 `<영문>_<숫자>` 접미 패턴을 배제할지 검토 — 단 실제 비밀번호 중 숫자만 든 것도 흔하므로 미탐 트레이드오프를 함께 볼 것. 부수: `account: \`svc-runner\` / \`deploy-2024\`` 같은 서비스 계정·배포 태그 표기가 **고신뢰(차단)** 판정을 받을 여지도 확인 필요. (출처: v1.119.0 F-7 2회차 m1)
- [2026-07-13] **한 호출 안에서 파일 생성 + 즉시 커밋은 여전히 통과** — `cat > R.md <<EOF … EOF; git add -A && git commit`은 hook 시점에 **파일이 아직 없어** exit 0이다(PreToolUse의 구조적 한계). 다만 **heredoc 본문이 command 문자열에 그대로 있으므로**, 명령 텍스트 자체를 `Get-SecretMatches`에 통과시키면 저비용으로 닫힌다. 오탐(명령에 시크릿 '언급'만 있는 경우) 위험을 골든으로 확인한 뒤 도입 검토. (출처: v1.119.0 F-7 2회차 m2)
- [2026-07-13] **커밋 게이트 스캔 상한** — `Invoke-WarnCommitSecrets`의 선행 `git add` 스캔이 파일 50개·1MB 상한을 둔다(hook 지연 방지). 상한 밖 파일은 미스캔이라 이론상 미탐 가능. 커밋 직전 신규 파일이 50개를 넘는 경우는 드물고 그때도 `--cached`/`diff HEAD` 경로는 작동하나, 상한 도달 시 경고를 남길지 검토. (출처: v1.119.0 T9)
- [2026-07-13] README 버전 이력의 **서술 정책 확정**(시점 스냅샷 vs 현행 반영) — v1.117.0에서 과거 v1.100.0 블록의 문장을 현재 동작으로 소급 수정하고 "(v1.117.0에서 …)" 각주를 달았는데, 이 repo의 다른 이력 블록은 append-only 관례다. 그 블록이 "Subagents(검토 담당)" 섹션 안에 있어 독자가 현재 기능 설명으로 읽는 위치라 이번엔 stale 방치보다 정확성을 택했으나, 정책 자체가 미확정 (출처: v1.117.0 T1 quality 리뷰 m1)
- [2026-07-10] 골든 러너 `-Filter "a,b"` 단일 문자열 바인딩 시 콤마 split 안 됨(배열 전달만 동작) — 기존 동작, 다음 러너 정비 때 (출처: v1.112.0 T1 quality 리뷰 m2)
- [2026-07-13] `pjc-systematic-debugging` 단독 세션이 **plan.md가 없는 레포**에서 조사 로그를 plan.md로 **신규 Write**하면 plan 작성 게이트에 차단된다(통과 흔적 목록에 debug 스킬 없음 — D3). 실해는 작다(SKILL.md:281이 `debug-<날짜>.md` 대안을 이미 제시, 차단 메시지도 그리로 유도). SKILL.md:281에 "plan.md가 없으면 `debug-<날짜>.md`" 1구 명문화 또는 흔적 허용 여부 재검토 (출처: v1.118.0 F-7 m2)
- [2026-07-13] 비표준 체크박스 문자(`- [-]` 취소·`- [~]`) 표기만 쓰는 외부 레포는 `$planTaskRx`(` `/`x`/`X`/`/`만 인정)에 무매치라 `docs/plans/`가 있어도 plan 판정 OFF → 코드 Write 전면 차단(종전엔 통과). 진단 문구로 회복 가능하나 문자 클래스 확장 검토 (출처: v1.118.0 F-7 m3)
- [2026-07-13] `$isInPlansDir` 정규식이 선행 구분자 필수(`[\\/]docs[\\/]plans[\\/]`)라, 상대경로 `docs/plans/x.md` Write가 오면 게이트 미발동인데 `Test-PlansDirHasPlan`은 그 파일을 plan으로 인정 → 게이트/판정 비대칭. Claude Code Write는 절대경로 계약이라 현 재현성 낮음(기존 AGENTS 게이트도 동일 가정) — 차기 hook 정비 때 `(^|[\\/])` 분기 추가 검토 (출처: v1.118.0 F-7 m4)
- [2026-07-08] block-destructive: `cat <<EOF > file`(데이터 싱크 스트립) 후 같은 Bash 호출에서 즉시 실행 시 위험 본문 미스캔 — "파일 작성+동일 호출 실행" 조합 감지 개선 (출처: harness-quality-part1)
- [2026-07-08] suggest-agents-record 명령 오인 오탐 2류 — ① 커밋 -m 값 속 명령 문자열 ② grep 패턴 문자열 속 "dotnet build" — hook 이벤트 로그 데이터 축적 후 근거 기반 수정 (출처: harness-quality-part1·v1.101.0)
- [2026-07-08] .state 디듑 마커(post-write-warn·require-plan-warn·suggest-agents-record) 누적 정리 정책(TTL/청소) 부재 (출처: harness-quality-part1)
- [2026-07-08] (저신뢰 audit) block-destructive 열거 `.` 제외의 이름필터 판정에서 `-Include *.tmp,*` 콤마 배열 catch-all 원소 통과 여지 — 차기 hook 정비 때 감사 케이스로 검토 (출처: harness-quality-part1 F-7)
- [2026-07-09] suggest(제안) 이벤트의 로깅 확장 — v1.105.0 T2는 차단+경고만(사용자 확정 범위) (출처: harness-wiki-part1)
- [2026-07-09] lint.py --fix 대상 확대(§7-19 누락 행 추가 등) — 키워드 요약을 기계 생성할 근거가 생기면 재검토 (출처: harness-wiki-part2)
- [2026-07-09] lint.py `section()` 헬퍼 Match 반환형 확장으로 `section_span()`과 단일화 — 호출부 리팩터링 동반, 다음 lint.py 정비 때 (출처: harness-wiki-part2 T2 리뷰 m2)
- [2026-07-09] 나머지 7개 프로젝트 메모리의 위키 순차 이전(AI-Agents·neighborhood-walk-rpg·Obsidian-Vault-WIKI·bitleader-dev-HomePage·Web-HomePage·ProjectDashboard·DevDashboard-WinUI) — 각 프로젝트 위키 절차 B 세션에서 (출처: v1.102.0)
- [2026-07-09] SessionStart hook의 위키 허브 컨텍스트 자동 주입 — v1.102.0 미채택(지침 방식 선택), 지침 방식 실효성이 낮다고 관찰되면 재검토 (출처: v1.102.0) **→ [2026-07-30] 부분 재수용 (v1.146.0)**: 재검토 조건이 **실제 사고로 충족**됐다 — 설정·실재하는 vault를 "미설정"으로 단정해 절차 K를 건너뛴 세션이 관측됐고, 지침만으로는 확인을 강제하지 못한다는 것이 실증됐다. 다만 이번에 채택한 것은 **vault 설정 상태 1줄 주입만**이며(사용자 확정), **허브 본문·`## 작업 규약·주의사항` 요약 주입은 이 항목에 그대로 잔존**한다(컨텍스트 비용이 크고 절차 K의 역할과 겹침).
- [2026-07-09] 위키 허브 `## 작업 규약·주의사항`이 수십 건으로 커지면 전용 페이지(worknotes류) 승격 검토 (출처: v1.103.0)
- [2026-07-10] warn-external-ops가 `gh release create --notes "…git merge…"` 노트 값 속 텍스트를 오탐 — 커밋 `-m`/`--message` 값은 스트립하나 `--notes` 값은 미스트립이라 노트 본문의 push/merge/tag 텍스트가 규칙에 걸림(비차단), `--notes`(및 유사 값 옵션) 스트립 또는 `git ` 접두 요구로 개선, 다음 hook 오탐 정비 라운드에서 (출처: v1.113.1 릴리즈 발행 시 실관찰)
- [2026-07-19] lint.py:160 주석 표현 "오계상"→"오계산" 통일 검토 — "계상(計上)"도 유효 표현이라 비수정 보류, 다음 lint.py 정비 때 문구 통일 여부만 판단 (출처: v1.125.0 T1 quality m1)
- [2026-07-20] **`lint.py` index.md 읽기 실패가 else 분기로 합류해 "없음"으로 오진** — index.md를 못 읽으면 `pages` 멤버십이 False가 되어 889-893행 else로 빠져 `"index.md 없음: … 카탈로그 골격 생성 필요"` ERR이 난다. **파일은 실재하는데 "없음"이라 하고 잘못된 처방을 지시**한다(v1.129.0 T3 전에는 crash해서 이 분기에 도달조차 못 했으므로 그 수정이 새로 노출한 부작용). 읽기 실패와 부재를 구분해 각각 다른 ERR을 내도록 개선 검토 — T3 Files 범위 밖이라 미수행. `lint-cases.json`의 `bad_index` 케이스 rationale에도 명시돼 있다. (출처: v1.129.0 T3 quality m1)
- [2026-07-20] **`protect-harness` 한글 경로 골든 부재** — v1.129.0 T2의 stdin UTF-8 수정으로 protect-harness도 한글이 든 보호 경로를 비로소 정확히 매치하게 됐다(성격상 이 훅에서도 미탐 보완). 그런데 `hook-cases.json`에 protect-harness 케이스가 0건이고 러너 내장 시나리오도 전부 ASCII 경로라, **AGENTS.md:72가 미탐 보완에 요구하는 실증이 protect-harness에는 없다**. 한글 경로 시나리오 1건 추가 검토. (출처: v1.129.0 F-7 m3)
- [2026-07-20] **`\rm` 델타 음성 케이스의 토큰 일반성 미실증** — 새 앞 경계는 `$delCmdAlt` 7토큰(`rm|Remove-Item|ri|rmdir|rd|del|erase`) 전체에 적용되는데 델타 음성 2건은 둘 다 `\rm` 형태다. `C:\del`·`C:\rd`로 끝나는 경로도 새로 매치된다. 경계 로직이 토큰 무관 알터네이션 공유라 구조 논증으로는 충분하나(F-7 m2 판정), `Get-ChildItem C:\del` 1건으로 토큰 일반성을 못박는 것을 검토. (출처: v1.129.0 F-7 m2)
- [2026-07-20] **`F-1~F-8` 라벨링이 실제 F-8 수행 시점과 표기상 어긋나 보임** — PRD 연결 plan은 Phase G 통과 후 수행인데 진입 조건표만 훑으면 F-7 직후로 오독할 여지. 바로 아래 타이밍 표(`phase-f-detail.md` F-8)가 해소하므로 실행 불가 수준은 아님. 라벨을 `F-1~F-7 + F-8(별도 시점)`으로 바꾸는 안 검토 — 다음 implement-task SKILL 정비 때. (출처: v1.127.0 T1 quality m1)
- [2026-07-20] **`final-report-template.md`의 `⏸️` 분기에 완성 코드 블록 부재** — 산문 설명(①②③ 차이점)만 있고 `🎉`처럼 완성된 템플릿 블록이 없어 세션마다 "안내 1줄"·"확인 요청 목록" 표기가 미세하게 갈릴 수 있음. `⏸️` 전용 완성 블록 추가 검토. (출처: v1.127.0 T1 quality S1 / SUGGEST)
- [2026-07-20] **plan-reviewer 항목 15 제목이 컨벤션과 어긋남** — 항목 1~14는 예외 없이 `(X 후보)` 형식인데 15만 `(조건부 BLOCKER)`. 본문은 동일하게 4갈래 "후보" 구조이므로 `(BLOCKER 후보)`로 통일 검토(본문 판정 로직은 유지). (출처: v1.127.0 T3 quality m1)
- [2026-07-20] **plan-reviewer 항목 14↔15 등급 비대칭의 대조 근거 문장 부재** — 14는 "설계 판단은 취향 경계가 넓다"로 BLOCKER를 금지하는데 15는 허용. 정당화는 타당하나(15의 판정은 ①+②·섹션 유무의 기계적 이진 조건이라 취향 경계 없음) 리뷰어가 스스로 재구성해야 함. 항목 15 도입부에 대조 1줄 추가 검토. (출처: v1.127.0 T3 quality m2)
- [2026-07-20] **`항목 I` 레이블이 두 리뷰어에 중복** — spec-compliance의 `I. 시각 요소 분해`와 code-quality의 기존 `I. UI 문구 사용자 친화성`. LLM 리뷰어는 자기 파일만 읽어 동작 영향 없으나 `implement-task/SKILL.md`의 두 참조가 10줄 간격(304·314행)이라 사람이 오독할 여지. 314행에 `(code-quality-reviewer)` 소속 명시 검토. (출처: v1.127.0 T2 quality m1)
- [2026-07-19] **가이드/레시피 섹션 미등록·리스트 불릿 등록 가이드의 기계 검출(§7-27 확장) 검토** — §7-27은 표 행(`| [[...]]`)만 스캔해 리스트 불릿 등록·섹션 미등록 가이드는 병기 검사 사각(현재는 고아 검사만 커버, v1.125.0 fixture 1차 FAIL로 실증). 규정(§2.6·I-2b "표 행 형식 등록")으로 절차 방어는 완료 — 기계 방어 확장은 다음 lint 정비 때 (출처: v1.125.0 T2)
- [2026-07-23] **AGENTS.md 주입 스냅샷 정합** — 주입분은 세션 시작 시점 스냅샷이라 세션 중 AGENTS.md를 편집하면 컨텍스트의 전문이 구버전으로 남음. 실제 혼동이 관측되면 post-write-checks에 "AGENTS.md 편집됨 — 주입 스냅샷은 구버전" 경고 추가 검토 (출처: v1.135.0 plan Deferred)
- [2026-07-23] **session-context.ps1 안전 읽기 패턴 공통화 검토** (**[2026-07-30] 갱신 — vault config 읽기 추가로 4번째 사용처 발생**, 문턱 초과 폭 확대. v1.146.0에서는 의도적으로 인라인 유지: 4곳의 후처리가 정규식 매치·JSON 파싱·바이트 상한 분기로 서로 달라 공통부가 2줄뿐이며, 공통화는 hook 3곳을 함께 손대는 별건이라 이 항목에 유지) — `Test-Path` + `try Get-Content -Raw` 패턴이 AGENTS.md 주입 추가로 3곳(plan·notes·AGENTS) 도달, 공통화 문턱(3회) 충족 — 다음 session-context 정비 때 `Read-TextFileSafe` 로컬 헬퍼 추출 검토 (출처: v1.135.0 T1 quality SUGGEST)
- [2026-07-23] **warn-commit-secrets 신규 추적 파일 분기가 50파일 스캔 캡 범위 밖** — `git add` 경로 나열 시 추적 파일은 파일당 git 서브프로세스 2회(`ls-files`·`diff HEAD`)를 개수 제한 없이 호출, 대량 나열의 드문 케이스에서 지연 가능(기능은 정확·실질 영향 낮음 — 서브프로세스가 전체 정독보다 가벼움). `$scanned` 카운터 공유 또는 별도 상한 검토 (출처: v1.136.0 T1 quality m1)
- [2026-07-23] **index 자동 분할의 희소 경로 2건(무수행 분기·사본 갱신)** — ① §4 1번이 무조건 수행 지시라, 이미 분할된 vault에서 본체가 재초과(레시피·질문·증상 인덱스 증가)하면 옮길 project feature 행이 없는데 그 분기가 없다 → "무수행 보고 + 1단계 구역화 제안" 1줄 ② §8 미덮어쓰기 규정이 `-presplit`에도 적용되는지 미정의 → 같은 날 2회째 분할에서 stale 사본으로 원복할 여지, "매 분할마다 최신 사본으로 갱신(미덮어쓰기 예외)" 명시 필요. 둘 다 재분할이라는 희소 경로 (출처: v1.137.0 F-7 m1·m2)
- [2026-07-23] **자동 분할의 절차 파일 대칭 결손 2건** — ① `procedures-ops.md` F-2의 log 기록 대상 열거(수정 적용·큐 소비·리포트 생성)에 "자동 분할 수행"이 없어, 승인 수정 0 + 자동 분할만 한 세션이 `[LINT]` 기록 여부를 판단할 근거가 없다 ② `procedures-content.md` B-3에 자동 분할 언급이 0건이라 B 세션은 SKILL 예산표·lint INFO 문구·§4로만 발동한다(3중 확보라 실동작 결함은 아님). 다음 llm-wiki 정비 때 정리 (출처: v1.137.0 F-7 m3·m4)
- [2026-07-23] **골든 러너가 세션 env `CLAUDE_HARNESS_ALLOW_SECRET`를 격리하지 않음** — ALLOW_SECRET=1 세션(이 값이 사전 조건인 구현 세션 포함)에서 전체 실행 시 차단 기대 케이스 4건이 우회 완화되어 거짓 FAIL(실측 401/405). 러너 서두에서 초기화 검토 — 이번 세션은 하위 pwsh에서 변수 해제로 회피 (출처: v1.136.0 구현 중 실관찰)
- [2026-07-27] **vault OKF v0.2 마이그레이션 2건** — ① vault 루트 `index.md` frontmatter의 `okf_version: "0.1"` → `"0.2"` ② 위키 `20_projects/personal/claude-harness-pjc/feat-llm-wiki.md:63`의 OKF 정합 미러 서술(`okf_version` 선언·"OKF timestamp 미채택")을 v0.2 기준으로 갱신. 코드 세션은 위키 쓰기 금지(절차 K)라 별도 `pjc:llm-wiki` 세션(절차 F/B) 소관이며, OKF 소비가 best-effort라 `"0.1"` 잔존이 깨짐을 만들지는 않는다. (출처: v1.143.0 OKF v0.2 정합)
- [2026-07-27] **미적합 잔여 ②③의 OKF 적합성 판정 문장 부재** — `wiki-schema §12`의 미적합 잔여 ①(wikilink)에만 "적합성 3요건 위반은 아님"이 붙어 있고, ②(index.md 표 카탈로그)·③(log.md 평면 목록)은 `okf-spec §11 Conformance` 요건 ③(예약 파일이 §8·§9 구조 준수)에 정면으로 닿는데도 판정이 없다 — §12만 읽고는 이 vault가 conformant인지 결정할 수 없다. v0.1 시절부터 이어진 공백이라 v0.2 갱신의 회귀는 아니며, 다음 §12 손질 때 1줄 판정 추가. (출처: v1.143.0 F-7 m4)
- [2026-07-27] **`deferred.md` 자체가 시크릿 경고를 상시 유발(오탐)** — 이 대장의 L40·L42가 `secret-patterns.ps1`의 탐지 정규식(`Password` 등)과 예시(`| admin | pw |`)를 **서술**하고 있어, 파일을 편집할 때마다 post-write-checks가 "password 값"·"자격증명 쌍(비인용)"을 경고한다. 실제 자격증명은 0건이며 기존 항목 L40·L42는 **미탐** 개선 후보인 반면 이것은 **오탐**이라 별개다. 탐지 규칙을 논하는 문서가 자기 자신을 잡는 구조 — 경고 피로가 쌓이면 진짜 경고를 흘려보내게 되므로, 규칙 문서·대장 경로 예외나 인용 문맥 판별을 검토. (출처: v1.143.0 F-6.5 실관찰)
- [2026-07-27] **`wiki-schema.md` frontmatter(`version`·`updated`) 관리 부재** — 이 파일을 건드린 최근 6개 커밋에서 `version`·`updated` 어느 쪽도 갱신되지 않았다(실측). 규칙 진실원천의 버전 표기가 사실상 방치 상태지만, 갱신 트리거를 새로 정하는 것은 단독 근거가 없어 이번 작업에서 손대지 않았다(D4). 관례를 세울지 별도 판단 필요. (출처: v1.143.0 OKF v0.2 정합)

## 종결

- [2026-07-30 → 2026-07-30] **판정 게이트 축약구의 리터럴 drift 후보** — 반영(v1.147.0 T2). 기계 검사 추가는 **기각**하고 **축약구 자체를 7지점에서 제거**했다 — 리터럴이 K 1 블록에 없고(§0-1에 있었다) 7지점 서식이 불통일해 "정본에서 추출해 대조"가 성립하지 않았으며, 게이트 언급 10건 vs 축약구 7곳의 차이 3곳을 예외 목록으로 하드코딩해야 했다(그 목록이 곧 새 stale 지점). 반복이 없으면 drift가 불가능하므로 감시 장치가 불필요해졌다. 포인터와 K 1 본문은 유지. **다음 세션이 기각된 원계획(검사 추가)을 다시 시도하지 않도록 이 사유를 남긴다.**
- [2026-07-30 → 2026-07-30] **`AGENTS.md`의 JSON 매니페스트 검증 명령이 파일 부재를 "OK"로 출력한다** — 반영(v1.147.0 T1, `-ErrorAction Stop` + 결과 변수). **등재 당시 서술을 정정한다**: "exit code는 0이 아니지만 출력만 오인된다"고 적었으나 실측 결과 **구 형태는 exit code까지 `0`**이었다 — 사람이 로그를 훑을 때만이 아니라 자동화 경로에서도 실패가 통과로 처리됐다(더 심각). 수정 후 부재 경로에서 exit 1 + OK 미출력, 정상 3파일은 종전과 동일.
- [2026-07-30 → 2026-07-30] **v1.146.0 hook의 실동작 스모크** — 확인 종결. 설치본 `~/.claude/plugins/cache/pjc-harness/pjc/1.146.0/scripts/session-context.ps1`에 vault 판정이 실재하고(18건), 2026-07-30 세션의 SessionStart가 `[pjc 세션 컨텍스트] 위키 vault: 설정됨 (…)` 라인을 **실제로 주입**했다. 재설치 후 새 세션에서만 관찰된다는 조건이 충족된 것을 실측으로 확인.
- [2026-07-30 → 2026-07-30] **hook 골든 무인자 전체 실행의 "완료 불가"는 환경 한계가 아니라 실행 방법 문제였다** — 반영(v1.146.0 F-2). v1.145.0에서 5회 중단, v1.146.0에서도 3회 실패했으나 **출력을 파일로 리다이렉트해 백그라운드로 실행하니 426/426 OK (FAIL 0, exit 0)** 로 완주했다. **원인은 둘이며 둘 다 피해야 한다**: ① `| Select-Object -Last N` 같은 **파이프라인의 출력 버퍼링**(백그라운드 killed·리뷰어의 40분+ 미종료의 원인) ② 스위트가 10분을 넘어 **전경 실행은 시간 캡에 걸린다**(리다이렉트만 해도 전경이면 실패 — 첫 시도의 원인). 실행 방법은 **`AGENTS.md`의 Hook 골든 블록에 기록**했다(F-2 실행 시점에 읽히는 위치이고 session-context hook이 매 세션 전문 주입하므로 도달률이 대장보다 높다 — 이 대장 항목은 이력 보존용). v1.145.0의 5회 중단도 같은 원인으로 추정(단정 아님). 러너 처리량 개선(in-process)은 여전히 가치 있으나 F-2 이행의 전제는 아니다. (출처: v1.146.0 F-7 B1 → 실측 해소, F-7 2회차 N1·n3)
- [2026-07-29 → 2026-07-30] **`disallowed-tools` 실동작 스모크 + effort 치환 확인** — `implement-task` frontmatter의 `disallowed-tools: AskUserQuestion`과 본문 effort 분기가 설치본에서만 실행되는 탓에 미검증으로 남아 있던 3건. **v1.145.0 릴리즈·설치가 완료된 뒤 스모크로 전부 실증**(2026-07-30): ① **질문 도구 제거 동작** — 스킬 로드 후 `AskUserQuestion` 호출이 `Permission ... denied`로 거부됨 ② **본문 effort 치환** — 101행이 `high`로 치환돼 도착(파일 원본은 변수 리터럴) ③ **Read 경로 미치환**(부수) — 같은 파일을 Read로 읽으면 리터럴로 보임을 확인. 치환은 스킬 로드 경로에서만 일어난다. ③은 압축 후 재읽기 경로(컨텍스트 관리 규칙 4)에 걸리지만 effort 확인이 루프 시작 전 1회라 재확인 대상이 아니고 리터럴이 low/medium 어느 쪽도 아니어서 경고가 오발화하지도 않는다 — **실해 없음**. 반영: SKILL.md의 "미검증" 서술을 실측 결과로 정정(해제 규칙 때문에 "1차 방어선은 문서 규칙"이라는 취지는 유지)·주석에 로드 경로 한정 명시·PRD FR-12/FR-13에 실증 이력 추가. (출처: Opus 5 대응 plan T9 → v1.145.1)
- [2026-07-25 → 2026-07-29] **`recovery.md:3` "아래 두 절" 분배 표현** — `pjc-systematic-debugging`은 「Subagent 호출 규약」 절만 참조하고 "Reviewer 호출 실패 대응" 절 포인터는 0건인데, 서두가 두 스킬 모두 "두 절"을 참조하는 것처럼 읽힌다(집합적으로는 참). "스킬별로 참조 절은 다르다" 한 구 보강 — 단독 커밋 가치는 없어 다음 하니스 편집 세션에 묶어 처리. (출처: 동기 호출 규약 명문화 F-7 재호출 m1) — **반영(Opus 5 대응 plan T8)** — 서두에 "다만 스킬마다 참조하는 절은 다르다(`plan-feature`는 두 절 모두, `pjc-systematic-debugging`은 「Subagent 호출 규약」만 참조한다)"를 보강해 분배 오독을 제거.
- [2026-07-25 → 2026-07-29] **`implement-task/SKILL.md:294-296` 헤더-내용 주제 경계** — "Reviewer 호출 실패 대응" 헤더 아래 첫 문단이 정상 경로(동기 규약)라 훑는 독자에게 순간 혼동 여지. `**호출 규약(정상 경로) — 전부 동기.**` 라벨이 이미 있어 실질 위험은 낮음(리뷰어도 "수정 강제 아님"). 소제목 분리 검토. (출처: 동기 호출 규약 명문화 T1 V-6 MINOR m2) — **반영(Opus 5 대응 plan T8)** — 헤더 아래에 "이 절은 정상 경로(동기 호출 규약)와 실패 대응(A/B/C)을 함께 담는다 — 바로 아래 첫 문단이 정상 경로이고, 실패 분기는 `A.`부터다" 1줄 추가. 같은 작업에서 헤더 이름도 "(과부하·도구 불가·거절)"로 갱신.
- [2026-07-24 → 2026-07-24] **SKILL.md 경량화 (규칙 폐기·통합 별도 접근)** — v1.138.1(`task/skill-consolidate`)에서 **통합**을 시도 → 실익 미미 실증: T1 pre-review 선설명 −55자(Phase D ③과 중복 부연 축약)·**T2 완전 일치 중복 0**. 통합의 실익도 rationale 분리처럼 제한적임이 references 레벨에서도 확인됐다 — **"겉보기 중복 ≠ 실제 중복"**(explorer가 꼽은 references Type 표는 각기 다른 도메인, 금지표현·정형구는 국면별 정당한 반복). 감량 기법을 D6("literal 중복·부연 삭제만, 재서술 금지")로 한정해 안전 유지. **규칙 폐기·구조 재편은 미수행 → 재-Deferred**(위 대기 "SKILL 방향 재편"). (출처: v1.138.0 T3 → v1.138.1)

- [2026-07-20 → 2026-07-23] **`warn-commit-secrets`가 시크릿 탐지기 자신의 패턴 정의를 신고하는 오탐** — `lint.py`를 `git add`하자 원인이 `lint.py:79`의 탐지 규칙 정의였던 사례(커밋 diff에 실제 시크릿 0건, grep 실증). 훅이 `git add` 대상의 전체 파일 내용을 스캔하는 규약의 구조적 부작용으로, 보안 도구·탐지기 소스를 다루는 레포에서 반복됐다(v1.135.0 리뷰 후속 커밋이 같은 오탐으로 차단된 실사례가 재수용 계기). (출처: v1.129.0 T3 구현 중 실관찰) — **반영(v1.136.0 T1~T2)** — `bash-hook-lib.ps1` `git add` 경로 나열 Leaf 분기에 추적 판정 추가: 추적 파일은 `git diff HEAD` 추가 라인만 스캔(이력 기존 내용 재신고 제거), untracked(ignored 강제 add 포함)는 전체 스캔 유지. 골든 (n1)~(n3) red-green 실증, 전체 408/408.
- [2026-07-22 → 2026-07-22] **lint.py의 pending 잔량 집계가 형식 위반 줄을 조용히 누락** — `lint.py:934`의 `^\s*-\s*\[\d{4}-\d{2}-\d{2}\]\s*\[TAG\]`는 **날짜 선두**를 요구하는데, 실제 vault 큐 17줄이 전부 날짜를 끝 괄호에 둔 형태(`- [DECISION] proj: … (2026-07-15)`)라 **집계가 0건**이었다(실측: lint에 pending INFO 자체가 미출력 → 정규화 후 17건 정상 집계). 형식 규약은 2026-07-02(`31db62c`)에 도입됐고 큐 항목은 07-15/16자라 legacy가 아니라 **규약 위반이 무검출로 방치된 것**. 결과적으로 K 5-2의 20건 잔량 경고·소비 유도가 전부 침묵했다. 보완 방향: ⓐ 태그는 있는데 날짜 선두가 아닌 줄을 `형식 위반 n건`으로 WARN 집계(무검출 제거) ⓑ 또는 날짜 위치에 관대한 파싱. **"0건"과 "형식이 틀려 못 셈"이 구분되지 않는 것이 핵심 결함** — 큐가 비었다는 보고가 곧 안전 신호로 읽힌다. (출처: v1.131.0 이후 위키 큐 정리 세션 실관찰) — **반영(v1.132.0 T1~T2)** — §7-25에 형식 위반 WARN 추가(불릿 ∧ 태그 존재 ∧ 선두 형식 미매치), 태그 목록 단일 출처화, schema §7-25·F-1 25·F-0 동기, 회귀 픽스처 pending-malformed + 정상 픽스처 오탐 0 고정, eval 33→34 PASS.

- [2026-07-19 → 2026-07-19] llm-wiki §7-27 양성 검출 eval fixture 추가 — 반영(v1.125.0 T2 — `no-hanyeong-guide` fixture + lint-cases 케이스, 31/31 PASS. 한글전용 alias ui-ux 가이드 행이 "가이드 한/영 병기 누락" WARN을 내는 것을 항구 회귀 그물로 승격)

- [2026-07-10 → 2026-07-13] AGENTS 게이트 임시폴더 예외 분기 골든 케이스 추가 — 반영(v1.118.0 T1 — TE1(plan 게이트)·TE2(AGENTS 게이트) 2건 신설. 픽스처를 `$iso`(GetTempPath 하위) + **흔적 없는 transcript 주입**으로 구성해, 미주입 시 fail-open으로 exit 0이 나는 거짓 green을 방지)
- [2026-07-08 → 2026-07-12] 위키 feat-safety-hooks 보호 집합 서술("hook 스크립트 8종") 현행화 — 확인 종결(2026-07-12 하네스 ingest 세션에서 grep 확인: "8종" 서술이 이미 없음 — 이전 ingest(v1.108~113 반영)가 "검사 10종 + 디스패처"로 현행화 완료)
- [2026-07-09 → 2026-07-12] llm-wiki fixture `archive-exempt/.../oldproj.md` UTF-8 BOM 정리 — 기각(v1.116.0 D6: 그 BOM은 lint-cases archive-exempt 케이스의 expect_absent "BOM"이 검증하는 **의도된 테스트 입력**(90_archive BOM 무경고 실증)이라 제거하면 검증이 공허해짐 — 컨벤션 위반이 아니라 테스트 대상 데이터)

- [2026-07-10 → 2026-07-10] warn-external-ops가 `git merge-base`(읽기 전용 조회)를 "git merge"로 오탐 — 반영(v1.113.1 T1 — bash-hook-lib.ps1:39 `merge\b`→`merge(?![-\w])`, merge-base·merge-tree·merge-file·merge-index plumbing 오탐 제거·실제 merge 경고와 --abort/continue/quit 제외 보존, 골든 merge-base·merge-tree 무경고 2건 추가)
- [2026-07-10 → 2026-07-10] 위키 feat-safety-hooks·feat-plan-feature에 v1.111.0 bootstrap 게이트·직접 작성 금지 반영 — 반영(2026-07-10 위키 ingest 세션 — feat-plan-feature Step 1 bootstrap 강화 + feat-safety-hooks require-plan 게이트 동작·지도 서술)
- [2026-07-10 → 2026-07-10] Type B 분류 정의가 구조 기준(단일 파일·메서드·호출자 없음)뿐이라 신규 비자명 로직이 B로 새어 검토(V-5 prefilter만·V-3/V-6 생략) 얕아짐 — 반영(v1.113.0 T1 — plan-feature:257에 경계 명문화(비자명 로직·알고리즘·제어흐름·상태변경 신규 심볼은 Type C 이상, 자명 접근자·상수·순수 위임은 B 유지) + plan-reviewer:39 오분류 검출 예시 병기, 방어선 2층)
- [2026-07-10 → 2026-07-10] SessionStart matcher `compact` 추가 + 요약 직후 재확인 컨텍스트 주입 — 반영(v1.112.0 T1 — session-context.ps1 신설, matcher 4종 + compact 리마인더, 골든 SC2)
- [2026-07-10 → 2026-07-10] post-write-checks notes.md 30,000자 초과 아카이브 경고 — 반영(v1.112.0 T2 — 섹션 2b, Test-WarnOnce 디듑, 골든 NA1~NA3)
- [2026-07-10 → 2026-07-10] SessionStart에 plan.md 존재·미완료 task 수·notes.md 최신 항목 날짜 주입(로컬 경량판) — 반영(v1.112.0 T1 — session-context.ps1에 compact 항목과 통합 구현, 골든 SC1·SC1b·SC3~SC5)
- [2026-07-10 → 2026-07-10] implement-task Phase D clean/dirty 판정×재리뷰 커밋 프로토콜 모순 + review-fix 커밋 메시지 미규정 — 반영(v1.110.0 T1 — Phase D ①을 HEAD 커밋 제목 판정으로 교체·review-fix checkpoint 규정·재개 트리 5분기+폴백·새 완료 커밋 --allow-empty)
- [2026-07-10 → 2026-07-10] plan-feature 분할×PRD 전수 커버×12-a 규정 교착 — 반영(v1.110.0 T2·T3 — part별 Coverage 스코프(`⏭️ 다음 part`/`✅ 이전 part 기구현` 행)+합집합=전수 게이트+두 part 동시 작성 명문화, 12-a 분할 인지+실질 MAJOR 게이트, 마지막 part Phase G/F-7 전수(전체 트리 기준))
- [2026-07-09 → 2026-07-10] 하네스 최근 버전분(v1.102.0~) 위키 ingest — 반영(2026-07-10 위키 세션 2회: v1.102~1.108 feature 5페이지 상세 ingest + v1.109.0 델타·run_eval 함정·A~L 표기 드리프트 정정, lint 델타 0)
- [2026-07-08 → 2026-07-10] plan-feature description 1,018자 하드 한도(1,024) 근접 — 반영(v1.109.0 T5 — 981자로 무손실 압축(연결부만), 여유 43자. pjc-systematic-debugging 1,019→921자도 동일 처리. 전후 트리거 eval 동률 실증)
- [2026-07-09 → 2026-07-09] hook 이벤트 로그 자동 요약(3개월 축적 후) — 반영(v1.107.0 T2 `report-hook-events.ps1` 수동 리포트로 대체 — 자동 실행은 사용자 확정으로 채택 안 함)
- [2026-07-08 → 2026-07-08] llm-wiki 본체에서 절차 K 초경량 분리 — 기각(정식 검토 결과 분리하지 않음: 최빈 경로가 본체를 어차피 로드해 토큰 중립+Read 1회 지연만 추가. 재논의는 로드 경로가 바뀔 때만 — deferred-followups-closure)
- [2026-07-08 → 2026-07-08] explorer subagent 모델 상향(haiku→상위) — 기각(현행 유지: effort medium 반영 직후 관찰 기간 0·실패 실사례 없음. 재검토 조건: locating 실패·부정확 실사례 관찰 시 — deferred-followups-closure)

<!-- 초기 시드 sweep 근거 (2026-07-09, v1.107.0 T4): notes.md 현행 전 항목의 "미처리 Deferred" 목록 +
     docs/plans/*.md 6파일의 "## Deferred / Follow-up" 섹션 + notes-archive/2026-07.md 대조.
     중복은 최초 출처로 병합(suggest-agents-record 오탐 2류는 동일 수정 라운드라 1항목).
     "재설치(install.ps1)"는 반복 등장하나 작업 항목이 아니라 사용자 지시로 보류 중인 운영 행위라 제외.
     분할 plan 상호 포인터(다음 plan 안내)는 실행 완료된 안내문이라 제외. -->
