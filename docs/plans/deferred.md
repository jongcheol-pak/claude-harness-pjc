# Deferred 대장

> 미처리 Deferred의 단일 추적 대장 (형식 정본: implement-task `references/phase-f-detail.md` F-6.5).
> 항목은 `- [등록일] {요지 1줄} (출처)`, 종결 시 삭제가 아니라 `## 종결`로 이동한다.
> plan-feature Step 1이 계획 때 `## 대기`를 조회하고, implement-task F-6.5가 완료 때 신규분을 append한다.

## 대기

### 대장 운영

- [2026-07-30] **이 대장 자체의 정리 — 대기 94건이 전부 2026-07 한 달치다** — 등재는 쉽고 소진은 어려워 한 달 만에 94건이 쌓였다. 그중 상당수는 **이미 해소됐거나 전제가 사라졌을 가능성**이 있으나 아무도 확인하지 않았다(대장이 커지면 `plan-feature` Step 1의 `## 대기` 조회가 노이즈에 묻혀, 정작 막으려던 "과거에 미뤄둔 일을 모르고 재발견"이 다시 일어난다).
  - **▶ 착수 지침**: ① **삭제가 아니라 이동**이다 — 해소된 것은 `## 종결`(`- [등록일 → 종결일] … — 반영|기각`), 전제가 사라진 것은 같은 형식에 `— 기각(사유)`, 같은 주제 중복은 **병합**(최신 항목에 흡수하고 나머지를 종결로). 형식 정본은 `implement-task/references/phase-f-detail.md` F-6.5. ② **판정은 실측으로** — "해소된 것 같다"가 아니라 해당 파일·코드를 grep/Read로 확인한 뒤 종결한다(대장 항목은 그 자체가 다음 세션의 근거이므로 추측 종결은 잘못된 근거를 남긴다). ③ **한 번에 다 하지 않아도 된다** — 오래된 것부터 batch로 나눠 처리하고, 판정이 애매한 항목은 그대로 두는 편이 잘못 종결하는 것보다 낫다. ④ **정리 후 잔량을 이 항목에 갱신**해 다음 세션이 진행도를 알 수 있게 한다.
  - **함께 검토할 것**: 이 대장이 `plan-feature` Step 1에서 **어떻게 읽히는지** — 94건을 다 읽는 것은 비현실적이라, 실제로는 상단 몇 건만 보고 넘어갈 가능성이 크다. 항목 수 상한·요약 헤더·주제별 그룹핑 같은 구조 개선이 필요한지도 함께 판단한다(구조를 바꾸면 F-6.5 형식 정본도 함께 갱신해야 한다).
  - **▶ 1차 정리 완료 (2026-07-30, v1.148.0 T6) — 정리 직후 94건 / 현행 98건**(2026-07-31 스크립트 실측). T6 이후 v1.148.0 F-6.5 등재분이 더해져 99건이 됐고, `block-destructive` 오탐 항목 1건이 기각 종결돼 98건이다. **잔량 표기는 정리 batch 때만이 아니라 등재로 늘어난 뒤에도 갱신해야 한다** — 94로 굳어 있던 나흘 동안 대장은 실제보다 작아 보였다(갱신 규약 ④의 사각이라 여기 적어 둔다).
    - **구조 개선 적용**: `## 대기`를 **주제별 5그룹**(대장 운영 / hook·안전망 / 하니스 스킬 / llm-wiki·vault / eval·측정)으로 재배치했다. 항목 이동은 스크립트 파싱→재조립으로 하고 **문자 수 31,737 동일·항목 95건 동일**을 대조해 무손실을 확인했다. **항목 수 상한은 도입하지 않았다** — 미처리 항목의 자동 삭제·만료는 "미뤄둔 일을 잃지 않는다"는 이 대장의 존재 이유와 정면 충돌한다.
    - **실측 판정 31건**(가장 오래된 2026-07-08~07-15 전량): 종결 1건(README 서술 정책 — 반영), 나머지 30건은 대상 코드·문서 **무변경 실측** 후 `(2026-07-30 재확인: 유지 …)` 표기. 다음 세션이 같은 조사를 반복하지 않게 하는 것이 이 표기의 목적이다.
    - **병합은 보류**: `..` 상위 탈출 미탐과 `$env:` 별칭 깊이 cap 미탐은 원인(정규식이 값이 아니라 표기만 본다)이 같아 병합 후보였으나, **서로 다른 미탐 케이스**라 합치면 재현 조건이 뭉개진다 — 같은 그룹에 인접 배치하는 것으로 탐색성 목적은 달성했다.
    - **다음 batch**: 2026-07-19~07-25 구간(약 30건)을 같은 방식으로 판정할 것. → **2차 정리에서 완료(실측 35건)**.
  - **▶ 3차 batch는 미실행 — v1.149.0(2026-07-31)이 관련 3건만 선별 종결: 정리 직후 96건 / 현행 97건**(같은 세션 F-6.5가 신규 1건 등재 → 대기 97 / 종결 36 / 합계 133, 스크립트 실측). 구간 batch가 아니라 **작업 연관 항목만** 처리한 경우이므로 07-27~07-30 구간의 3차 batch는 그대로 남아 있다. **정리 직후 수치만 적고 등재분을 반영하지 않는 것이 이 대장의 반복 실수**라, 아래 규약대로 등재 후 즉시 갱신했다.
  - **▶ 2차 정리 완료 (2026-07-31) — 정리 직후 95건 / 현행 98건**(같은 날 F-6.5가 신규 3건을 등재. **이 시점 분리 표기가 바로 아래 규약이 요구하는 형태다** — 정리 직후 수치만 적고 등재분을 반영하지 않으면 1차 정리의 "94건" 실수를 그대로 반복한다. 실제로 F-7 리뷰가 이 블록의 초안을 "등재 직후 stale"로 잡았다):
    - **실측 판정 35건**(2026-07-19~07-25 전량 — 그룹 분포 hook·안전망 9 / 하니스 스킬 16 / llm-wiki·vault 10 / eval·측정 0): **종결 3건**(반영 2 · 기각 1), **유지 32건**. 유지 항목에는 전부 `(2026-07-31 재확인: 유지 — 근거)`를 붙여 다음 세션이 같은 조사를 반복하지 않게 했다.
      - 종결 상세 — **반영 2건**: `post-write-checks` 오탐(v1.147.0의 추적 파일 추가 라인 한정 스캔으로 편집 경로 한정 해소) · `B-1` 라벨 충돌(`recovery.md`가 A/B/C 단일 depth로 정리되며 전제 소멸). **기각 1건**: 커밋 계수 오기(등재 당시 이미 "정정 불가·기록만"으로 결론난 **액션 아이템 아님**).
    - **카운트 기준(중요)**: `^- \[YYYY-MM-DD\]` 앵커 + **`## 대기` 구간 한정**. 이 95에는 **이 대장 운영 항목 자신(1건)이 포함**된다. 종결 항목은 `- [등록일 → 종결일]` 화살표 형식이라 같은 앵커에 걸리므로 구간으로 분리해 센다. *(기준을 적지 않으면 세는 사람마다 값이 갈린다 — 1차 정리의 "94건" 표기가 등재 증가분을 반영하지 못한 채 나흘간 굳어 있던 것도 같은 원인이다.)*
    - **무손실 대조**: 정리 전후 `대기 + 종결` 총수 **128 불변**(대기 98→95, 종결 30→33 — 감소분 3 = 증가분 3). 이후 F-6.5 신규 등재 3건으로 대기 98 · 총수 131. **현행 그룹별 잔량**: 대장 운영 3 · hook·안전망 31 · 하니스 스킬 37 · llm-wiki·vault 23 · eval·측정 4 = **98**.
    - **이번 회차에서 확립된 판정 규약 2건** (리뷰 지적에서 나옴 — 3차 batch도 따를 것):
      - **종결 근거는 백틱 `경로:라인` 필수.** 자유서술 근거는 진위 확인이 불가해 "종결했다"는 주장만 남는다. 라인이 과거 시점 값이면 `— 등재 당시 라인` 단서를 붙인다(현재 라인으로 갱신하면 "과거의 오기"를 다루는 항목에서 사실이 왜곡된다).
      - **미실행 task를 근거로 인용할 때는 3요소.** ⓐ 괄호로 실행 여부 명시(`계획 단계`·`아직 미실행`) ⓑ 완료 동사(`해결했다`·`실증한다`) 대신 계획·시도 동사(`채택했다`·`시도한다`·`설계다`) ⓒ 검증 시점 미래 조건화(`실행 후 확인하면`). **같은 시제 오류가 두 task 연속 발생**해 리뷰어가 세운 기준이다.
    - **다음 batch**: 2026-07-27~07-30 구간 — **판정 대상 32건 + 이 대장 운영 항목 1건 = 33건**(자기 항목은 판정 대상이 아니지만 총계에는 들어간다).

- [2026-07-31] **대장 운영 항목에 "기록성 항목" 기각 범주 명문화** — `deferred.md:12`의 착수 지침 ①이 나열한 기각 유형은 "해소=종결 / 전제 소멸=기각 / 중복=병합" 셋인데, **애초에 액션 아이템이 아닌 기록성 항목**(등재 당시 이미 "정정 불가·기록만"으로 결론난 것)이 빠져 있다. 2차 batch에서 그 범주로 1건을 기각했고 spec·quality 두 리뷰어가 *"명문 목록은 폐쇄가 아니므로 타당하나, 명문화가 다음 batch의 애매함을 줄인다"* 로 판정했다. 3차 batch 전에 한 구 추가. (출처: 2차 batch T2 spec 리뷰 M1)
- [2026-07-31] **미실행 task 교차 참조 3요소 체크리스트 명문화** — 대장 항목이 **아직 실행되지 않은 task**를 근거로 인용할 때 ⓐ 괄호로 실행 여부 명시(`계획 단계`·`아직 미실행`) ⓑ 완료 동사(`해결했다`·`실증한다`) 대신 계획·시도 동사(`채택했다`·`시도한다`·`설계다`) ⓒ 검증 시점 미래 조건화(`실행 후 확인하면`). **2차 batch에서 같은 시제 오류가 T1→T2로 연속 재발**(T1 MINOR → T2 MAJOR)해 리뷰어가 세운 기준이며 T3부터 적용해 재발 0이었다. 대장 운영 항목이나 `phase-f-detail.md` F-6.5 규약 중 어디에 둘지 결정 필요. (출처: 2차 batch T2 quality 리뷰 M1)

### hook · 안전망 (차단·경고·골든 러너)

- [2026-08-02] **[미채택안] 사용자 발화 길이 상한 백스톱 (조건부 재검토)** — "사용자 발화는 N자 이하"라는 상한을 `isMeta` 제외와 **함께** 두는 안. 스키마가 바뀌어도 41K자 페이로드는 걸러진다는 이점이 있으나, 상한값의 근거가 임의이고 **로그·코드를 길게 붙여넣은 정당한 발화에 든 "그만"을 놓쳐 오차단 방향 위험**이 새로 생겨 v1.151.0에서는 채택하지 않았다(사용자 선택 — `isMeta` 제외만). **`isMeta` 의존 한계가 실제로 관측되면**(스키마 변경으로 fail-open 재발) 그때 재검토한다 — 그 한계는 아래 「`isMeta` 스키마 의존의 잔존분」 항목이 추적하며, 관측 수단은 `check-transcript-assumptions.ps1`이다(v1.152.0 T4에서 신설. **종전 문면의 "위 ⓐ"는 그 항목이 `## 종결`로 이동해 가리킬 대상을 잃었다 — 2026-08-03 정정**). (출처: v1.151.0 D1)

- [2026-08-02, **v1.154.0에서 범위 축소**] **⑤(순수 진행 요약 정지)의 의도적 미탐 2종** — **관측된 것은 등재된 ⓐ/ⓑ 어느 쪽도 아닌 제3의 형태였다**: *"T9~T13을 진행했습니다. 컨텍스트가 상당히 차서 여기서 상황을 정리해 보고합니다."* — task 번호도 있고 대문자인데 **완료 어휘 자체가 없어서**(`진행했습니다`) ⑤가 안 걸렸고, 동시에 ②③④도 전부 빗나갔다(실측 4/4 미매치). 즉 시그니처 확대로는 닫히지 않는 유형이라 **루프 활성 화이트리스트**로 해소했다(루프가 도는 중이면 문면 무관 차단). **미탐이 남는 곳은 폴백 경로뿐**이다(루프 종료 후·사용자 개입 후 재개 구간) — 아래 원 서술은 그 경로에 한해 유효하다. (원 등재) — ⓐ **task 번호 없는 완료 보고**(*"구현을 마쳤습니다. 빌드와 테스트 모두 통과했습니다."*): 탐지 범위를 `T<N>` 결합형으로 좁힌 사용자 선택의 대가다 ⓑ **소문자 `t3 완료`**: `(?-i)` 도입의 대가이며, SKILL.md 템플릿·골든 프로브·plan task 표기가 전부 대문자라 모델 출력이 소문자로 갈 경로는 사실상 없다고 판정했다. 둘 다 문서 규칙(SKILL.md ⑤ 절)이 받으며, 그 형태의 정지가 실제로 관측되면 시그니처를 넓힐지 재평가한다. (출처: v1.150.0 D1 + 2회차 리뷰 m3)

- [2026-08-02] **[SUGGEST] `require-evidence`의 `$rxUserAsk` 오억제 여지** — `설명`·`왜`·`인가` 등이 단어 경계 없이 매치돼 *"이 함수는 알고리즘 설명을 포함합니다"* 같은 **평서형 사용자 발화도 ⑤ 억제를 켠다**(실측 확인). 억제가 늘어나는 방향이라 **미탐 쪽**이고 이 hook의 fail-open 철학과 정합해 v1.150.0에서는 수용했다. 오억제로 ⑤가 실제로 안 잡히는 사례가 관측되면 `\b`·조사 결합 형태로 좁힌다. (출처: v1.150.0 T2 quality m1 — 판정 유보) **(2026-08-04 v1.154.0: 실효 범위 축소 — 이 억제는 `-not $loopActiveAfterUser`일 때만 적용되므로 이제 폴백 경로 한정이다. 루프가 도는 중에는 화이트리스트가 문면과 무관하게 판정하므로 오억제가 발생하지 않는다.)**

- [2026-07-31] **`AGENTS.md:72` 라인 참조가 두 곳에서 stale** — ① `block-destructive.ps1:395` 주석이 미탐 보완 승인 선례를 "AGENTS.md:72에 인라인 기록"이라 가리키고 ② 이 대장의 `protect-harness` 한글 골든 항목도 "AGENTS.md:72가 미탐 보완에 요구하는 실증"이라 인용한다. **실제 조항은 83행이며, 이 stale은 2차 batch 이전부터 있었다** — 2026-07-31 슬림화는 전부 in-place 치환이라 **총 라인 수 93→93 불변이고 그 조항도 base·head 모두 83행**이다(F-7 실측. 초안에 "슬림화로 또 이동했다"고 적었으나 검증 없는 주장이라 정정한다 — 이 batch가 세운 "판정은 실측으로"를 신규 등재분이 어긴 사례). **근본 해법은 라인 번호가 아니라 조항 제목 참조**(`## DO NOT`의 block-destructive 조항) — 라인 참조는 문서가 바뀔 때마다 낡으므로 두 곳을 한 건으로 묶어 처리한다. hook 코드 수정이라 2차 batch의 Out of Scope였다. (출처: 2차 batch plan-reviewer m5 + F-7 M1)

- [2026-07-30] **`require-evidence.ps1`의 transcript 재독 4회** — 검사 2-1·3·4가 같은 `transcript_path`를 각각 `Get-Content -Tail 3000`으로 읽고(`:124`·`:147`·`:258` 근방), **T8이 검사 4에 `Select-String` 전 파일 스캔 1회를 더했다**(`:249-256` — 조건 ③의 fail-open 수정). 즉 실제 I/O는 **tail 3회 + 전 파일 1회**다. 기능 결함은 아니나 **매 종료 시도마다** 동일 파일을 4번 읽는 중복이며, 한 번 읽어 공유하도록 리팩터링하는 것이 정석이다 — 다만 기존 2검사의 판정 경로(비차단 규약이 걸린 코드)를 건드리게 되어 v1.148.0 범위 밖으로 뒀다. ~~**성능 자체는 문제없음이 실측됐다**(최악 경로 399ms).~~ (출처: v1.148.0 T2 quality m1 → F-7 2회차 m2'로 현행화)
  - **[2026-08-04 v1.154.0 — 해소 + 성능 판정 정정]** 재독을 **1회로 통합**했다(`Get-TranscriptTail` 헬퍼가 경로 단위로 캐시). **그리고 "성능 문제없음"은 틀린 판정이었다** — 399ms는 조건 ③의 `Select-String` 전 파일 **무매치 스캔만** 잰 값이고(재실측 6ms), **hook 전체 경로는 같은 7.3MB에서 9,823ms**였다. hook timeout이 10초이므로 **이미 임계였고**, 넘으면 경고뿐 아니라 검사 4의 차단까지 통째로 죽는다(성능이 곧 기능). 병목은 `Get-Content -Tail`이었다 — 3.7~5.1초 대 `-Raw` 47ms(약 90배). 원인은 입력 특성이다: transcript JSONL은 한 줄이 수만 자(스킬 발동 페이로드 약 41K자)라 `-Tail`의 역방향 줄 단위 읽기가 극단적으로 비효율적이다. 교체 후 **1,425ms**(실 7.3MB) / **1,628ms**(합성 14.6MB). **교훈: 부분 구간 실측을 전체 경로 성능의 근거로 쓰지 말 것.**
- [2026-07-30] **`last_assistant_message`의 실환경 도착 경로 확인 (낮은 우선순위)** — v1.148.0 T2의 차단 조건 ④가 이 stdin 필드를 쓰는데 실환경 제공 여부가 미실증이다. **다만 위험은 해소됐다** — quality BLOCKER 지적을 받아 **transcript 폴백**(마지막 assistant 텍스트 직접 추출)을 넣었고 폴백 경로 단독 BLOCK 발화를 pwsh 7·PS 5.1 양쪽에서 실측했다. 즉 필드가 안 와도 검사는 동작한다. 남은 것은 **어느 경로가 실제로 쓰이는지** 확인뿐이며, 릴리즈 후 hook 이벤트 로그로 보면 된다. (출처: v1.148.0 T2)

- [2026-07-30] **`bash-hook-lib.ps1`의 `diff HEAD` 경로에 같은 클래스의 결함이 있을 수 있다 (실증 필요)** — 커밋 시점 검사(`warn-commit-secrets`, **조건부 차단** hook)도 `git diff HEAD --unified=0`을 개별 exit code 검사 없이 쓴다(`:215`·`232`·`247`·`256`). v1.147.0 T3이 고친 것과 동일 패턴이다. **다만 실질 위험은 낮아 보인다** — 기본 경로는 `git diff --cached`(`:207`)이고 그것은 HEAD가 없어도 빈 트리와 비교해 정상 동작하므로 초기 커밋 전에도 staged 시크릿을 잡는다. `diff HEAD`는 `-a/-am` 자동 스테이징분·`git add` 동반 경로의 **보완 스캔**이라 그 경로만 조용히 빠질 수 있다. **차단 hook이라 미탐 비용이 편집 시점보다 크므로 실증이 필요하다** — 초기 커밋 전 저장소에서 `git commit -am`으로 재현되는지 확인하고, 재현되면 T3과 같은 방식으로 고친다.
  - **▶ 착수 지침 (새 세션이 이 항목만 읽고 시작할 수 있게)**: ① **수정 지점** — `plugins/pjc/scripts/bash-hook-lib.ps1`의 `git diff HEAD --unified=0` 호출 4곳(`:215`·`:232`·`:247`·`:256`). ② **패턴은 v1.147.0 T3과 동일** — 결과를 변수로 받고 **직후 `$LASTEXITCODE`를 검사**해 0이 아니면 그 보완 스캔분을 빈 배열로 두지 말고 **폴백**한다(편집 경로는 전재 폴백이었지만 커밋 경로는 `--cached` 결과가 이미 있으니 "보완분 없음"으로 처리할지 전재로 갈지 **설계 결정이 필요**하다 — 차단 hook이라 보수적으로). 참고 구현: `post-write-checks.ps1:196-214`. ③ **골든** — `run-hook-evals.ps1` §9(L910 근방) `warn-commit-secrets` 케이스에 "**초기 커밋 전 저장소 + 기존 staged 파일 + 미스테이징 시크릿 추가 + `git commit -am`**" 케이스를 추가하고 **red-green으로 실증**(검사를 무력화하면 그 케이스만 FAIL해야 한다 — T3이 이 방식으로 결함을 확증했다). ④ **검증** — Build(전 ps1 parse) + **Hook 골든 무인자 전체**(출력을 파일로 리다이렉트 + 백그라운드 — AGENTS.md `## Build & Test` 명시. 기준선 **429/429**). ⑤ **범위 밖** — `secret-patterns.ps1`(탐지 패턴)·`block-destructive`·`protect-harness`는 건드리지 않는다.
  - **⚠ F-7이 실증의 절반을 끝냈고, 결론은 "위험이 낮다"가 아니라 "재현 가능성이 높다"로 상향됐다**: 임시 저장소 실측 — HEAD 없는 repo에서 `git diff --cached`는 **exit 0**(빈 트리 비교로 staged 내용을 출력)이고 `git diff HEAD`는 **exit 128**이다. 그리고 **staged 이후의 미스테이징 추가분은 `--cached` 출력에 없다** → `fresh repo + 기존 staged 파일 + 미스테이징 시크릿 추가 + git commit -am` 경로에서 보완 스캔이 조용히 0줄을 기여한다. 남은 것은 **hook 레벨 재현**뿐이다. (출처: v1.147.0 T3 quality 리뷰의 범위 밖 관찰 → F-7이 git 원시 동작을 실측 확인)
- [2026-07-30, **v1.147.0에서 부분 해소**] **시크릿 탐지 오탐의 재귀 구조** — **추적 파일은 해소됐다**(T3: 편집 시점 검사가 `git diff HEAD`의 추가 라인만 스캔 → 이력에 이미 있는 예시는 재신고 안 함. 탐지 패턴은 무수정이라 미탐 위험 0). **남은 것은 셋이다**: ⓐ `plan.md`·`notes.md` 같은 gitignore 파일과 신규 파일 — `git diff`가 성립하지 않아 전재 폴백이므로 오탐이 그대로다(**오탐 사례를 그 문서에 기록하면 그 기록이 다시 오탐을 만드는 재귀**도 여기에 속한다) ⓑ **추적 파일도 커밋 전까지는 반복 경고된다** — 스캔 범위가 `git diff HEAD`(HEAD 대비 **미커밋 추가분 전체**)라 "이번 편집분"만이 아니어서, 시크릿 형태를 새로 적은 파일은 **커밋할 때까지 매 저장마다 같은 경고**가 난다(F-7 m3 지적 — 줄어든 것은 *이력에 이미 있는* 내용의 재신고뿐이다) ⓒ 커밋 경로의 동일 클래스 결함(**위** 별도 항목 — 단 그 성질은 오탐이 아니라 **미탐**이다). 완화 후보는 `Test-WarnOnce`(세션·파일당 디듑) 확대이며, 탐지 규칙에 "설명 문맥 제외"를 넣는 방향은 **기각**했다(미탐 위험 + `decisions.md:32` 차단 완화 기각 선례). 원 서술: 시크릿 규칙·예시를 *서술하는* 문서가 저장할 때마다 자기 자신을 오탐으로 잡는다(v1.146.0에서 `plan-template.md`·`deferred.md`·`run-hook-evals.ps1` 3파일 관측). 더 나쁜 것은 **그 오탐 사례를 문서에 기록하면 그 기록이 다시 오탐을 만든다**는 점이다(plan.md에서 실측). 탐지 규칙에 "설명 문맥(❌/✅ 대비·코드펜스 안 예시) 제외"가 없는 한 반복된다. (출처: v1.146.0 T2·T5 · notes 기존 항목의 확장)
- [2026-07-25] **Opus 5 신규 도구 표면의 hook 커버리지** — `Artifact`(파일 내용을 claude.ai에 게시 = 외부 전송)·MCP 도구가 `hooks.json` matcher(`Write|Edit|MultiEdit|NotebookEdit`, `Bash|PowerShell`) 어디에도 안 걸려 시크릿 유출 경로가 무방비. `Artifact`는 tool_input 구조가 달라 기존 hook 재활용이 안 되고 얇은 hook이 하나 더 필요 — 실제로 Artifact를 쓰는 워크플로가 생길 때 착수. (출처: Opus 5 하니스 점검) **(2026-07-31 재확인: 유지 — `hooks.json` matcher는 여전히 4종(`Bash|PowerShell` · `Write|Edit|MultiEdit|NotebookEdit` · `startup|resume|clear` · `+compact`)이고 `Artifact` 매치 0건. 전제·조건 모두 불변)**
- [2026-07-24] **Hook 골든 러너 실행 시간(전체 19분/413케이스)** — 각 케이스가 pwsh를 콜드스타트해 케이스 수에 선형 비례. 3줄 변경에도 전체를 돌려야 한다(부분 실행 판정 불가 규정). pwsh 프로세스 재사용·병렬 실행·변경 파일 기반 선택 실행으로 검증 비용을 변경 규모에 비례시킬 것. (출처: v1.138.0 T5) **(2026-08-02 재확인: 유지 — 개선 미실시. 수치는 계속 나빠진다: 413케이스/19분 → 449케이스/약 32분 → **505케이스/30분 32초**(v1.150.0 실측). 러너에 케이스 수·시간 하드코딩은 없어 코드 변경 없이 수치만 갱신. **이번 회차에 새 비용이 드러났다** — 전체 실행이 30분대라 세션의 백그라운드 대기 task가 timeout·kill될 때 **골든 프로세스가 함께 죽는 것이 2회 관측**됐다(19분·64분 지점). 재실행이 곧 30분 재소요라, 검증 비용이 실행 시간뿐 아니라 **완주 실패 리스크**로도 나타난다)**
- [2026-07-14] **문자열 형태의 상위 탈출(`..`)·`/.*` 미탐** — `Remove-Item -Recurse -Force "$env:APPDATA\..\..\.."`(드라이브 루트로 상승)·`rm -rf "/.*"`이 현행에서 통과한다(실측 PASS). v1.121.0의 `Join-Path` 정규화가 만든 구멍이 **아니다** — 조인 조건 ②③이 이런 자식을 걸러 `Join-Path` 경로에서는 차단 유지되며, 남은 것은 원래부터 뚫려 있던 문자열 형태다. `$dangerTarget`에 `\.\.`·`/\.?\*` 성분을 추가하는 강화는 정상 상대 경로(`../build` 정리) 오탐과 트레이드오프가 있어 별도 검토 필요. (출처: v1.121.0 plan Deferred / plan-reviewer 2회차 M1) **(2026-07-30 재확인: 유지 — 대상 코드·정규식 무변경 grep 실측)**
- [2026-07-14] **`$env:` 별칭 시스템 루트의 깊이 cap 미탐** — `$env:SystemRoot`·`$env:SystemDrive`·`$env:ProgramFiles`·`$env:windir`처럼 **환경변수가 시스템 경로를 가리키는 경우**, 그 하위 삭제(`"$env:SystemDrive\Windows"`·`(Join-Path $env:SystemDrive "Windows")`)가 통과한다. `$dangerTarget`이 `$env:NAME`을 리터럴 토큰으로만 보고 **값이 무엇인지 모르기 때문**(깊이 cap의 구조적 한계 — v1.98.0 승인 설계). **v1.121.0이 만든 구멍이 아니다**: 등가 문자열 형태가 수정 전에도 이미 통과했고(실측), Join-Path 형태가 그와 대칭이 된 것뿐이라 실질 공격면 증가는 0이다. 다만 37케이스 매트릭스·골든 어디에도 이 축이 없어 의식적 판단 없이 통과했다. 위 `..` 미탐 항목과 **같은 성격**(정규식이 값이 아니라 표기만 본다)이라 함께 검토. (출처: v1.121.0 F-7 m1 — 리뷰어 독립 프로브) **(2026-07-30 재확인: 유지 — 대상 코드·정규식 무변경 grep 실측)**
- [2026-07-14] **`Join-Path` 3인자·중첩 괄호·역순 명명 인자의 오탐 잔존** — `(Join-Path $env:TEMP "claude" "x")`(PS6+ AdditionalChildPath)·`(Join-Path $env:TEMP (Get-Date -F yyyy))`·`(Join-Path -ChildPath "x" -Path $env:TEMP)`는 정규화 정규식에 매치되지 않아 무변경 → 현행 차단이 유지된다(정상 작업이 막히는 오탐이지만 **오차단 방향이라 안전**). 골든에 `[알려진 오탐 — 3인자 미커버]` 라벨로 현행 동작만 고정해 뒀다. 실제 오차단이 관측되면 커버 확대. (출처: v1.121.0 Out of Scope / D5) **(2026-07-30 재확인: 유지 — 대상 코드·정규식 무변경 grep 실측)**
- [2026-07-13] **DB 연결 문자열 패턴이 흔한 실제 형태를 미탐** — `secret-patterns.ps1`의 `(Server|Data Source)=[^;]+;\s*(User|Uid|Password|Pwd)=`는 `Server=` **바로 뒤에** 자격증명 키가 와야 매치한다. 실제로 흔한 `Server=x;Database=y;User Id=sa;Password=z;`(중간 키 + `User Id=` 공백 표기)는 **불매치**(저신뢰 `password 값` 경고만). 이 라벨이 고신뢰 **차단** 등급이라 실효성이 걸린 문제. `[^;]*(;[^;]+)*` 류로 중간 키 허용 + `User\s*Id` 표기 수용 검토 — 오탐 회귀(정상 설정이 연결 문자열로 오인)를 골든으로 함께 확인할 것. (출처: v1.119.0 T2 골든 FAIL 조사 / F-7 M1) **(2026-07-30 재확인: 유지 — 대상 코드·정규식 무변경 grep 실측)**
- [2026-07-13] **도메인형 비밀번호 미탐** — `Test-CredentialPairToken`의 파일명 배제(마지막 `.` 뒤 2~5 영문자)가 pw 토큰에도 적용돼 `Secret1.io`·`Pw123.dev` 류를 놓친다. **의도적 트레이드오프**(pw에서 배제를 빼면 `계정: admin / config2.yml` 류 파일명 쌍이 오탐되고, 이 라벨은 커밋 차단 기준이라 오탐 1건이 자율 루프를 세운다). 엔트로피·사전 대조 등 더 정밀한 판별 근거가 생기면 재검토. (출처: v1.119.0 T1 quality M1) **(2026-07-30 재확인: 유지 — 대상 코드·정규식 무변경 grep 실측)**
- [2026-07-13] **자격증명 쌍의 나머지 미탐 형태** — 마크다운 **표 형태**(`| admin | pw |`)는 라벨 0건(경고조차 없음), **줄 분리형**(`- 아이디: admin` / 다음 줄 `- 비밀번호: pw`)은 `password 값` 경고만. 표·줄분리는 문서에서 흔한 표기라 차단 등급 확장 시 오탐 위험이 크므로, 경고 계층(post-write)에서 먼저 데이터를 모은 뒤 결정할 것. (출처: v1.119.0 F-7 M1 실측) **(2026-07-30 재확인: 유지 — 대상 코드·정규식 무변경 grep 실측)**
- [2026-07-13] **비인용 자격증명 쌍(경고 라벨)의 숫자 접미 열거형 오탐** — `Account 상태: active / suspended_30d`·`Login 실패 코드: AUTH_401 / AUTH_403` 같은 **상태·에러코드 열거**가 pw 토큰 요건(6자 + 숫자)을 만족해 `자격증명 쌍(비인용)` 경고를 받는다. 차단이 아니라 경고라 루프는 안 서지만, 이 라벨은 **편집 시점 hook이 모든 파일 쓰기마다 발동**하므로 늑대소년화 대상이다(D9가 경계한 바로 그 실패 모드). pw 토큰 요건을 "숫자만으로는 부족, 특수문자 필요" 쪽으로 좁히거나 `<영문>_<숫자>` 접미 패턴을 배제할지 검토 — 단 실제 비밀번호 중 숫자만 든 것도 흔하므로 미탐 트레이드오프를 함께 볼 것. 부수: `account: \`svc-runner\` / \`deploy-2024\`` 같은 서비스 계정·배포 태그 표기가 **고신뢰(차단)** 판정을 받을 여지도 확인 필요. (출처: v1.119.0 F-7 2회차 m1) **(2026-07-30 재확인: 유지 — 대상 코드·정규식 무변경 grep 실측)**
- [2026-07-13] **한 호출 안에서 파일 생성 + 즉시 커밋은 여전히 통과** — `cat > R.md <<EOF … EOF; git add -A && git commit`은 hook 시점에 **파일이 아직 없어** exit 0이다(PreToolUse의 구조적 한계). 다만 **heredoc 본문이 command 문자열에 그대로 있으므로**, 명령 텍스트 자체를 `Get-SecretMatches`에 통과시키면 저비용으로 닫힌다. 오탐(명령에 시크릿 '언급'만 있는 경우) 위험을 골든으로 확인한 뒤 도입 검토. (출처: v1.119.0 F-7 2회차 m2) **(2026-07-30 재확인: 유지 — 대상 코드·정규식 무변경 grep 실측)**
- [2026-07-13] **커밋 게이트 스캔 상한** — `Invoke-WarnCommitSecrets`의 선행 `git add` 스캔이 파일 50개·1MB 상한을 둔다(hook 지연 방지). 상한 밖 파일은 미스캔이라 이론상 미탐 가능. 커밋 직전 신규 파일이 50개를 넘는 경우는 드물고 그때도 `--cached`/`diff HEAD` 경로는 작동하나, 상한 도달 시 경고를 남길지 검토. (출처: v1.119.0 T9) **(2026-07-30 재확인: 유지 — 대상 코드·정규식 무변경 grep 실측)**
- [2026-07-10] 골든 러너 `-Filter "a,b"` 단일 문자열 바인딩 시 콤마 split 안 됨(배열 전달만 동작) — 기존 동작, 다음 러너 정비 때 (출처: v1.112.0 T1 quality 리뷰 m2) **(2026-07-30 재확인: 유지 — 대상 코드·정규식 무변경 grep 실측)**
- [2026-07-13] 비표준 체크박스 문자(`- [-]` 취소·`- [~]`) 표기만 쓰는 외부 레포는 `$planTaskRx`(` `/`x`/`X`/`/`만 인정)에 무매치라 `docs/plans/`가 있어도 plan 판정 OFF → 코드 Write 전면 차단(종전엔 통과). 진단 문구로 회복 가능하나 문자 클래스 확장 검토 (출처: v1.118.0 F-7 m3) **(2026-07-30 재확인: 유지 — 대상 코드·정규식 무변경 grep 실측)**
- [2026-07-13] `$isInPlansDir` 정규식이 선행 구분자 필수(`[\\/]docs[\\/]plans[\\/]`)라, 상대경로 `docs/plans/x.md` Write가 오면 게이트 미발동인데 `Test-PlansDirHasPlan`은 그 파일을 plan으로 인정 → 게이트/판정 비대칭. Claude Code Write는 절대경로 계약이라 현 재현성 낮음(기존 AGENTS 게이트도 동일 가정) — 차기 hook 정비 때 `(^|[\\/])` 분기 추가 검토 (출처: v1.118.0 F-7 m4) **(2026-07-30 재확인: 유지 — 대상 코드·정규식 무변경 grep 실측)**
- [2026-07-08] block-destructive: `cat <<EOF > file`(데이터 싱크 스트립) 후 같은 Bash 호출에서 즉시 실행 시 위험 본문 미스캔 — "파일 작성+동일 호출 실행" 조합 감지 개선 (출처: harness-quality-part1) **(2026-07-30 재확인: 유지 — 대상 코드·정규식 무변경 grep 실측)**
- [2026-07-08] suggest-agents-record 명령 오인 오탐 2류 — ① 커밋 -m 값 속 명령 문자열 ② grep 패턴 문자열 속 "dotnet build" — hook 이벤트 로그 데이터 축적 후 근거 기반 수정 (출처: harness-quality-part1·v1.101.0) **(2026-07-30 재확인: 유지 — 대상 코드·정규식 무변경 grep 실측)**
- [2026-07-08] .state 디듑 마커(post-write-warn·require-plan-warn·suggest-agents-record) 누적 정리 정책(TTL/청소) 부재 (출처: harness-quality-part1) **(2026-07-30 재확인: 유지 — 대상 코드·정규식 무변경 grep 실측)**
- [2026-07-08] (저신뢰 audit) block-destructive 열거 `.` 제외의 이름필터 판정에서 `-Include *.tmp,*` 콤마 배열 catch-all 원소 통과 여지 — 차기 hook 정비 때 감사 케이스로 검토 (출처: harness-quality-part1 F-7) **(2026-07-30 재확인: 유지 — 대상 코드·정규식 무변경 grep 실측)**
- [2026-07-10] warn-external-ops가 `gh release create --notes "…git merge…"` 노트 값 속 텍스트를 오탐 — 커밋 `-m`/`--message` 값은 스트립하나 `--notes` 값은 미스트립이라 노트 본문의 push/merge/tag 텍스트가 규칙에 걸림(비차단), `--notes`(및 유사 값 옵션) 스트립 또는 `git ` 접두 요구로 개선, 다음 hook 오탐 정비 라운드에서 (출처: v1.113.1 릴리즈 발행 시 실관찰) **(2026-07-30 재확인: 유지 — 대상 코드·정규식 무변경 grep 실측)**
- [2026-07-20] **`protect-harness` 한글 경로 골든 부재** — v1.129.0 T2의 stdin UTF-8 수정으로 protect-harness도 한글이 든 보호 경로를 비로소 정확히 매치하게 됐다(성격상 이 훅에서도 미탐 보완). 그런데 `hook-cases.json`에 protect-harness 케이스가 0건이고 러너 내장 시나리오도 전부 ASCII 경로라, **AGENTS.md:72가 미탐 보완에 요구하는 실증이 protect-harness에는 없다**. 한글 경로 시나리오 1건 추가 검토. (출처: v1.129.0 F-7 m3) **(2026-07-31 재확인: 유지 — `hook-cases.json`의 protect-harness 케이스 여전히 **0건**, 러너 내장 시나리오 53건은 전부 ASCII 경로(한글 매치 0). 실증 공백 그대로)**
- [2026-07-20] **`\rm` 델타 음성 케이스의 토큰 일반성 미실증** — 새 앞 경계는 `$delCmdAlt` 7토큰(`rm|Remove-Item|ri|rmdir|rd|del|erase`) 전체에 적용되는데 델타 음성 2건은 둘 다 `\rm` 형태다. `C:\del`·`C:\rd`로 끝나는 경로도 새로 매치된다. 경계 로직이 토큰 무관 알터네이션 공유라 구조 논증으로는 충분하나(F-7 m2 판정), `Get-ChildItem C:\del` 1건으로 토큰 일반성을 못박는 것을 검토. (출처: v1.129.0 F-7 m2) **(2026-07-31 재확인: 유지 — 골든에 `\rm` 외 토큰(`del`·`rd`·`ri`·`erase`)의 델타 음성 케이스 **0건**. 구조 논증만 있는 상태 그대로)**
- [2026-07-23] **AGENTS.md 주입 스냅샷 정합** — 주입분은 세션 시작 시점 스냅샷이라 세션 중 AGENTS.md를 편집하면 컨텍스트의 전문이 구버전으로 남음. 실제 혼동이 관측되면 post-write-checks에 "AGENTS.md 편집됨 — 주입 스냅샷은 구버전" 경고 추가 검토 (출처: v1.135.0 plan Deferred) **(2026-07-31 재확인: 유지 — `post-write-checks.ps1`에 `AGENTS` 언급 **0건**(경고 미추가). 발동 조건인 "실제 혼동 관측"도 아직 없음. **단 이번 세션의 T5가 AGENTS.md를 편집할 예정이라, 그 시점부터 이 세션의 주입 스냅샷은 구버전이 된다** — 이 항목이 말하는 상황이 실제로 발생하는 첫 사례가 되므로, T5 수행 후 혼동 여부를 관찰해 이 항목의 발동 조건("실제 혼동 관측")을 재평가할 것)**
- [2026-07-23] **session-context.ps1 안전 읽기 패턴 공통화 검토** (**[2026-07-30] 갱신 — vault config 읽기 추가로 4번째 사용처 발생**, 문턱 초과 폭 확대. v1.146.0에서는 의도적으로 인라인 유지: 4곳의 후처리가 정규식 매치·JSON 파싱·바이트 상한 분기로 서로 달라 공통부가 2줄뿐이며, 공통화는 hook 3곳을 함께 손대는 별건이라 이 항목에 유지) — `Test-Path` + `try Get-Content -Raw` 패턴이 AGENTS.md 주입 추가로 3곳(plan·notes·AGENTS) 도달, 공통화 문턱(3회) 충족 — 다음 session-context 정비 때 `Read-TextFileSafe` 로컬 헬퍼 추출 검토 (출처: v1.135.0 T1 quality SUGGEST) **(2026-07-31 재확인: 유지 — `session-context.ps1`에 `Read-TextFileSafe` **0건**(미추출). 2026-07-30 갱신분(4번째 사용처·인라인 유지 판단)이 현행 상태이며 그 이후 변화 없음)**
- [2026-07-23] **warn-commit-secrets 신규 추적 파일 분기가 50파일 스캔 캡 범위 밖** — `git add` 경로 나열 시 추적 파일은 파일당 git 서브프로세스 2회(`ls-files`·`diff HEAD`)를 개수 제한 없이 호출, 대량 나열의 드문 케이스에서 지연 가능(기능은 정확·실질 영향 낮음 — 서브프로세스가 전체 정독보다 가벼움). `$scanned` 카운터 공유 또는 별도 상한 검토 (출처: v1.136.0 T1 quality m1) **(2026-07-31 재확인: 유지 — 지적 그대로다. `bash-hook-lib.ps1`의 `$scanned` 상한 50은 **untracked 루프에만** 걸려 있고(`:264-276`), 추적 파일 분기(`:246-248` — `git ls-files -- $rt`로 추적 여부를 보고 `git diff HEAD` 호출)는 **개수 제한 없이** 돈다. 주석(`:261-263`)도 "파일 수·크기 상한"을 그 루프 기준으로만 서술한다. *(`:250-258`은 추적 파일 분기가 아니라 untracked `else` + 디렉터리·글롭 분기다 — 2026-07-31 spec 리뷰 M1로 인용 정정)*)**
- [2026-07-23] **골든 러너가 세션 env `CLAUDE_HARNESS_ALLOW_SECRET`를 격리하지 않음** — ALLOW_SECRET=1 세션(이 값이 사전 조건인 구현 세션 포함)에서 전체 실행 시 차단 기대 케이스 4건이 우회 완화되어 거짓 FAIL(실측 401/405). 러너 서두에서 초기화 검토 — 이번 세션은 하위 pwsh에서 변수 해제로 회피 (출처: v1.136.0 구현 중 실관찰) **(2026-07-31 재확인: 유지 — 러너 서두(`run-hook-evals.ps1:1-40`)에 `CLAUDE_HARNESS_ALLOW_SECRET` 초기화 **없음**. 이 변수를 다루는 곳은 개별 케이스뿐이고(`:1202` set → `:1205` `$null`), 세션 env가 `1`이면 그 밖의 차단 기대 케이스가 여전히 완화된다. v1.148.0의 449/449 완주는 그 세션에 변수가 없었기 때문이지 구조가 고쳐진 것이 아니다)** **(2026-08-03 재확인: 유지 — v1.152.0 T5 전체 실행에서 **세 번째로 실현**됐다. T1 커밋이 `warn-commit-secrets`에 막혀 사용자가 세션 전에 설정한 값이 자식 hook까지 상속됐고, 20분짜리 실행 1회를 버렸다. **총계는 정확히 513인데 FAIL만 4건**이라 원인을 모르면 코드 회귀로 오진한다는 것이 이 결함의 실질 위험이다. 위 재확인이 지목한 `:1202`/`:1205`는 러너 분리 후 `scenarios/warn-commit-secrets.ps1:137-140`이다. 대책은 그대로 — 격리 블록에 `$env:CLAUDE_HARNESS_ALLOW_SECRET = $null` 1줄, 그리고 hook이 환경변수로 동작을 바꾸는 지점을 전수로 훑어 같은 성격의 변수가 더 있는지 확인. **세 번 관찰되고도 1줄이 안 들어간 이유는 매번 "이번 범위 밖"이었기 때문이다 — 다음에 러너를 손대는 작업이 반드시 함께 처리할 것.**)**
- [2026-07-31] **[SUGGEST] `$rxManualAsk`(④ 수동 실행 요청)의 인위적 구성 오탐 1건** — *"…대신 확인 절차를 자동화해두면 다음에 편하게 **주시면 좋**겠다는 의견입니다"* 처럼 ⓐ`대신`→ⓑ`확인`→ⓒ`주시면 좋`이 우연히 어순대로 배열된 문장은 매치된다. 실제 자율 루프 응답에서 나올 가능성이 낮은 구성이라 v1.149.0에서는 수용했고, **오차단이 실제로 관측되면** ⓐ를 복합구(`여기서 직접`·`한번 대신`)로 좁힌다. 미탐 방향(2인칭 지칭형·어순 역전)은 의도적 트레이드오프이므로 이 항목의 대상이 아니다. (출처: v1.149.0 T2 quality S1) **(2026-08-04 v1.154.0: 실효 범위 축소 — 4정규식은 폴백(루프 비활성) 경로 전용이 됐다. 루프가 도는 중에는 이 오탐 구성이 매치돼도 판정 결과가 같으므로(어차피 차단) 오차단 증가로 이어지지 않는다.)**
- [2026-07-27] **`deferred.md` 자체가 시크릿 경고를 상시 유발(오탐)** — 이 대장의 L40·L42가 `secret-patterns.ps1`의 탐지 정규식(`Password` 등)과 예시(`| admin | pw |`)를 **서술**하고 있어, 파일을 편집할 때마다 post-write-checks가 "password 값"·"자격증명 쌍(비인용)"을 경고한다. 실제 자격증명은 0건이며 기존 항목 L40·L42는 **미탐** 개선 후보인 반면 이것은 **오탐**이라 별개다. 탐지 규칙을 논하는 문서가 자기 자신을 잡는 구조 — 경고 피로가 쌓이면 진짜 경고를 흘려보내게 되므로, 규칙 문서·대장 경로 예외나 인용 문맥 판별을 검토. (출처: v1.143.0 F-6.5 실관찰)
- [2026-08-03] **`AGENTS.md` 주입 상한 여유가 21B뿐이다** — 16,363 / 16,384B. v1.152.0 T4의 검증 매핑 1행이 65B를 썼고(경로 접두를 빼 12B 회수한 뒤), 계획 시점 86B였던 여유가 그만큼 줄었다. **사실상 다음 한 문장이 곧 초과**이며, 넘으면 세션 시작 시 전문 주입이 목차 폴백으로 떨어져 프로젝트 가이드가 전달되지 않는다(v1.151.0에 1B 초과 실례). **항구 대책**: 검증 매핑 표를 `docs/hook-conventions.md`로 옮기고 AGENTS엔 포인터만 둔다(표는 이미 그 문서와 축이 같다). **그때 「골든 부분 실행의 판정 자격」 예외 포인터도 함께 넣을 것** — 그 예외의 성립 조건 ⓐ가 "plan에 미리 적는다"인데, plan을 쓰는 시점에 읽히는 문서는 세션에 전문 주입되는 `AGENTS.md`이고 현재 그 파일에는 예외로 이어지는 경로가 없다(여유 21B라 이번엔 포인터를 못 넣었다 — F-7 n2). 그 전까지 **AGENTS.md를 손대는 작업은 편집 전에 바이트부터 잴 것.** (출처: v1.152.0 T4 · F-7 n2) **[2026-08-03 병합 — 같은 병목의 세 번째 항목]**: 검증 매핑 표에 **「골든 러너가 `implement-task/SKILL.md`를 입력으로 읽는다」**는 의존도 함께 넣어야 한다 — 표는 `skills/**`를 "그 외 → Build + Test"로 분류하지만 `hooks/evals/scenarios/require-evidence.ps1`의 L12 블록이 그 SKILL.md를 열어 **금지 표현 ②③④⑤ 절의 문구 목록을 파싱**한다. 즉 **hook을 한 줄도 안 고쳐도 그 파일 편집이 골든을 깨뜨릴 수 있는데 매핑 표만 보면 골든이 필요 없어 보인다**(v1.153.0 T4가 그 사실을 알고 절을 건드리지 않는 설계를 택했으나, 그 지식이 문서가 아니라 plan에만 있었다). 여유 21B라 이번에도 못 넣었고, 표를 `hook-conventions.md`로 옮길 때 한 행으로 함께 추가한다. (출처: v1.153.0 T4 전제 검증 #1) **[2026-08-05 재확인 — 세 번째 실현]**: v1.155.0의 전제 검증이 *"골든 러너는 문서를 읽지 않는다"* 를 **거짓 전제로 확정**했다가 F-7 m2에 반증당했다. 즉 **이 항목의 지식이 대장에 2026-08-03부터 있었는데도 계획 단계가 조회하지 않아 같은 오판을 반복**했다(등재 위치: `scenarios/require-evidence.ps1:216`). 실피해는 없었다 — 그 절을 건드리지 않았고 **골든 전체 527/527 OK**로 실증했다(부분 실행이 아니라 전체 — `AGENTS.md:43`). 여유는 25B로 소폭 늘었으나 여전히 한 행이 안 들어간다. **추가할 행의 요구 수준**: 그 파일 편집 시 최소한 `-Filter require-evidence`는 돌리게 하는 것 — 다만 `AGENTS.md:43`이 **부분 실행을 검증 판정 근거로 금지**하므로, 매핑 행은 「골든 부분 실행의 판정 자격」 예외(같은 블록 위 문단)와 **함께** 들어가야 성립한다. 그 예외 없이 부분 실행만 요구하면 규칙끼리 어긋난다. **덧붙는 대책**: `## 전제 검증` 작성 시 이 대장 `## 대기`를 **반증 소스로** 훑는 규약을 세울 것(F-7 라운드2 Follow-up).
- [2026-08-03] **러너 분리 후속 정리 5건 (순수 이동 원칙 때문에 이번에 미룬 것들)** — v1.152.0 T1이 "이동만 하고 아무것도 고치지 않는다"를 판정 기준으로 삼아, 눈에 띈 정리를 전부 다음으로 넘겼다. ⓐ `§4`의 `foreach ($ph in $phraseList)` 루프 변수가 `§2b`의 디렉터리 변수와 **이름이 같다**(dot-source로 스코프 공유 — 현재는 실행 순서상 무해하나 파일이 갈려 충돌이 눈에 덜 띈다) → `$phrase`로 rename. ⓑ 러너의 실행 배너·부분 실행 경고 헤더가 블록 #1 범위에 있어 **`scenarios/stateless.ps1`에 종속**됐다 → 본체 헤더로 승격. ⓒ 본체 헤더 9행 *"② 이 파일의 시나리오 섹션"* 이 분리 후 문면상 어긋난다(바로 아래 ※ 2줄로 정정해 뒀다) → 9행 자체를 `② 시나리오 섹션(scenarios/*.ps1)`으로 정리. ⓓ 시나리오 헤더의 경계 표식 1줄은 재조립 등가 검사를 자동화하려고 넣은 것인데 규약으로 명문화돼 있지 않다(블록 본문도 `#`로 시작할 수 있어 주석 개수로는 헤더 끝을 못 잡는다). ⓔ `New-TranscriptLine`에 `-IsMeta`·`-ToolUseSkill`을 추가해 `$metaPayload`·`$skillEntry` 같은 나머지 리터럴 픽스처도 전환. (출처: v1.152.0 T1·T3)
- [2026-08-03] **`warn-commit-secrets`의 "이력에 이미 있는 내용" 면제가 파일 이동에는 적용되지 않는다** — 추적 파일은 `git diff HEAD` 추가 라인만 스캔해 자기참조 오탐을 막지만, **같은 내용을 신규 파일로 옮기면 untracked라 워킹트리 전체가 스캔**돼 기존 골든 시크릿 픽스처가 "신규 유입"으로 차단된다. v1.152.0 T1에서 실현됐고(차단 지점 `scenarios/post-write-checks.ps1:28-29` = 구 `run-hook-evals.ps1:1153-1154`), 사용자가 `CLAUDE_HARNESS_ALLOW_SECRET=1`로 우회해야 커밋이 됐다. **이 레포처럼 탐지 픽스처를 보유한 곳에서는 파일 분리·이동 작업마다 재발한다.** 대책 후보 ⓐ 신규 파일이어도 `git log -S`로 이력 실재를 확인해 면제 ⓑ 스캔 제외 경로(`hooks/evals/`) 도입 — 둘 다 차단 게이트 완화라 별도 계획·승인이 필요하다. (출처: v1.152.0 T1)
- [2026-08-03] **검증 자산이 자기 결함을 못 보는 두 형태 (v1.152.0에서 각각 실현)** — ⓐ **골든 green인데 픽스처가 깨져 있다**: T3에서 `New-TranscriptLine` 초안이 단일 요소 배열을 언랩해 `content`를 `[{...}]`가 아니라 `{...}`로 직렬화했는데, hook이 `@($mc)`로 감싸 읽는 덕에 **골든은 96/96 green**이었다. 검출한 것은 전환 전 리터럴과의 문자열 대조뿐 → **픽스처 생성 방식을 바꾸는 작업은 골든 통과를 등가의 근거로 쓸 수 없다.** ⓑ **판정 로직을 "재현"하는 도구가 과탐한다**: T4에서 `check-transcript-assumptions.ps1` 초안이 hook `require-evidence.ps1:392`의 구조 필터(`if (-not $isUser -and -not $isAsst) { continue }`)를 빠뜨려, skip 4종은 정확히 옮겼는데도 예산을 과소비해 **없는 DRIFT 5건을 "발견"으로 보고**했다(최댓값 200 → 필터 추가 후 1~2). → **pin 검사 대상은 "skip"이 아니라 "예산에 관여하는 판정식 전부"** 여야 하고, 재현형 도구의 첫 결과는 **원본 주석의 기존 실측치와 자릿수를 대조**해야 한다. (출처: v1.152.0 T3·T4)
- [2026-08-03] **폴백 경로 파싱 최댓값이 hook 주석의 실측치와 43배 다르다 — 어느 쪽이 맞는지 미확인** — `require-evidence.ps1`의 `[추출 원칙]` 주석은 skip 4종 적용 후 실측 최댓값을 **stdin 1 · 폴백 87**로 기록한다(v1.151.0). 그런데 v1.152.0 T4의 `check-transcript-assumptions.ps1` 전수 스캔은 **stdin 1 · 폴백 2**를 낸다 — **stdin은 1:1로 맞는데 폴백만 43배 낮다**. 한쪽이 정확히 일치할수록 다른 쪽 괴리를 데이터 차이로만 보기 어렵다. 가능성은 둘이고 함의가 정반대다: ⓐ **재현부가 폴백을 원본보다 얕게 잰다** → 가정 4가 예산 드리프트를 **못 잡는다**(도구의 핵심 기능 무력화) ⓑ **주석의 87이 stale하다** → 200 예산의 근거 서술이 틀렸다. **판별 방법**: v1.151.0이 쓴 전 시점 스캔 harness와 같은 세션 집합으로 양쪽을 돌려 대조하거나, 폴백 경로에서 `$parsed`가 큰 시점의 엔트리 구성(텍스트 없는 user 엔트리 비율)을 직접 세어 87이 나올 수 있는 데이터 형태인지 확인한다. **이 항목 자체가 T4가 세운 교훈 ⓑ("재현형 도구의 첫 결과는 원본 주석의 기존 실측치와 자릿수를 대조")를 200에는 적용하고 87에는 적용하지 않아 생겼다.** (출처: v1.152.0 F-7 M5)
- [2026-08-03] **`isMeta` 스키마 의존의 잔존분 (부분 해소 후 남은 것)** — 본 항목은 `## 종결`의 「`isMeta` 필드 의존의 구조적 한계」에서 **닫히지 않은 부분만** 떼어 대기에 남긴 것이다. v1.152.0 T4가 완화 후보 ⓑ(실 데이터 점검)를 `check-transcript-assumptions.ps1`로 자산화했지만 **근본 의존 자체는 그대로다** — 검사 4의 사용자 발화 추출은 여전히 Claude Code transcript의 비공식 내부 스키마(`isMeta` · `<task-notification>` 리터럴 · 원시 `"type":"text"` 표기)에 걸려 있고, 필드가 바뀌면 조용히 fail-open(또는 skip ②의 경우 그 반대 방향)이 된다. **골든은 픽스처를 우리가 만들므로 그 회귀를 못 잡는다.** 남은 관측 수단은 둘 — ⓐ `require-evidence.ps1` 수정 시 점검 스크립트 실행(`AGENTS.md` 검증 매핑에 배선됨) ⓑ 이벤트 로그에서 검사 4 차단 건수가 0으로 지속되는지 관찰(`report-hook-events.ps1`). **어느 쪽도 "주기적으로 도는" 장치가 아니라 사람이 기억해야 발동한다는 것이 잔존 위험의 핵심이다.** 종결 항목을 닫으면서 이 부분을 함께 닫으면 추적이 사라지므로 스텁으로 남긴다. (출처: v1.152.0 F-7 M2)

### 하니스 스킬 (implement-task · plan-feature · 리뷰어 · 템플릿)

- [2026-08-05] **`## 전제 검증`을 쓸 때 이 대장을 "반증 소스"로 훑는 규약이 없다** — v1.155.0의 전제 4(*"골든 러너는 문서를 읽지 않는다"*)를 **반증하는 지식이 `deferred.md:78`에 2026-08-03부터 있었는데** 계획 단계가 그것을 조회하지 않아 거짓 전제가 확정됐고, F-7에서야 반증됐다(그리고 같은 갭을 별건으로 중복 등재해 F-7 라운드2 MAJOR까지 받았다). **원인은 조회의 목적이 하나뿐이라는 것** — `plan-feature` Step 1의 대장 조회는 *"이번에 함께 처리할 할 일 후보"* 를 찾지, *"내가 세운 전제를 이 대장이 반증하는가"* 를 보지 않는다. 두 목적은 읽는 방식이 다르다(전자는 주제 매칭, 후자는 전제 문장별 역질의). 대책: `plan-feature`의 전제 검증 단계 또는 `plan-reviewer`의 전제 검증 항목에 *"각 전제에 대해 대장 `## 대기`에 반대 근거가 있는지 확인"* 축을 추가. (출처: v1.155.0 F-7 라운드2 M1 Follow-up)
- [2026-08-05] **리뷰어 4종 「재리뷰 규약」이 "행이 0개인 이력 표"를 규정하지 않는다** — 규약은 *"이력이 **전달되지 않은** 첫 호출에는 이 절이 적용되지 않는다"* 만 두는데, 형식상 전달됐으나 행이 비어 있는 표가 어느 쪽인지 문면으로 갈리지 않는다. **실해는 사실상 없다** — 되돌릴 이전 지적이 0건이면 어느 해석이든 동작이 같다. 다만 네 파일이 같은 문면을 복제 보유하는 규약이라 고칠 때 넷을 함께 고쳐야 하므로, 다음 리뷰어 개정 task에 묶는다. (출처: v1.155.0 F-7 m6 / 판정 유보 — T1 Edge Case *"이력 표가 비어 있음 → 첫 호출과 동일 취급"* 이 산출물에 흔적을 남기지 못한 건)

- [2026-08-05] **acceptance에 "특정 표현·리터럴의 부재"를 조건으로 걸면 작업과 동시 만족이 안 될 수 있다** — 한 plan에서 **두 번 연속** 정정이 필요했다: T2의 *"카운터 리터럴이 있는 **행** diff 0줄"*(카운터 열거 동기화가 곧 그 행 편집이라 모순) → 값 보존으로, T3의 *"「코드 라인 0줄」 **표현**을 쓰지 않는다"*(재도입을 막는 경고문까지 금지) → 채택 금지로. **문자열 부재는 기계 검증이 쉬운 대신 의도를 담지 못한다.** 다음 plan부터 acceptance를 **판정 축**(그 기준을 채택했는가 · 그 값이 보존됐는가)으로 쓰는 관행을 검토한다. (출처: v1.155.0 T3 V-5 MINOR·판정 유보)
- [2026-08-05] **증분 재리뷰 판정의 기계화** — v1.155.0이 신설한 D4 3단계는 메인이 `review-fix` diff의 성격을 **문서 규칙으로** 판정한다(① 순수 표현 생략 / ② 파일 한정 / ③ 전량). 오판이 관측되면 기계 판정 보조(변경 라인이 전부 주석·공백인지 자동 검사 등)를 검토한다. (출처: v1.155.0 plan Deferred)
- [2026-08-05] **`[CONFLICT]` 발신이 리뷰어의 자기 인지에 의존한다 (설계 한계 — 결함 아님)** — 리뷰어가 이전 지적과의 상충을 알아채야 발신되므로, 못 알아채면 그대로 진동한다. `[CONFLICT]` 판정을 리뷰어에 맡긴 D2의 필연적 귀결이며 대안(메인의 기계 대조)은 그 시점에 기각했다. 1차 방어는 이력 표 전달, 2차는 소비 게이트·2회 Halt. **미탐이 실제로 관측되면** 메인 측 대조(이전 지적 ↔ 이번 지적의 파일:라인 교집합)를 재검토한다. (출처: v1.155.0 plan Deferred)
- [2026-08-05] **Retry Ledger 기록 포맷 문자열이 두 곳에 리터럴 중복** — `CONFLICT <지점> N회 — <채택|유지> <근거 1줄>`이 `implement-task/SKILL.md` V-5(정본)와 `references/recovery.md` 양쪽에 글자 그대로 있다. 지금은 일치하지만 한쪽만 고치면 드리프트가 난다. 리뷰어 판정은 *"이 repo가 이미 취하는 '정본 포인터 + 요약 재기술' 관례와 같은 패턴"* 이라 결함으로 보지 않았다 — 다음 정비에서 `recovery.md`를 포인터로 축약할지 판단한다. (출처: v1.155.0 T2 V-6 MINOR·판정 유보)
- [2026-08-05] **리뷰어 effort를 `xhigh`까지 올릴지** — v1.155.0이 코드 리뷰 2종(`spec-compliance`·`code-quality`)을 `medium`→`high`로 올렸다(Opus 5 공식 권장 기본값). 공식은 **코딩·에이전틱에 `xhigh`**를 권하지만 Sonnet·`maxTurns: 30` 조합에서 실익이 불확실해 한 단계만 올렸다. **효과는 리뷰 라운드 수로 측정**해 판단한다. (출처: v1.155.0 plan Deferred)
- [2026-08-05] **Opus 5 대응표 잔여 2건 — PRD 변경 선행 필요** — 사용자 제공 이미지 「덜어내면 빨라져요」의 5항목 중 ⓐ *"다시 검토·double-check 지운다"*(→ `SKILL.md` V-8 자기점검 11개·`phase-f-detail` F-6 제거) ⓑ *"검증용 서브에이전트를 작은 작업엔 안 붙인다"*(→ Type 판정의 *"애매하면 D"* 완화)가 미반영이다. **PRD `FR-6`이 *"재시도 카운터·상한·V-8 체크박스는 불변"*으로 고정**했으므로 PRD 변경 승인이 선행돼야 한다(plan-feature Step 0.5 — PRD → plan → 코드 순). v1.155.0의 효과를 측정한 뒤 별도 plan으로 제안. (출처: v1.155.0 plan Out of Scope)

- [2026-08-04] **루프 중 사용자가 개입한 뒤 재개하면 화이트리스트가 비활성으로 남는다** — `$loopActiveAfterUser`는 "마지막 사용자 발화 **이후**에 스킬 발동 엔트리가 있는가"로 판정하므로, 루프 도중 사용자가 무언가 말하고(예: *"지금 뭐 하는 중이야?"*) 모델이 답한 뒤 **스킬 재발동 없이** 루프를 이어가면 그 이후 구간은 계속 거짓이라 4정규식 폴백으로 내려간다. **미탐 방향(fail-open)이라 안전측**이고, 사용자가 "계속"이라고 하면 대개 스킬이 재발동돼 게이트가 다시 켜진다. 그 구간의 정지가 실제로 관측되면 "재개 신호"를 무엇으로 볼지(예: plan.md 체크박스 변경 커밋) 별도 검토한다. (출처: v1.154.0 plan Deferred)
- [2026-08-04] **tail 상한(6000)을 넘는 초장기 루프는 여전히 fail-open** — 실측 최대가 2817줄이라 6000은 2배 여유지만 상한이 사라진 것은 아니다. 넘으면 루프 시작 지점이 창 밖으로 밀려 `$userFound`·게이트를 함께 잃는다. `$loopSkill`처럼 전 파일 스캔으로 올리는 안은 hook 10초 예산에서 `ConvertFrom-Json` 비용이 위험해 채택하지 않았다 — 다만 v1.154.0의 `Get-TranscriptTail`(`-Raw` 기반)로 읽기 비용이 90배 낮아졌으므로 **재평가할 여지가 생겼다**(전 파일 로드 자체는 47ms). (출처: v1.154.0 plan Deferred + D7)
- [2026-08-04 → 2026-08-04] **게이트 오점화 — 실제 표면은 "다른 도구의 input"이었다** — **해소(v1.154.0 T6/D8)**. 등재 당시 이 항목은 "text와 tool_use가 공존하는 혼합 엔트리"를 잔여로 지목했으나, **전수 재분석에서 규모와 성격이 둘 다 달랐다**: 리터럴을 가진 assistant 엔트리 중 진짜 Skill 발동은 59건이고 **32건이 다른 도구의 input**이었다(Edit 11 · Write 10 · Bash 8 · Agent 2 · PowerShell 1). **그 32건도 전부 `tool_use` 동반**이라 D6의 "tool_use 동반 요구"로는 하나도 걸러지지 않았다 — 즉 잔여가 아니라 **주요 표면이 통째로 뚫려 있었다.** 해소는 후보 줄을 파싱해 **`type=tool_use ∧ name=Skill ∧ input.skill`을 구조로 확인**하는 방식이고, 평문 패턴은 assistant 쪽이 전수 오점화라 제거했다(미탐 0). 실 데이터 대조에서 새 게이트는 정확히 **59건에만 점화**한다. **교훈: "83건 전건 tool_use 동반"이라는 참인 관측을 "83건이 전부 발동"이라는 검증 안 된 전제와 묶어 근거로 삼았다 — 관측을 분류 없이 세면 그 수가 무엇의 수인지 모른 채 결론이 된다.** (출처: v1.154.0 plan-reviewer 2회차 M5 → Phase F-4)
- [2026-08-04] **마커 규약이 기계로 강제되는 범위는 `implement-task` 루프뿐이다** — `$loopSkill`·`$loopActiveAfterUser` 두 신호가 모두 `pjc:implement-task` 발동 리터럴에만 반응하므로, `bootstrap-agents-md`의 아키텍처 확인·`llm-wiki`의 필수 사용자 게이트처럼 **다른 스킬의 사용자 개입 지점은 마커가 없어도 검사 4에 걸리지 않는다**(방향은 fail-open). SKILL.md 마커 규약이 *"implement-task 밖의 스킬에도 적용된다"*고 쓴 것은 **`plan-feature`처럼 같은 세션에서 루프 흔적을 공유하는 경우**를 가리킨다. **재평가 트리거**: 그 스킬들이 implement-task 루프 **중에** 호출되는 구조가 생기면 그때는 마커가 필요해진다 — 현재 구조에서는 발생하지 않아 이번 범위에 넣지 않았다. 사실 자체는 `docs/hook-conventions.md` 조건 3 마지막 불릿에 커밋돼 있고, 이 항목은 **추적 트리거**를 남기는 것이다. (출처: v1.154.0 T3 spec 리뷰 M2 → F-7 2회차 M1'로 이관 누락 발견)
- [2026-08-04] **[구조] "신규 N건 등재" 형태의 acceptance가 누락을 못 잡는다** — v1.154.0에서 **같은 뿌리의 사고가 두 번** 났다: ⓐ T5가 `AGENTS.md` 기준선을 513→523으로 옮겼는데 **T6가 케이스를 늘리고도 재적용하지 않았다**(F-7 1회차 B1) ⓑ T5 acceptance가 *"신규 4건 등재"*로 **숫자를 고정**해 둔 사이 T3·T4가 5번째 Deferred를 plan에만 추가해 대장·notes에서 빠졌다(F-7 2회차 M1'). 둘 다 **"조건이 나중에 재성립했는데 고정된 목록이 안 늘어난"** 패턴이다. 대안: acceptance를 숫자 대신 **"plan `## Deferred / Follow-up`의 미해소 전 항목 ↔ 대장 항목 단위 대조"**로 쓰면 등재 단계에서 걸린다. 같은 원리로 기준선류 수치는 **"변경 후 실측값과 문서값 일치"**로 표현한다. (출처: v1.154.0 F-7 1·2회차)
- [2026-08-04] **사용자가 범위를 "한정"한 지시가 `$userStop` 어휘를 빗나가면 이제 오차단이 된다** — `$userStop`은 중단·한정 어휘의 **블랙리스트**이고, 화이트리스트 전환으로 그것이 **마커와 함께 유일한 방어**로 승격됐다. 전환 전에는 목록에 없는 한정 표현(예 *"T5까지만"*)이 빠져나가도 문면이 4정규식에 안 걸리면 통과였지만, **이제는 게이트가 켜져 있으므로 문면 무관 차단**이라 사용자가 한정한 작업에 *"계속하라"*는 reason을 들이댄다 — 위키 규약이 *"미탐보다 나쁘다"*로 지목한 방향이다. v1.154.0에서 `T\d+\s*만` → `T\d+[^\r\n]{0,4}만`으로 넓혀 대표 형태는 닫았고 골든 L61-c로 고정했으나, **목록이 블랙리스트라는 구조는 그대로다**(예: *"3번까지 하고 멈춰"*는 `멈춰`가 있어 걸리지만 *"앞의 세 개만 해줘"*는 안 걸린다). 오차단이 관측되면 어휘를 넓히되 **항상 fail-open 방향**임을 확인할 것. **부수 성질(F-7 2회차 m3)**: `$userStop`이 한 번 참이 되면 **그 사용자 발화 이후 루프 전 구간**의 차단이 꺼진다(예 *"T5까지만"* 뒤 T2에서 조기 정지해도 무차단). 어휘를 넓히면 이 지속성이 더 많은 문장에 적용되므로, 확대 시 *"이 발화 하나로 남은 루프 전체의 방어를 끄는 것이 맞는가"*를 함께 판단할 것(종전 `T\d+\s*만`에도 있던 성질이라 회귀는 아니다). (출처: v1.154.0 F-7 M2 · 2회차 m3)
- [2026-08-04] **마커 전수 목록이 `hook-conventions.md`와 `implement-task/SKILL.md` 두 곳에 복제된다** — SKILL.md 자신이 *"전수 목록은 `docs/hook-conventions.md`가 정본 … 새 지점이 추가될 때 한 곳만 고치면 되게 한다"*고 선언하는데도 8곳을 나열하고 있어, v1.154.0 이전에 실제로 **정본 6곳 ↔ SKILL.md 8곳**으로 어긋나 있었다(정본이 더 좁은 역전). 이번엔 양쪽을 10곳으로 맞춰 증상을 없앴을 뿐 **구조는 그대로**다. **증상이 하나 더 있다(F-7 m1)**: 두 문서의 **갱신 지시가 서로 반대**다 — `SKILL.md`는 *"새 지점이 추가될 때 **한 곳만** 고치면 되게 한다"*, `hook-conventions.md`는 *"이 목록을 고치면 SKILL.md 예외 목록도 **같이** 고친다"*. 현재는 10곳 일치라 무증상이지만, 상충 문면을 두면 다음 편집자가 한 곳만 고쳐 역전이 재발한다. 대안: ⓐ SKILL.md를 대표 예시 + 정본 포인터로 축약(기존 서술의 가독성 손실 동반) ⓑ 두 목록 동기화를 골든·셀프체크로 기계 고정. **화이트리스트 전환으로 이 목록이 오차단 방어의 1층 전부가 됐으므로 우선순위가 올라갔다.** (출처: v1.154.0 plan-reviewer 2회차 m4 + 전제 #5-1)

- [2026-08-03] **`sed -i`·`printf`로 md·json을 편집하면 워킹트리가 CRLF → LF로 깨지는데 `git diff`는 비어 있다** — `core.autocrlf=true`라 커밋 blob은 LF 정규화가 정상이므로 **내용 diff가 0**이고, 유일한 신호는 `git commit` 시의 *"LF will be replaced by CRLF"* 경고 한 줄이다. v1.153.0에서 **두 번**(T5 이관 스크립트·T7 대장/README 편집) 재발했고 둘 다 그 경고로만 발견했다 — 흘려보내면 워킹트리만 어긋난 채 남아 다음 편집이 LF 기준이 되고, 도구에 따라 전체 파일 재작성 diff가 난다. **Edit 도구로 고친 파일은 두 번 다 멀쩡했다.** 대책 후보: ⓐ 작업 규약으로 "md·json은 Edit 도구 우선, `sed -i` 사용 시 CRLF 복구 동반" 명문화 ⓑ `.gitattributes`에 `* text=auto eol=crlf`를 두어 체크아웃·복원 기준을 고정. ⓑ는 레포 전역 영향이라 별도 검토. (출처: v1.153.0 T5·T7)
- [2026-08-03] **`## 동반 변경 판정`이 늘 "해당 없음"으로 채워지면 형식만 남는다** — v1.153.0 T1이 신설한 이 섹션은 판정 대상이 없을 때 `해당 없음 — <확인한 축 1줄>`을 허용한다(과잉 방지). 그래서 **실제로 확인 축 5개를 훑지 않고 그 한 줄만 적는 회피**가 가능하다. `plan-reviewer` 항목 16이 "근거 없는 해당 없음 → MAJOR"로 받지만, 리뷰어가 판정할 수 있는 것은 **근거의 존재**이지 **근거의 참됨**이 아니다. 몇 회 실사용 후 회피가 관측되면 "무관 판정에도 훑은 축을 명시" 같은 강화를 검토한다. (출처: v1.153.0 plan Deferred)
- [2026-08-03] **규칙 4-1의 커버리지 경계 — "기록조차 되지 않은" 자기 유발 결함** — F-4와 `plan-completion-reviewer` 항목 8은 **Deferred/Follow-up에 기록된 항목의 분류**를 검사하므로, 아예 적지 않고 넘어간 자기 유발 결함은 두 게이트에 걸리지 않는다. task 단위의 V-8 체크박스가 그 층을 받지만 **자기 점검**이라는 한계가 있다(T3 quality 리뷰어가 관찰로 지목했으나 "이번 diff가 만든 결함이 아니다"며 SUGGEST로도 올리지 않았다). 실사용에서 그 형태가 관측되면 완료 리뷰어에 **"diff 대비 미기록 결함 탐색"** 축을 추가할지 검토한다. (출처: v1.153.0 T3 V-6)
- [2026-08-03] **[MINOR·판정 유보] `halt-conditions.md` carve-out ⓐ의 세 항목이 왜 함께인지 설명 1구 부재** — carve-out ⓐ는 V-9 원본 미접근·F-8 미확인 잔존·컨트롤 대체 불가를 한 불릿에 묶었는데, 뒤따르는 근거 문장은 **원본 미접근에만** 해당한다(그것만 '계획 결함' 행 본문에 박혀 있다). F-8·컨트롤 대체는 표에서 별도 카테고리 행이라 「'계획 결함' 4행에만 적용」 범위 밖이며 **방어적 중복**으로 함께 적은 것이다. 다음 정비 때 그 사유 1구를 덧붙인다. (출처: v1.153.0 T2 V-6)
- [2026-08-03] **[MINOR·판정 유보] 4-E 확인 축 ③(검증 자산)과 기존 4-C(영향 받는 테스트)의 개념 겹침** — 4-C는 "변경 대상을 호출하는 테스트", 4-E ③은 "이 변경이 입력이 되는 검증 자산(eval·골든·루브릭)"으로 초점이 다르지만, 작성자가 실제로 구분해 적을지는 해석에 달렸다. 두 항목이 뒤섞이는 것이 관측되면 4-E ③의 문면을 "4-C가 다루지 않는 것"으로 좁힌다. (출처: v1.153.0 T1 V-6)
- [2026-08-03] **[MINOR·판정 유보] `plan-template.md`의 `## 동반 변경 판정` 주석만 Type 게이트에 침묵한다** — v1.153.0 T8이 적용 범위(Type C/D task가 있는 plan)를 3곳(4-E 본문·통과 체크리스트·`plan-reviewer` 항목 16)으로 정합시켰으나, **저자가 실제로 채우는 폼**인 템플릿 주석에는 언급이 없다. 같은 템플릿의 자매 항목(`### 전제 검증` 주석)은 "Type C/D는 의무, A/B는 생략"을 담고 있어 처리가 갈린다. 오지시는 아니지만(무언급) T8이 세운 "셋이 어긋나면 사각" 논리의 **4번째 자리**다. (출처: v1.153.0 F-7 n2)
- [2026-08-03] **[MINOR·판정 유보] `phase-v9-detail.md`의 `현재 값`·`일치` 열 규정 지칭이 이관 후 한 단계 우회한다** — 그 파일은 *"plan-feature Step 2.5가 시각 속성 표에 '현재 값' 열을 두지 않는 것과 같은 이유"*로 남았는데, 같은 규정을 가리키는 `plan-reviewer.md`는 *"규정 위치는 `references/visual-decomposition.md`"*로 갱신됐다. **진술 자체는 여전히 참**이고(Step 2.5라는 단계는 존재한다) 우회 1단계뿐이라 조치하지 않았다. 다음 정비 때 1줄 정리. (출처: v1.153.0 F-7 m5)
- [2026-07-30] **축약구 제거(D3)의 재검토 조건 — 결정을 되돌릴 유일한 안전판** — v1.147.0 T2가 판정 게이트 축약구를 7지점에서 제거해 각 지점에서 **"무엇을 확인해야 하는지"가 즉시 보이지 않게** 됐다(K 1을 여는 한 단계가 늘었다). 이 결정은 **직전 세션 quality 리뷰어의 "필요한 반복(문서들이 개별 세션에 로드되므로)" 판정을 뒤집은 것**이며, 사용자 선택으로 진행했다. **재검토 트리거**: 실사용에서 *게이트 내용을 몰라 확인을 건너뛰는* 사례가 관측되면 D3(제거)을 재검토한다 — 그때는 축약구를 되살리는 대신 K 1 포인터 옆에 **한 단어 요약**을 붙이는 중간안도 후보다. (출처: v1.147.0 T2·D3 / F-7 M1 — plan.md는 `.gitignore`라 이 한 줄이 없으면 되돌림 근거가 감사 경로에서 유실된다)
- [2026-07-30] **판정 게이트 포인터 문구의 문체 불통일 (선택)** — v1.147.0 T2가 축약구를 제거한 뒤 남은 포인터 표현이 지점마다 다르다("거친 확인 결과여야 한다" 4곳 · "따르고" 1곳 · "다.**" 1곳 · "거치지 않으면 고를 수 없다" 1곳). **의미론적 drift 위험은 아니다** — 포인터는 K 1의 내용을 담지 않아 K 1이 어떻게 바뀌어도 항상 참이다(plan-reviewer 판정). 순수 문체 다듬기라 우선순위 낮음.
- [2026-07-29] **요구 원본 ↔ FR 매핑이 산출물에 없다** — Opus 5 대응 plan의 `## 요구 이해`가 "P0~P10 개선안 전부 반영"을 요구로 세웠으나 **P 항목 목록과 P→FR 매핑이 커밋된 산출물 어디에도 없다**(plan·docs·notes 전수 grep 0건). PRD 15개 FR로 흡수된 것이 사실이어도 **감사할 수단이 없어**, P 하나가 PRD 작성 때 조용히 빠졌다면 PRD 대조도 요구 이해 대조도 잡지 못한다. 다음 유사 작업에서는 요구 원본 번호 ↔ FR 매핑 표를 PRD 또는 plan에 남길 것. (출처: Opus 5 대응 plan F-7 m1)
- [2026-07-29] **`plan-feature` 트리거 갭 후보 — "여러 파일에 걸쳐 …를 바꿔야 해" 형태** — T1 기준선에서 **격리·비격리 양쪽 모두 미발동**한 유일한 케이스라 환경 요인이 아니다. description은 "여러 곳"·"전체"를 트리거로 적고 있으나 실제로는 발동하지 않았다. description 문구를 조정한 뒤 이 케이스로 A/B 재측정하면 효과가 즉시 보인다. (출처: Opus 5 대응 plan T1 기준선)
- [2026-07-29] **`antipatterns.md`의 "Review subagent 생략 금지" 예외 표에 C 분기(거절) 미언급** — 표는 B(호출 불가 환경의 대체)만 예외로 인정한다. C의 "범위를 좁힌 재의뢰"는 생략이 아니라 재시도이고 두 번째 거절 시 B로 수렴하므로 실질 모순은 없으나 문면상 명시적이지 않다. 표가 안전 임계 문구라 별도 검토. (출처: Opus 5 대응 plan T8 quality m1)
- [2026-07-29] **리뷰어 판정 표의 두 MINOR 행 경계** — "결함 여부가 해석에 달림"(MINOR 강등)과 신설 "결함인지 확신이 낮음"(MINOR + `(판정 유보)`)이 서술만으로는 경계가 모호해 태깅이 흔들릴 수 있다. **두 행의 종착지가 모두 MINOR라 오분류 비용은 없다.** `(판정 유보)` 운용 실적이 쌓인 뒤 두 행을 합칠지 판단. (출처: Opus 5 대응 plan T7 quality m1)
- [2026-07-29] **`plan-feature` 위임 상한 ②의 경계값 정밀도** — "파일 한둘"(직접)과 "두어 곳으로 좁혀진 뒤"(직접)의 숫자 경계가 겹쳐 몇 개부터 위임 가능한지 애매하다. 판정 축(대상이 특정됐는가)은 이미 관측 가능해 실행에 지장은 없다. 실사용에서 애매함이 관측되면 "파일 3개 이상 또는 대상 디렉터리 미특정" 같은 수치로 다듬는다. (출처: Opus 5 대응 plan T6 quality m1·m2)
- [2026-07-29] **SKILL.md 선두 구간 재구성 (구 PRD FR-5) — 별도 plan으로 재수립** — auto-compact 후 스킬은 **앞 5,000토큰만 재부착**되므로(공식 skills 문서) 34k 토큰급 SKILL.md의 규칙·Halt·위치판정이 통째로 유실된다. Opus 5 대응 plan에서 다뤘으나 **plan-reviewer 4회차까지 BLOCKER 미해소**로 분리했다(나머지 요구는 2회차 이후 지적 0건). 압축 대응 자체는 그 plan의 hook 주입(FR-4)이 담당한다. **후속 plan이 재조사 없이 쓸 자산**:
  - **선두 9,000B 경계 실측**: `implement-task/SKILL.md` **75행** / `plan-feature/SKILL.md` **84행** / `llm-wiki/SKILL.md` **79행**— **경계 행은 실측 불변**. ⚠️ **전체 행수는 이 불릿이 아니라 아래 「가용 예산」이 정본**이다(분리 시점 기록값 512·521·200행은 낡았고 실측은 **538·521·199행** — 같은 수치를 두 불릿에 둔 것이 이번 드리프트의 원인이라 한쪽만 정본으로 남긴다). 환산: 74,663B = 34,445토큰 → 1토큰 ≈ 2.17B → 5,000토큰 ≈ 10,850B(게이트는 여유 두어 9,000B).
  - **가용 예산 ⚠️ (Opus 5 대응 plan 완료 후 실측 갱신 — 착수 시 재측정 필수)**: `## 절대 규칙` 헤딩까지의 선두가 **3,131B**(9,000B 게이트 기준 아래 가용 **5,869B**). 전체 **538행**.
    - **분리 시점 기록값은 1,945B / 가용 7,055B였다** — 그 뒤 **같은 plan의 T9가 `## 절대 규칙` 바로 앞에** 질문 도구 제거 블록쿼트를 삽입해 선두가 **+1,214B** 늘었다(가용 -17%). 즉 "T5·T6·T9 편집은 L91 이후라 선두 측정에 영향 없다"고 적었던 종전 판단은 **틀렸다**(T9만 선두 안쪽을 건드렸다).
    - **재측정 대상은 포인터 건수만이 아니다** — **선두 바이트·가용 예산·전체 행수**도 함께 재측정할 것. 이 값들로 설계를 시작하면 17% 좁은 예산을 원래 값으로 착각한다.
  - **절대 규칙 항목 수**: `^[0-9]+(-[0-9]+)?\. ` 기준 **17항목**(하위 `5-1`·`6-1`·`7-1`·`7-2`·`7-3` 포함 — "1~12"로 세면 5개가 조용히 빠진다).
  - **`llm-wiki` 사전 준수 사항**: 16항목이 **전부 `- ` 불릿**이라 번호 기반 기계 검사가 그 스킬에서 무효(`0 == 0` 통과).
  - **`규칙 [0-9]` 포인터**: **42건 / 15파일**(2026-07-29 측정). `record-project-fact`(5건)·`add-viewmodel`은 **그 스킬 자체의 규칙**이라 귀속 분류 없이 전수 대조하면 게이트가 항상 실패. ⚠️ **건수는 착수 시 재측정할 것** — Opus 5 대응 plan의 T5·T6·T9가 `implement-task/SKILL.md`를 편집하므로 값이 달라진다(귀속 분류 **방식**은 그대로 재사용). **실측 확인된 불변 자산**: 선두 경계 행(75/84/79행)·절대 규칙 17항목·`llm-wiki` 불릿 16항목. **바뀐 자산은 위 「가용 예산」 항목 참조** — T9가 `## 절대 규칙` 앞을 건드려 선두 바이트·가용 예산·전체 행수가 달라졌다("L91 이후라 불변"이라던 종전 판단은 틀렸다).
  - **밀어내면 안 되는 기존 선두 내용**: `plan-feature` `## 절대 규칙`(L64) · `llm-wiki` `## 0. 시작 절차`(L47)·`## 사전 준수 사항`(L73) · `implement-task` `🚦 Phase 0`(L102, 현재 이미 선두 밖).
  - **앵커로 쓰면 안 되는 문자열**(무변경 상태에서 이미 통과): `절차 목차`(L20)·`Trivial Bypass`(L11)·`K. 작업 참조`(L35).
  - **검증된 설계 방향**: 3단(원문 축자 이관 → 판정선 압축본 신설 → 항목 1:1 대조표) + 게이트 4축(앵커 존재·바이트 상한·대조표 행 수 == 항목 매치 수·포인터 도달성). **단 ③축은 스킬별 정규식이 필요하고 ④축은 귀속 분류가 필요하다.**
  - **리뷰어가 제시한 두 봉쇄책 — 후속 plan의 설계 전제로 삼을 것**: ① **게이트 축마다 "무변경 상태에서 반드시 실패하는가" 셀프테스트**(AGENTS.md가 hook 차단 확대에 요구하는 red-green 실증과 동종) ② **plan 내 수치·목록의 단일 출처화**(사본 드리프트가 4회차 결함의 절반을 만들었다).
  (출처: Opus 5 대응 plan 분리 — plan-reviewer 1~4회차)
- [2026-07-28] **인벤토리 전수 추출의 토큰·시간 비용 관측** — 원본 화면 항목이 30~50개면 인벤토리 표가 그만큼 길어져 plan 작성·V-9 대조·reviewer 입력이 모두 커진다. 사용자가 지적한 "시간이 오래 걸린다"와 상충할 여지가 있으므로 실제 이식 작업 1~2회 후 상한(화면당 행 수·그룹 축약)을 둘지 판단. **이번엔 상한을 두지 않았다** — 누락 0이 요구의 핵심이고 상한을 먼저 두면 개선이 무력해진다. **관측 대상에 `spec-compliance-reviewer` I-2의 20행 읽기 예산도 포함** — V-9 본 대조는 전수(상한 없음)라 D4를 뒤집지 않으나, 21행 이상 화면에서는 2차 검증 커버리지가 상위 20행으로 제한된다. (출처: v1.144.0 parity 인벤토리 plan Deferred + F-7 m3)
- [2026-07-28] **`⏳` 금지 축의 플랫폼 확장 검토** — v1.144.0의 정적 판정 축 규정은 XAML(WinUI 3/WPF/MAUI)처럼 **선언적 마크업으로 컨트롤 타입·문구가 소스에 드러나는** 스택을 전제한다. 코드로 UI를 조립하는 스택(C# Markup·Compose·SwiftUI 등)에서도 같은 강도로 판정 가능한지 실사용 후 확인. (출처: v1.144.0 plan Deferred)
- [2026-07-28] **[SUGGEST] `plan-completion-reviewer`(F-7)에 인벤토리 축이 없다** — plan 전체 관점의 인벤토리 미달 검출이 **F-8 단일 방어**에 걸려 있다. v1.144.0 작업에서 F-8 절차 2가 "인벤토리 미달만 남은 상태"를 🎉로 보내는 사슬 끊김이 실제로 있었고 **F-7이 그것을 잡았지만, F-7 자신은 인벤토리 대조 항목 없이 잡았다** — 같은 유형이 재발하면 놓칠 수 있다. 이번 세션이 그 필요성을 실증한 사례이므로 다음 하니스 정비 때 우선 검토. (출처: v1.144.0 F-7 SUGGEST)
- [2026-07-28] **[SUGGEST] 지목한 `파일:라인`의 실재 확인** — V-9가 인벤토리 표의 `원본 근거`·`구현 근거`에 **존재하지 않는 파일:라인을 적는 환각**은 v1.144.0 변경과 무관한 일반 위험이나, 지목 의무를 강화할수록 "형식만 채우는" 유인이 생긴다. V-8 자기점검에 "지목한 파일:라인이 실제로 존재하는지 재확인" 항목을 넣을지 검토. (출처: v1.144.0 T2 V-6 참고 관찰)
- [2026-07-28] **caller 갱신 기록에 부수 변경까지 적기** — v1.144.0 T1의 V-7 caller 갱신 주석이 "문단명 참조 갱신"만 적었으나 실제로는 해소책 예시 1건도 함께 확장됐다. 판정 로직 영향은 없으나 다음 유사 작업에서는 부수 변경도 함께 적어 추적성 유지. (출처: v1.144.0 T1 V-5 재리뷰 MINOR)
- [2026-07-25] **`implement-task/SKILL.md:316` 중첩 볼드** — `**여기서 "병렬"은 **동기 호출**(...) …이다**`가 한 문장에 `**` 4개라 편집 시 짝 착각 위험(CommonMark 파싱 자체는 정상). 단일 볼드 쌍으로 정리 검토. (출처: 동기 호출 규약 명문화 T1 V-6 MINOR m1) **(2026-07-31 재확인: 유지 — 문장 현존(라인만 316 → **347**로 이동), 중첩 볼드 그대로)**
- [2026-07-25] **모델·effort 티어 재검토 (Opus 5 신설분)** — Claude 5 세대에 `fable` alias가 추가돼 `spec-prefilter`·`explorer`의 haiku(4.5 세대) 대안이 생겼고(deferred [2026-07-08] explorer 상향 기각의 재검토 조건 변화), effort에 `xhigh`/`max`가 추가돼 [2026-07-15] "품질 게이트 모델 티어 조건부 상향"을 **모델 상향이 아니라 effort 상향**으로 더 싸게 해결할 수 있다. 단 agent frontmatter가 이 값들을 실제로 수용하는지 **미검증** — 바꾸기 전 agent 1종으로 스모크 테스트 선행. (출처: Opus 5 하니스 점검) **(2026-07-31 재확인: 유지 — `agents/*.md`의 `model:` 분포가 **haiku 2 · opus 2 · sonnet 2**로 불변. 재검토·스모크 모두 미실시)**
- [2026-07-24] **references 9개 전수 정밀 조사로 통합 후보 최대 도출** — v1.138.1은 "확실히 안전한 통합"만 하고 전수 조사(Q3-B)는 미채택. implement-task references 9개를 메인이 전부 정독해 "통합 여지 작음"을 추측이 아닌 전수로 확정. 조사 비용이 크고 효과 불확실이라 보류. (출처: v1.138.1 Q3) **(2026-07-31 재확인: 유지 — 전수 조사 미실시)**
- [2026-07-24] **SKILL 방향 재편(규칙 폐기·구조 재편)** — 통합의 실익이 미미함(v1.138.1)이 확인됐으므로, hook 이벤트 로그 근거로 저효용 규칙 폐기(위험 감수) 또는 최빈/희소 경로 분리·스킬 분할을 재고. 사용자 "토큰 과다" 지적의 실질 해결은 이 방향에 남아 있으나 안전·설계 재검토가 선행. (출처: v1.138.1 Q3-C) **(2026-07-31 재확인: 유지 — 재편 미실시. 오히려 v1.148.0이 `implement-task`·`plan-feature`에 규칙을 더했다(선두 바이트 무증가로 관리했으나 총량은 증가 방향))**
- [2026-07-24] **D6 "재서술 금지 ↔ 참조 축약" 경계 명문화** — 부연 삭제로 남는 골격의 조사·어순 재구성이 "재서술 금지" 위반인지 삭제의 부수효과인지 경계가 불명(T1 V-5 MINOR). 다음 유사 감량 task 전 "골격의 문법적 재구성은 허용, 규칙 내용 변경은 불허" 기준을 D6에 명문화 검토. (출처: v1.138.1 T1 V-5) **(2026-07-31 재확인: 유지 — 명문화 미실시. **이번 T5(AGENTS.md 압축)가 정확히 이 경계에 걸리는 작업**이라, T5의 D1(보존/제거 목록 + "애매하면 보존")이 실질적으로 같은 문제를 plan 수준에서 다룬다 — 그 선례를 D6 명문화의 초안으로 쓸 수 있다)**
- [2026-07-24] **severity 상한 규칙 4개 reviewer 파일 리터럴 복제** — 한 곳만 수정되는 드리프트 위험. 각 agent가 자기 파일만 읽어 단일 소스화는 불가하나, "이 규칙은 4개 reviewer에 동일 존재" 상호 참조 각주를 4곳에 추가하면 편집 시 자기 점검 신호가 된다. (출처: v1.138.0 T2 quality SUGGEST) **(2026-07-31 재확인: 유지 — `agents/`에 "4개 reviewer"·"severity 상한" 상호 참조 각주 **0건**(미추가))**
- [2026-07-24] **rationale 이관 임계치 미설정** — T3에서 소량(0.7%) 이관에도 파일 2개를 신설해 "신설 오버헤드 > 감축 이득"이 될 뻔했다. 다음 유사 task 전에 "항목당 N자 이상만 이관" 같은 임계를 plan Design에 미리 못박을 것. (출처: v1.138.0 T3 quality SUGGEST) **(2026-07-31 재확인: 유지 — 임계 미설정. **이번 T5의 D2가 같은 문제에 다른 접근을 채택했다(계획 단계 — T5는 아직 미실행)**: 자수 임계 대신 "압축만으로 목표 달성 시 분할하지 않음"이라는 **결과 기준**을 미리 못박았다. 신설 오버헤드를 사전에 막는 목적은 동일하므로, **T5 실행 후 효과를 확인해** 다음 정비 때 임계안과 비교할 것)**
- [2026-07-24] **규칙 유실 측정(집합 grep)의 구조적 한계** — 리터럴 매치라 ⓐ 재서술된 판정 문장("~해야 한다") 증감을 못 잡고 ⓑ "카운트 불변"이 "같은 문장 잔존"을 보장 못 함(삭제+신규 상쇄). 순수 삭제 작업엔 유효하나 재작성 동반 작업엔 이 측정을 신뢰 금지. (출처: v1.138.0 T3 spec) **(2026-07-31 재확인: 유지 — 이 한계는 방법론 주의사항이라 코드로 해소되는 종류가 아니다. **이번 T5가 그 한계를 우회하는 방법을 시도한다(T5 acceptance 2 — 아직 미실행)**: 리터럴 카운트 대신 **의무 어휘 grep 전후 1:1 대응표**(줄 13 + 매치 16회 동시 고정)를 써서 ⓐ 재서술 증감과 ⓑ 삭제+신규 상쇄를 둘 다 잡으려는 설계다. **T5 실행 결과로 유효성이 확인되면** 이 기법을 측정 표준으로 승격 검토)**
- [2026-07-24] **`references/*.md`에도 rationale 분리 확장 검토** — T4에서 `phase-f-detail.md`에 "이 점검만 남긴 이유(v1.138.0)" 블록쿼트(버전 태그+과거형 이력 서사)가 생겼다. T3의 rationale 분리가 두 SKILL.md에만 적용돼 references 파일엔 이력이 본문에 쌓인다. 확장 여부 결정 필요. (출처: v1.138.0 T4 quality MINOR) **(2026-07-31 재확인: 유지 — 확장 여부 미결정)**
- [2026-07-15] **Fast-Path 표 B행에 ESCALATE→C격상 각주 추가** — 표는 PASS 경로 기준이라 무변경이 옳으나(v1.123.0 plan 확정), 표만 읽는 독자가 ESCALATE에서도 V-3/V-6 생략으로 오독할 여지. §267 포인터 각주 1구로 가독 개선. 다음 implement-task SKILL 정비 때. (출처: v1.123.0 T2 quality m1) **(2026-07-30 재확인: 유지 — 해당 장치·작업 미도입 확인, grep 0건)**
- [2026-07-15] **직접 수정(Trivial Bypass) 누적 품질 드리프트 점검 장치** — plan 없는 직접 수정(3줄 이내·순수 값 치환)은 리뷰 0회로 커밋되며, 여러 세션 누적돼도 어떤 게이트도 재점검하지 않는다(F-7 7.5는 plan 단위 작업에만 발동). 트리거 설계 필요(누적 횟수 기준? 다음 plan-feature Step 1에서 경량 훑기?). (출처: v1.123.0 plan Deferred) **(2026-07-30 재확인: 유지 — 해당 장치·작업 미도입 확인, grep 0건)**
- [2026-07-15] **품질 게이트 모델 티어 조건부 상향** — 계획 검토(plan-reviewer)·전체 검증(F-7)은 Opus인데 아키텍처 판단이 가장 어려운 Type D 대형 diff의 V-6은 Sonnet/effort medium 고정. Type D 시 effort 상향 등 조건부 에스컬레이션 검토 — v1.122.0 Out of Scope "reviewer 모델 상향(비용 관찰 후 별도 검토)"와 동일 사안. (출처: v1.123.0 plan Deferred) **(2026-07-30 재확인: 유지 — 해당 장치·작업 미도입 확인, grep 0건)**
- [2026-07-15] **Phase F에 전체-diff(BASE..HEAD) 품질 패스 추가** — task별 diff 리뷰(V-6)는 task 간 누적 중복·패턴 불일치·여러 task에 걸쳐 비대해진 파일을 원리적으로 못 본다. 전 task 완료 후 전체 diff에 1회 품질 리뷰를 수행하는 F-단계 신설 검토. v1.122.0 3레버(Design 필드·V-6 기본화·SUGGEST)의 효과 관찰 후. v1.123.0 분석에서 재확인: 4-D 재사용 조사는 계획된 심볼만 대상이고 V-6 항목 J②(중복)는 non-blocking이라, task 간 누적 중복의 실효 검출 수단은 이 패스뿐이다. (출처: v1.122.0 plan Deferred) **(2026-07-30 재확인: 유지 — 해당 장치·작업 미도입 확인, grep 0건)**
- [2026-07-15] **simplify(정리) 패스** — 리뷰 통과 후 diff를 더 단순하게 정리하는 단계. 자율 루프 churn(수정→재리뷰 반복) 위험이 있어 범위·트리거 별도 설계 필요. (출처: v1.122.0 plan Deferred) **[2026-07-30 보강 — 선행 사례 확보]** 같은 아이디어의 외부 구현이 있다: `DietrichGebert/ponytail`의 **`/ponytail-review`** 는 현재 git diff를 읽어 과도 엔지니어링을 찾아 **"삭제 가능 목록(delete-list)"** 을 반환하고, `/ponytail-audit`은 diff가 아니라 저장소 전체를 훑는 변형이다. **설계 힌트 둘**: ⓐ 출력이 *수정된 코드*가 아니라 **삭제 후보 목록**이라 churn 위험이 낮다(제안만 하고 적용은 분리) ⓑ **diff 대상**과 **전체 대상**을 별도 명령으로 나눠 트리거 문제를 회피했다 — 우리 대장의 "Phase F 전체-diff 품질 패스" 항목과 이 항목의 경계도 같은 축(전체 vs diff)이라 **둘을 함께 설계**하는 편이 낫다. (근거: v1.148.0 T5 ponytail 대조) **(2026-07-30 재확인: 유지 — 해당 장치·작업 미도입 확인, grep 0건)**
- [2026-07-13] `pjc-systematic-debugging` 단독 세션이 **plan.md가 없는 레포**에서 조사 로그를 plan.md로 **신규 Write**하면 plan 작성 게이트에 차단된다(통과 흔적 목록에 debug 스킬 없음 — D3). 실해는 작다(SKILL.md:281이 `debug-<날짜>.md` 대안을 이미 제시, 차단 메시지도 그리로 유도). SKILL.md:281에 "plan.md가 없으면 `debug-<날짜>.md`" 1구 명문화 또는 흔적 허용 여부 재검토 (출처: v1.118.0 F-7 m2) **(2026-07-30 재확인: 유지 — 대상 코드·정규식 무변경 grep 실측)**
- [2026-07-20] **`F-1~F-8` 라벨링이 실제 F-8 수행 시점과 표기상 어긋나 보임** — PRD 연결 plan은 Phase G 통과 후 수행인데 진입 조건표만 훑으면 F-7 직후로 오독할 여지. 바로 아래 타이밍 표(`phase-f-detail.md` F-8)가 해소하므로 실행 불가 수준은 아님. 라벨을 `F-1~F-7 + F-8(별도 시점)`으로 바꾸는 안 검토 — 다음 implement-task SKILL 정비 때. (출처: v1.127.0 T1 quality m1) **(2026-07-31 재확인: 유지 — `implement-task/SKILL.md`에 `F-1~F-8` 표기 현존(1건), 라벨 변경 미실시)**
- [2026-07-20] **`final-report-template.md`의 `⏸️` 분기에 완성 코드 블록 부재** — 산문 설명(①②③ 차이점)만 있고 `🎉`처럼 완성된 템플릿 블록이 없어 세션마다 "안내 1줄"·"확인 요청 목록" 표기가 미세하게 갈릴 수 있음. `⏸️` 전용 완성 블록 추가 검토. (출처: v1.127.0 T1 quality S1 / SUGGEST) **(2026-07-31 재확인: 유지 — 완성 코드 블록은 여전히 없다(`final-report-template.md`의 코드펜스 2개는 `🎉` 양식용). **다만 부분 완화됨** — `:27`에 *"`⏸️` 분기의 본문은 아래 양식과 동일하다"* 는 명시가 생겨 표기 흔들림 폭이 줄었다. 남은 것은 "안내 1줄·확인 요청 목록"의 형태뿐)**
- [2026-07-20] **plan-reviewer 항목 15 제목이 컨벤션과 어긋남** — 항목 1~14는 예외 없이 `(X 후보)` 형식인데 15만 `(조건부 BLOCKER)`. 본문은 동일하게 4갈래 "후보" 구조이므로 `(BLOCKER 후보)`로 통일 검토(본문 판정 로직은 유지). (출처: v1.127.0 T3 quality m1) **(2026-07-31 재확인: 유지 — `plan-reviewer.md:233`이 여전히 `### 15. 시각 요소 분해 (조건부 BLOCKER)`, 바로 위 `:222`는 `### 14. 설계 스켈레톤 (MAJOR 후보)`. 비대칭 그대로)**
- [2026-07-20] **plan-reviewer 항목 14↔15 등급 비대칭의 대조 근거 문장 부재** — 14는 "설계 판단은 취향 경계가 넓다"로 BLOCKER를 금지하는데 15는 허용. 정당화는 타당하나(15의 판정은 ①+②·섹션 유무의 기계적 이진 조건이라 취향 경계 없음) 리뷰어가 스스로 재구성해야 함. 항목 15 도입부에 대조 1줄 추가 검토. (출처: v1.127.0 T3 quality m2) **(2026-07-31 재확인: 유지 — `plan-reviewer.md:233-237` 도입부는 "검증 사슬의 입력"·"발동 판정은 plan-feature Step 2.5를 따른다"만 설명하고, **14와의 등급 비대칭을 대조하는 문장은 없다**)**
- [2026-07-20] **`항목 I` 레이블이 두 리뷰어에 중복** — spec-compliance의 `I. 시각 요소 분해`와 code-quality의 기존 `I. UI 문구 사용자 친화성`. LLM 리뷰어는 자기 파일만 읽어 동작 영향 없으나 `implement-task/SKILL.md`의 두 참조가 10줄 간격(304·314행)이라 사람이 오독할 여지. 314행에 `(code-quality-reviewer)` 소속 명시 검토. (출처: v1.127.0 T2 quality m1) **(2026-07-31 재확인: 유지 — 소속 명시 미추가. 두 참조는 라인 이동만 있었다(304·314 → **346·356**), V-6 검토 기준 줄은 여전히 *"사용자 노출 UI 문구 친화성(항목 I — …)"* 로 소속 표기가 없다)**

### llm-wiki · vault

- [2026-07-28] **llm-wiki vault의 OKF v0.2 미러 갱신 (별도 `pjc:llm-wiki` 세션 — 절차 F/B)** — ① vault 루트 `index.md` frontmatter의 `okf_version: "0.1"` → `"0.2"` ② 위키 페이지 `20_projects/personal/claude-harness-pjc/feat-llm-wiki.md:63`의 OKF 정합 미러 서술을 v0.2 기준으로 갱신. 코드 세션에서 위키 쓰기 금지 규약(SKILL 절차 K — 예외는 `pending.md` 1줄 append뿐)이라 하니스 세션 범위 밖. [2026-07-25] "병렬 = 결과를 안 기다림 오독 문장 ①"과 같은 위키 세션에서 묶어 처리 가능. (출처: OKF v0.2 갱신 plan Deferred)
- [2026-07-28] **wiki-schema §12 미적합 잔여 ②③의 적합성 판정 문장 부재** — ①(wikilink)에만 "적합성 3요건 위반은 아님"이 붙어 있고 ②(index.md 표)·③(log.md 평면 목록)은 판정 문장이 없다. okf-spec §11 Conformance 요건 ③(예약 파일이 §8·§9 구조 준수)에 정면으로 닿아, §12만 읽고는 이 vault가 conformant인지 결정할 수 없다. v0.1 시절부터 이어진 공백이라 회귀는 아님 — 다음 §12 손질 때 1줄 판정 추가. (출처: OKF v0.2 갱신 F-7 m4)
- [2026-07-28] **`confidence` 행 해석의 뉘앙스 보강** — §12 매핑 표의 "OKF가 배제하는 것은 소스별 credibility 점수"는 okf-spec §5.1 문면상 정확하고 결론(§4.1·§11의 확장 키 보장)도 뒷받침되나, §5.1이 든 배제 사유("subjective, unportable, goes stale")는 페이지 단위 점수에도 성립할 여지가 있다. 그 뉘앙스를 한 구절로 덧붙일지 검토. (출처: OKF v0.2 갱신 F-7 Goal Assessment)
- [2026-07-28] **`wiki-schema.md` frontmatter(`version`·`updated`) 관리 부재** — 최근 6개 수정 커밋에서 `version: "2.40"`·`updated: 2026-07-12` 어느 쪽도 갱신되지 않았다(실측). 규칙 진실원천의 버전 표기가 사실상 방치 상태인데, 단독 근거 없이 관례를 바꾸는 셈이라 그 작업에서는 손대지 않았다. 갱신 트리거를 정할지 별도 판단 필요. (출처: OKF v0.2 갱신 plan Deferred)
- [2026-07-25] **"병렬 = 결과를 안 기다림" 오독 문장 2곳 정비** — ① 위키 `feat-review-subagents.md` L35 "V-5와 병렬 실행이라 spec 판정을 기다리지 않는다"(별도 `pjc:llm-wiki` 절차 F 세션) ② 레포 내 `plugins/pjc/agents/code-quality-reviewer.md:15` "V-6은 V-5와 병렬 실행되어 spec 판정을 기다리지 않습니다". 둘 다 실제로는 "두 리뷰 간 의존 없음"을 뜻하나 백그라운드 실행으로 오독될 여지. ②는 callee-side 정의라 호출 지시가 아니고 말미에 자체 완화구가 있어 실해는 낮음. 한 건으로 묶어 처리. (출처: 동기 호출 규약 명문화 — plan Deferred + F-7 m2) **[2026-07-29 부분 반영 — Opus 5 대응 plan T8]** ②(`code-quality-reviewer.md`)는 "V-6은 V-5와 **동시에 호출**되므로 spec 판정 결과를 입력으로 받지 않습니다(결과를 기다리지 않고 진행한다는 뜻이 아닙니다)"로 정정 완료. **①(위키 `feat-review-subagents.md` L35)만 남음** — 코드 세션의 위키 쓰기 금지 규약상 별도 `pjc:llm-wiki` 세션 필요. **(2026-07-31 재확인: 유지 — ②(레포 쪽)는 이미 해소됐다. `plugins/pjc/agents/code-quality-reviewer.md:15`가 "V-6은 V-5와 *동시에 호출*되므로 spec 판정 결과를 입력…"으로 바뀌어 "기다리지 않는다"는 오독 표현이 사라졌다. ①(위키 `feat-review-subagents.md` L35)은 미확인 — vault는 코드 세션의 쓰기 금지 대상이라 이 항목은 그 1건 때문에 유지한다)**
- [2026-07-24] **lint.py `mentioned` 정규식 경계 부재** — `index-[a-z]+(?:-\d+)?`가 `index-work-1abc`·`myindex-work-1` 근접 문자열을 `index-work-1`로 매치할 이론적 여지(§7-15 미등록 검사 정밀도). 실사용 wikilink는 정확 파일명이라 실무 영향 낮음. `(?<![\w-])…(?![\w-])` 경계 추가 검토. (출처: sub-index 3단계 자동 분할 V-6 MINOR) **(2026-07-31 재확인: 유지 — `lint.py:873`이 여전히 `re.findall(r"index-[a-z]+(?:-\d+)?", itext)`로 경계 없음)**
- [2026-07-24] **wiki-schema §4 분할 수행 절차 L342 '1단계 소제목 구역화' 스코프 문구** — 하드 임계 초과 후 실행 스텝 문맥이 근접 단계 1단계 정의(L329)와 시점이 달라 '1단계는 비분할 섹션에만'으로 오독 여지. '이 절차는 초과 후 스텝이라 근접 1단계와 무관' 한 구절 보강 검토. (출처: sub-index 3단계 자동 분할 V-6 MINOR) **(2026-07-31 재확인: 유지 — 구조 그대로. `wiki-schema.md:329`에 1단계 정의(`**1단계(소제목 구역화 — 사람 판단 가이드)**`), `:342`에 분할 범위 서술(`범위는 2단계 category 분할과 3단계 순번 파일 분할이다`)이 근접 배치돼 있고 보강 구절은 미추가)**
- [2026-07-24] **위키 feature 136개 `## 구현 방법` 섹션 축소** — T6이 규칙(코드 답하는 '어떻게'는 빼고 '왜 이 구조인가' 3~5줄)만 바꿔 신규·갱신 페이지부터 적용된다. 기존 136개(구현 방법 섹션 합 236KB)의 소급 정리는 vault 작업이라 하니스 세션 권한 밖 — 후속 `pjc:llm-wiki` 절차 F/B 세션에서 수행. (출처: v1.138.0 T6·Q4) **(2026-07-31 재확인: 유지 — 소급 정리 대상이 vault(`20_projects/{personal|work}/{프로젝트명}/feat-*.md` — `wiki-schema.md:120`이 정의하는 feature 경로. 레포 밖)라 코드 세션에서 판정·수행 모두 불가. 전제 불변)**
- [2026-07-22] **pending 형식 위반 검출의 불릿 판별 오탐 여지** — `lint.py`의 위반 검출이 불릿을 `^\s*-\s*`로 보므로, `-`로 시작하되 불릿이 아닌 줄(`-- 구분선 …`·`-5 …`)에 태그 문자열이 **우연히** 포함되면 위반으로 잡힐 이론적 여지가 있다. 실측 오탐 0(현행 vault + eval 34/34)이라 결함으로 단정할 근거는 없어 관측 시 대응. 좁히려면 `^\s*-\s*\[`로. **단 공백 폭을 `\s`로 되돌리면 안 된다** — `-[TAG]` 줄이 정상에도 위반에도 안 잡히는 2차 사각지대가 재발한다(v1.132.0 T1 quality m1이 잡은 바로 그 케이스, 픽스처 `pending-malformed`가 고정). (출처: v1.132.0 T1 quality m1) **(2026-07-31 재확인: 유지 — `lint.py:994`의 불릿 판별이 여전히 `re.match(r"^\s*-\s*", line)`. 제안된 `^\s*-\s*\[` 로의 축소는 미적용이며, 관측 조건("실측 오탐 발생")도 아직 충족되지 않았다)**
- [2026-07-09] suggest(제안) 이벤트의 로깅 확장 — v1.105.0 T2는 차단+경고만(사용자 확정 범위) (출처: harness-wiki-part1) **(2026-07-30 재확인: 유지 — 해당 장치·작업 미도입 확인, grep 0건)**
- [2026-07-09] lint.py --fix 대상 확대(§7-19 누락 행 추가 등) — 키워드 요약을 기계 생성할 근거가 생기면 재검토 (출처: harness-wiki-part2) **(2026-07-30 재확인: 유지 — 해당 장치·작업 미도입 확인, grep 0건)**
- [2026-07-09] lint.py `section()` 헬퍼 Match 반환형 확장으로 `section_span()`과 단일화 — 호출부 리팩터링 동반, 다음 lint.py 정비 때 (출처: harness-wiki-part2 T2 리뷰 m2) **(2026-07-30 재확인: 유지 — 대상 코드·정규식 무변경 grep 실측)**
- [2026-07-09] 나머지 7개 프로젝트 메모리의 위키 순차 이전(AI-Agents·neighborhood-walk-rpg·Obsidian-Vault-WIKI·bitleader-dev-HomePage·Web-HomePage·ProjectDashboard·DevDashboard-WinUI) — 각 프로젝트 위키 절차 B 세션에서 (출처: v1.102.0) **(2026-07-30 재확인: 유지 — 해당 장치·작업 미도입 확인, grep 0건)**
- [2026-07-09] SessionStart hook의 위키 허브 컨텍스트 자동 주입 — v1.102.0 미채택(지침 방식 선택), 지침 방식 실효성이 낮다고 관찰되면 재검토 (출처: v1.102.0) **→ [2026-07-30] 부분 재수용 (v1.146.0)**: 재검토 조건이 **실제 사고로 충족**됐다 — 설정·실재하는 vault를 "미설정"으로 단정해 절차 K를 건너뛴 세션이 관측됐고, 지침만으로는 확인을 강제하지 못한다는 것이 실증됐다. 다만 이번에 채택한 것은 **vault 설정 상태 1줄 주입만**이며(사용자 확정), **허브 본문·`## 작업 규약·주의사항` 요약 주입은 이 항목에 그대로 잔존**한다(컨텍스트 비용이 크고 절차 K의 역할과 겹침). **(2026-07-30 재확인: 유지 — 잔존분(허브 본문·규약 요약 주입) 기준, 미도입 확인)**
- [2026-07-09] 위키 허브 `## 작업 규약·주의사항`이 수십 건으로 커지면 전용 페이지(worknotes류) 승격 검토 (출처: v1.103.0) **(2026-07-30 재확인: 유지 — 해당 장치·작업 미도입 확인, grep 0건)**
- [2026-07-19] lint.py:160 주석 표현 "오계상"→"오계산" 통일 검토 — "계상(計上)"도 유효 표현이라 비수정 보류, 다음 lint.py 정비 때 문구 통일 여부만 판단 (출처: v1.125.0 T1 quality m1) **(2026-07-31 재확인: 유지 — `lint.py`에 "오계상" 표현 잔존, 통일 미실시)**
- [2026-07-20] **`lint.py` index.md 읽기 실패가 else 분기로 합류해 "없음"으로 오진** — index.md를 못 읽으면 `pages` 멤버십이 False가 되어 889-893행 else로 빠져 `"index.md 없음: … 카탈로그 골격 생성 필요"` ERR이 난다. **파일은 실재하는데 "없음"이라 하고 잘못된 처방을 지시**한다(v1.129.0 T3 전에는 crash해서 이 분기에 도달조차 못 했으므로 그 수정이 새로 노출한 부작용). 읽기 실패와 부재를 구분해 각각 다른 ERR을 내도록 개선 검토 — T3 Files 범위 밖이라 미수행. `lint-cases.json`의 `bad_index` 케이스 rationale에도 명시돼 있다. (출처: v1.129.0 T3 quality m1) **(2026-07-31 재확인: 유지 — `lint.py:936`의 `else` 분기가 여전히 `"index.md 없음: … 카탈로그 골격 생성 필요 (schema §4)"` ERR을 낸다. **읽기 실패와 파일 부재가 같은 분기로 합류하는 구조 그대로**라, 파일이 실재하는데 "없음"이라 보고하고 잘못된 처방을 지시하는 경로가 남아 있다)**
- [2026-07-19] **가이드/레시피 섹션 미등록·리스트 불릿 등록 가이드의 기계 검출(§7-27 확장) 검토** — §7-27은 표 행(`| [[...]]`)만 스캔해 리스트 불릿 등록·섹션 미등록 가이드는 병기 검사 사각(현재는 고아 검사만 커버, v1.125.0 fixture 1차 FAIL로 실증). 규정(§2.6·I-2b "표 행 형식 등록")으로 절차 방어는 완료 — 기계 방어 확장은 다음 lint 정비 때 (출처: v1.125.0 T2) **(2026-07-31 재확인: 유지 — 규정 측 방어는 그대로 실재하나(`wiki-schema.md:226` *"허브+하위 전 행을 표 행 형식으로 등록"*, `:297` §7-27 병기 검사), 리스트 불릿 등록·섹션 미등록을 잡는 **기계 검출 확장은 미실시**)**
- [2026-07-23] **index 자동 분할의 희소 경로 2건(무수행 분기·사본 갱신)** — ① §4 1번이 무조건 수행 지시라, 이미 분할된 vault에서 본체가 재초과(레시피·질문·증상 인덱스 증가)하면 옮길 project feature 행이 없는데 그 분기가 없다 → "무수행 보고 + 1단계 구역화 제안" 1줄 ② §8 미덮어쓰기 규정이 `-presplit`에도 적용되는지 미정의 → 같은 날 2회째 분할에서 stale 사본으로 원복할 여지, "매 분할마다 최신 사본으로 갱신(미덮어쓰기 예외)" 명시 필요. 둘 다 재분할이라는 희소 경로 (출처: v1.137.0 F-7 m1·m2) **(2026-07-31 재확인: 유지 — ① `wiki-schema.md:342`는 분할 **범위**(2·3단계)만 규정하고 "옮길 행이 없을 때"의 무수행 분기는 여전히 없다 ② presplit 사본은 위치만 규정돼 있고(`:345`·`:509`) **같은 날 2회째 분할에서 덮어쓸지 여부는 미정의** — stale 사본으로 원복할 여지 그대로)**
- [2026-07-23] **자동 분할의 절차 파일 대칭 결손 2건** — ① `procedures-ops.md` F-2의 log 기록 대상 열거(수정 적용·큐 소비·리포트 생성)에 "자동 분할 수행"이 없어, 승인 수정 0 + 자동 분할만 한 세션이 `[LINT]` 기록 여부를 판단할 근거가 없다 ② `procedures-content.md` B-3에 자동 분할 언급이 0건이라 B 세션은 SKILL 예산표·lint INFO 문구·§4로만 발동한다(3중 확보라 실동작 결함은 아님). 다음 llm-wiki 정비 때 정리 (출처: v1.137.0 F-7 m3·m4) **(2026-07-31 재확인: 유지 — 대칭 결손 그대로다. `procedures-ops.md`·`procedures-content.md` 양쪽 모두 "자동 분할" 매치 **0건**)**
- [2026-07-27] **vault OKF v0.2 마이그레이션 2건** — ① vault 루트 `index.md` frontmatter의 `okf_version: "0.1"` → `"0.2"` ② 위키 `20_projects/personal/claude-harness-pjc/feat-llm-wiki.md:63`의 OKF 정합 미러 서술(`okf_version` 선언·"OKF timestamp 미채택")을 v0.2 기준으로 갱신. 코드 세션은 위키 쓰기 금지(절차 K)라 별도 `pjc:llm-wiki` 세션(절차 F/B) 소관이며, OKF 소비가 best-effort라 `"0.1"` 잔존이 깨짐을 만들지는 않는다. (출처: v1.143.0 OKF v0.2 정합)
- [2026-07-27] **미적합 잔여 ②③의 OKF 적합성 판정 문장 부재** — `wiki-schema §12`의 미적합 잔여 ①(wikilink)에만 "적합성 3요건 위반은 아님"이 붙어 있고, ②(index.md 표 카탈로그)·③(log.md 평면 목록)은 `okf-spec §11 Conformance` 요건 ③(예약 파일이 §8·§9 구조 준수)에 정면으로 닿는데도 판정이 없다 — §12만 읽고는 이 vault가 conformant인지 결정할 수 없다. v0.1 시절부터 이어진 공백이라 v0.2 갱신의 회귀는 아니며, 다음 §12 손질 때 1줄 판정 추가. (출처: v1.143.0 F-7 m4)
- [2026-07-27] **`wiki-schema.md` frontmatter(`version`·`updated`) 관리 부재** — 이 파일을 건드린 최근 6개 커밋에서 `version`·`updated` 어느 쪽도 갱신되지 않았다(실측). 규칙 진실원천의 버전 표기가 사실상 방치 상태지만, 갱신 트리거를 새로 정하는 것은 단독 근거가 없어 이번 작업에서 손대지 않았다(D4). 관례를 세울지 별도 판단 필요. (출처: v1.143.0 OKF v0.2 정합)

### eval · 측정

- [2026-07-15] **품질 루브릭 eval(Opus judge 채점)** — 고정 task 세트를 돌려 아키텍처 품질을 1-10 루브릭으로 채점, 파이프라인 변경 효과를 수치로 측정(eval 기준선은 Opus 실작업). (출처: v1.122.0 plan Deferred) **(2026-07-30 재확인: 유지 — 해당 장치 미도입 확인, grep 0건)**
- [2026-07-29] **eval 기준선의 2회차(전후 비교) 측정** — T1·T2가 낸 수치는 변경 *전* 기준선이다(트리거 격리 0.6/비격리 0.9, 루브릭 평균 7.79·편차 0.25). 이번 plan의 변경이 실제로 효과가 있었는지는 **같은 러너를 다시 돌려 `compare_evals.py`로 대조**해야 나온다. 격리·비격리는 다른 것을 재므로 같은 모드끼리만 비교할 것. (출처: Opus 5 대응 plan T1·T2)
- [2026-07-29] **`rubric_eval.py` `extract_json`의 관대한 파싱** — 첫 `{`~마지막 `}` 절단이라, judge가 JSON 앞뒤 설명에 별도 `{...}` 예시를 넣으면 잘못된 구간을 파싱할 수 있다. 프롬프트가 "JSON 한 덩어리만"을 지시하고 실패 시 재시도·`error`로 이어져 현재는 실해가 없다. 오파싱이 실제로 관측되면 좁힌다. (출처: Opus 5 대응 plan T2 quality m1)
- [2026-07-29] **[SUGGEST] `compare_evals.py` `main()`의 회귀/개선 출력부 삼항식 단순화** — `for line in regressions or [...]` 루프가 매 반복 같은 조건을 재평가한다. 접두사를 루프 밖에서 정하면 결과는 같고 더 단순하다. (출처: Opus 5 대응 plan T3 quality S1)

## 종결

- [2026-08-03 → 2026-08-05] **[MINOR·판정 유보] 자기 유발 MINOR를 수정한 뒤 재리뷰가 필요한지가 규정되지 않았다** — 반영(리뷰 루프 개선 plan T3). 대장이 제안한 *"자기 유발 MINOR 수정은 재리뷰 없이 V-7로 진행"* 을 그대로 못박는 대신, 같은 task가 신설한 **증분 재리뷰 3단계 표에 편입**했다(`implement-task/SKILL.md` V-5) — 자기 유발 MINOR도 다른 수정과 같은 축(*"그 문장을 고치지 않으면 실행 결과·절차 경로가 달라지는가"*)으로 판정해 ①이면 생략, ②·③이면 그 범위대로 재리뷰한다. 무조건 생략으로 못박지 않은 이유는 **자기 유발 MINOR가 판정 조건을 바꾸는 문면일 수 있기 때문**이다(그 경우 carve-out ②에 걸려 전량). 등재 당시 제안이 상정한 "무해한 표현 수정"은 ①에 그대로 해당한다.
- [2026-07-28 → 2026-08-03] **`plan-feature/SKILL.md` 분량이 AUTHORING.md 권장 상한 초과** — 반영(v1.153.0 T5). 이번 정비가 규칙을 더해 543 → 약 560줄이 될 상황이라 **선택 동반 변경으로 사용자에게 제시해 채택**받았고, 대장이 제안한 대로 **Step 2.5 전체를 묶음 단위로** `references/visual-decomposition.md`에 이관했다(발동 조건만 본문 원문 잔류 + 포인터 1줄). 결과 **505줄**(작업 전 543 대비 −38, 신규 규칙 3건 추가분 포함). 등가 대조로 순수 이동을 실증했고, 이관으로 무증상 비활성될 뻔한 `plan-reviewer` 항목 15의 세 참조도 함께 재지목했다(구버전 호환 조건을 "SKILL.md 또는 references 어디에도 없으면"으로 확대). **여전히 권장선 500줄은 5줄 초과** — 추가 감량은 다음 정비로.
- [2026-08-02 → 2026-08-03] **`isMeta` 필드 의존의 구조적 한계 (v1.151.0이 만든 것)** — 검사 4의 사용자 발화 추출이 Claude Code transcript의 **비공식 내부 스키마**(`"isMeta":true`)에 의존한다. 필드가 사라지거나 이름이 바뀌면 검사 4는 **다시 조용히 fail-open** 되는데, **골든은 픽스처를 우리가 만들므로 그 회귀를 잡지 못한다** — v1.148.0부터 이 결함이 골든 green 상태로 숨어 있던 것과 정확히 같은 구조다. 완화 후보 ⓐ 길이 상한 백스톱(`## 대기`의 「[미채택안] 사용자 발화 길이 상한 백스톱」 — 2026-08-03 정정: 종전 "아래 항목"은 이 항목이 종결로 이동하며 방향이 뒤집혔다) ⓑ 실 transcript로 조건값을 재확인하는 점검을 주기화 ⓒ hook 이벤트 로그에서 검사 4 차단 발화 건수를 관측해 0이 지속되면 의심. **최소 방어로 "릴리즈 후 첫 자율 루프 세션에서 이벤트 로그로 실발화 1회 확인"**을 둔다. (출처: v1.151.0 plan Deferred)
  - **⚠ 의존 대상은 `isMeta` 하나가 아니고, 방향도 한쪽이 아니다** (F-7 M2): skip ②③④가 각각 `<task-notification>` 리터럴·`"type":"text"` 원시 표기에 의존한다. 특히 **`<task-notification>` 형식이 바뀌면 결과는 fail-open이 아니라 그 반대다** — 알림이 다시 마지막 사용자 발화 자리를 차지하는데 알림 본문엔 정지 어휘가 없으므로 `$userStop=false`가 되어 **차단**된다. 이것이 D2·위키 허브가 *"미탐보다 나쁘다"* 고 지목한 "사용자가 중단시킨 작업에 재개를 강요" 경로다. 따라서 관측 지표를 **"차단 발화 0 지속"(fail-open 탐지)뿐 아니라 "사용자 중단 지시 직후에 차단 reason이 뜨는지"(오차단 탐지)까지** 둘 다로 본다.
  - **부분 해소(v1.152.0 T4)** — 등재가 적은 완화 후보 ⓑ(실 transcript로 조건값을 재확인하는 점검)를 `plugins/pjc/hooks/evals/check-transcript-assumptions.ps1`로 자산화했다. 최근 6세션 5,084줄 실측에서 **5가정 전건 성립**(isMeta 15건 전건 시스템 주입 형태 · `<task-notification>` 필드 시작 형태 32건 실재 · assistant 2,055건 중 원시 `"type":"text"` 유해 불일치 0 · 예산 소진 발화 손실 0 · 재현 pin 5종 실재). 위 ⚠ 하위 항목이 지목한 **다른 의존 대상 둘도 같은 도구가 함께 본다**(가정 2·3). **다만 근본 의존 자체는 남는다** — 이 도구는 드리프트를 조기에 알리는 수단이지 의존을 없애지 않으며, 실행 시점은 `docs/hook-conventions.md` 조건 5의 점검 수단 문단과 `AGENTS.md` 검증 매핑이 규정한다(`require-evidence.ps1` 수정 시).

- [2026-08-02 → 2026-08-03] **골든에 skip④(텍스트 없는 assistant 제외) 단독 red가 없다** — v1.151.0 G7은 `last_assistant_message`를 주므로 `$needAsst=false`가 되어 **skip③만으로도 red가 성립**한다. 즉 skip④만 지워도 전 케이스가 green이며, 그 줄의 유일한 방어는 "지우지 말 것" 주석뿐이다 — 이번 변경이 스스로 최대 위험으로 지목한 형태다. **닫는 방법**: G7의 폴백 변형(stdin `last_assistant_message` **없이** + 텍스트 없는 assistant 250줄 + 그 너머에 assistant 텍스트)을 1건 추가하면 skip④ 단독 red가 생긴다. (출처: v1.151.0 F-7 m3)
  - **해소(v1.152.0 T2)** — 등재가 제시한 "닫는 방법" 그대로 G8을 신설했다(stdin `last_assistant_message` 없이 폴백 진입 + 텍스트 없는 assistant 250줄이 가장 최근 + 그 너머에 assistant 텍스트). **red 실증**: skip④ 판정식만 주석 처리 → **G8 단 1건 FAIL**(PASS 95) / skip③만 주석 처리 → **96/96 PASS** = 이 케이스가 skip④ 전용임의 축 분리 실증. 프로브는 hook을 바이트 사본으로 원복해 `scripts/` diff 0줄을 유지했다.

- [2026-08-02 → 2026-08-03] **[SUGGEST] 골든 픽스처의 JSON 문자열 보간** — `run-hook-evals.ps1`의 ⑤ 게이트 회귀 케이스가 `'{"…"content":"' + $gate.u + '"}}'` 형태로 값을 직접 이어 붙인다. 현재 값에는 따옴표·백슬래시가 없어 안전하나, 이 패턴을 복제해 케이스를 늘리면 그런 문자가 든 문면에서 **JSON이 조용히 깨진다**(픽스처가 깨지면 그 케이스는 무엇을 검증하는지 알 수 없게 된다). 재사용 계획이 생기면 `ConvertTo-Json`으로 값만 이스케이프하는 헬퍼로 바꾼다. (출처: v1.150.0 T3 quality m1 — 판정 유보)
  - **해소(v1.152.0 T3)** — 본체 헤더에 `New-TranscriptLine`을 신설하고 보간 3곳(`$loopMsg`·`$realIncident`·`$gate.u`)을 전환해 이스케이프를 `ConvertTo-Json`에 맡겼다. 전환 전 리터럴과 산출 문자열이 `-ceq`로 완전 일치함을 4종 값에 대해 기계 대조했다. **부수 발견**: 최초 구현이 단일 요소 배열을 언랩해 `content`를 `{...}`로 직렬화했는데 **골든은 그 상태에서도 96/96 green**이었다(hook이 `@($mc)`로 감싸 읽는다) — 픽스처 생성 방식을 바꾸는 작업은 골든 통과를 등가의 근거로 쓸 수 없다.

- [2026-07-30 → 2026-08-03] **`run-hook-evals.ps1`이 2039라인 — 분리 "검토" 신호** (v1.151.0 T2에서 사용자 발화 오염 차단 골든 7건 추가로 1961 → **2039**. 직전: v1.150.0 T3에서 ⑤ 골든 26건 추가로 1800 → 1961. 직전: v1.149.0 T3에서 ③④ 확대 골든 25건 추가로 1663 → 1800. 직전 서술: v1.148.0 T3에서 루프 차단 골든 18건 추가로 1516 → 1663. quality 리뷰어가 구체 방향을 제시했다: *"다음 대규모 케이스 추가 시점엔 hook별 시나리오 파일 분리(`evals/scenarios/<hook>.ps1` dot-source)를 검토할 만하다"* — 현재는 단일 책임 유지라 분리하지 않음) — 원 등재 시점 서술: v1.147.0 T3의 델타 케이스 3건 추가로 1500을 넘겼다. AGENTS.md는 이것을 **강제 분리선이 아니라 검토 신호**로 규정하며, 이 파일은 단일 책임(hook 골든 러너)이고 hook별 섹션(§1~§13)으로 이미 구조화돼 억지 분리는 지역성을 해친다. 분리한다면 "hook별 파일 + 공용 헬퍼(`Assert-Case`·`Invoke-Hook`·격리 홈)" 단위가 자연스러우나 그때 **공용 헬퍼의 위치·로드 방식**을 정해야 한다. 케이스 추가로 계속 늘어나면 그 시점에 검토.
  - **해소(v1.152.0 T1)** — 게이트 블록 15개를 `plugins/pjc/hooks/evals/scenarios/*.ps1` **13개 파일**로 순수 이동했다(신규 코드 0 · 순서 변경 0 · 케이스 내용 변경 0). 본체는 헤더(공용 헬퍼 5종·공유 변수) + dot-source 13줄 + 종료부만 남아 **2,047 → 189행**(T1 시점. T3이 `New-TranscriptLine` 25행을 헤더에 더해 **최종 214행**), 최대 시나리오 파일은 `require-evidence.ps1` 618행(T2의 G8 추가 후 642행)이다. 판정은 **재조립 텍스트 등가**(허용 추가 4줄 외 diff 0)를 최상단에 두고 총계 512/512 · 케이스 이름 순서까지 일치 · `-Filter` 부분 실행 대조를 겹쳤다. `protect-harness`가 비인접 2블록이라 "hook별 1파일"과 "순서 불변"이 동시 만족 불가였고 **순서 불변을 택했다**(파일 13개).

- [2026-08-02 → 2026-08-02] **[BLOCKER급] `require-evidence` 검사 4가 스킬 발동 직후 구간에서 통째로 fail-open** — **반영(v1.151.0)**. 원인은 등재 내용 그대로였다 — `implement-task`/`plan-feature` SKILL.md 본문(약 41K자)이 transcript에 **`isMeta:true`인 순수 user 텍스트**로 기록되고, 스킬 문서가 Halt·중단 조건을 논하는 이상 그 본문의 `중단`·`새 세션`이 조건 5(`$userStop`)를 켜는 것이 구조적으로 필연이었다. **수정 방향 ⓐ(스킬 페이로드 제외)를 채택**했고(사용자 선택 — ⓑ 길이 상한은 오차단 방향 위험으로 미채택, 위 대기 항목으로 이관), 조사 중 **같은 클래스의 두 번째 오염원**을 실측해 함께 닫았다(`<task-notification>` subagent 완료 알림도 user 엔트리이며 8세션 45건 = 진짜 사용자 발화와 동수. 사용자가 "그만" 한 직후 알림이 도착하면 중단 지시가 가려진다). 결과적으로 **역순 스캔에 skip 4종**이 들어갔고(`require-evidence.ps1`의 `[추출 원칙]` 주석이 정본), 전 시점 스캔에서 `$userStop`이 참→거짓으로 정정되는 지점이 **세션당 27~65%**로 측정됐다. **게이트(`$loopActiveAfterUser`)도 함께 정상화**된다 — 페이로드가 `$userFound`를 즉시 켜 판정 창이 0이던 것이 진짜 발화까지 넓어져, 41~239개 시점에서 값이 뒤집혔다(⑤ 억제 해제 = 차단 증가 방향이므로 골든 G6가 이 표면을 고정). 골든 7건 신설 + **skip별 red 3회차 실증**(R1→G1·G6 / R2→G2 / R3→G7).
  - **⚠ 등재 시 함께 적었던 "수정 범위를 `$userStop` 경로로 좁혀도 된다(상한 재산정은 불필요)"는 이번 실측으로 반증됐다.** 사용자 엔트리 2종만 제외하면 스캔이 더 과거로 내려가 `$parsed` 200 상한에 도달하고, **`$userFound`를 잃는 시점이 104개** 생긴다(= 한 fail-open을 닫고 다른 fail-open을 여는 것). 진짜 원인은 알림이 아니라 **텍스트 없는 assistant 엔트리를 파싱한 뒤 버리는 기존 낭비**였다(어떤 세션은 653건 중 텍스트 보유 172건). 그 2종을 함께 걷어내야 해소되며, 적용 후 파싱 최댓값은 stdin 경로 1·폴백 87로 **종전(최대 175)보다 낮다**. 종전 반증(“최대 143 — 200 상한 선도달은 재현 안 됨”)은 **종료 시점 1개만 잰 값**이었고, Stop hook은 매 turn 끝에 돌므로 **전 시점(assistant 엔트리마다 절단)으로 재야** 최댓값이 보인다. (출처: v1.150.0 F-7 B1 → v1.151.0 T1~T3 실측 종결)

- [2026-07-30 → 2026-07-31] **③(중간 테스트 요청 정지)은 문서 규칙 단독 방어** — **해소(v1.149.0)**. 이 항목이 예고한 실패가 **다른 문구로 먼저 실현됐다** — 루프 중간에 "새 세션으로 옮길지 알려주세요"로 정지한 사고가 관측됐고, 원인은 이 항목이 지목한 바로 그 구멍(`$rxLegit`의 `\?`가 질문형 종료를 전부 fail-open)이었다. 요구했던 "더 좁은 시그니처 + 오차단 재평가"를 그대로 수행했다: 정당 정지 신호를 **Strong/Weak 2층으로 분리**해 ③④에서는 Weak(물음표·"확인 요청")를 통과 근거로 인정하지 않고, 대신 **정당 개입 지점 6곳에 Strong 마커(⛔·⏸️)를 문면으로 부여**했다. ④(수동 실행 요청)는 **위임 부사 → 실행 동사 → 요청 어미 3요소 결합**으로 좁혔다(리뷰가 재현한 오차단 반례 2건을 반영해 두 번 좁힘). 골든 음성 17건 + **마커 유무만 다른 델타 짝 3조**로 경계를 실증.
- [2026-07-30 → 2026-07-31] **`halt-conditions.md`와 `implement-task/SKILL.md`의 예외 목록 범위 차이** — **해소(v1.149.0)**. 예고대로 1구씩으로 닫혔다. 양쪽에 **축이 다르다**는 설명을 넣었다 — `halt-conditions.md`는 *중단 사유*(무엇이 루프를 세우는 것을 정당화하는가)를, SKILL.md 금지 표현 예외는 *정지해도 되는 말하기*(어떤 형태의 발화가 허용되는가)를 센다. 개수가 달라도 모순이 아니며 한쪽을 다른 쪽에 맞추지 말라는 경고까지 함께 남겼다.
- [2026-07-30 → 2026-07-31] **`implement-task/SKILL.md` 금지 표현 블록쿼트가 hook의 조건·상한 수치를 본문에 복제** — **해소(v1.149.0)**. 수치 나열을 걷어내고 `docs/hook-conventions.md` 포인터로 대체했다. 같은 확대로 조건이 6개에서 늘고 신호가 2층으로 갈렸으므로 **바로 드리프트가 생길 지점**이었다. 정본(`hook-conventions.md`)은 3유형 표·2층 표·마커 규약까지 담도록 확장했고, SKILL.md에는 **문구 목록이 정본**이라는 방향 선언만 남겨 `$rxAdvance`·`$rxHandoff`·`$rxManualAsk` 셋 전부를 지목하게 했다(골든 L12가 이 정합을 기계로 강제한다).

- [2026-07-25 → 2026-07-31] **`B-1` 라벨 이름 충돌** — **반영(전제 소멸로 자연 해소)**. 등재 당시 `implement-task/references/recovery.md`의 `B-1`/`B-2`(Reviewer 실패 대응 B 분기의 하위 유형)가 `llm-wiki` 절차 B의 `B-1`과 같은 라벨을 써서, 하니스 전체 일괄 치환 시 위키 절차가 오염될 위험이 있었다. **현재 `recovery.md`에 `B-1` 매치가 0건**이다(실측) — 그 파일의 실패 대응은 `### A. 과부하 (HTTP 529)` · `### B. 도구 사용 불가` · `### C. 안전 분류기 거절`의 **A/B/C 단일 depth 구조**로 정리되면서 하위 라벨 자체가 사라졌다(`recovery.md:62·71·88`). 충돌 대상이 없어졌으므로 제안했던 개명(`B-정책`/`B-물리`)도 불필요하다. **남는 것**: `llm-wiki` 쪽 `B-1`은 그대로 존재하나(`check_consistency.py` 등) 이제 하니스 내 유일 사용처라 치환 오염 위험이 없다. (출처: B 분기 분리 작업 → 2026-07-31 실측 종결)
- [2026-07-25 → 2026-07-31] **T1 커밋 메시지 계수 오기** — **기각(액션 아이템이 아님)**. `fbdc0aa` 본문의 "6지점"이 실제 열거 7개(`plugins/pjc/skills/implement-task/SKILL.md:264`·`:294`·`:308`·`:314`·`:322`·`:473`·`:497` — 등재 당시 라인)와 어긋나는 것은 사실이나, **등재 당시 이미 "이미 완료된 커밋이라 rebase 없이는 못 고침 — 이력 정정 가치가 낮아 기록만"으로 결론이 나 있었다.** 즉 처음부터 수행할 작업이 없는 **기록**이었고, 대장의 `## 대기`는 "미뤄둔 **할 일**"을 추적하는 자리다. 할 일이 없는 항목이 대기에 남으면 매 batch마다 판정 비용만 발생하고 잔량을 부풀린다. 정정 자체는 지금도 불가하며(공유 이력 rewrite는 위임 불가 파괴적 작업), 사실관계는 이 종결 항목에 보존된다. (출처: F-7 m5 → 2026-07-31 기각 종결)
- [2026-07-22 → 2026-07-31] **post-write-checks가 "시크릿 금지"를 가르치는 문서 자체를 신고하는 오탐** — **반영(v1.147.0, 스캔 범위 축소로 해소)**. 등재 당시 원인은 편집분이 아니라 `plan-template.md`에 **이미 있던** 금지 예시 줄(`❌` 마커가 붙은 DB 연결 URI 예시)이 전재 스캔에 걸린 것이었다. v1.147.0이 **추적 파일은 `git diff HEAD --unified=0`의 추가 라인만 스캔**하도록 바꾸면서(`plugins/pjc/scripts/post-write-checks.ps1:186-206` — 주석 *"스캔 범위: 추적 파일은 HEAD 대비 미커밋 추가 라인만"*), **그 줄을 건드리지 않는 편집은 더 이상 스캔 대상이 아니다**. 금지 예시 자체는 지금도 실재하지만(`plan-template.md:43`) 추적 파일이므로 재발하지 않는다. 탐지 패턴(`secret-patterns.ps1`)은 무수정이라 미탐 위험 없이 "어디를 보는가"만 좁힌 것이다. **남는 범위**: untracked·gitignore·비 git·`git diff` 실패는 여전히 전재 폴백이라 그 경로에서는 같은 오탐이 가능하다. 또 등재 시 동일 계열로 지목한 **커밋 경로**(`bash-hook-lib.ps1`의 `diff HEAD` 보완 스캔)는 미적용이며 대장에 별도 등재돼 있다 — 이 항목은 **편집 경로 한정으로 종결**한다. `❌`/`WRONG` 마커 배제안은 채택하지 않았다(범위 축소로 목적이 달성돼 불필요). (출처: v1.131.0 T1 편집 중 실관찰 → 2026-07-31 실측 종결)
- [2026-07-30 → 2026-07-31] **`block-destructive`가 `Remove-Item Env:<이름>`을 파일 삭제로 오탐** — **기각(오진)**. 등재 당시 *"hook이 `Set-Location "D:\…"` 인자를 대상으로 오인한다 → `Env:`·`Variable:`·`Function:`·`Alias:` PSDrive를 차단 대상에서 제외하는 것이 정확하다"* 고 적었으나, **차단 주체가 이 hook이 아니다**. 실측 6종: ① 보고된 명령 + 변형 **11종을 hook에 stdin 주입 → 11/11 exit=0**(차단 0건, `-Recurse`·`-Force`를 붙여도 통과) ② hook 이벤트 로그(`~/.claude/.state/hook-events/2026-07.jsonl`)의 2026-07-30 `block-destructive` block 기록은 **1건뿐이고 그것은 `Get-ChildItem $env:TEMP … | Remove-Item`**(열거 파이프 삭제) — `Env:` 삭제 차단 기록 **0건** ③ 메시지 형식이 다르다 — 하니스는 `BLOCKED: … / Command: …` 형식이고 `is blocked` 문자열은 **레포 전체에 없다** ④ **실물 재현**: 조사 세션에서 같은 조합을 PowerShell 도구로 실행하니 `Remove-Item on system path ''D:\Personal' is blocked. This path is protected from removal.` ⑤ 같은 명령을 **Bash 도구로 실행하면 통과**(`BASH-TOOL-OK`) ⑥ hook matcher는 `Bash|PowerShell` **양쪽**이라, hook이 동일하게 통과시키는데 도구에 따라 결과가 갈리는 것은 차단이 hook 바깥에서 일어난다는 뜻이다. **진짜 원인은 Claude Code의 PowerShell 도구 내장 경로 보호** — 명령 문자열에 `Remove-Item`이 보이면 삭제 대상을 추출해 보호 경로와 대조하는데, 추출기가 실제 대상(`Env:…`)이 아니라 **같은 문자열 안 다른 위치의 따옴표 경로**를 집는다(`'D:\Personal Project\…'`가 공백에서 잘려 `'D:\Personal` → 드라이브 루트 근처로 판정). **"비결정적"의 정체**: 비결정이 아니라 **조합 의존**이다 — `Remove-Item` 토큰과 공백 포함 경로 리터럴이 **같은 명령 문자열에 함께 있을 때만** 발화하며, 앞선 호출들엔 그 조합이 없어 통과했다. 조사 세션에서 인라인 배치는 차단·스크립트 파일로 옮기니 통과로 그대로 재현됐다. **회피법(실증)**: ⓐ 검증 배치를 **스크립트 파일로 분리**(명령 문자열에 조합이 안 생김) ⓑ **Bash 도구** 사용 ⓒ `$env:NAME = ''` 대입. ⚠️ **hook 수정은 하지 않는다** — 실제 오탐이 아닌데 차단 범위를 좁히면 마지막 방어선만 약화된다(등재 당시의 수정 지시를 여기서 명시적으로 철회한다). 근본 원인은 Claude Code 내장 코드라 이 레포에서 고칠 수 없다. (출처: v1.148.0 T3 실관찰 → 2026-07-31 디버깅 세션 실측 기각)
- [2026-07-30 → 2026-07-30] **`Select-String -Quiet` 전 파일 스캔의 최악 경로 성능** — **실측 종결**. v1.148.0 T8이 검사 4의 조건 ③을 tail에서 전 파일 스캔으로 바꾸면서(F-7 M2 — 긴 루프에서 흔적이 tail 밖으로 밀려 fail-open), *"흔적이 없으면 전량 스캔인데 매 종료마다 도는 hook의 10초 예산에서 안전한가"* 가 미실측으로 남았다. **F-7 2회차 리뷰어가 직접 측정: 최악 경로(무매치=전량 스캔), 이 repo 최대 transcript 7.3MB / 2817줄 → 399ms** — 예산의 **4%**. `-Quiet` 조기 종료가 걸리는 정상 경로는 이보다 짧다. 성능 위험 없음으로 종결한다.
- [2026-07-13 → 2026-07-30] **README 버전 이력의 서술 정책 확정**(시점 스냅샷 vs 현행 반영) — **반영**. 정책은 `AGENTS.md`의 **`README.md 갱신 규약`** 으로 이미 확정돼 있다(2026-07-30 실측): *버전별 수정 내역(`> v1.x.y: …` changelog 블록) 기재 **금지**, 현재 기능 설명만 유지, 변경은 이력 append가 아니라 **기존 문장을 현재 동작에 맞게 수정**, 제거된 기능 설명은 삭제, 버전 표기는 상단 1곳만 두고 `plugin.json`과 함께 갱신.* 즉 **"현행 반영"으로 확정**됐고 append-only 관례는 폐기됐다 — 등재 당시 "정책 자체가 미확정"이던 상태가 해소됐다. 실제 적용도 확인: `README.md`에 `> v1.` changelog 블록 **0건**.
- [2026-07-30 → 2026-07-30] **판정 게이트 축약구의 리터럴 drift 후보** — 반영(v1.147.0 T2). 기계 검사 추가는 **기각**하고 **축약구 자체를 7지점에서 제거**했다 — 리터럴이 K 1 블록에 없고(§0-1에 있었다) 7지점 서식이 불통일해 "정본에서 추출해 대조"가 성립하지 않았으며, 게이트 언급 10건 vs 축약구 7곳의 차이 3곳을 예외 목록으로 하드코딩해야 했다(그 목록이 곧 새 stale 지점). 반복이 없으면 drift가 불가능하므로 감시 장치가 불필요해졌다. 포인터와 K 1 본문은 유지. **다음 세션이 기각된 원계획(검사 추가)을 다시 시도하지 않도록 이 사유를 남긴다.**
- [2026-07-30 → 2026-07-30] **`AGENTS.md`의 JSON 매니페스트 검증 명령이 파일 부재를 "OK"로 출력한다** — 반영(v1.147.0 T1, `-ErrorAction Stop` + 결과 변수). **등재 당시 서술을 정정한다**: "exit code는 0이 아니지만 출력만 오인된다"고 적었으나 실측 결과 **구 형태는 exit code까지 `0`**이었다 — 사람이 로그를 훑을 때만이 아니라 자동화 경로에서도 실패가 통과로 처리됐다(더 심각). 수정 후 부재 경로에서 exit 1 + OK 미출력, 정상 3파일은 종전과 동일.
- [2026-07-30 → 2026-07-30] **v1.146.0 hook의 실동작 스모크** — 확인 종결. 설치본 `~/.claude/plugins/cache/pjc-harness/pjc/1.146.0/scripts/session-context.ps1`에 vault 판정이 실재하고(18건), 2026-07-30 세션의 SessionStart가 `[pjc 세션 컨텍스트] 위키 vault: 설정됨 (…)` 라인을 **실제로 주입**했다. 재설치 후 새 세션에서만 관찰된다는 조건이 충족된 것을 실측으로 확인.
- [2026-07-30 → 2026-07-30] **hook 골든 무인자 전체 실행의 "완료 불가"는 환경 한계가 아니라 실행 방법 문제였다** — 반영(v1.146.0 F-2). v1.145.0에서 5회 중단, v1.146.0에서도 3회 실패했으나 **출력을 파일로 리다이렉트해 백그라운드로 실행하니 426/426 OK (FAIL 0, exit 0)** 로 완주했다. **원인은 둘이며 둘 다 피해야 한다**: ① `| Select-Object -Last N` 같은 **파이프라인의 출력 버퍼링**(백그라운드 killed·리뷰어의 40분+ 미종료의 원인) ② 스위트가 10분을 넘어 **전경 실행은 시간 캡에 걸린다**(리다이렉트만 해도 전경이면 실패 — 첫 시도의 원인). 실행 방법은 **`AGENTS.md`의 Hook 골든 블록에 기록**했다(F-2 실행 시점에 읽히는 위치이고 session-context hook이 매 세션 전문 주입하므로 도달률이 대장보다 높다 — 이 대장 항목은 이력 보존용). v1.145.0의 5회 중단도 같은 원인으로 추정(단정 아님). 러너 처리량 개선(in-process)은 여전히 가치 있으나 F-2 이행의 전제는 아니다. (출처: v1.146.0 F-7 B1 → 실측 해소, F-7 2회차 N1·n3)
- [2026-07-29 → 2026-07-30] **`disallowed-tools` 실동작 스모크 + effort 치환 확인** — `implement-task` frontmatter의 `disallowed-tools: AskUserQuestion`과 본문 effort 분기가 설치본에서만 실행되는 탓에 미검증으로 남아 있던 3건. **v1.145.0 릴리즈·설치가 완료된 뒤 스모크로 전부 실증**(2026-07-30): ① **질문 도구 제거 동작** — 스킬 로드 후 `AskUserQuestion` 호출이 `Permission ... denied`로 거부됨 ② **본문 effort 치환** — 101행이 `high`로 치환돼 도착(파일 원본은 변수 리터럴) ③ **Read 경로 미치환**(부수) — 같은 파일을 Read로 읽으면 리터럴로 보임을 확인. 치환은 스킬 로드 경로에서만 일어난다. ③은 압축 후 재읽기 경로(컨텍스트 관리 규칙 4)에 걸리지만 effort 확인이 루프 시작 전 1회라 재확인 대상이 아니고 리터럴이 low/medium 어느 쪽도 아니어서 경고가 오발화하지도 않는다 — **실해 없음**. 반영: SKILL.md의 "미검증" 서술을 실측 결과로 정정(해제 규칙 때문에 "1차 방어선은 문서 규칙"이라는 취지는 유지)·주석에 로드 경로 한정 명시·PRD FR-12/FR-13에 실증 이력 추가. (출처: Opus 5 대응 plan T9 → v1.145.1)
- [2026-07-25 → 2026-07-29] **`recovery.md:3` "아래 두 절" 분배 표현** — `pjc-systematic-debugging`은 「Subagent 호출 규약」 절만 참조하고 "Reviewer 호출 실패 대응" 절 포인터는 0건인데, 서두가 두 스킬 모두 "두 절"을 참조하는 것처럼 읽힌다(집합적으로는 참). "스킬별로 참조 절은 다르다" 한 구 보강 — 단독 커밋 가치는 없어 다음 하니스 편집 세션에 묶어 처리. (출처: 동기 호출 규약 명문화 F-7 재호출 m1) — **반영(Opus 5 대응 plan T8)** — 서두에 "다만 스킬마다 참조하는 절은 다르다(`plan-feature`는 두 절 모두, `pjc-systematic-debugging`은 「Subagent 호출 규약」만 참조한다)"를 보강해 분배 오독을 제거.
- [2026-07-25 → 2026-07-29] **`implement-task/SKILL.md:294-296` 헤더-내용 주제 경계** — "Reviewer 호출 실패 대응" 헤더 아래 첫 문단이 정상 경로(동기 규약)라 훑는 독자에게 순간 혼동 여지. `**호출 규약(정상 경로) — 전부 동기.**` 라벨이 이미 있어 실질 위험은 낮음(리뷰어도 "수정 강제 아님"). 소제목 분리 검토. (출처: 동기 호출 규약 명문화 T1 V-6 MINOR m2) — **반영(Opus 5 대응 plan T8)** — 헤더 아래에 "이 절은 정상 경로(동기 호출 규약)와 실패 대응(A/B/C)을 함께 담는다 — 바로 아래 첫 문단이 정상 경로이고, 실패 분기는 `A.`부터다" 1줄 추가. 같은 작업에서 헤더 이름도 "(과부하·도구 불가·거절)"로 갱신.
- [2026-07-24 → 2026-07-24] **SKILL.md 경량화 (규칙 폐기·통합 별도 접근)** — v1.138.1(`task/skill-consolidate`)에서 **통합**을 시도 → 실익 미미 실증: T1 pre-review 선설명 −55자(Phase D ③과 중복 부연 축약)·**T2 완전 일치 중복 0**. 통합의 실익도 rationale 분리처럼 제한적임이 references 레벨에서도 확인됐다 — **"겉보기 중복 ≠ 실제 중복"**(explorer가 꼽은 references Type 표는 각기 다른 도메인, 금지표현·정형구는 국면별 정당한 반복). 감량 기법을 D6("literal 중복·부연 삭제만, 재서술 금지")로 한정해 안전 유지. **규칙 폐기·구조 재편은 미수행 → 재-Deferred**(위 대기 "SKILL 방향 재편"). (출처: v1.138.0 T3 → v1.138.1)

- [2026-07-20 → 2026-07-23] **`warn-commit-secrets`가 시크릿 탐지기 자신의 패턴 정의를 신고하는 오탐** — `lint.py`를 `git add`하자 원인이 `lint.py:79`의 탐지 규칙 정의였던 사례(커밋 diff에 실제 시크릿 0건, grep 실증). 훅이 `git add` 대상의 전체 파일 내용을 스캔하는 규약의 구조적 부작용으로, 보안 도구·탐지기 소스를 다루는 레포에서 반복됐다(v1.135.0 리뷰 후속 커밋이 같은 오탐으로 차단된 실사례가 재수용 계기). (출처: v1.129.0 T3 구현 중 실관찰) — **반영(v1.136.0 T1~T2)** — `bash-hook-lib.ps1` `git add` 경로 나열 Leaf 분기에 추적 판정 추가: 추적 파일은 `git diff HEAD` 추가 라인만 스캔(이력 기존 내용 재신고 제거), untracked(ignored 강제 add 포함)는 전체 스캔 유지. 골든 (n1)~(n3) red-green 실증, 전체 408/408.
- [2026-07-22 → 2026-07-22] **lint.py의 pending 잔량 집계가 형식 위반 줄을 조용히 누락** — `lint.py:934`의 `^\s*-\s*\[\d{4}-\d{2}-\d{2}\]\s*\[TAG\]`는 **날짜 선두**를 요구하는데, 실제 vault 큐 17줄이 전부 날짜를 끝 괄호에 둔 형태(`- [DECISION] proj: … (2026-07-15)`)라 **집계가 0건**이었다(실측: lint에 pending INFO 자체가 미출력 → 정규화 후 17건 정상 집계). 형식 규약은 2026-07-02(`31db62c`)에 도입됐고 큐 항목은 07-15/16자라 legacy가 아니라 **규약 위반이 무검출로 방치된 것**. 결과적으로 K 5-2의 20건 잔량 경고·소비 유도가 전부 침묵했다. 보완 방향: ⓐ 태그는 있는데 날짜 선두가 아닌 줄을 `형식 위반 n건`으로 WARN 집계(무검출 제거) ⓑ 또는 날짜 위치에 관대한 파싱. **"0건"과 "형식이 틀려 못 셈"이 구분되지 않는 것이 핵심 결함** — 큐가 비었다는 보고가 곧 안전 신호로 읽힌다. (출처: v1.131.0 이후 위키 큐 정리 세션 실관찰) — **반영(v1.132.0 T1~T2)** — §7-25에 형식 위반 WARN 추가(불릿 ∧ 태그 존재 ∧ 선두 형식 미매치), 태그 목록 단일 출처화, schema §7-25·F-1 25·F-0 동기, 회귀 픽스처 pending-malformed + 정상 픽스처 오탐 0 고정, eval 33→34 PASS.

- [2026-07-19 → 2026-07-19] llm-wiki §7-27 양성 검출 eval fixture 추가 — 반영(v1.125.0 T2 — `no-hanyeong-guide` fixture + lint-cases 케이스, 31/31 PASS. 한글전용 alias ui-ux 가이드 행이 "가이드 한/영 병기 누락" WARN을 내는 것을 항구 회귀 그물로 승격)

- [2026-07-10 → 2026-07-13] AGENTS 게이트 임시폴더 예외 분기 골든 케이스 추가 — 반영(v1.118.0 T1 — TE1(plan 게이트)·TE2(AGENTS 게이트) 2건 신설. 픽스처를 `$iso`(GetTempPath 하위) + **흔적 없는 transcript 주입**으로 구성해, 미주입 시 fail-open으로 exit 0이 나는 거짓 green을 방지)
- [2026-07-08 → 2026-07-12] 위키 feat-safety-hooks 보호 집합 서술("hook 스크립트 8종") 현행화 — 확인 종결(2026-07-12 하네스 ingest 세션에서 grep 확인: "8종" 서술이 이미 없음 — 이전 ingest(v1.108~113 반영)가 "검사 10종 + 디스패처"로 현행화 완료)
- [2026-07-09 → 2026-07-12] llm-wiki fixture `archive-exempt/.../oldproj.md` UTF-8 BOM 정리 — 기각(v1.116.0 D6: 그 BOM은 lint-cases archive-exempt 케이스의 expect_absent "BOM"이 검증하는 **의도된 테스트 입력**(90_archive BOM 무경고 실증)이라 제거하면 검증이 공허해짐 — 컨벤션 위반이 아니라 테스트 대상 데이터)

- [2026-07-10 → 2026-07-10] warn-external-ops가 `git merge-base`(읽기 전용 조회)를 "git merge"로 오탐 — 반영(v1.113.1 T1 — bash-hook-lib.ps1:39 `merge\b`→`merge(?![-\w])`, merge-base·merge-tree·merge-file·merge-index plumbing 오탐 제거·실제 merge 경고와 --abort/continue/quit 제외 보존, 골든 merge-base·merge-tree 무경고 2건 추가)
- [2026-07-10 → 2026-07-10] 위키 feat-safety-hooks·feat-plan-feature에 v1.111.0 bootstrap 게이트·직접 작성 금지 반영 — 반영(2026-07-10 위키 ingest 세션 — feat-plan-feature Step 1 bootstrap 강화 + feat-safety-hooks require-plan 게이트 동작·지도 서술)
- [2026-07-10 → 2026-07-10] Type B 분류 정의가 구조 기준(단일 파일·메서드·호출자 없음)뿐이라 신규 비자명 로직이 B로 새어 검토(V-5 prefilter만·V-3/V-6 생략) 얕아짐 — 반영(v1.113.0 T1 — plan-feature:257에 경계 명문화(비자명 로직·알고리즘·제어흐름·상태변경 신규 심볼은 Type C 이상, 자명 접근자·상수·순수 위임은 B 유지) + plan-reviewer:39 오분류 검출 예시 병기, 방어선 2층)
- [2026-07-10 → 2026-07-10] SessionStart matcher `compact` 추가 + 요약 직후 재확인 컨텍스트 주입 — 반영(v1.112.0 T1 — session-context.ps1 신설, matcher 4종 + compact 리마인더, 골든 SC2)
- [2026-07-10 → 2026-07-10] post-write-checks notes.md 30,000자 초과 아카이브 경고 — 반영(v1.112.0 T2 — 섹션 2b, Test-WarnOnce 디듑, 골든 NA1~NA3)
- [2026-07-10 → 2026-07-10] SessionStart에 plan.md 존재·미완료 task 수·notes.md 최신 항목 날짜 주입(로컬 경량판) — 반영(v1.112.0 T1 — session-context.ps1에 compact 항목과 통합 구현, 골든 SC1·SC1b·SC3~SC5)
- [2026-07-10 → 2026-07-10] implement-task Phase D clean/dirty 판정×재리뷰 커밋 프로토콜 모순 + review-fix 커밋 메시지 미규정 — 반영(v1.110.0 T1 — Phase D ①을 HEAD 커밋 제목 판정으로 교체·review-fix checkpoint 규정·재개 트리 5분기+폴백·새 완료 커밋 --allow-empty)
- [2026-07-10 → 2026-07-10] plan-feature 분할×PRD 전수 커버×12-a 규정 교착 — 반영(v1.110.0 T2·T3 — part별 Coverage 스코프(`⏭️ 다음 part`/`✅ 이전 part 기구현` 행)+합집합=전수 게이트+두 part 동시 작성 명문화, 12-a 분할 인지+실질 MAJOR 게이트, 마지막 part Phase G/F-7 전수(전체 트리 기준))
- [2026-07-09 → 2026-07-10] 하네스 최근 버전분(v1.102.0~) 위키 ingest — 반영(2026-07-10 위키 세션 2회: v1.102~1.108 feature 5페이지 상세 ingest + v1.109.0 델타·run_eval 함정·A~L 표기 드리프트 정정, lint 델타 0)
- [2026-07-08 → 2026-07-10] plan-feature description 1,018자 하드 한도(1,024) 근접 — 반영(v1.109.0 T5 — 981자로 무손실 압축(연결부만), 여유 43자. pjc-systematic-debugging 1,019→921자도 동일 처리. 전후 트리거 eval 동률 실증)
- [2026-07-09 → 2026-07-09] hook 이벤트 로그 자동 요약(3개월 축적 후) — 반영(v1.107.0 T2 `report-hook-events.ps1` 수동 리포트로 대체 — 자동 실행은 사용자 확정으로 채택 안 함)
- [2026-07-08 → 2026-07-08] llm-wiki 본체에서 절차 K 초경량 분리 — 기각(정식 검토 결과 분리하지 않음: 최빈 경로가 본체를 어차피 로드해 토큰 중립+Read 1회 지연만 추가. 재논의는 로드 경로가 바뀔 때만 — deferred-followups-closure)
- [2026-07-08 → 2026-07-08] explorer subagent 모델 상향(haiku→상위) — 기각(현행 유지: effort medium 반영 직후 관찰 기간 0·실패 실사례 없음. 재검토 조건: locating 실패·부정확 실사례 관찰 시 — deferred-followups-closure)

<!-- 초기 시드 sweep 근거 (2026-07-09, v1.107.0 T4): notes.md 현행 전 항목의 "미처리 Deferred" 목록 +
     docs/plans/*.md 6파일의 "## Deferred / Follow-up" 섹션 + notes-archive/2026-07.md 대조.
     중복은 최초 출처로 병합(suggest-agents-record 오탐 2류는 동일 수정 라운드라 1항목).
     "재설치(install.ps1)"는 반복 등장하나 작업 항목이 아니라 사용자 지시로 보류 중인 운영 행위라 제외.
     분할 plan 상호 포인터(다음 plan 안내)는 실행 완료된 안내문이라 제외. -->
