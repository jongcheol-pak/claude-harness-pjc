> **이 문서는 `docs/harness-conventions.md`에서 분리됐다** (v1.223.0) — 그 파일이 조건부 참조 상한 137,000B에 여유 20B로 닿아, **hook 골든을 돌릴 때만 읽히는** 이 절을 별도 문서로 옮겼다. **규약 문면은 줄지 않았다 — 이동하며 바뀐 것은 상대 참조 3곳뿐이다**(*"아래 「골든 부분 실행의 판정 자격」"* 등, 분리로 「아래」가 다른 파일을 뜻하게 돼 `docs/harness-conventions.md` 경로를 앞에 붙였다). 그 밖의 서술은 이동만 했다(이관은 이동이지 요약이 아니다).

## 골든 러너 운용 (실행·대기·판정)

> `AGENTS.md` 「Hook 골든 회귀」가 지목하는 정본이다. **이 절을 읽지 않고 돌리면 완주하지 않는다** — 과거 "환경상 실행 불가"로 Phase F-2를 갈음한 사례들이 전부 이 절차를 쓰지 않은 것이다.
>
> **세 골든 모두 전경 실행으로 완주한다**(v1.225.0부터 — hook 골든의 분리 실행 절차가 사라졌다) — **llm-wiki lint 골든**(`run_lint_evals.py`, 102케이스)은 약 45초, **AGENTS.md 이관 골든**(`run_relocation_evals.py`, 14케이스)은 1초 미만이다(2026-08-22 실측). 셋을 한 덩어리로 읽고 전부 백그라운드로 돌리면 오히려 느려지고, 결과 회수도 한 단계 늘어난다.

### 검사 대상

격리 USERPROFILE에서 hook 11종(block-destructive·protect-harness·warn-external-ops·require-plan-for-write·require-task-checkbox·suggest-agents-record·post-write-checks·warn-commit-secrets·warn-version-drift·session-context·session-end-cleanup)을 stdin JSON 케이스로 실행해 exit code·출력을 대조한다(케이스 정본: `plugins/pjc/hooks/evals/hook-cases.json` + 러너 내장 시나리오). 전부 OK면 exit 0.

### 실행 모드 4종

> **⚠ 아래 「명령」 열은 모드를 식별하는 이름이지 그대로 돌릴 명령이 아니다 — 실행 방법은 아래 「실행·대기 절차 (정본)」이 정본이다.** 전경 실행·`run_in_background`·`Start-Process` 리다이렉트는 그 절이 **이름으로 기각한 세 방법**이고, 각각 도구 시간 캡(10분)과 0바이트 출력으로 실패한다. **이 경고를 표 앞에 두는 이유**: 표의 명령이 그대로 실행 가능한 형태라 복사해 쓰기 쉬운데 정본 절차는 그 아래에 있어, 표만 읽고 착수하면 그 셋 중 하나를 고르게 된다(v1.204.0 회차가 실제로 셋을 전부 시도해 두 번 killed됐다 — AGENTS.md 가 이 절을 지목하는 경고를 이미 달아 두었는데도 그랬다).

| 모드 | 명령 | 소요(실측 — 값마다 케이스 수 병기) | 용도 |
|---|---|---|---|
| **병렬 (기본)** | `run-hook-evals.ps1` | **약 6~15분**(898초·922초 — 531케이스 시점 / **338초** — 527케이스, 2026-08-09 / **326초** — 769케이스, 2026-08-27 실측 / **930초** — 635케이스, 2026-08-20 실측(고아 프로세스 자동 회수 도입 후 첫 측정)) | **판정 정본.** 시나리오를 13그룹으로 나눠 자식 프로세스에서 동시 실행(동시 상한 6) |
| **순차** | `-Sequential` | **약 27분**(1,645초 — 531케이스 시점) | 병렬과의 등가 대조 기준 · 병렬 경로 장애 시 폴백. **판정 정본 자격은 병렬과 동일** |
| **이어하기** | `-Resume` | 완료 그룹은 즉시 스킵(실측 3초) | 중단·kill 후 남은 그룹만. 완료 마커 없는 그룹은 판정 파일을 버리고 다시 돌린다 |
| **부분** | `-Filter <hook명>` | 좁힌 범위만 | **개발 중 반복 확인 전용** — 판정 근거로 쓸 수 없다(`docs/harness-conventions.md` 「골든 부분 실행의 판정 자격」의 예외 조건을 갖춘 경우만) |

- **`-Resume`이 스킵하지 *않는* 범위**: 상태는 **필터 집합 + HEAD 커밋 + 검증 자산 내용 해시**로 격리된다(디렉터리 이름에 각인 — `scope.txt`로 확인 가능). 그래서 **다른 필터·다른 커밋·러너나 시나리오나 hook을 고친 뒤의 판정은 재사용되지 않는다** — 조건이 다르면 상태가 애초에 보이지 않아 그 그룹을 다시 돌린다. 이 격리가 없던 v1.159.0 개발 중에 **`-Filter`로 2그룹만 돌린 뒤 무필터 `-Resume`을 실행하자 12그룹을 전부 건너뛰고 `112/112 OK (FAIL 0)` exit 0**이 나왔다(419케이스가 조용히 빠졌고 부분 실행 경고도 없었다). **내용 해시를 함께 넣는 이유**: HEAD만 각인하면 *"전체 실행 → kill → 코드 수정 → `-Resume`"* 에서 **낡은 코드로 낸 판정**이 재사용되고 스코프 줄은 유효한 것처럼 보인다(커밋이 그대로이므로). 스킵이 일어나면 출력에 `[RESUME] … (스코프 filter=…|head=…|assets=…)`가 찍히므로 재사용 사실과 범위를 그 줄로 확인한다.
- **`-Filter`는 단일 문자열 콤마도 받는다**(`-Filter "a,b"`). v1.159.0 이전에는 배열 전달만 동작해 `"a,b"`가 통째로 한 이름이 되어 **매칭 0건 실패로만 드러났다**.
- **고아 콘솔 프로세스는 자동 회수된다 — 그래도 느려지면 그것부터 센다** — `orphan-process-cleanup.ps1`이 **Stop·SessionStart·SessionEnd** 세 시점에서 부모가 죽은 `more.com`·`find.exe`를 걷는다(회수 사실은 화면에 안 뜨고 `hook-events` jsonl에만 쌓이므로 `report-hook-events.ps1`로 확인한다). **자동 회수가 닫지 못하는 경우가 둘 있다** — ⓵ **커널에 갇힌 프로세스**: `Stop-Process`가 성공을 반환하고 `taskkill`이 *"실행 중인 인스턴스 없음"*이라 답하는데도 CPU가 계속 증가한다(핸들 0·스레드 1·커널 모드 93% — 2026-08-20 `find.exe` 12개 실측). 회수는 이를 `Unkillable`로 계상하고 마커를 남겨 재시도만 멈춘다. ⓶ **회수 시점 사이**: 세션이 크래시로 끝나면 SessionEnd가 발화하지 않아 다음 SessionStart까지 남는다. 진단은 `Get-CimInstance Win32_Process -Filter "Name='more.com' OR Name='find.exe'"`로 세고, 부모가 죽었는데 남아 있으면 위 ⓵ 상태다. **애초에 생기지 않게 하는 쪽이 본류다** — `find /` 류 전역 탐색은 Bash 도구 10분 캡 안에 끝나지 않아 실행하는 순간 고아가 확정되므로 `warn-global-find` 경고가 실행 전에 알린다. 2026-08-19에는 이 장치가 없어 전체 실행이 37분에서 2시간 이상으로 늘었고(고아 `more.com` 54개, 각 누적 CPU 20,000초+) 「멈췄는가」를 반복 판정하는 데 시간을 썼다.
- **격리 폴더는 부모 하나 아래 모이고, 중단이 남긴 것은 3일 뒤 걷힌다 (v1.184.0)** — 러너는 실행마다 두 곳을 만든다: **작업 폴더** `%LOCALAPPDATA%\pjc-hook-evals\run\<접미>`(시나리오 픽스처가 놓이는 곳으로, **반드시 임시 폴더 밖이어야 한다** — `require-plan-for-write`가 temp 하위를 무조건 통과시키므로 픽스처가 temp 안에 있으면 차단 시나리오가 통째로 무력화된다)와 **격리 홈** `%TEMP%\pjc-hook-evals\run\<접미>`. 같은 임시 쪽 부모에 `state`(`-Resume` 상태)와 `scratch`(temp 완화 경로 픽스처)가 나란히 있다.
  - **정상 종료는 자기 폴더를 지운다**(순차 경로와 자식의 `finally`). 남는 것은 **kill·타임아웃으로 그 자리에 닿지 못한 실행분**이며, 걷는 코드가 없던 시절 2026-08-20 시점에 평면 폴더 80개가 쌓여 있었다.
  - **코디네이터가 기동할 때 3일 경과분을 정리한다**(`eval-paths.ps1`의 `Invoke-EvalSweep`). 대상은 셋이다 — 현행 `run\` 하위 · **부모 폴더 도입 이전에 최상위에 흩어져 만들어지던 평면 이름**(`pjc-hook-evals-<접미>`, 구 `-Resume` 상태 루트 `pjc-hook-evals-state` 포함) · 단수형 `pjc-hook-eval-scratch`. **v1.184.0의 첫 실행이 지운 59개는 대부분 이 평면 축**이었으므로, 오래 안 돌린 기계에서 큰 숫자가 뜨는 것은 정상이다. 1건 이상이면 `[SWEEP] …` 한 줄이 뜨고 0건이면 무출력이다. **`state`·`scratch`는 대상이 아니다** — 실행 간 재사용되어 수명이 다르다. 자식은 정리하지 않는다(13개가 동시에 훑으면 서로의 진행 중 폴더를 건드린다).
  - **왜 3일인가 — 잔여 폴더 이름의 그룹명이 "어느 그룹이 죽었는지"를 알려주는 진단 창이기 때문이다.** 즉시 지우면 그 정보가 사라지고, 동시 실행 중인 다른 러너의 픽스처를 지우는 사고 위험도 생긴다. 사흘보다 오래 보려면 그 전에 복사해 둘 것.
- **소요 시간을 완료 판정에 쓰지 말 것** — 같은 스위트가 **19분 6초 ↔ 27분 14초로 실측**된 만큼 wall-clock 편차가 크다. 완료는 `결과: N/N OK` 라인과 exit 0으로만 판정한다. 같은 이유로 **성능 비교는 같은 세션에서 두 모드를 연속 측정**해야 한다(교차 세션 비교는 근거가 못 된다).
- **현행 케이스 수 744가 회귀 기준선이다 — 케이스를 추가·삭제하면 이 값을 함께 갱신할 것.** **(v1.225.0 · 2026-09-03 무인자 전체 실행 실측 `744/744 OK (FAIL 0)`, 429초 — 하니스 재작성 회차 2가 순감 70을 냈다: **`require-evidence` 시나리오 773줄 삭제**(그 hook 제거) · **`report-hook-events` 스모크 3건 삭제**(그 수동 도구 제거). **차단 사유 문구를 재는 케이스 7건을 강화했다** — 종전에는 `BLOCKED` 같은 짧은 앵커만 대조해 **차단 사유가 바뀌어도 골든이 못 잡았다**(`check-block-coverage.py`가 그 공백을 찾아냈다). hook 이름 매핑은 `pre-bash-dispatch`·`warn-external-ops`·`require-task-checkbox`·`warn-commit-secrets` → `guard-bash`, `require-plan-for-write` → `guard-write`, `protect-harness`·`guard-agents-content` → `guard-harness`다.)** **(v1.224.0 · 2026-09-03 무인자 전체 실행 실측 `814/814 OK (FAIL 0)`, 7분 12초 — 하니스 재작성 회차 1이 순감 3을 냈다: **SC14b·SC14c 삭제**(압축 주입의 경로 지시가 3경로에서 `implement/SKILL.md` 하나로 줄어 `halt-conditions.md`·`recovery.md`를 `ExpectContains`로 재던 두 케이스의 대상이 사라졌다) · **SC39b 삭제**(halt-conditions 절 주입 자체를 없앴다 — 중단 조건이 새 「자율 루프」 절 안에 있어 추출 1회로 같은 내용이 온다). **음성 대조 문자열도 교체했다** — SC15·SC16·SC38c 가 `halt-conditions` 부재를 재고 있었는데 그 문자열이 이제 어디에도 없어 **항상 통과하는 공허한 어서션**이 된다(v1.220.0 SC43c·SC43d 와 같은 자리). **문서↔코드 대조 축은 대상 파일이 바뀌었다** — 구 스킬의 「금지 표현」 절에서 `implement/references/loop-stop-patterns.md`로. 그 절을 옮기지 않은 채 경로만 바꿔 2차 실행에서 FAIL 4건이 났고, 문구 14건을 새 파일에 작성해 해소했다.)** **회차 항목은 끝에 덧붙이므로 817의 근거는 이 줄 `끝`의 `v1.221.0` 항목이다** — 그 앞의 v1.218.0 항목(796)·머리말 바로 뒤의 v1.217.0 항목(790)으로 읽지 말 것. (항목 배열이 등록 시간순은 아니다 — 끝이 최신이라는 것만 참이다.) **(v1.217.0 · 2026-09-01 무인자 전체 실행 실측 `790/790 OK (FAIL 0)` — T1이 `session-context`의 **계획 세션 절 원문 주입** 케이스 **5건**을 더했다(순증 5): **양성 2**(SC41 — `plan/SKILL.md` 「Step 3. 영향 범위 실측」 절 본문이 실려 오는가 · SC41b — 발췌 표기) · **델타 음성 2**(SC41c `startup` 미주입 · SC41d 미완료 task 세션 미주입 — **대조 문자열은 주입 본문**이다. `plan/SKILL.md` 경로 문자열로 걸면 기존 SC29와 완전히 같아 새 회귀를 못 잡는다) · **회귀 고정 1**(SC41e — 주입이 vault 게이팅 신호가 아닌가. `$cwdBaseCount` 짝 증가를 빼면 vault 라인이 샌다). **⚠ SC41e의 픽스처는 AGENTS.md가 0바이트여야 한다** — 분기 조건은 `Test-Path`로 존재만 보지만 주입 블록은 `$agentsInfo.Length -gt 0`이라(`session-context.ps1:474`) 내용이 있으면 AGENTS 라인이 먼저 들어가 **그 자체가 정당한 vault 신호가 되어 델타가 0**이 된다. 내용 있는 픽스처로 시도해 FAIL을 본 뒤 *「원리상 잴 수 없다」*로 오판했다가 F-7이 잡았다 — **한 픽스처의 실측을 원리로 일반화하지 말 것.** 직전 기준선 785는 v1.214.0의 부분 실행 계수 기반 잠정값이었고 이번 무인자 전체 실행이 그 확정도 겸한다.)** **(v1.214.0 · 2026-08-28 — `session-context`의 **Deferred 미판정 건수 주입** 어서션 **11건**을 더했다(순증 11): **양성 4**(SC40 미판정 계수 · SC40d 마커 혼재 · SC40h 기호 없는 `[미등재]` · SC40i 종료 앵커 부재) · **델타 음성 3**(SC40b 전부 판정됨 · SC40c 절 부재 · **SC40g 템플릿 placeholder** — 없으면 미판정 1건이 상시 오발화한다) · **폴백 고정 1**(SC40e 구형 plan) · **회귀 고정 2**(SC40f `전부 완료` 라인 · SC40f2 건수 동시 표기) · **문면 고정 1**(SC40j). **대조 문자열은 부기 리터럴 `Deferred 미판정`이다** — 숫자만 재면 문면이 바뀌어도 통과한다. 변이 확인: 부분 실행 `77/89 (FAIL 12)` → 복원 후 89/89. 부분 실행 계수 기반 잠정값(774 + 11)이며 F-2 전체 실행으로 정정한다.)** **(직전 기준선은 v1.212.0 · 2026-08-28 무인자 전체 실행 실측 `774/774 OK (FAIL 0)` — T2가 `session-context`의 **compact 직후 내용 주입** 케이스 **4건**을 더했다(순증 4): **양성 2**(SC39 절대 규칙 절 · SC39b halt-conditions 절) · **델타 음성 1**(SC39c `startup` 미주입 — 없으면 "항상 주입"으로 바꿔도 전건 통과한다) · **발췌 표기 1**(SC39d). **대조 문자열은 라벨에 없고 절 본문에만 있는 것을 고른다** — 라벨에도 있는 문자열로 재면 절이 통째로 빠져도 통과한다(SC39b가 `위임 경계 (4분류`를 쓰는 이유). 변이 확인: `75/78 (FAIL 3)` → 복원 후 전건 통과.)** **(직전 기준선은 v1.210.0 · 2026-08-27 실측 `770/770 OK (FAIL 0)`, 9분 10초 — T4가 plan 존재 판정을 루트 단일계로 좁히며 `require-plan-for-write`의 PD 블록을 3 → 4케이스로 바꿨다: **PD2 기대값 뒤집기**(유일한 델타 음성 — 구 `exit 0` / 신 `exit 2`) · **PD3 교체**(해소 경로 고정) · **PD4 신설**(정상 레포 무회귀). `session-context` SC4도 기대값이 뒤집혔다. **직전 기준선 766은 낡은 값이었다** — 순증 1을 빼도 769라 v1.207~1.209의 3건이 이 줄에 반영되지 않았다(v1.190.0·v1.182.0 선례와 같은 자리).)** **(v1.206.0 · 2026-08-26 무인자 전체 실행 실측 `766/766 OK (FAIL 0)` — `session-context`의 «허브 매칭 URL 축»으로 **순증 5**가 생겼다(SC37~SC37d): **양성 4**(URL 일치 · 정규화 3축 · cwd origin 부재 시 URL 축 off · 하위 폴더 cwd) · **델타 음성 1**(SC37b — 경로가 일치해도 URL 이 다르면 미발화. 이것이 「경로가 아니라 URL 로 골랐다」의 반증 가능한 근거다).** 직전 기준선은 **(v1.205.0 실측 `761/761 OK (FAIL 0)` — `warn-version-drift`의 «원격 태그 폴백»으로 **순증 5**(시나리오 8 → 13): **픽스처 사전조건 1**(bare origin ref + 태그 부재를 `Assert-Case`로 승격 — 어긋나면 세 케이스가 공허 통과) · **양성 2** · **델타 음성 2**(로컬 히트 시 원격 무시 / **원격에만 태그 있음 → 무출력** — 이번 오탐의 정확한 형태) · **문면 고정 1**(「로컬·원격」 두 소스를 밝히는가 — F-7 지적으로 추가, v1.204.0 SC33d와 같은 자리). **6건인데 순증 5인 이유는 구 케이스 5의 짝 어서션 1건이 기대값 전환으로 흡수**됐기 때문이고, **구 케이스 5는 삭제가 아니라 이관**이다(기대값이 무출력·fail-open으로 뒤집히고 발화 축은 bare origin 픽스처가 승계). 변이 확인: `10/12 (FAIL 2)` → 복원 후 12/12. 나머지 2건이 변이에도 PASS하는 것은 **회귀 고정** 케이스라 정상이다. 문면 고정은 따로 실증했다(옛 문구로 되돌리니 `12/13 (FAIL 1)`).)** **(v1.204.0 · 2026-08-26 무인자 전체 실행 실측 `756/756 OK (FAIL 0)` — T2가 `session-context`의 «위키 뒤처짐 알림»으로 17건을, T6이 잔량 단독 문구 고정(SC33d)으로 1건을 더해 **18건**이 늘었다: **축별 단독 양성 3**(커밋 축 SC32 / 일수 축 SC32c / K-DRIFT 축 SC33 — 한 축만 양성으로 걸면 나머지가 죽어도 전건이 통과한다) · **경계 4**(29/30커밋 SC32d·SC32d2, 13/14일 SC32e·SC32e2 — `-ge` ↔ `-gt` off-by-one 을 잡는 유일한 그물) · **델타 음성 3**(3축 미달 SC33c · 허브 무매치 SC34 · `synced_commit` 부재 SC35) · **수치 표기 4**(커밋 수 SC32b · 경과일 SC32c2 · 잔량 단독 시 커밋 수치 미표기 SC33b · 미계산 sentinel 미노출 SC35b2) · **축 하나만 계산 1**(SC35b) · **삽입 순서 2**(비하네스 SC36 / 큐 라인 동반 SC36b — `Assert-Case` 는 순서를 못 보므로 `IndexOf` 직접 판정) · **문면 고정 1**(SC33d — F-7이 *"그 문면을 재는 케이스가 없다"*고 지적해 더했다). 변이 확인: 임계 판정을 `$true`로 치환하니 60/65(FAIL 5: SC32d2·SC32e2·SC33b·SC33c·SC35), 복원 후 65/65. SC34만 통과하는 것은 그쪽이 임계가 아니라 **허브 매칭 축**을 지키기 때문이다.) (v1.202.0 · 2026-08-26 무인자 전체 실행 실측 `738/738 OK (FAIL 0)` — T1이 `warn-commit-secrets`의 스캔 캡 fail-closed화로 12건을 더했다: **캡 게이트별 양성 2**(추적 파일 51개 경로 나열 / untracked 51개 `add -A`) · **델타 음성 2**(각 게이트의 50개 경계값 — 경계가 실제로 발화하되 정상 커밋을 막지 않음) · 차단 문면 2(상한 사유 · 우회 변수 안내) · 우회 1(`ALLOW_SECRET=1` + 캡 사유 문면) · **동시 검출 2**(캡+시크릿 → 시크릿이 주 메시지 · 캡은 부기) · **정보 소실 고정 3**(ⓓ 캡+시크릿+우회는 두 차단 블록을 지나쳐 최하단 경고로 떨어지는데 그때도 캡 부기가 나가야 한다 — quality 1R M1 / ⓔ 캡 차단이 저신뢰 검출분을 삼키지 않고 "없음" 고정 문구를 쓰지 않는다 2건 — spec 1R M1). **뒤 3건은 리뷰가 찾은 조합이다** — 앞 9건이 `hits`·`allowSecret`·`capHit` 세 축의 조합 중 셋만 덮어 나머지 두 조합의 정보 소실을 못 잡았다. **게이트마다 쌍을 둔 이유**: 캡 게이트가 두 분기라 한쪽만 걸면 다른 쪽이 fail-open이어도 전건 green이다(대장 `[2026-08-19]`의 사각). 수정 전 부분 실행 `96/101 (FAIL 5)`, 음성 3건 PASS 유지.)** (2026-08-15 무인자 전체 실행 실측 `535/535 OK (FAIL 0)`. 직전 기록값 528은 그 사이 늘어난 케이스가 반영되지 않아 낡은 값이었다 — `docs/plans/deferred.md`가 535로 적고 있었던 것이 맞다.) **(2026-08-18 재실측 `539/539 OK (FAIL 0)` — v1.180.0이 SessionStart `fork` matcher 케이스 4건(SC2b·SC2c·SC22b·SC22c)을 더했다.)** **(2026-08-19 재실측 `596/596 OK (FAIL 0)` — v1.182.0이 57건: secret-patterns 라벨 24 · block-destructive 8 · `.state` TTL 2 · `$autoStage` 호출 횟수 8 · 폴백 경고 가드 3 · 그 밖 회귀. **오차단을 고정하는 델타 음성이 다수**이며 그 자리의 부재가 F-7 BLOCKER 2회의 원인이었다.)** **(2026-08-21 재실측 `643/643 OK (FAIL 0)` — 자기보호 이름 집합에 고아 회수 계열 2종 합류, 8건: protect-harness 양성 3 · **델타 음성 3**(개발 repo · 유사 이름 · 캐시 밖 CLAUDE~1) · post-write H2 양성 2.)** **(2026-08-21 재실측 `647/647 OK (FAIL 0)` — v1.190.0 T1이 `session-context`의 AGENTS.md 주입 상한 임박 경고 케이스 4건(SC9a 비율 축 양성 · SC9b 전문 주입 유지 · SC9c 음성 · SC9d 초과 폴백 문구)을 더했다. 이 줄은 T1에서 함께 갱신됐어야 했는데 누락돼 F-2 재실행에서 643 ↔ 647 드리프트로 드러났다 — 위 v1.182.0 선례와 같은 자리다.)** **(2026-08-23 재실측 `665/665 OK (FAIL 0)` — `guard-agents-content` hook 신설로 시나리오 1개·케이스 18건을 더했다: 차단 양성 5(인계 서술 3 · 트리 2) · **델타 음성 6**(정상 AGENTS.md · plan.md 같은 문구 · 본문 산문 스침 · 명령 파이프 · 마크다운 표 · basename 불일치) · 우회 1 · protect-harness 공존 3 · fail-open 2 · basename 판정으로 하위 경로 AGENTS.md 차단 1.)** **(2026-08-24 재실측 `690/690 OK (FAIL 0)` — v1.195.0 T1이 `suggest-agents-record` 인용 인자 스트립 13건: **델타 음성 4** · 오탐 억제 5 · **포기 고정 4**(화이트리스트 밖 `echo` · 이스케이프 중첩 인용 · 짝 없는 따옴표 · 인용 안 명령 치환). 직전 앞머리 677은 v1.194.0이 재실측 표기 없이 숫자만 올린 값이라 회차별 재구성이 안 됐다 — 드리프트의 약한 형태다.)** **(2026-08-25 재실측 `692/692 OK (FAIL 0)` — v1.197.0 T1이 `session-context`의 vault 라인 행동 지시 케이스 2건(SC18c 허브 직행 · SC18d 글로벌 절 포인터)을 더했다. **두 요소를 각각 재는 이유**: 한쪽만 걸어두면 나머지가 지워져도 전건 통과한다 — 실제로 초안은 SC18c 하나였고 그 공백을 plan 리뷰 2R이 잡았다. 두 케이스는 수정 전 부분 실행에서 `41/43 (FAIL 2)`로 결정적 FAIL을 냈다(무회귀 케이스가 아님을 실증).)** **(2026-08-25 v1.198.0 실측 `697/697 OK (FAIL 0)` — v1.198.0 T8이 `session-context`의 **계획 세션 압축 리마인더** 케이스 **5건**을 더했다 — SC28(양성: plan 없음 → 발화) · SC29(델타 음성: 미완료 task 세션엔 미발화) · **SC30**(델타 음성: plan도 AGENTS.md도 없는 비 pjc 폴더엔 미발화) · **SC31·SC31b**(회귀 고정: task 0개 plan에도 발화하되 **vault 라인 유지** — SC31b가 그 축이며, `$cwdBaseCount++`를 `= $lines.Count`로 되돌리면 **FAIL 1**로 잡는 것을 실증했다). **왜 둘인가**: 양성만 걸면 *"미완료 task 세션에도 계획 지시가 새는"* 회귀를 못 잡는다. 수정 전 부분 실행에서 SC28이 `44/45 (FAIL 1)`로 결정적 FAIL을 냈고, 최종 `47/47`이다. **SC30·SC31은 리뷰가 찾은 사각지대다** — 기존 케이스들이 같은 픽스처를 쓰면서도 각각 다른 문자열만 assert해 ① 비 pjc 폴더 노이즈 ② `$cwdBaseCount` 전체 재설정이 「plan 존재」 신호까지 흡수하는 회귀를 둘 다 놓치고 있었다. 그 과정에서 **자기 유발 회귀 1건**(새 라인이 `$cwdBaseCount` 이후라 vault 게이팅을 켜 SC23이 FAIL)을 같은 루프에서 해소했다.)** **(v1.199.0 · 2026-08-25 무인자 전체 실행 실측 `726/726 OK (FAIL 0)`, 7분 26초 — T1이 `pre-bash-dispatch`의 **위험값 대입 경고**(`warn-dangerous-assignment`) 케이스 **14건**을 더했다 — **양성 7**(기본형 `X=/` · 같은 줄 재대입 · PowerShell `$X = "/"` + `Remove-Item` · `$HOME` · 드라이브 루트 `/c` · `export` 접두어 + `~` · **드라이브 루트 백슬래시 `C:\`**) · **델타 음성 7**(상대 경로 `./build` · 루트 아닌 절대 경로 `/tmp/build` · 홈 하위 `$HOME/proj` · 삭제 계열 아님 2종(`echo`·`ls`) · 대입 없는 루트 삭제 · **드라이브 하위 경로 `C:\Users\me\proj`**). **뒤 2건은 quality 리뷰 m1이 찾은 커버리지 공백이다**(위험값 정규식이 백슬래시 `C:\`도 인정하는데 그 확장이 골든에 없었다). `block-destructive.ps1`은 주석 2줄만 바뀌어 차단 동작 불변. 이어서 **T2가 적대적 코퍼스 15건**을 더해 이 회차 합계 **29건**이 되었다 — **미탐 고정 6**(명령치환+변수 합성 · eval 문자열 안 변수 · 파라미터 확장 기본값 `${X:-/}` · `bash -c` 작은따옴표 안 변수 · `xargs -I{}` placeholder · 변수 2단 전달) · **차단 확인 4**(`rm -r -f` 플래그 분리 · `--recursive --force` 롱 플래그 · `find`의 루트 `-delete` · `Remove-Item -Path /` 명시형) · **경고 발화 경계 3**(`sudo` 접두 · 인용 형태 혼합 · 중괄호 참조) · **경고 델타 음성 2**(명령치환 대입 · 변수 2단 전달). 기존 `[알려진 미탐]` 5건은 판정 동일(전건 PASS).)** 이 숫자는 `docs/harness-conventions.md` 「문서 로드 예산 기준선」처럼 기계 대조되지 않으므로(그 표는 스킬·리뷰어 파일 바이트만 추적한다) **문서를 갱신하지 않으면 드리프트를 막을 장치가 없다** — v1.182.0이 실제로 이 줄을 지나쳐 F-7 2R에 잡혔다. **(v1.218.0 · 2026-09-02 무인자 전체 실행 실측 `796/796 OK (FAIL 0)` — T2가 `session-context`의 **계획 세션 큐 기록 규약 주입** 케이스 **6건**을 더했다(순증 6): **양성 3**(SC42 — `queue-rules.md` K 5-2 본문이 실려 오는가 · SC42b — 발췌 표기 · **SC42f — 추출이 K 5-3까지 닿는가**, 종료 앵커 `### K 5-4.`를 고정한다. SC42 하나로는 앵커를 5-3 앞으로 옮겨도 통과한다) · **델타 음성 2**(SC42c `startup` 미주입 · SC42d 미완료 task 세션 미주입 — 계획 주입과 같은 이유로 **대조 문자열은 주입 본문**이다) · **회귀 고정 1**(SC42e — 주입이 둘로 늘었으므로 `$cwdBaseCount++`도 둘이어야 한다). **첫 실행은 795/796이었다** — assert 문자열과 hook 라벨이 `발추`·`발찌`로 서로 다르게 깨져 SC42b가 FAIL했고, 두 블록 한글을 전수로 고쳐 재실행했다.)** **(v1.218.0 후속 · 2026-09-02 무인자 전체 실행 실측 `802/802 OK (FAIL 0)` — T1이 `session-context`의 **계획 세션 위키 조회 절차 주입** 케이스 **6건**을 더했다(순증 6): **양성 3**(SC43 — `SKILL.md` K 1 본문 · SC43b — **범위까지 밝히는 라벨**(「절차 K 1~2」) · **SC43f — 추출이 K 2까지 닿는가**, 종료 앵커 `#### K 3~4 · K 5 포인터`를 고정한다) · **델타 음성 2**(SC43c `startup` 미주입 · SC43d 미완료 task 세션 미주입) · **회귀 고정 1**(SC43e — 주입이 셋으로 늘어 `$cwdBaseCount++`도 셋이어야 한다). **⚠ 축 전환 — v1.220.0**(개수 6 불변, 축이 바뀜): 주입이 경로 지시로 돌아가 「범위 라벨」·「추출 경계」 축이 소멸하고 **경로 정확성**·**행동 지시**가 그 자리를 잇는다. **델타 음성 2건도 대조 문자열을 새 라인 것으로 교체** — 옛 문자열은 이제 어디에도 없어 **항상 통과하는 공허한 어서션**이 된다. **첫 실행은 3건 FAIL이었다** — SC41e·SC42e·SC43e가 모두 `-ExpectNotContains '위키 vault'`인데 **주입 대상이 된 K 1 본문이 그 라인을 예시로 인용**해 기존 통과 케이스 둘까지 함께 깨졌다. 대조 문자열을 `위키 vault: 설정`으로 좁혀 세 주입 구간 어디와도 겹치지 않게 했다.)** **(v1.221.0 · 2026-09-02 무인자 전체 실행 실측 `817/817 OK (FAIL 0)` — T1이 `session-context`의 **Deferred 대장 최고령 주입** 케이스 **15건**을 더했다: **양성 3**(주입·항목별 최신 스탬프·최솟값 선택) · **단독 검출 3**(형식 한정 / 미래 배제 / 항목 단위 묶기) · **`vN` 부기 2형식 2**(접두 · 제목 뒤 괄호) · **델타 음성 4**(부기 없음 · 비하네스 cwd · 대장 부재 · 유효 판정일 0건) · **짝 계약 1**(`$cwdBaseCount`) · **방어 경로 1**(무효 날짜만 건너뛴다) · **구조 줄 1**(앵커·인용 블록을 항목 본문으로 흡수하지 않는다 — F-7이 잡은 축이고, 흡수하면 거기 적힌 날짜가 그 항목의 판정일로 밀려 올라간다). **단독 검출 3을 가른 것이 요점이다** — 형식 한정과 미래 배제를 한 픽스처에 섞으면 둘 중 하나만 살아도 통과해 어느 회귀도 못 잡는다. 기대는 **날짜부로** 재고 일수는 재지 않는다(매일 늘어 픽스처가 다음 날 깨진다).)**

### 실행 절차 (정본)

**전경에서 그냥 돌린다.**

```
pwsh -NoProfile -ExecutionPolicy Bypass -File plugins/pjc/hooks/evals/run-hook-evals.ps1
```

- **v1.225.0에서 분리 실행·`Monitor` 폴링 절차가 사라졌다** — 무상태 그룹(313케이스)이 한 자식에서 순차로 돌아 전체 wall-clock을 혼자 결정하던 것을 `stateless-<N>` 8샤드로 나누고 동시 상한을 논리 코어의 절반으로 올렸다. 실측 **422초**(744케이스, 2026-09-03)로 Bash 도구의 10분 캡 안에 들어온다.
- **그래도 완료는 시간이 아니라 `결과: N/N OK` 라인과 exit 0으로 판정한다** — 아래 「소요 시간을 완료 판정에 쓰지 말 것」이 그대로 유효하다. 시간은 성능 축이고 편차가 크다.
- **10분 캡에 걸리면** `-Filter <hook>`로 좁혀 원인을 먼저 찾고, 전체 판정은 `-MaxParallel <N>`을 올려 다시 돌린다. 캡을 넘기는 상태가 지속되면 그것 자체가 회귀 신호다.
- **실행 중 검사 대상 파일을 편집하지 않는다** — 러너는 시작 시점의 파일을 읽으므로, 도중에 고치면 FAIL이 「내 수정이 만든 결함」인지 「옛 파일과 새 파일을 섞어 읽은 경합」인지 갈리지 않는다.

### ⚠ 우회 변수 오염 — 실행 전에 `CLAUDE_HARNESS_QUICK`을 지울 것

**환경에 `CLAUDE_HARNESS_QUICK=1`이 남아 있으면 골든은 돌긴 하는데 41건이 FAIL한다.** 그 변수가 `guard-write`·`guard-harness`를 통째로 우회시켜 「차단 기대」 케이스가 전부 통과(exit 0)하기 때문이다.

- **어떻게 들어오는가**: 사용자가 세션 시작 전에 그 변수를 설정하면 Claude가 띄운 프로세스가 그것을 **상속**한다.
- **해소**: 실행 직전에 `$env:CLAUDE_HARNESS_QUICK = $null; $env:CLAUDE_HARNESS_ALLOW_SECRET = $null` (bash에서는 `unset`).
- **판정**: FAIL이 「차단 기대인데 exit 0」 형태로 무더기로 나오면 이것부터 의심한다 — 개별 케이스 결함이면 그런 패턴이 나오지 않는다.

### ⚠ 코디네이터가 취합 전에 죽으면 state 에서 직접 집계한다

**결과 라인이 안 나왔는데 격리 `state\<접미>\*.jsonl` 파일들이 모두 최근 시각으로 남아 있으면 러너는 완주한 것이다** — 코디네이터가 판정을 끝에 일괄 출력하므로 그 직전에 죽으면 화면 결과만 사라진다. 그때는 jsonl 의 `"ok":false` 개수로 판정한다.

그 경우 jsonl 을 직접 집계한다 — 각 줄이 한 케이스의 판정이다:

```python
import json, glob, collections
tot = collections.Counter()
for f in glob.glob(r'<state>\<접미>\*.jsonl'):
    for line in open(f, encoding='utf-8'):
        if line.strip():
            o = json.loads(line); tot[str(o.get('status')).upper()] += 1
print(tot)
```

**단 이 집계는 「그룹이 전부 기록됐는가」를 먼저 확인해야 유효하다** — 일부 그룹의 jsonl 이 없으면 그만큼이 조용히 빠진 채 개수만 맞아 보인다(위 「커버리지 판정」과 같은 함정).

### 커버리지 판정 — 개수만 보지 말 것

**`N/N OK (FAIL 0)`은 그 자체로 무회귀의 증거가 아니다.** 병렬화 회차에 **순차 531/531 ↔ 병렬 502/502**가 나왔는데 둘 다 exit 0이었다 — 29건이 조용히 빠진 것이 개수만 보면 통과로 읽혔다(원인: 증분 기록이 `Assert-Case`만 후킹했고 일부 시나리오는 `$results.Add()`를 직접 호출한다). 러너 자체를 고칠 때는 **`[PASS|FAIL|PENDING] + 케이스명`을 정렬해 diff 0줄**을 확인한다(FAIL 라인의 출력 head는 GUID 임시 경로가 실려 실행마다 달라지므로 제외).

### 구조 (v1.159.0)

- `run-hook-evals.ps1` — 코디네이터(그룹 병렬 디스패치 · 판정 취합 · `-Sequential` 순차 경로)
- `run-scenario.ps1` — 자식(그룹 1개를 자기 격리 홈에서 실행 · 판정을 JSON 라인으로 증분 기록 + 완주 시 전량 덤프)
- `eval-common.ps1` — 공용 격리 구성·헬퍼(`Invoke-Hook`·`Assert-Case`·`New-*Json`·`Test-HookSelected`)
- `filter-spec.ps1` — `-Filter` 유효 이름 목록 + 정규화 규칙(부작용 없음 — 코디네이터와 `eval-common`이 함께 읽는다)
- `eval-paths.ps1` — 격리 폴더 경로 계산(`Get-EvalRoot`) + 잔여 정리(`Invoke-EvalSweep`). `filter-spec.ps1`과 같은 이유로 분리돼 있다(양쪽이 필요로 하는 것을 복제하면 갈린다)
- `scenarios/*.ps1` 15개 — 케이스 본문(코디네이터를 모른다). 케이스 정본은 `hook-cases.json` + 이 15개.
- **그룹은 14개다** — 대부분 시나리오가 독립이지만 `protect-harness-installed`와 `hook-event-log`는 `$vdCache`를 공유하고 후자가 격리 홈의 이벤트 로그 적재를 관찰하므로 **한 프로세스로 묶어** 분리 전 관찰 조건을 유지한다.
