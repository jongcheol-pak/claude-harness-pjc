# plan.md — notes 아카이브 정합 보강 (감사 ②)

## Goal
하니스의 notes 아카이브 메커니즘에서 **`## 아카이브 인덱스` ↔ `notes-archive/` 파일 정합을 검증하는 절차가 전무**(인덱스 갱신이 "선언뿐 미검증")했던 빈틈을, `phase-f-detail.md` F-6.5에 **아카이브 후 정합 자가검증 체크리스트**(인덱스↔파일 양방향 대조 + 항목 손실 0 + due 미수행 점검)를 산문으로 명문화해 메운다. 더불어 아카이브 기준(1달/30,000자)이 정식임을 명시해 프로젝트 CLAUDE.md(1주일)와의 "조용한 덮어씀"을 "명시적 기준"으로 전환한다.

## 배경 — 감사 ② (split-mechanism-audit 메모리)
하니스 파일 분할 메커니즘 4개 중 ①PRD(1.66.1)·④plan.md(1.66.2)·③위키 recipe(1.66.3) 보강 완료. 이번은 **② notes 아카이브**(MAJOR·남은 마지막). 위키는 `lint.py`가 sub-index↔index.md 정합을, PRD는 plan-reviewer가 검증하지만 **notes 아카이브만 대응 검사가 전무** — F-6.5 산문 지침이 유일하고, 인덱스 갱신을 "추가/갱신한다"고 지시만 할 뿐 갱신이 올바른지 확인하는 단계가 없다.

## Investigation Log (직접 grep/Read + explorer 조사)
- `phase-f-detail.md` F-6.5(L55~73): notes 아카이브 규칙 보유 — 기준(L61~63: 1달/30,000자), 인덱스 갱신 지시(L64: `## 아카이브 인덱스`에 `- {YYYY-MM}.md: {키워드}` 추가/갱신), 항목 단위 절단(L65), 즉시 수행(L68), 부분 편집 방법(L69). **그러나 "인덱스가 실제 파일과 맞는지·항목이 손실 없이 옮겨졌는지" 검증 단계는 없음.**
- 현재 레포 상태: `notes-archive/` 폴더·`notes.md`의 `## 아카이브 인덱스` 섹션 **둘 다 미존재**(아직 아카이브가 한 번도 발생 안 함 — notes.md 약 23,000자로 30,000 미만). 따라서 순수 **예방적** 보강.
- 위키 `lint.py`(326줄, `python lint.py "<vault>"` 호출): sub-index↔index.md 정합을 L247~254에서 검사(정규식 `index-[a-z]+` 추출 → glob 실재 파일과 양방향 비교 → WARN), ERR/WARN/INFO 3단계 출력. → notes lint 스크립트의 모델 후보였으나 Q1에서 미채택.
- **레포 내 CLAUDE.md/AGENTS.md 없음**(explorer 전수 확인): 글로벌(`C:\Users\jongc\.claude\CLAUDE.md`)·프로젝트(`D:\Personal Project\AI\CLAUDE.md`) 둘 다 레포 밖. → 이 plan은 프로젝트 CLAUDE.md(1주일 기준)를 **직접 수정할 수 없다**. 레포 안에서 가능한 정식화는 phase-f-detail.md 명문화뿐.
- 기준 충돌(메모리): 글로벌 CLAUDE.md=1달/30,000자, phase-f-detail=1달/30,000자(일치), 프로젝트 CLAUDE.md=1주일·분량없음(구버전) → phase-f-detail이 프로젝트 지침을 조용히 덮어쓰는 중.

## 결정 (사용자 확정)
- **Q1 = phase-f-detail 산문 강화.** notes-lint.py 스크립트·Stop 훅 신설 안 함. F-6.5에 정합 자가검증 체크리스트를 명문화한다. 근거: 아카이브는 드문 예방적 경로 + pjc 최소·과한 기계화 회피 + 감사 ③ "명문화만(lint 미추가)" 선례와 일관. (검증은 에이전트가 F-6.5를 따라 수행 — 위키 lint처럼 자동은 아님.)
- **Q2 = 1달/30,000자 정식화.** phase-f-detail에 "이 기준이 정식이며 글로벌 지침과 일치, 다른 문서(프로젝트 CLAUDE.md 등)에 1주일 같은 값이 있으면 구버전이므로 이 값을 따른다"를 한 줄 명시. 프로젝트 CLAUDE.md(레포 밖) 자체 수정은 사용자가 별도(수동) 정렬 — Deferred.
- **수정 대상 1파일 + 버전**: `phase-f-detail.md`(F-6.5) → 버전 patch 1.66.3 → 1.66.4.

## Impact Analysis (전수 확인)
- 수정 파일 **1개**(+버전 2파일), 전부 마크다운 지침 문서. 코드·시그니처·DI·심볼·호출자 영향 0. 신규 스크립트·훅 없음.
- **F-6.5 내부 무손상**: 기존 기준·인덱스 갱신·항목 절단·즉시 수행·부분 편집 문구는 불변, 정합 자가검증 블록과 기준 정식화 한 줄만 **추가**(역대조).
- **읽기 측 무손상**: "과거 작업 참조 시"(L71)·"민감 정보"(L73)·위키 반영(L75~) 등 F-6.5 다른 단락 불변.
- **기준 일관성**: 정식화 문구(1달/30,000자)가 기존 L61~63 기준값과 동일해야 함(드리프트 방지 — 새 값 도입 아님, 기존 값 정식 선언).
- 자동 생성·lock 파일 아님. plugin.json/README(1.66.3→1.66.4) bump.

## Tasks
<!-- 전부 Type A(마크다운 지침 문서). Decision Points·Edge Cases는 Type A라 skip, 무손상·기준 일관만 Acceptance에 명시. -->

### T1 — phase-f-detail.md F-6.5: 정합 자가검증 체크리스트 + 기준 정식화 명문화  [Type A]
대상: `plugins/pjc/skills/implement-task/references/phase-f-detail.md` (F-6.5, L55~73)
- **정합 자가검증 블록 추가(Q1)**: 아카이브 규칙 블록(L69 부분편집 방법 뒤) 다음에 "**아카이브 후 정합 자가검증(인덱스 갱신이 선언뿐 미검증이 되지 않게)**" 소블록을 추가. 항목:
  1. **인덱스→파일**: `## 아카이브 인덱스`의 각 `- {YYYY-MM}.md:` 엔트리에 대응하는 `notes-archive/{YYYY-MM}.md` 파일이 실재하는가(`ls`/`grep`로 확인).
  2. **파일→인덱스**: `notes-archive/`의 각 `*.md` 파일이 인덱스에 엔트리를 갖는가(양방향 — 위키 lint sub-index 검사와 동형 취지).
  3. **항목 손실 0**: 이동한 항목이 `notes-archive/{YYYY-MM}.md` 끝에 **통째로**(중간 절단 0) 있고 `notes.md`에서 제거됐는가 — 이동 전 항목이 "잔존 또는 이동" 둘 중 정확히 하나에만 있어 중복·누락이 없는가.
  4. **due 미수행 점검**: notes.md가 30,000자 초과거나 가장 오래된 항목이 1달 경과인데(=아카이브 due) 이번에 수행했는가 — due인데 미수행으로 끝내지 않는다. **단 현재 날짜를 확인할 수 없으면 기간 due는 제외하고 분량(30,000자) due만 적용한다**(L66 "날짜 확인 불가 시 기간 기준 이동 안 함"과 정합 — 날짜 불가는 정당한 skip이지 누락이 아님).
  5. **월당 1줄(동일 월 중복 없음)**: 같은 `{YYYY-MM}.md`에 2차 아카이브 시 인덱스 엔트리는 "추가"가 아니라 "갱신"이어야 한다(L64) — 인덱스에 같은 월 줄이 2개 생기지 않았는가. (양방향 존재 점검은 *존재 여부*만 보므로 중복 줄은 이 항목으로 별도 확인.)
  6. **미존재 통과(오탐 방지)**: `notes-archive/`·`## 아카이브 인덱스`가 **둘 다 없으면**(아직 아카이브 전) 통과 — 미아카이브는 정상 상태다. 한쪽만 있으면(인덱스만/파일만) 불일치로 보고 바로잡는다.
- **기준 정식화 한 줄(Q2)**: 기준 블록(L61~63) 근처에 "**이 기준(1달/30,000자)이 정식이며 글로벌 공통 지침과 일치한다. 다른 문서(예: 일부 프로젝트 CLAUDE.md)에 1주일 등 다른 값이 있으면 구버전이므로 이 값을 따른다.**" 추가 — "조용한 덮어씀"을 명시적 기준으로 전환(새 값 도입 아님, 기존 1달/30,000자를 정식 선언).
- Edge cases(Type A): ⓐ F-6.5 다른 단락·문구 불변(블록 추가 + 한 줄만) ⓑ 정식화 값이 기존 L61~63과 동일(드리프트 0) ⓒ 미존재 통과 항목으로 첫 아카이브 전 오탐 차단.
- Halt Forecast: 없음.
- Acceptance:
  1. F-6.5에 "아카이브 후 정합 자가검증" 블록 존재, 6개 점검(인덱스→파일·파일→인덱스·항목 손실 0·due 미수행[날짜 불가 시 분량만]·월당 1줄 중복 없음·미존재 통과) 모두 명시.
  2. "1달/30,000자가 정식 기준·글로벌 지침 일치·다른 값은 구버전" 정식화 문구 존재.
  3. 정식화 값이 기존 기준(1달/30,000자)과 동일(새 값 도입 없음).
  4. 기존 F-6.5 다른 문구(기준 L61~63·인덱스 갱신 L64·항목 절단 L65·즉시 수행 L68·부분 편집 L69·과거 참조 L71·민감 정보 L73) 불변.
  5. UTF-8(BOM 없음), 마크다운 구문 정상.

### T2 — 버전·문서 갱신  [Type A]
- `notes.md`: 본 작업(notes 아카이브 정합 ② 보강) 항목 추가 — 무엇·왜·어떻게·검증.
- `plugin.json`·`README.md`: 1.66.3 → 1.66.4 (patch — ①④③과 동형 빈틈 보강 시리즈 마지막). README 기능 목록 변경 없음(버전 줄만).
- Acceptance: notes.md에 본 작업 항목 1건; plugin.json·README 버전 일치(1.66.4), JSON 파싱 OK.
- Halt Forecast: 없음.

## Verification Strategy
1. **문구 grep**: T1 정합 자가검증 6개 점검 + 기준 정식화 문구 / T2 버전 1.66.4 — 각 acceptance를 grep으로 확인.
2. **기준 일관성**: 정식화 문구의 값(1달/30,000자)이 기존 L61~63 기준값과 동일(새 값 도입·드리프트 0).
3. **F-6.5 무손상**: 기존 단락(기준·인덱스 갱신·항목 절단·즉시 수행·부분 편집·과거 참조·민감 정보)이 git diff에서 추가 외 변화 없음.
4. **인코딩·구문**: 수정 파일 BOM 없음, 마크다운 구문 깨짐 없음.
5. **역대조 표**: Q1(산문 — notes-lint.py·새 hook **미생성**)·Q2(1달/30,000자 정식화 — 1주일로 **안 바꿈**) 결정이 산출물에 실제 존재하는지, 미채택안(lint 스크립트·Stop 훅·1주일 변경) 흔적이 없는지 대조.

## Out of Scope (이번 제외)
- **프로젝트 CLAUDE.md(레포 밖 `D:\Personal Project\AI\CLAUDE.md`)의 1주일 기준 수정** — 레포 밖 파일이라 이 plan(플러그인 레포)이 수정 불가. 사용자가 별도(수동) 정렬. → Deferred로 권고 기록.
- **notes 아카이브 자동 실행** — 정합 *검증*만 보강. 실제 아카이브 트리거·실행 로직은 기존 F-6.5 그대로.

## Deferred / Follow-up
- **notes 아카이브 정합의 기계 검증**(lint 스크립트/Stop 훅): 이번엔 산문 자가검증으로(Q1). 향후 아카이브가 빈번해지거나 정합 오류가 실제 발생하면 위키 lint.py 모델로 재검토(감사 ③ recipe 귀속 lint Deferred와 동형).
- **프로젝트 CLAUDE.md 1주일→1달/30,000자 정렬**(레포 밖, 사용자 수동): phase-f-detail 정식화 후 사용자가 직접 맞추면 "조용한 덮어씀"이 완전 해소.
- **split-mechanism-audit 메모리 삭제**: ②가 마지막 항목이므로, 본 작업 완료 후 메모리 규칙("셋 다 끝나면 memory 삭제")에 따라 `split-mechanism-audit` 메모리 + MEMORY.md 인덱스 줄 제거.

## 승인 필요 항목
- 본 plan(지침 문서 1파일 보강 + notes/버전) — 승인 요청.
- T2 버전 bump — **patch 1.66.4 추천**, 사용자 확정 필요.
- commit/push 및 GitHub 릴리즈(v1.66.4) — 구현·검증 후 별도 승인.

## Progress Log
- T1~T2 완료 (미커밋 — 커밋 승인 대기): ① phase-f-detail.md F-6.5에 "아카이브 후 정합 자가검증" 6점검 블록(인덱스→파일·파일→인덱스·항목 손실 0·due 미수행[날짜 불가 시 분량만]·월당 1줄·미존재 통과) + 기준 정식화 한 줄(1달/30,000자 정식, 1주일은 구버전). 순수 append(9줄 추가/0 삭제). ② notes 항목 + plugin.json·README 1.66.4. 전부 Type A.
  - 검증: 6점검+정식화 grep 전부 반영 / git diff 9 add·0 delete(기존 F-6.5 무손상) / 값 드리프트 0(1달/30,000자 동일) / BOM 없음 / 역대조(notes-lint.py·Stop 훅 미생성, 1주일로 임계 안 바꿈 — 미채택안 흔적 0).
  - plan-reviewer: OK(BLOCKER/MAJOR 0, MINOR 2=날짜 불가 due 오탐·동일 월 인덱스 중복 → 둘 다 체크리스트 6점검에 흡수).
  - F-7 plan-completion-reviewer: BLOCKER 0 / MAJOR 0 / MINOR 1(plan 자구 "5개"→"6개" 오기) → 즉시 정정.
  - 결정(사용자 확정 반영): Q1=산문 강화(기계화 회피), Q2=1달/30,000자 정식화(프로젝트 CLAUDE.md 1주일은 레포 밖·사용자 수동 정렬).

## Next Steps
- 권장 다음 액션: 변경 5파일 commit(`기능: notes 아카이브 정합 빈틈 보강(감사 ②) + 1.66.4`) → push → GitHub 릴리즈 v1.66.4(메모리 release-on-version-bump). **commit/push·릴리즈는 사용자 승인 필요**(plan 승인 항목).
- 완료 후: split-mechanism-audit 메모리 + MEMORY.md 인덱스 줄 삭제(② = 분할 시리즈 마지막, 메모리 규칙 "셋 다 끝나면 삭제").
- Suggested skills: (커밋 후) 공식 /code-review.

## Open Questions
- 없음 (Q1 산문 강화·Q2 1달/30,000자 정식화 사용자 확정, 버전 종류만 승인 항목).
