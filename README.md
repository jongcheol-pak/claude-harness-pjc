<p align="center">
  <img src="assets/logo.png" alt="claude-harness-pjc logo" width="180" />
</p>

# claude-harness-pjc (자율 루프)

> Claude Code가 **계획하고 검증하며** 일하도록 만드는 plugin (Windows 우선 · pwsh 7 또는 내장 PowerShell)
> <br>(계획·검증 로직은 OS 무관, 자동 안전망 hook은 pwsh 7 우선·없으면 Windows 내장 PowerShell로 폴백 — Windows 검증·macOS/Linux 실험적, [호환 환경](#호환-환경) 참고)

**버전**: 1.117.0
**저장소**: https://github.com/jongcheol-pak/claude-harness-pjc

---

## 한눈에 보기

Claude Code에게 코딩을 맡기면 가끔 이렇게 합니다:

- 계획 없이 바로 코드를 쓰고, 있지도 않은 함수를 호출
- A 파일만 고치고 그걸 부르는 B·C 파일은 그대로 둬서 빌드가 깨짐
- 제대로 확인 안 하고 "다 됐습니다"라고 보고
- task마다 "다음 진행할까요?"를 물어 흐름이 끊김

**pjc는 이걸 막습니다.** 코드를 바꾸기 전에 먼저 계획을 세우게 하고, 바꾼 뒤에는 여러 단계로 검증하게 하며, 한 번 승인하면 끝까지 자동으로 진행합니다. "꼼꼼한 시니어 개발자의 작업 습관"을 Claude Code에 입히는 도구라고 보면 됩니다.

비유하자면, Claude Code가 "재능 있지만 가끔 덤벙대는 주니어"라면, pjc는 그 옆에서 "계획서 보여줘 → 이 부분 빠졌네 → 검증했어? → 좋아 통과"를 챙기는 **작업 절차와 안전장치**입니다.

> 특정 언어 전용이 아닙니다 — 빌드/테스트 명령만 알면 **어떤 언어·프로젝트에서도** 동작합니다. (.NET·Android·Node/TS·Python·Go·Rust는 자동 설정, 그 외는 generic 템플릿)

---

## 무엇을 해결하나요

| 자주 겪는 문제 | pjc의 대응 |
|---|---|
| 추측으로 코드를 쓰고, 없는 함수를 호출 | 코드 변경 전 영향 범위를 전부 조사하고, 계획을 적대적으로 검토 |
| A만 고치고 B·C를 빠뜨림 | 변경할 때마다 그걸 쓰는 다른 파일을 자동으로 찾아 함께 점검 |
| 확인 없이 "다 됐다"고 보고 | 빌드·테스트 + 2단계 검토 + 자기점검을 통과해야만 완료 |
| task마다 "다음 할까요?" 물음 | 한 번 승인하면 모든 task를 끝까지 자동 진행 |
| 사소한 오타 수정에도 거창한 절차 | 3줄 이내 단순 수정은 절차를 건너뛰고 바로 처리 |
| 빌드·테스트 명령을 매번 모름 | 프로젝트를 분석해 명령을 자동으로 파악·기록 |
| DB 데이터를 실수로 삭제·변조 | 위험한 DB 명령(전체 삭제/변조 등)을 자동 차단 |
| 과거에 한 작업을 매번 다시 조사 | 프로젝트 지식을 위키에 쌓고, 다음 작업 때 참조 |

---

## 빠른 시작

### 1. 사전 요구사항

- Windows 10/11 + PowerShell
- Claude Code v2.0 이상

### 2. 설치

```powershell
claude plugin marketplace add jongcheol-pak/claude-harness-pjc
claude plugin install pjc@pjc-harness
claude plugin enable pjc@pjc-harness
```

설치가 끝나면 Claude Code를 다시 시작합니다.

### 3. 첫 사용

평소처럼 말하면 됩니다. pjc가 알아서 끼어듭니다:

```
> 로그인 화면에 "비밀번호 찾기" 링크 추가해줘

[pjc가 자동으로]
1. 계획을 세우고 → 검토한 뒤 → 당신에게 승인을 받습니다
2. 승인하면 → 구현하고 → 검증하고 → 완료를 보고합니다
```

처음 코드 작업을 시도하면, pjc가 프로젝트 정보(빌드·테스트 명령 등)를 담은 `AGENTS.md` 파일이 없다며 자동 생성을 제안합니다. 승인하면 프로젝트를 분석해 만들어 줍니다.

---

## 어떻게 동작하나요

pjc는 코드 작업을 **계획 → 구현 → 검증 → 완료**의 흐름으로 진행합니다.

### 1단계 — 계획 (당신과 함께)

요청을 받으면 바로 코드를 쓰지 않고 먼저 계획을 세웁니다.

- 어떤 파일을 건드려야 하는지 **전부 조사**합니다 (영향 범위 파악)
- 새로 만들기 전에 **이미 있는 코드·공통 컴포넌트를 먼저 검색**해 재사용합니다 (중복 구현 방지 — UI 요소도 기존 공용 컴포넌트 재사용 우선)
- 작업을 작은 단위(T1, T2, …)로 나눕니다
- 별도의 "검토 담당"이 계획을 **적대적으로 검토**합니다 — 빠진 것, 위험한 것을 찾아냅니다
- 완성된 계획을 보여주고 **당신의 승인을 기다립니다** ✋

> **계획에 명시된 "알려진 승인 필요 항목"(비파괴 DB 스키마 변경·의존성 추가 등)은 이 1회 승인에 함께 포함됩니다** — 그래서 자율 진행 중 그런 항목마다 다시 멈추지 않습니다. 단 push·릴리즈 같은 외부 작업과, 계획에 없던 위험한 변경(파괴적 작업·DB 삭제 등)은 그 지점에서 따로 승인받습니다.

> 큰 작업(앱 신규 개발 등)이면 계획 전에 요구사항 정의서(PRD)부터 만들어 합의합니다.

### 2단계 — 구현 (자동)

승인하면 여기서부터는 **끝까지 자동**입니다. task마다 다음을 반복합니다:

1. 이 변경이 다른 곳에 영향을 주는지 미리 추적
2. 최소한으로 코드 수정
3. 빌드·테스트 + 검토 담당의 검증
4. 통과하면 저장하고 즉시 다음 task로

중간에 "다음 할까요?"라고 묻지 않습니다. 문제가 생겼을 때만 멈춰서 물어봅니다.

> 수정으로 호출부가 사라져 더 이상 쓰이지 않게 된 코드(고아 코드)도 감지합니다 — 안전한 것은 함께 정리하고, 애매한 것은 완료 보고에서 알려줍니다.

### 3단계 — 검증과 완료

모든 task가 끝나면 전체를 한 번 더 통합 검증하고, 빌드·테스트·검토를 모두 통과한 뒤에야 완료를 보고합니다.

### 안전장치 (항상 작동)

작업 흐름과 별개로, **위험한 명령은 자동으로 차단**됩니다. 이건 끌 수 없습니다.

- 파일 전체 삭제(`rm -rf /`·`rm -rf ~/*` 등 홈·시스템 루트와 그 내용물 글롭), git 히스토리 파괴
- **DB 데이터 삭제·변조**: 전체 삭제(`DELETE` WHERE 없이), 전체 변조(`UPDATE` WHERE 없이), `DROP`/`TRUNCATE`, ORM 대량 삭제 등
- 단, `WHERE 조건`이 있는 일상적인 DB 작업은 그대로 통과합니다

---

## 단순 작업은 빠르게

모든 작업에 거창한 절차를 강제하면 오히려 불편합니다. 그래서 **3줄 이내의 단순 수정**은 계획 없이 바로 처리합니다:

- UI 문구·라벨 변경, 색상·아이콘 교체
- 오타·주석 수정
- 단일 라인 설정·리소스 변경

기준은 "3줄 이내 + 새 함수/구조 추가 없음 + 의도가 명확"입니다. 이 경우에도 다른 파일에 영향이 가면 자동으로 알려줍니다.

---

## 주요 기능

### Skills (작업을 수행하는 능력)

| 기능 | 언제 작동하나 | 무엇을 하나 |
|---|---|---|
| 계획 수립 | "기능 추가", "구현" 등 | 코드 변경 전 계획 + 검토 + 요구 이해 확인(이해한 요구를 승인 시 재진술) |
| 구현 | 계획 승인 후 자동 | 모든 task를 끝까지 자동 진행 |
| 디버깅 | "버그", "에러", "안 됨" 등 | 근본 원인을 찾는 체계적 디버깅 |
| 위키 운영 | "위키에 등록/업데이트" | 프로젝트 지식을 쌓고 다음 작업 때 참조 |
| 프로젝트 사실 기록 | "AGENTS.md에 기록" 또는 hook 제안 수락 | 빌드·DB·테스트 방식을 AGENTS.md에 축적(재확인 줄이기) |
| 그 외 | — | MVVM/DDD 코드 생성, 프로젝트 분석 등 |

### Subagents (검토 담당)

작업을 직접 하는 Claude와 **별개의 검토자**들이 결과를 적대적으로 점검합니다. 작성자와 검토자를 분리해 "자기 일을 자기가 검토하는" 맹점을 줄입니다.

- 계획 검토자: 계획에 빠진 것·위험한 것을 찾음
- 명세 준수 검토자: 구현이 계획대로 됐는지, 빠진 파일은 없는지
- 품질 검토자: 코드 품질·보안·규칙 준수
- 완료 검토자: 전체가 요구사항을 충족하는지

> v1.100.0에서 검토·워크플로를 다듬었습니다:
> - **검토 지적에 근거로 이의 제기**: 검토자의 지적이 사실 오류일 때, Claude가 근거 없이 무시하지 않고 **파일·라인을 인용한 반증을 붙여 재검토를 요청**할 수 있습니다(재차 같은 지적이면 수용하거나 사용자에게 확인). "묵살"이 아니라 근거 있는 반증만 허용됩니다. (v1.117.0에서 별도 횟수 캡을 없애고 상한을 수정 사이클 카운터에 위임했습니다.)
> - **테스트 범위 비례**: 프로젝트 가이드(AGENTS.md)에 영향 범위 필터 명령이나 검증 매핑 표가 있으면 각 작업에서는 **영향받는 부분만** 테스트하고, **전체 테스트는 마지막 통합 검증에서 1회** 보장합니다(매 작업 전량 실행의 중복 제거 — 축소 시 커밋에 범위를 명시). 근거가 없으면 스위트 크기와 무관하게 전체를 돕니다. (v1.117.0에서 "스위트가 크면"이라는 시간 기준을 제거했습니다.)
> - **간단한 계획은 경량 검토**: 문서·간단 수정만으로 이뤄진 계획은 값비싼 계획 검토자(Opus) 대신 자체 체크리스트로 검토할 수 있습니다(코드 변경이 하나라도 있으면 정식 검토).
> - **간단한 버그는 경량 경로**: 컴파일러·스택트레이스가 원인을 이미 짚어 준 소규모 단일 파일 수정은 전체 디버깅 단계를 축약합니다(원인이 불명확하거나 다중 파일로 번지면 표준 절차로 복귀).

> v1.101.0에서 검토 그물을 더 촘촘히 했습니다:
> - **실현 불가능한 계획 차단**: 계획 검토자가 한 작업의 완료 조건들이 서로 모순(동시 만족 불가)이지 않은지, 성립을 좌우하는 확인이 구현 단계로 미뤄지지 않았는지 검사합니다(과거 실측 사례에서 도출).
> - **검토 잔여분 소비**: 계획 검토자가 "미검증 N개"를 정직하게 보고하면, 이제 그것을 통과로 흘려보내지 않고 직접 검증하거나 재검토를 요청합니다.
> - **완료 시 요구 재대조**: 완료 검토자가 구현 산출물이 계획의 "요구 이해" 각 포인트를 커버하는지 사후 대조합니다 — PRD 없는 계획의 유일한 사후 요구 재검증.

> v1.103.0: 계획 질문의 **추천(★) 기준을 "근본 해결 우선"으로 명문화** — 쉽게 끝나는 선택지가 아니라 문제를 근본적으로 해결하는 선택지를 추천하고, 복잡도·작업량 차이는 트레이드오프로 함께 표시해 비교할 수 있게 합니다(단순함 자체가 근본 해결인 경우는 예외).

> v1.118.0: **계획 게이트의 구멍 차단** — 하네스는 "계획 없이는 코드를 못 고친다"를 hook으로 강제해 왔는데, 정작 **그 계획 자체를 누가 만드느냐**는 열려 있었습니다. 계획 도구를 거치지 않고 `plan.md`를 손으로 하나 써 두면 게이트가 켜져서, 적대적 계획 검토·영향 범위 전수 조사·작업 분류·사전 승인 항목이 통째로 건너뛰어진 채 이후 모든 코드 변경이 무검증 통과됐습니다(AGENTS.md 신규 생성에는 이미 스킬 경유 게이트가 있었던 것과 비대칭이었습니다). 이제 **세 개의 문을 같은 자물쇠로 잠급니다**: ① 계획 파일을 통째로 쓰는 것(Write), ② 체크박스가 없는 문서에 **체크박스를 새로 심는 편집**(Edit — 이걸 안 막으면 "빈 파일 쓰고 → 체크박스 채우기" 두 번으로 우회됩니다. 여러 편집을 한 번에 보내는 경우도 편집 **단위**로 판정해 순차 적용 우회를 막습니다), ③ `docs/plans/` **디렉터리가 있다는 이유만으로** 계획이 있다고 판정하던 것 — 이제 **체크박스가 있는 실제 계획 파일**이 있어야 켜집니다. 셋이 **하나의 판정 기준을 공유**하는 것이 핵심입니다(기준이 갈리면 그 차이가 곧 우회로). **정상 작업은 그대로입니다** — 체크박스 상태 변경(`[ ]`→`[x]`)·진행 로그·보류 항목 기록 등 기존 계획의 부분 갱신, `docs/plans/`의 대장·메모 문서, `+`나 `1.` 같은 다른 표기의 계획, 임시 폴더 작업은 전부 통과합니다(골든 회귀 17건으로 차단 7건·통과 10건을 양방향 실증). 계획이 없어 막혔을 때 **왜 인식되지 않았는지**(체크박스 없는 문서만 있음)도 함께 알려줍니다 — 진단할 수 없는 차단은 안전장치가 아니라 장애물이기 때문입니다.

> v1.117.0: **실효 없는 제한 조건 정리** — 하네스가 스스로 지킬 수 없는 규칙을 걷어냈습니다. ① **비용 폭증 Halt**는 "평균 대비 토큰·시간 3배"라는 **측정 불가 조건만 제거**하고(모델은 자기 토큰·경과시간을 관측할 수 없어 판정 자체가 불가능했습니다), **"빌드·수정 사이클 10회" 상한은 존치**해 "무한 그라인딩"으로 개명했습니다 — 기존 5회 캡들은 각각 "원인 미상"·"리뷰 지적"에만 걸려, *원인은 매번 알지만 끝나지 않는* 그라인딩은 이 10회가 유일하게 잡습니다(그래서 지우지 않았고, 대신 카운터 영속화에 배선해 압축 후에도 이어 세도록 했습니다). ② **작업별 테스트 축소 조건에서 "스위트가 크면(3분 초과)"을 제거** — 소요 시간은 돌려봐야 알고 돌리고 나면 축소할 이유가 사라지는 순환이었습니다. 이제 AGENTS.md에 영향 범위 필터 명령이나 검증 매핑 표가 **있을 때만** 축소하고, 없으면 크기와 무관하게 전체를 돕니다. ③ **검토 지적 이의의 "1회" 재호출 캡 제거** — 절차와 종결 규칙(재차 같은 지적이면 수용 또는 사용자 확인)은 그대로 두고, 상한은 이미 존재하던 수정 사이클 카운터(누적 5회·동일 지적 3회)에 위임했습니다. ④ **검토자의 confidence(0-100) 숫자 스케일을 "근거 지목 기준"으로 교체** — LLM은 자기 확신도를 숫자로 캘리브레이션하지 못해 그 숫자가 판단을 만들지 못하고 이미 내린 판단에 붙는 사후 라벨이었습니다. 이제 "근거를 파일·라인으로 지목할 수 있는가"로 판정합니다(지목 가능 → 보고 / 결함 여부가 해석에 달림 → MINOR 강등 / 지목 불가 → 미보고). **실제로 작동하던 강등 금지 예외**(호출자 누락·완료 조건 미충족·PRD 요구 미충족)는 그대로 보존했습니다. ⑤ 사전 필터의 "3분 이내" → "maxTurns 예산 안에서 단일 패스"(서브에이전트도 시계가 없습니다). 반대로 **F-7 재진입 3회·계획 검토 3회·PRD 분할 임계 등 "도달하지 않는 상한"들은 유지**했습니다 — 도달하지 않는다는 건 비용이 0이라는 뜻이지 무용하다는 뜻이 아니며, 실제로는 무한 루프·교착을 막는 종결 보장이었습니다.

> v1.116.0: llm-wiki **스킬 검토 결함 6종 수정** — 절차 문서 2건: ① 절차 M(큐 소비)에 빈 위키 예외 신설 — §0-2 부트스트랩이 M의 "범위 고정" 선언보다 먼저 발동해 큐 정리 요청이 vault 골격을 만들던 순서 충돌 해소(G·K와 동형, 미부트스트랩 vault의 큐는 잔량 보고만), §0-2에 읽기측(G·K·M) 예외 포인터 명시. ② SKILL.md 병렬 분업 요약의 공유 파일 목록에 `pending.md` 합류(정본 §9와 드리프트 — 병렬 에이전트가 각자 append하면 read-modify-write 유실). lint.py 4건: ③ 루트 `pending.md`(소비 대기 큐)를 깨진 링크 검사에서 제외 — 큐 항목의 wikilink 대상이 삭제되면 lint가 exit 1로 죽던 것(시크릿·잔량 집계는 유지). ④ guide_kind **부재** 시 WARN — 오타는 경고하면서 부재는 침묵으로 기본 200줄이 적용돼 recipe 120줄 예산이 우회되던 비대칭 해소. ⑤ §7-20/21 실존 검사의 레포 루트 글롭 이스케이프(`[ ]` 메타문자 경로에서 글롭 토큰 상시 오탐) + --fix §7-24 깨진 포인터 제거를 `## 아카이브` 섹션 안으로 한정(결정 항목 본문의 경로 인용까지 지우던 것 — §2.8 항목 불변 보호). ⑥ --fix 질문 등록이 표 형식 섹션엔 컬럼 수 맞춘 표 행으로 삽입(부트스트랩 골격 정합·불릿형 하위호환). 골든 회귀 23→25케이스(guide_kind 부재·--fix 보수 동작 실증 — 러너에 after_expect_keywords 필드 추가), wiki-schema v2.40.

> v1.115.0: bootstrap-agents-md **감지 로직 결함 3종 + 템플릿 정비** — ① **Case B 죽은 분기 해소**: "표식은 알지만 템플릿 없는" stack(Flutter·Swift·Ruby·Elixir·Zig·Java/Maven)의 표식을 Step 2가 아예 탐색하지 않아 Case B가 도달 불가였던 것을 마커 목록에 합류시켜 실동작하게 했습니다. ② **Gradle JVM→Android 오탐 차단**: `build.gradle`만으로 Android로 판정해 Spring Boot 등 순수 JVM 프로젝트가 `android.md`를 받던 것을, `AndroidManifest.xml` 또는 `com.android.*` 플러그인이 있을 때만 Android로 좁혔습니다(그 외 Gradle은 Case B). ③ **의존성 디렉터리 오탐 차단**: 재귀 탐색에 node_modules/bin/obj 등 제외를 추가(하위 node_modules의 package.json이 Node stack으로 오탐되던 것). 부수: `[ordered]` 해시테이블(표 순서 보장)·`*.slnx` 마커. 템플릿 쪽은 `dotnet test tests/`가 테스트 프로젝트 2개 이상이면 MSB1011로 실패하는 자기모순 수정(4종), winui3 TFM 예시 정합, **Plan Location을 옵션 나열→택일 설정 형식으로 통일**(10종 — 생성된 AGENTS.md를 읽는 에이전트가 어느 쪽인지 판별 가능), multi-stack 예시에 보안 DO NOT 합류, UI 계열 4종에 산출물·데이터 접근 섹션 추가, python `poetry shell`(2.x 제거됨)·rust Cargo.lock 지침 현행화. (모의 프로젝트 6종 실행 테스트로 판정 실증)

> v1.114.2: 증상별 인덱스 **정보 품질 가드 4종**(3차 자기검토 — "인덱스가 잘못된 정보를 먹이는 경로" 관점) — ① **사례≠진단 가드**: 행의 원인은 *그 사례에서* 검증된 것이지 지금 증상의 원인이 아니다(같은 증상 ≠ 같은 원인). 작업 세션은 행을 가설·진입점으로만 쓰고 원인 확정은 현재 코드·증거로(디버깅 Iron Law), 질의 응답은 "{프로젝트} 사례에서 검증된 원인은 …였다"로 사례 프레이밍 — "바로 점프해 재조사를 줄인다"던 기존 문구가 조사 생략·원인 단정으로 읽힐 수 있던 것을 교체. ② **한 증상 다원인 병렬**: 소비 중복 판정을 증상 텍스트가 아니라 증상+원인 기준으로 조임 — 같은 증상의 다른 검증된 원인이 침묵 폐기돼 인덱스가 "첫 원인=유일 원인"으로 오도하던 경로 차단. ③ **폐기 해법 행 정리**: feature 폐기 시 그 해법을 가리키는 증상 행 제거를 명시(링크가 유효해 깨진 링크 검사로 안 잡히던 사각). ④ 원인 칸에 검증 프로젝트 병기 권장(recipe 해법의 출처 가시화).

> v1.114.1: v1.114.0 증상별 인덱스의 **후속 수정**(자기검토에서 발견한 결함 3종) — ① lint가 증상별 인덱스 행을 기능별 인덱스 행으로 오인식해 증상(한글 관찰 표현)에 영문 병기를 요구하던 **오탐 제거**(§7-16·§7-6 스캔에서 증상 섹션 제외, `without_section` 헬퍼 + 회귀 픽스처). ② 하네스가 버그·오류 작업 시 **실제 참조·질의 절차**(SKILL K·procedures-ops G)에서 증상별 인덱스를 조회하도록 배선 — 규칙(schema §6)엔 있으나 실행 절차에 빠져 있어 정작 자동 발동 경로에서 안 켜지던 것을 연결. ③ `[SYMPTOM]` 태그를 lint 세션 절차(F-0/F-2)·경량 소비(M)·헤더 주석 등 v1.114.0에서 놓친 열거처에 합류. (기계 정합 eval은 통과했으나 태그 열거 산문·lint 행 인식 충돌은 그 범위 밖이라, 릴리즈 후 자기검토로 잡음.)

> v1.114.0: llm-wiki에 **증상별 인덱스** 추가 — 디버깅을 시작할 때 손에 있는 건 해법 이름이 아니라 *증상*("메일 알림이 안 뜬다")이므로, 증상 → 검증된 근본원인 → 해법 페이지(recipe/feature)로 이어지는 역인덱스를 위키에 둡니다. 디버깅 스킬이 근본원인을 확정하면 `[SYMPTOM]` 큐(pending.md, 절차 K 5-5)로 이 매핑을 자동 수집하고, 위키 세션이 등재 게이트(증상=관찰 표현·원인=검증된 인과·해법 페이지 실존)를 검증해 인덱스에 반영합니다. **실패한 접근은 저장하지 않습니다** — 미검증 실패("A를 시도했으나 안 됨")는 잘못된 진단을 위키 권위로 고착시키고, 나중에 유효해진 경로를 영구 봉쇄하며, 검색 시 정답과 경쟁해 성공 해법을 오염시키기 때문입니다(재사용 가치가 있는 실패 지식은 이미 recipe 함정 섹션이 담습니다). 함께 **과잉 저장 축소** 규칙도 넣었습니다 — 위키가 레포 소유 changelog(정확한 버전 번호·개수 카운트·릴리즈 마커)를 미러링해 매 릴리즈 stale되던 것을 막고, "무엇이 왜 그렇게 설계됐나"만 담도록 조였습니다.

> v1.113.2: 디버깅 스킬에 **병렬 조사 지시** 추가 — 버그를 조사할 때 서로 독립적인 코드베이스 탐색(같은 오류의 다른 처리 위치·같은 API를 쓰는 다른 모듈)이 여러 건이면 탐색 전담 서브에이전트(explorer)를 동시에 띄워 후보 위치를 병렬로 모으도록 했습니다(대기 시간만 줄고 결과는 동일). 마찬가지로 여러 가설을 세웠을 때 각 가설의 증거가 있을 코드 위치도 가설별로 병렬 수집합니다. 단 **위치를 찾는 것(locating)까지만 위임**하고 원인 판정·가설 검증·진단 실행(로그 추가·테스트·스크립트)은 메인이 직접 합니다(발췌만 읽는 서브에이전트에 판정을 맡기면 오판 위험). 위키 교차 검색은 이미 단일 검색 구조이고 정확도가 중요한 판정을 포함하므로 이 병렬화 대상에서 제외했습니다.

> v1.113.1: warn-external-ops의 `git merge-base` 오탐 수정 — 병합 등 외부 작업을 경고하는 안전망이 `git merge-base`(병합 기준 커밋을 찾는 읽기 전용 조회)를 실제 브랜치 병합으로 잘못 보고 불필요한 경고를 내던 것을 고쳤습니다. 정규식이 하이픈을 단어 경계로 인정해 `merge-base`·`merge-tree`·`merge-file`·`merge-index` 같은 plumbing 명령까지 걸리던 것을, 하이픈을 명령 이름의 일부로 취급하도록 조여 오탐만 제거했습니다(실제 `git merge`·bare `git merge` 경고와 `--abort`/`--continue` 제외는 그대로 유지). 골든 회귀 321/321 — merge-base·merge-tree 무경고 케이스 2종 추가.

> v1.113.0: Type B(Trivial Code) 분류 정의 조임 — 신규 심볼이라도 **비자명 로직·알고리즘·제어흐름·상태 변경을 담으면 Type C 이상**으로 분류하도록 경계를 명문화했습니다(자명한 접근자·상수·순수 위임은 Type B 유지). Type B는 적대적 리뷰(V-5 Sonnet·V-6)를 건너뛰고 Haiku prefilter만 타므로, 검토가 필요한 신규 로직이 단일 파일이라는 이유로 B로 분류돼 무검증 통과되던 공백을 막습니다. plan-reviewer의 Type 오분류 검출(항목 9)에도 같은 케이스를 BLOCKER 예시로 병기해 계획·계획리뷰 두 단계에서 방어합니다.

> v1.112.0: 세션 컨텍스트 자동 주입 + notes 아카이브 경고 — ① **세션 시작 컨텍스트 주입**(신규 hook `session-context`): 세션 시작·재개·`/clear` 후는 물론 **컨텍스트 요약(compact) 직후**에도 현재 프로젝트의 plan.md 진행 상태(미완료 task 수)와 notes.md 최근 항목 날짜를 자동으로 알려줍니다. 특히 요약 직후에는 "plan·스킬 규약을 요약 기억에 의존하지 말고 원문에서 재확인하라"는 리마인더를 함께 주입해, 긴 자율 루프 후반의 규칙 유실을 막습니다(plan/notes가 없는 프로젝트에서는 조용히 침묵 — 노이즈 없음). ② **notes.md 아카이브 시점 경고**: notes.md가 30,000자를 넘으면 편집 시점에 아카이브 안내를 1회 경고해, "미루면 영원히 안 되는" 아카이브 규칙에 집행 장치를 답니다. (골든 회귀 317/317 — 신규 케이스 11종 포함)

> v1.111.0: AGENTS.md 생성 우회 방지 — 계획 단계가 AGENTS.md를 `bootstrap-agents-md` 스킬 없이 즉석 작성해 스택 템플릿 자산·사용자 승인([Y/E/N])이 통째로 생략되던 실사례를 막습니다. ① **hook 게이트**: AGENTS.md **신규 생성** Write에서 스킬 발동 흔적(대화 기록 2패턴)이 없으면 차단하고 스킬 호출 경로를 안내합니다(기존 파일 편집·record-project-fact는 비대상, 대화 기록을 확인할 수 없는 환경은 통과 — 오차단 방지). ② **지시 강화**: plan-feature·bootstrap 스킬 문서에 직접 작성 금지를 명문화했습니다. (골든 회귀 306/306 — 신규 게이트 케이스 6종 포함)

> v1.110.0: 자율 루프 규정 모순 2건 해소 + 사용성 점검 수정 12건 — ① **implement-task 완료 커밋 판정 정합**: 리뷰 지적 수정분을 `checkpoint: T<N> review-fix`로 커밋하는 규정을 신설하고, Phase D의 수정분 유무 판정을 워킹트리 상태(`git status`)에서 **HEAD 커밋 제목 기준**으로 교체했습니다(재리뷰 커밋 프로토콜과 신호 일원화 — 도달 불가 분기·재개 트리 공백 해소, 새 완료 커밋은 `--allow-empty`로 gitignore plan 레포에서도 안전). ② **분할 plan×PRD 교착 해소**: 분할 plan의 PRD Coverage에 `⏭️ 다음 part`/`✅ 이전 part 기구현` 행 표기를 신설해 part별은 자기 몫만 커버하되 **두 part 합집합=전수**를 Step 7.5가 검증하고, plan-reviewer 12-a가 분할을 인지해 다음 part 대응 task 실재를 확인합니다(마지막 part의 Phase G/F-7은 이전 part 몫 포함 전수 — 전체 트리 기준). ③ 사용성 점검(실행 에이전트 마찰 관점)에서 나온 12건: llm-wiki lint 호출 경로 인용(공백 포함 설치 경로에서 기계 검증이 조용히 스킵되던 실질 버그)·529 개입 배선·V-8에 V-9 관문 편입·빈 위키 질의 부트스트랩 방지·커밋 0 git vault 사전 백업 등.

> v1.109.0: 스킬 문서 드리프트 전건 정정(판정·트리거 무손실) — ① 검토에서 발견된 오지칭·구식 열거·모호 문구 27건을 현행화했습니다(규칙 라벨 통일·G-1 예외 3가지·절차 M 열거·읽기 비례 원칙 반영 등 — 판정 로직·검사 강도 무변경). ② 한도 턱밑 3건에 안전 여유 확보: implement-task 본문 492→484줄(V-9 상세를 references로 무손실 이관), plan-feature·pjc-systematic-debugging description을 각각 981자·921자로 무손실 압축(트리거 어휘·경계 보존을 기계 대조 + 전후 트리거 eval 동률로 실증). ③ [PROJECT-FACT] 큐 배치 트리거를 plan-feature 승인 시점·implement-task 종료 시점에 배선해 [DECISION]과 대칭을 맞췄습니다.
>
> v1.108.0: 하네스 속도 개선(품질 판정 무변경) — ① **hook 이중 스폰 제거**: 안전망 hook이 자식 PowerShell을 다시 띄우던 것을 Claude Code가 띄운 셸에서 직접 실행하도록 바꿔 hook당 약 0.3초를 줄였습니다(도구 호출당 hook 3개 ≈ 1초 — 차단·경고 판정 로직과 스크립트는 무변경, 골든 회귀 300/300). ② **자율 루프 실행 지침 3건**: 파일 병렬 Read·심볼 배치 grep·빌드/테스트/린트 체이닝으로 같은 검증을 더 적은 턴·호출로 수행합니다(검사 범위·강도 무변경).
>
> v1.107.0: 하네스 자기운영 개선 3건 — ① **Deferred 통합 대장**: "다음에 하자"고 미뤄둔 항목이 계획서마다 흩어져 수동 이월되다 유실되던 것을, 커밋되는 단일 대장(`docs/plans/deferred.md`)에 자동 축적합니다(작업 완료 시 기록, 다음 계획 때 자동 조회 — 실제로 유실됐던 3건을 시드에서 회수). ② **골든 러너 부분 실행**: hook 하나 고치는 개발 루프에서 `-Filter <hook명>`으로 해당 케이스만 빠르게 돌립니다(최종 검증은 여전히 전체 실행 — 부분 실행 결과로 완료 판정 금지, 오타는 exit 1로 가시화). ③ **이벤트 로그 리포트**: v1.105.0부터 쌓이는 차단·경고 로그를 hook별·규칙별로 집계하는 수동 도구가 생겨, 오탐 검토를 축적 대기 없이 언제든 실측 기반으로 시작할 수 있습니다.
>
> v1.106.0: 위키 스킬 개선 4건 — ① **[K-MISS] 큐**: 코드 작업 중 위키에서 찾았으나 없던 지식이 자동으로 큐에 남아, 다음 위키 갱신이 실사용 수요 기반으로 페이지를 만듭니다(자동 생성 아님 — 근거 대조 후 반영/기각). ② **lint --fix**: 판단이 필요 없는 참조 무결성 3종(질문 인덱스·결정 아카이브 포인터·log 인덱스 stale 행)을 스크립트가 자동 수정합니다(백업 자동, 기본 실행은 여전히 읽기 전용). ③ **경량 큐 정리**: "큐 정리"라고 요청하면 전체 점검 없이 대기 큐만 소비합니다. ④ **위키 참조 기록**: 계획 세션이 참조한 위키 페이지가 계획서에 남아 리뷰·재개 세션이 재조회 없이 씁니다.

> v1.105.0: 하네스 자기계측·정확도 개선 5건 — ① **버전 드리프트 자동 경고**: 하네스 레포에서 세션을 시작하면 설치본과 레포의 플러그인 버전이 다를 때 자동으로 알려줍니다(재설치 전까지 세션이 구버전으로 동작한다는 혼동 제거). ② **hook 이벤트 로그**: 차단·경고가 발생할 때마다 로컬 jsonl로 적재해 오탐 검토가 실측 기반이 됩니다(시크릿 감지 시 명령 미기록, 로깅 실패는 hook 동작에 무영향). ③ **분할 plan 핸드오프**: part1 완료 시 part2 계획서에 함정·기각된 접근·검증 지름길을 물려줘 재개 세션의 시행착오를 줄입니다. ④ **검증 매핑 표**: 변경 파일 패턴별 필수 검증을 AGENTS.md 표로 결정론화(전체 검증은 마무리 단계 보장 유지). ⑤ **리뷰어 LSP 보강**: 코드 인텔리전스가 설치된 프로젝트에서 리뷰어·탐색기가 참조 추적에 LSP를 우선 사용합니다(미설치 환경은 기존 grep 그대로).

> v1.104.0: 분할 plan(part1/part2) 첫 part 완료 시 **최종 보고의 다음 part 안내를 복붙 실행 가능한 형태로 강화** — 남은 plan 경로와 함께 "`/clear` 후 `<part2 경로> 구현` 붙여넣기" 절차를 경로 치환 완료 상태로 제시해, 사용자가 경로를 찾아 조립하는 마찰을 없앱니다.

<details>
<summary>모델 라우팅 — 작업 무게에 따라 Haiku/Sonnet/Opus 분배</summary>

검토자마다 작업 무게가 다르므로, 모든 것을 비싼 모델로 처리하지 않고 적절한 모델에 분배합니다. 이렇게 해서 검증 품질을 지키면서 비용·속도를 절약합니다.

| 검토자 | 모델 | 역할 | 모델 선택 이유 |
|---|---|---|---|
| spec-prefilter | Haiku | 간단한 작업의 빠른 1차 필터 | 가볍고 저렴 — 비싼 모델 호출을 줄임 |
| explorer | Haiku | 컨텍스트 수집용 빠른 탐색 | 단순 조회 — 빠르고 저렴 |
| spec-compliance-reviewer | Sonnet | 구현이 계획대로 됐는지 검증 | 정확성과 비용의 균형 |
| code-quality-reviewer | Sonnet | 코드 품질·보안 검증 | 균형 |
| plan-reviewer | Opus | 계획 적대적 검토 | 가장 깊은 추론 필요 |
| plan-completion-reviewer | Opus | 전체 완료 통합 검증 | 가장 깊은 추론 필요 |

**예**: 오타 수정 같은 간단한 작업은 Haiku 필터가 빠르게 통과시켜 Opus·Sonnet을 부르지 않고, 복잡한 기능의 계획은 Opus로 철저히 검토합니다.

**과부하 시 대응**: Opus가 과부하(529)면 재시도 후 Sonnet으로 대체하되 "검증 깊이가 낮을 수 있음"을 알립니다. Sonnet은 Haiku로 내리지 않습니다(검증 신뢰도 유지). 모델을 대체할 때는 항상 그 사실을 표시합니다.

> 참고: 위 모델은 별도 검토자(subagent)에 적용되며, 사용자와 대화하며 실제 코딩하는 메인 Claude의 모델과는 별개입니다.

</details>

### Hooks (자동 안전망)

당신이 신경 쓰지 않아도 **자동으로 작동**하는 장치들입니다.

- 위험한 명령 차단 (끌 수 없음)
- 하니스 안전장치(hook) 자체를 코드로 개조·무력화하는 시도 차단 (끌 수 없음 — 설치된 hook 스크립트·설정을 조작하지 못하게 함)
- 외부 작업(push·릴리즈·패키지 배포) 경고
- 커밋 직전 스테이징된 변경에서 시크릿 감지 (커밋 직전 최종 방어선). **자격증명으로 보이는 값(개인키·DB 연결 문자열·DB URI 인증정보·아이디/비밀번호 쌍)은 커밋을 차단**하고, 오탐 여지가 있는 나머지(API 키·비밀번호 값·토큰·IP·`.env` 스테이징)는 경고만 한다 — 공개 저장소에 한 번 올라간 자격증명은 이력에서 회수할 수 없기 때문. 오탐 시 우회는 사용자만 설정한다(`CLAUDE_HARNESS_ALLOW_SECRET=1`, Claude Code 시작 전 터미널에서)
- 계획 없이 코드 쓰는 것 방지
- AGENTS.md(프로젝트 가이드) 신규 생성을 전용 스킬 경유로 유도 (템플릿·사용자 승인 없이 직접 만들려 하면 차단)
- task 완료 커밋 전 계획서 체크박스 갱신 강제 (완료 표시 없이 완료 커밋을 하려 하면 차단 — 계획서와 실제 진행이 어긋나는 것 방지)
- 파일 인코딩·줄 수 검사
- 변경이 영향을 주는 곳 자동 경고
- 완료 전 검증 증거 확인 (빌드·테스트 흔적 없이 끝내려 하면 경고 — 커밋에 "통과"라고만 적고 이 세션에 실제 실행 흔적이 없어도 경고)
- 빌드·테스트·DB 명령 실행 시 AGENTS.md에 기록 제안 (다음 작업 때 재확인을 줄임 — 승인 시에만 기록)
- 세션 시작·재개·컨텍스트 요약 직후에 plan.md 진행 상태·notes.md 최근 항목을 자동 주입 (요약 직후엔 규약 재확인 리마인더 포함 — 긴 자율 루프의 규칙 유실 방지)
- notes.md가 아카이브 조건(30,000자 초과)에 도달하면 편집 시점에 이동 안내 경고 (세션당 1회)

> v1.99.0에서 명령 실행 시 작동하는 안전망들을 한 프로세스로 묶어 반응 속도를 높였습니다(도구 호출당 PowerShell 기동 4→2회). 위험 명령 차단(끌 수 없는 마지막 방어선)은 다른 안전망과 분리해 독립 실행하므로, 통합의 영향을 받지 않습니다. v1.101.0부터는 통합 검사 모듈이 로드에 실패하면(설치 손상 등) 조용히 통과하는 대신 **경고를 표시**해, 안전망이 꺼진 상태를 바로 알 수 있습니다.

**차단/경고 이벤트 로그 (오탐 리뷰용)**: hook이 차단·경고를 낼 때마다 발생 시점·hook 이름·발동 규칙·명령 요약이 `~/.claude/.state/hook-events/{YYYY-MM}.jsonl`에 1줄씩 쌓입니다(90일 지난 파일은 자동 정리). 정상 작업이 자꾸 차단·경고되는 것 같으면 Claude에게 "hook 이벤트 로그에서 최근 차단 내역 검토해줘"라고 요청하세요 — 오탐 수정이 추측이 아니라 실측 데이터 기반이 됩니다. 명령에 시크릿이 감지되면 그 명령 원문은 로그에 기록하지 않으며(안전 우선), 로깅이 실패해도 차단·경고 동작에는 영향이 없습니다.

쌓인 로그는 집계 리포트 도구로 바로 볼 수 있습니다 (v1.107.0 — hook별·판정별·발동 규칙별 집계, 읽기 전용):

```powershell
pwsh -NoProfile -File plugins/pjc/scripts/report-hook-events.ps1            # 보존분 전체
pwsh -NoProfile -File plugins/pjc/scripts/report-hook-events.ps1 -Days 30  # 최근 30일만
```

---

## 자주 쓰는 명령

대부분 자동으로 작동하지만, 직접 부를 수도 있습니다:

| 이렇게 말하면 | 이게 작동합니다 |
|---|---|
| "기능 추가", "리팩토링", "구현" | 계획 수립 |
| "버그", "에러", "안 돼" | 디버깅 |
| "위키에 등록/업데이트" | 위키 운영 |

---

## 위키 연동 (선택 기능)

프로젝트 지식을 쌓아두고 재사용하는 기능입니다. **쓰려면 먼저 위키 폴더를 설정해야 하며, 설정하지 않으면 이 기능만 비활성화될 뿐 나머지 pjc 기능은 정상 작동합니다.**

### 설정 방법 (최초 1회)

위키를 처음 쓸 때 폴더를 한 번만 지정하면 됩니다:

```
"이 프로젝트 위키에 등록해줘"   ← 이렇게 요청하면
   ↓
"LLM WIKI 폴더 경로를 알려주세요"  ← Claude가 경로를 물어봄
   ↓
경로 입력 (예: D:\MyWiki 또는 기존 Obsidian vault 경로)
   ↓
설정 완료 — 이후 다시 묻지 않음
```

- 설정한 경로는 PC의 개인 설정 파일(`~/.claude/llm-wiki-config.json`)에 저장됩니다. **plugin을 업데이트해도 유지**되며, 위키 내용도 plugin 밖 폴더에 있어 안전합니다.
- 위키 폴더를 옮기거나 삭제하면, 다음 사용 때 Claude가 새 경로를 다시 물어봅니다.
- 위키 폴더가 git으로 관리되지 않으면, Claude가 기존 파일을 수정·삭제하기 전에 사본을 위키 안 백업 폴더(`90_archive/backup/{날짜}/`)에 남깁니다 (30일 지난 백업은 자동 정리 — 단 **삭제된 프로젝트 백업은 유일한 사본이라 보존**합니다). 잘못 갱신됐을 때 "위키 되돌려줘"라고 요청하면 이 백업으로 복구합니다 (차이를 보여주고 확인 후 되돌림 — 복구 직전 상태도 별도로 백업해 복구 자체를 되돌릴 수 있습니다). git 관리 중인 폴더면 git이 이력을 보존하므로 백업·복구 모두 git으로 대신합니다 — 다만 **커밋이 하나도 없으면 git 복구가 불가**하므로, git 관리 위키는 주기적으로 커밋하시길 권합니다.
- 폴더를 지정하지 않으면 위키 참조·저장 단계는 그냥 건너뜁니다 (계획·구현·검증은 정상 진행).
- **스킬 개선 큐 ([SKILL-IMPROVE])**: 어느 프로젝트에서든 pjc 스킬을 쓰다 스킬 자체의 결함·불편을 발견하면 위키 대기 목록(`pending.md`)에 1줄 기록해 두고, 다음 위키 세션에서 "플러그인 개선 후보"로 알려줍니다. 위키를 설정하지 않았으면 묻지 않고 건너뛰며, 대신 그 프로젝트의 plan/notes에 기록해 유실을 막습니다.
- **결정 이력 ([DECISION])**: 기획 대화·계획 승인·구현 완료 때 정해진 결정(채택·보류·기각·번복)을 대기 목록에 기록해 두면, 다음 위키 세션이 프로젝트별 **결정 이력**(`decisions.md`)으로 옮겨 보존합니다. 다음 계획을 세울 때 Claude가 이 이력을 먼저 확인해 **예전에 보류·기각한 기획을 모르고 다시 논의하는 일을 막습니다.** "이 기능 언제 넣었지 / 왜 안 하기로 했지" 같은 질문도 이 이력으로 답합니다.
- **프로젝트 작업 사실 ([PROJECT-FACT], v1.102.0)**: 그 프로젝트에서 작업할 때 알아야 할 규약·주의사항(예: "버전 올리면 곧바로 릴리즈 발행")을 대기 목록에 기록해 두면, 다음 위키 세션이 프로젝트 허브의 **"작업 규약·주의사항"** 섹션으로 옮겨 보존합니다. 작업을 시작할 때 Claude가 이 섹션을 읽어오므로, **PC를 바꾸거나 Claude 메모리가 지워져도 프로젝트 작업 지식이 유실되지 않습니다** (Claude 메모리는 PC 로컬 — 프로젝트 사실은 위키에, 빌드·테스트 명령 같은 레포 귀속 사실은 AGENTS.md에, 개인 선호만 메모리에 저장하는 역할 분담).
- **공용 레시피 역방향 갱신**: 여러 프로젝트가 공유하는 기능 레시피를 한 프로젝트에서 개선하면, 위키 갱신 때 그 개선이 공용 레시피에도 반영됩니다 (프로젝트마다 제각각 진화해 위키가 갈라지는 것 방지).
- **질문 답변 개선**: 위키에 질문하면 원문 나열이 아니라 결론 요약 → 상세 → 출처 순으로 정리해 답하며, 시간·이력 질문("그동안 무슨 변화가 있었지")은 결정 이력·최근 변경·작업 로그를 시간순으로 합성해 답합니다.

### 사용 흐름

```
1. (설정 후) "이 프로젝트 위키에 등록해줘"
   → 프로젝트를 분석해 지식 정리

2. 이후 작업할 때 (자동)
   → 계획·디버깅 단계에서 위키의 과거 기록을 참조
   → "예전에 비슷한 걸 했었지" 하고 재조사를 줄임

3. 작업 끝나면
   → 위키에 남길 내용(기능 변경·개선 교훈)이 있으면 반영할지 물어봄
```

> 위키는 Obsidian 같은 마크다운 기반 도구로 열어볼 수 있는 폴더 구조입니다. 기존 vault를 그대로 써도 됩니다. 위키 형식은 공개 지식 표준인 **OKF(Open Knowledge Format) v0.1**과 정합하도록 만들어져(번들 경계·버전 선언·한 줄 요약 필드), 나중에 다른 도구·조직과 지식을 교환하기 쉽습니다.

> 기능 문서마다 "관련 파일" 지도(그 기능을 구성하는 파일 목록)를 기록해, 기능 이름으로 검색하면 관련 파일에 바로 도달합니다. 코드 작업 중 위키와 코드가 어긋난 걸 발견하면 위키의 대기 목록(pending)에 남겨 다음 위키 갱신 때 자동으로 챙기고, 위키 점검(lint)은 링크·분량·인덱스 동기·파일 경로 실존·시크릿 유입 등 20여 항목을 기계적으로 검사합니다.

---

## 더 알아보기 (고급)

<details>
<summary>작업 흐름 상세 다이어그램</summary>

```
사용자 요청
    ↓
[계획] 당신과 함께
  - 컨텍스트 수집 (AGENTS.md 없으면 자동 생성)
  - 영향 범위 전수 조사
  - task 분해 (T1, T2, …) + 작업 유형 분류
  - 검토자의 적대적 검증
  - 사용자 승인 1회 ✋
    ↓
[구현] 끝까지 자동
  각 task 반복:
    - 영향받는 곳 사전 추적
    - 최소 변경 구현
    - 유형별 검증 (작은 건 빠르게, 큰 건 철저하게)
    - 저장 후 즉시 다음 task
    ↓
[통합 검증]
  - 전체 빌드 + 테스트
  - 완료 검토자의 최종 점검
    ↓
최종 보고 ✋
```
</details>

<details>
<summary>작업 유형별 검증 깊이</summary>

작업의 위험도에 따라 검증 강도를 다르게 합니다 — 작은 변경은 빠르게, 큰 변경은 철저하게.

| 유형 | 예 | 검증 |
|---|---|---|
| 문서/설정 | `.md`, `.json` | 최소 |
| 단순 코드 | 한 파일·한 메서드 | 가벼운 검토 |
| 일반 코드 | 2-3 파일, 호출처 갱신 | 표준 검토 |
| 복잡/광범위 | 다중 파일, 시그니처 변경 | 전체 검토 |

</details>

<details>
<summary>공식 Claude Code 기능과의 관계</summary>

pjc는 Claude Code 내장 기능을 **대체하지 않고 보완**합니다.

`/code-review`, `/security-review`: pjc는 **작성 중** 계획 준수를 검증하고, 공식 기능은 **PR 단계**에서 버그·보안을 점검합니다. 둘 다 쓰면 이중 점검이 됩니다.

권장 흐름: `pjc로 작성·검증 → PR 생성 → 공식 /code-review로 재점검`

</details>

<details>
<summary>AGENTS.md란</summary>

프로젝트의 빌드·테스트 명령, 기술 스택, 규칙을 담은 파일입니다. pjc가 작업할 때 이 파일을 참조합니다. 없으면 자동 생성을 제안하며, 직접 작성할 수도 있습니다.

pjc는 **특정 언어 전용이 아닙니다.** .NET·Android·Node/TS·Python·Go·Rust는 전용 템플릿으로 자동 설정되고, 그 외 모든 언어(Flutter·Swift·Java·C++·Ruby·Elixir 등)는 generic 템플릿으로 동작합니다 — 빌드/테스트 명령만 알려주면 됩니다. pjc의 계획·검증·자율 루프는 언어를 가리지 않습니다.

**생성된 AGENTS.md는 그대로 써도 동작하지만, 프로젝트 스타일에 맞게 다듬으면 더 좋은 성능을 냅니다.** 빈 칸을 채우고 프로젝트 고유의 규칙·함정·컨벤션을 추가할수록 Claude가 추측을 줄여 더 정확하게 작업합니다. 처음엔 빌드·테스트 명령만으로 시작해 점진적으로 보강하는 것을 권장합니다.

</details>

---

## 설치 관리

```powershell
# 업데이트
claude plugin marketplace update pjc-harness
claude plugin update pjc@pjc-harness

# 제거
claude plugin uninstall pjc
claude plugin marketplace remove pjc-harness
```

<details>
<summary>로컬 개발 모드 / 배포 워크플로</summary>

개발용으로 로컬 폴더에서 직접 쓰거나, 수정 후 GitHub에 배포하려면:

```powershell
# 로컬 설치 (개발용)
.\install.ps1

# GitHub 모드 일괄 설치 (정리 + enable + 검증)
.\install.ps1 -GitHub jongcheol-pak/claude-harness-pjc

# 배포 (개발 PC에서 수정 후)
git add -A && git commit -m "변경 내용" && git push
# → 사용 PC에서 위의 "업데이트" 명령 실행
```

</details>

---

## 트러블슈팅

<details>
<summary>plugin 명령이 안 보여요</summary>

Claude Code를 재시작하거나 `/reload-plugins`를 실행하세요. 그래도 안 되면 `claude plugin enable pjc@pjc-harness`로 활성화 상태를 확인하세요.

</details>

<details>
<summary>안전장치(hook)가 너무 자주 막아요</summary>

하니스의 hook은 항상 켜져 있어 끌 수 없습니다. 정상 작업인데 막혔다면, 계획(plan)을 먼저 만들거나 명령을 더 구체적으로(예: 삭제 명령에 `WHERE 조건` 추가) 바꾸면 통과합니다. 위험한 명령 차단(파일 삭제·DB 삭제 등)은 안전을 위한 것이니, 정말 필요한 작업이면 더 좁은 범위로 나눠 실행하세요.

> v1.98.0에서 정상 작업 오차단을 줄였습니다 — 임시 폴더 정리(`$env:TEMP\...` 하위), 프로젝트 내 단일 파일 `chmod +x`, 현재 폴더 상대 정리(`Get-ChildItem . -Recurse | ...`), 테스트/재현용 신규 파일(`tests/`·`repro*`, 30줄 이하), 마크업·스타일 파일(`.xml`/`.html`/`.css`)이 이제 계획 없이 통과합니다. 위험 차단(홈·시스템 삭제, DB 파괴 등)은 그대로입니다.

</details>

<details>
<summary>한글이 깨져요</summary>

PowerShell 인코딩 문제일 수 있습니다. pjc의 모든 스크립트는 UTF-8(BOM)로 저장되어 있으니, Claude Code와 터미널이 UTF-8을 쓰는지 확인하세요.

</details>

---

## 호환 환경

pjc는 두 부분으로 나뉘며, OS 의존성이 다릅니다.

| 구성 | OS 의존 | 설명 |
|---|---|---|
| **Skills · Subagents** (계획·구현·디버깅·검증 로직) | OS 무관 | 지침(markdown)이라 어디서나 동작 |
| **Hooks** (위험 명령 차단·권한/보안 변경 차단·외부 작업(push·릴리즈) 경고·민감정보 경고·plan 강제 등 자동 안전망) | **pwsh 7 우선·5.1 폴백** | hook 스크립트는 Claude Code가 띄운 PowerShell에서 직접 실행(v1.108.0 — 자식 셸 재기동 없음). 실행 셸은 Claude Code의 powershell 해석을 따르며(실측: pwsh 7 있으면 `pwsh` 우선), 스크립트는 내장 `powershell.exe`(5.1) 호환을 유지; macOS/Linux는 `pwsh` 7 |

| 항목 | 지원 |
|---|---|
| OS | **Windows 10/11**: 완전 지원·검증. **macOS/Linux**: pwsh 7 설치 시 동작하도록 구현됐으나 **실제 환경 미검증(실험적)** |
| 런타임 | **Windows**: 추가 설치 불요 — 실행 셸은 Claude Code가 결정(실측: pwsh 7 있으면 그쪽 우선), 스크립트는 내장 `powershell.exe`(5.1) 호환 유지. **macOS/Linux**: pwsh 7 필요(`brew install powershell` 등) |
| Claude Code | v2.0 이상 |
| 대상 언어 | **모든 언어 동작** — .NET, Android, Node/TS, Python, Go, Rust는 전용 템플릿으로 자동 설정. 그 외(Flutter·Swift·Java·C++·Ruby 등)는 generic 템플릿으로 빌드/테스트 명령만 입력하면 동일하게 작동 |

> **요약**: 자동 안전망(hook)은 OS에 무관하게 동작하도록 구현됐습니다. **Windows는 검증 완료** — pwsh 7이 있으면 그쪽에서, 없으면 내장 `powershell.exe`(5.1)에서 동작해 **추가 설치 없이 안전망이 항상 동작**합니다. **macOS/Linux는 pwsh 7 필요·실험적(미검증)** 입니다(`brew install powershell` 등). (v1.70.0은 pwsh 7 전용 → v1.70.1에서 5.1 지원 추가 → v1.108.0부터 hook이 자체적으로 셸을 재기동하지 않고 Claude Code가 띄운 셸에서 직접 실행됩니다 — 실행 셸 선택은 Claude Code의 powershell 해석을 따르며(실측: pwsh 우선), 스크립트의 5.1 호환은 유지됩니다.)

---

## 설계 철학

- **재작업 방지 > 토큰 절약**: 검증에 드는 비용은 잘못 만든 걸 다시 만드는 비용보다 쌉니다.
- **계획은 함께, 구현은 자동**: 방향은 사람이 정하고, 실행은 Claude가 끝까지.
- **확실한 안전망**: 검토는 확률적이지만, 위험 명령 차단 같은 hook은 결정적으로 작동합니다.

---

## 라이선스

MIT
