# Intent: 회차 59 가 남긴 다섯 — 초안 (미승인)

Author: 회차 59 (`pjc:implement`). Status: **superseded — `2026-09-11-shrink-conventions-and-silent-budget.md` 가 이월해 받았다(회차 60).**

> **이 파일의 처방 셋이 재측정에서 틀렸다** — 착수 전에 다시 잰 결과다. ⓔ 의 예산 수치는 97,363 이 아니라 **98,252 B** 였고(+889), ⓒ 의 대상은 4건이 아니라 **5건**이었으며(`SKILL.md:97` 누락 — 볼드 뒤 백틱 경로라 육안 판정이 놓쳤다), ⓓ 의 *"어느 검사기의 스캔 범위에도 없다"* 는 **내용 축에 한해서만** 참이었다(축 ⑧「줄바꿈 정합」과 `guard-write` 는 이미 그 경로를 본다). 회차 57·58·59 에 이어 **네 번째로 「큐·문서가 적어 둔 것이 틀렸다」**가 반복된 자리다.

> **이 파일은 요구가 확정된 기록이 아니라 인계 수단이다.** 회차 59 의 `[다음 회차]` 를 `plan.md`(`.gitignore`)가 아니라 **커밋된 `intent/`** 에 둔 이유는 아래 「왜 여기 있는가」에 있다. 착수 전에 `pjc:plan` 의 인터뷰로 범위를 확정한다.

## 왜 여기 있는가

`[다음 회차]` 는 지금까지 `plan.md` 에만 있었는데 그 파일은 `.gitignore` 라 다음 회차에 덮어쓰인다. **전달 수단이 사람이었다** — 회차 58 의 3건도 사용자가 손으로 옮겨 회차 59 에 왔고, **셋 다 처방이 틀렸다**(모집단 오염 · 검사기 수 오기 · 전제 오류). 요지만 옮겨지며 근거 맥락이 떨어져 나간 것과 무관하지 않아 보인다(추정 — 인과를 실측하지 않았다).

`AGENTS.md` 「Plan Location」이 *"`plan.md`·`notes.md`·`notes-archive/`는 `.gitignore`(**`intent/`는 추적**)"* 라 적어 경로가 이미 규약 안에 있었다. 회차 59 가 그것을 쓴다.

## 남은 다섯

### ⓐ Haiku·Sonnet 트리거 eval — **구현이 아니라 실행 작업이다**

`trigger_eval.py:560` 이 이미 `--model` 을 받고 기본값이 `opus` 다. **코드 변경이 필요 없고 `--model haiku` / `--model sonnet` 으로 돌리면 된다.** 공식 Agent Skills 체크리스트의 *"Tested with Haiku, Sonnet, and Opus"* 가 근거다.

- **비용**: 43케이스 × 2 run. 케이스당 상한 180초라 최악 각 129분이다. 회차 59 의 Opus run 은 실측 **약 20분**이었다(43/43 PASS).
- **대조 기준선은 이미 있다** — 회차 59 가 뜬 `20260911-174931`(격리·opus·43건 · 발동률 1.000 / 오발동률 0.000). **같은 모드(`--isolation isolated`)로 떠야 대조가 선다.**
- ⚠ **run 중에 `plugins/pjc/` 아래를 편집하지 않는다** — 러너가 `PLUGIN_DIR`(레포 워킹트리)을 `--plugin-dir` 로 읽어, 편집하면 케이스마다 다른 트리를 본다.
- ⚠ **러너 종료 코드는 `fail` 이 있으면 1 이다.** `exit 2` 만 관측 실패로 본다.

### ⓑ 위키 뒤처짐 — claude-harness-pjc 위키가 미반영

회차 59 착수 시점에 **40커밋 미반영**(synced `da02dfb2`)이었고, 이 회차가 그 위에 8커밋을 더했다. **회차 59 의 변경 중 위키가 받아야 할 것**: 축 ⑱ 신설(검사기가 16축이 됐다) · `AGENTS.md`「DO NOT」 사정거리 4개 확대 · `.github/` CI 신설.

별도 위키 세션에서 *"위키 업데이트"* 로 착수한다.

### ⓒ `DESIGN.md` §6 의 `record-project-fact` 「미판정」

§6 이 *"`record-project-fact` 는 **미판정**이다"* 라 적어 둔 것이 그대로 남아 있다. 회차 59 는 사용자 결정으로 **§6 을 고치지 않고 그대로 따랐다**(대상 18건). 그 스킬을 §1 대상에 넣을지는 아직 아무도 판정하지 않았다.

- 판정하면 대상이 **4건** 늘어난다(`record-project-fact/SKILL.md` 3 · `references/record-fact-rationale.md` 1 — 회차 59 실측).
- **판정 자체가 규약 개정이라 사용자 결정이다.**

### ⓓ `.github/` 를 `check-stale-refs.py` 스캔 범위에 넣을지

회차 59 가 `.github/workflows/checks.yml` 을 만들었는데 **어느 검사기의 스캔 범위에도 없다** — `check-stale-refs.py` 의 범위는 `plugins/**`·`docs/**`·루트 `*.ps1`·`*.md` 다. 워크플로가 죽은 스크립트 이름을 담아도 아무도 잡지 않는다.

- ⚠ **전역 `~/.gitignore_global:4` 가 `.github/` 를 무시한다**(이 레포 것이 아니다). 회차 59 가 레포 `.gitignore` 에 예외 3줄을 넣어 되살렸다 — 그 예외가 없으면 파일이 추적되지 않는다.

### ⓔ `check-harness-consistency.py` 예산 초과 — **97,363 / 95,000 B**

회차 59 T2 가 축 하나를 더하자 즉시 넘었다. **BASE 가 94,747 로 이미 상한의 99.7% 였다** — 축을 하나만 더해도 넘는 구조였다.

- 회차 59 는 처방(근거 산문을 `*-rationale.md` 로 내린다)을 **자기 축 분에만 적용했다**(5,070 → 2,616 B).
- 남은 2,363 B 는 **기존 산문을 7개 무관한 블록에서 걷어내야** 한다. 그 블록들은 판정 기전을 사용 지점에서 설명하는 것이라 포인터로 바꾸면 코드가 나빠진다 — **별도 과제다.**
- **통지 등급이라 막지 않는다.** 급하지 않으나 다음 축 신설은 이것을 먼저 풀어야 한다.

## Open questions

- ⓐ 를 2 모델 다 돌릴지 하나만 돌릴지 — 비용이 회차 59 Opus run 의 2배다.
- ⓒ 는 규약 개정이라 사용자 결정이 선행한다.
- ⓔ 의 2,363 B 를 어느 블록에서 걷을지 — 어느 것도 명백한 중복이 아니다.
